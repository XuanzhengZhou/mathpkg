A subset $A \subset \mathbb{C}$ is convex if whenever $z$ and $w$ are in $A$, $tz + (1-t)w$ is in $A$ for $0 \leq t \leq 1$; that is, $A$ is convex when for any two points in $A$ the line segment joining the two points is also in $A$.
