If $[a, b]$ is an interval in the real line, a function $f: [a, b] \to \mathbb{R}$ is convex if for any two points $x_1$ and $x_2$ in $[a, b]$

$$f(tx_2 + (1-t)x_1) \leq tf(x_2) + (1-t)f(x_1)$$

whenever $0 \leq t \leq 1$.
