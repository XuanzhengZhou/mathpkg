A sequence $(x_n)_{n=1}^{\infty}$ of real numbers \textbf{converges} to a limit $L \in \mathbb{R}$ if for every $\epsilon > 0$ there exists $N \in \mathbb{N}$ such that $|x_n - L| < \epsilon$ for all $n \geq N$. We write $\lim_{n \to \infty} x_n = L$ or $x_n \to L$.
