Let $f$ have an isolated singularity at $z = a$ and let
$$f(z) = \sum_{n=-\infty}^{\infty} a_n(z-a)^n$$
be its Laurent Expansion about $z = a$. Then the \textbf{residue} of $f$ at $z = a$ is the coefficient $a_{-1}$. Denote this by
$$\operatorname{Res}(f; a) = a_{-1}.$$
