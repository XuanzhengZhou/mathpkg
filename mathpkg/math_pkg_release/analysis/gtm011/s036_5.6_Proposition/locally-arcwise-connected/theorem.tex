The topological space $(X, \mathcal{T})$ is called locally arcwise or pathwise connected if for each point $x$ in $X$ and each open set $U$ which contains $x$ there is an open arcwise connected set $V$ such that $x \in V$ and $V \subset U$.
