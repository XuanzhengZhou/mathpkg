Let $K$ be a compact subset of the region $G$; then there are straight line segments $\gamma_1, \ldots, \gamma_n$ in $G - K$ such that for

(there are only a finite number of them because $K$ is compact). Also let $\partial R_j$ be the boundary of $R_j$, $1 \leq j \leq m$, considered as a polygon with the counter-clockwise direction.

If $z \in R_j$, $1 \leq j \leq m$, then $d(z, K) < \sqrt{2}\delta$ so that $R_j \subset G$ by the choice of $\delta$. Also, many of the sides of the rectangles $R_1, \ldots, R_m$ will intersect. Suppose $R_j$ and $R_i$ have a common side and let $\sigma_j$ and $\sigma_i$ be the line segments in $\partial R_j$, and $\partial R_i$ respectively, such that $R_i \cap R_j = \{\sigma_j\} = \{\sigma_i\}$. From the direction given $\partial R_j$ and $\partial R_i$, $\sigma_j$ and $\sigma_i$ are directed in the opposite sense. So if $\varphi$ is any continuous function on $\{\sigma_j\}$,

$$\int_{\sigma_j} \varphi + \int_{\sigma_i} \varphi = 0.$$

Let $\gamma_1, \ldots, \gamma_n$ be those directed line segments that constitute a side of exactly one of the $R_j$, $1 \leq j \leq m$. Thus

1.2 $$\sum_{k=1}^{n} \int_{\gamma_k} \varphi = \sum_{j=1}^{m} \int_{\delta R_j} \varphi$$

for every continuous function $\varphi$ on $\bigcup_{j=1}^{m} \partial R_j$.

We claim that each $\gamma_k$ is in $G - K$. In fact, if one of the $\gamma_k$ intersects $K$, it is easy to see that there are two rectangles in the grid with $\gamma_k$ as a side and so both meet $K$. That is, $\gamma_k$ is the common side of two of the rectangles $R_1, \ldots, R_m$ and this contradicts the choice of $\gamma_k$.

If $z$ belongs to $K$ and is not on the boundary of any $R_j$ then

$$\varphi(w) = \frac{1}{

This next lemma provides the first step in obtaining approximation by rational functions.