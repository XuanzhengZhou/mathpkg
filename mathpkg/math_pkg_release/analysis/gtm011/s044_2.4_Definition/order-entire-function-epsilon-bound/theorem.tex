Let $f$ be an entire function of finite order $\lambda$. If $\epsilon > 0$ then
$$|f(z)| < \exp(|z|^{\lambda+\epsilon})$$
for all $z$ with $|z|$ sufficiently large; and a $z$ can be found, with $|z|$ as large as desired, such that
$$|f(z)| \geq \exp(|z|^{\lambda-\epsilon}).$$
