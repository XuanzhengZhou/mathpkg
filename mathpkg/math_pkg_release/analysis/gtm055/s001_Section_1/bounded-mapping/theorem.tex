A mapping $f$ of a set $X$ into a partially ordered set $Y$ is \textit{bounded} [resp. \textit{bounded above}, \textit{bounded below}] if its range $f(X)$ is bounded [resp. bounded above, bounded below] in $Y$.
