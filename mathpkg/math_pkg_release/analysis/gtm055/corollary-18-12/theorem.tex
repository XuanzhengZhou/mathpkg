If $K$ is an arbitrary compact subset of $\mathbb{C}$, a necessary and sufficient condition for $\mathcal{P}(K)$ to coincide with $\mathcal{C}(K)$ is that the function $\bar{\lambda}$ belong to $\mathcal{P}(K)$. Similarly, $\mathcal{A}(K) = \mathcal{

The following idea is also useful in dealing with polynomial approximation.
