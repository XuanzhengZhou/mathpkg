Let $\mathcal{E}$ and $\mathcal{F}$ be linear spaces over the same scalar field, let $X$ be a subset of $\mathcal{E}$, let $Y$ be a subset of $\mathcal{F}$, and let $T = \{x \otimes y : x \in X, y \in Y\}$. If $X$ and $Y$ generate $\mathcal{E}$ and $\mathcal{F}$, respectively, then $T$ generates $\mathcal{E} \times \mathcal{F}$. If $X$ and $Y$ are each linearly independent, then $T$ is linearly independent in $\mathcal{E} \times \mathcal{F}$. Finally, if $X$ and $Y$ are Hamel bases for $\mathcal{E}$ and $\mathcal{F}$, respectively, then $T$ is a Hamel basis for $\mathcal{E} \times \mathcal{F}$.

Proof. The first assertion is obvious, and is included here only for completeness, while the third assertion is an immediate consequence of the first two. Thus the proof comes down to showing that $T$ is linearly independent when $X$ and $Y$ are, and here it is clearly enough to treat the case in which $X$ and $Y$ are nonempty finite sets. Suppose, then, that $X = \{x_1, \ldots, x_m\}$ and $Y = \{y_1, \ldots, y_n\}$ are linearly independent, and let $\mathcal{L}$ and $\mathcal{M}$ denote the linear manifolds generated by $X$ and $Y$, respectively. For each vector $x = \sum_{i=1}^{m} \alpha_i x_i$ in $\mathcal{L}$ and $y = \sum_{j=1}^{n} \beta_j y_j$ in $\mathcal{M}$, we define the $m \times n$ matrix

$$\varphi(x, y)

maximal linearly independent set in $\mathcal{E}$ is a Hamel basis for $\mathcal{E}$. (The empty set $\varnothing$ is a Hamel basis for (0).) Show similarly that if $M$ is an arbitrary subset of $\mathcal{E}$ and $J$ an arbitrary linearly independent subset of $M$, and if $\mathcal{L}$ denotes the linear submanifold of $\mathcal{E}$ generated by $M$, then there exists a Hamel basis $X$ for $\mathcal{L}$ such that $J \subset X \subset M$.

B. (i) Let $X = \{x_1, \ldots, x_m\}$ and $Y = \{y_1, \ldots, y_n\}$ be linearly independent sets of vectors in a (real or complex) vector space $\mathcal{E}$, and suppose $X$ is contained in the submanifold $M$ of $\mathcal{E}$ that is generated by $Y$. Show that $m \leq n$ and that it is possible to select a set $Z$ of exactly $n-m$ vectors from $Y$ so that $X \cup Z$ is also a basis for $M$. (In particular, then, if $m = n$, the set $X$ is itself a basis for $M$.) Conclude that if a vector space $\mathcal{E}$ possesses a finite basis, then any two bases for $\mathcal{E}$ contain the same number of vectors, and that, if $\dim \mathcal{E} = n$, then $\mathcal{E}$ is itself the only $n$-dimensional submanifold of $\mathcal{E}$. (Hint: The heart of the matter is that if a vector $x$ belongs to $M$, and if in the expression $x = \sum_{i=1}^{n} \lambda_i y_i$ some $\lambda_{i_0} \neq 0$, then the set $\hat{T}$ consisting of $x$ and $\{y_i : i \neq i_0\}$ is linearly independent, and is therefore another basis for $M$.)

(ii) Let $\mathcal{E}$ be an infinite dimensional linear space (real or complex), and suppose given two (infinite) Hamel bases $X$ and $Y$ for $\mathcal{E

manifold in $\mathcal{E}$ and that the mapping $\tau_{\gamma_0}$ obtained by restricting to $M_{\gamma_0}$ the projection $\pi_{\gamma_0}$ of $\mathcal{E}$ onto $M_{\gamma_0}$ is an isomorphism of $M_{\gamma_0}$ onto $M_{\gamma_0}$. (This isomorphism is frequently used to identify $M_{\gamma_0}$ with its counterpart $M_{\gamma_0}$.) Show also that the indexed family of submanifolds $\{M_{\gamma}\}_{\gamma \in \Gamma}$ has the following properties:

(a) $\sum_{\gamma} M_{\gamma} = \mathcal{E},$

(b) $M_{\gamma} \cap M_{\gamma'} = (0), \quad \gamma \neq \gamma'.$

Use this observation to prove that the Hamel dimension of $\mathcal{E}$ is the sum $\sum_{\gamma} d_{\gamma}$ where $d_{\gamma}$ denotes the Hamel dimension of $\mathcal{E}, \gamma \in \Gamma$. (Hint: If $X_{\gamma}$ is a Hamel basis for $M_{\gamma}, \gamma \in \Gamma$, then the sets $X_{\gamma}$ are pairwise disjoint, and $\bigcup_{\gamma} X_{\gamma}$ is a Hamel basis for $\mathcal{E}$. Recall that the cardinal number of the union of a disjoint collection of sets is the sum of the cardinal numbers of the sets in the collection.)

(ii) An indexed family $\{M_{\gamma}\}_{\gamma \in \Gamma}$ of linear submanifolds of a linear space $\mathcal{E}$ is said to form an internal direct sum decomposition of $\mathcal{E}$ if (a) and (b) of (i) are satisfied. Thus, according to (i), to every (external) full algebraic direct sum $\mathcal{E} = \sum_{\gamma} + \mathcal{E}_{\gamma}$ there corresponds a similarly indexed internal direct sum decomposition $\{M_{\gamma}\}$ of $\mathcal{E}$, where each $M_{\gamma}$ is isomorphic to $\mathcal{E}_{\gamma}$ in a natural way. Show, conversely, that if $\{M_{\gamma}\}$ is any internal direct sum decomposition of a linear

(ii) Let $Z = \{z_1, \ldots, z_m\}$ be an ordered basis for a third linear space $\mathcal{G}$, and let $A$ be the matrix of a linear transformation $S: \mathcal{F} \rightarrow \mathcal{G}$ with respect to $Y$ and $Z$. Verify that $AB$ is the matrix of $ST$ with respect to the bases $X$ and $Z$.
...
