A \textbf{Jordan domain} in $\mathbb{C}$ is a domain whose (entire) boundary consists of the union of a finite number of pairwise disjoint Jordan curves. The closure of a Jordan domain is called a \textbf{Jordan region}; it is the union of the domain and the various Jordan curves constituting its boundary.
