If $\alpha$ is an ordinal number, the \textbf{ordinal number segment} $W(\alpha)$ is the set of all ordinal numbers $\xi$ such that $\xi < \alpha$. The ordinal number of the well-ordered set $W(\alpha)$ is $\alpha$, and $\operatorname{card} W(\alpha) = \operatorname{card} \alpha$.
