Let $X$ be a set and $Y$ a partially ordered set. A mapping $f : X \to Y$ is:
\begin{itemize}
\item \textbf{bounded} if its range $f(X)$ is bounded in $Y$,
\item \textbf{bounded above} if $f(X)$ is bounded above in $Y$,
\item \textbf{bounded below} if $f(X)$ is bounded below in $Y$.
\end{itemize}
