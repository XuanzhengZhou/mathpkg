If $\mathcal{E}$ is a quasinormed space, then there exists an $F$-space $\hat{\mathcal{E}}$ and a linear isomorphism $\varphi$ of $\mathcal{E}$ onto a dense linear manifold $\varphi(\mathcal{E})$ in $\hat{\mathcal{E}}$ such that $|\varphi(x)| = |x|$ for every $x$ in $\mathcal{E}$. Furthermore, the pair $(\hat{\mathcal{E}}, \varphi)$ is unique in the following sense: If $\hat{\mathcal{E}}_1$ is another $F$-space and $\varphi_1$ a linear isomorphism of $\mathcal{E}$ into $\hat{\mathcal{E}}_1$ possessing the properties that $\varphi_1

$v_0$ also satisfies condition (ii') in the definition of a norm when $||$ is a norm on $\mathcal{E}$. It follows from these observations that the set

$$\mathcal{M} = \{ \{x_n\} \in \mathcal{C} : v_0(\{x_n\}) = 0 \}$$

is a linear manifold in $\mathcal{E}$. Moreover, if two sequences $\{x_n\}$ and $\{y_n\}$ in $\mathcal{C}$ are congruent modulo $\mathcal{M}$, that is, if there exists a sequence $\{z_n\}$ in $\mathcal{M}$ such that $\{x_n\} = \{y_n + z_n\}$, then $v_0(\{x_n\}) \leq v_0(\{y_n\}) + v_0(\{z_n\}) = v_0(\{y_n\})$. Similarly $v_0(\{y_n\}) \leq v_0(\{x_n\})$, of course, so $v_0(\{x_n\}) = v_0(\{y_n\})$. Hence we may, and do, define

$$|[\{x_n\}]|_0 = v_0(\{x_n\}), \quad \{x_n\} \in \mathcal{C},$$

where $[\{x_n\}]$ denotes the coset $\{x_n\} + \mathcal{M}$ of $\{x_n\}$ in the quotient vector space $\mathcal{C}/\mathcal{M}$. Clearly $||$ is a value on $\mathcal{C}/\mathcal{M}$ that satisfies condition (ii'') in the definition of a quasinorm. Hence it is only necessary to verify condition (iv) in that definition to show that $||$ is a quasinorm on $\mathcal{C}/\mathcal{M}$. This we shall do by showing that if $\{\alpha_k\}_{k=1}^{\infty}$ is a null sequence in $\mathbb{C}$, if $\{x_n\}_{n=1}^{\infty}$ is a Cauchy sequence in $\mathcal{E}$, and if $\varepsilon > 0$, then there exists a positive integer $K$ such that $\lim_n |\alpha_k x_n

the completion of $\mathcal{E}$. We shall regularly adopt the common practice of using the isometric isomorphism $\varphi$ to identify $\mathcal{E}$ with $\varphi(\mathcal{E})$. Thus we shall ordinarily regard $\mathcal{E}$ as a dense linear submanifold of its completion $\hat{\mathcal{E}}$. (The reader may wish to compare the above argument with that sketched in Problems 4H–4L.)

Up to this point we have concerned ourselves exclusively with linear spaces that are equipped with some sort of invariant metric. Consideration of the role played by the function $v_0$ in the proof of Theorem 11.16 suggests the desirability of considering yet other functions on linear spaces that, like quasinorms, are similar to but more general than norms, and experience shows that this is, indeed, frequently useful.
