Let $\langle \mathcal{E}, \mathcal{F} \rangle$ be a dual pair. If $M$ and $N$ are subsets of $\mathcal{E}$ such that $M \subset N$, then $M^0 \supset N^0$. Likewise if $\{M_\gamma\}_{\gamma \in \Gamma}$ is a nonempty indexed family of subsets of $\mathcal{E}$, then

$$\left( \bigcup_{\gamma

be nonnegative). Thus, replacing $g$ by a functional $f$ of the form $\gamma g$, where $\gamma$ denotes a suitable complex number of modulus one, we obtain

$$|f(x)| \leq c, x \in A, \quad \text{while} \quad f(x_0) > c.$$

If $c > 0$ we have but to define $f_0 = f/c$; if $c = 0$ we may set $f_0 = 2f/f(x_0)$. $\square$
