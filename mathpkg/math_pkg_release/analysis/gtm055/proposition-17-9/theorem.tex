Let $(X, \mathbf{S}, \mu)$ be a measure space, let $\mathcal{E}$ be a normed space, and let $p$ be a positive real number. Then the collection $\mathcal{L}_p(X; \mathcal{E}) = \mathcal{L}_p(X, \mathbf{S}, \mu; \mathcal{E})$ of all those measurable $\mathcal{E}$-valued mappings $\Phi$ on $X$ such that the function $N_{\Phi}$ defined in (6) satisfies the condition

$$\int_X (N_{\Phi})^p d\mu < +\infty$$

is a linear space with respect to pointwise linear operations. (The integral indicated in (7) makes sense by virtue of the foregoing lemma.) Moreover, for $p \geq 1$ (and, for nontrivial $X$ and $\
