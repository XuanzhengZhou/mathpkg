Let $(X, \mathbf{S}, \mu)$ be a measure space, and let $\mathcal{E}$ be a separable Banach space. Then there exists a unique bounded linear transformation $L$ of $L_1(X; \mathcal{E})$ into $\mathcal{E}$ satisfying the condition

$$L(\chi

satisfied. To complete the proof we note that if the sets $E_i$ in (9) are chosen to be pairwise disjoint, as they always can be, then the function $N_\Sigma$ defined as in (6) is the simple function $\sum_{i=1}^{N} \|v_i\| \chi_{E_i}$, whence it follows that

$$\|L_0(\Sigma)\| \leq \sum_{i=1}^{N} \|v_i\| \mu(E_i) = \|N_\Sigma\|_1 = \|\Sigma\|_1.$$

Thus $L_0$ is bounded (by one) and therefore possesses a unique bounded linear extension to $\mathcal{L}_1(X; \mathcal{E})$ (that is also bounded by one; see Problem 12I).

Example I. If $\mathcal{E}$ is a separable Banach space and if, for some fixed $n$, $\Phi$ is a mapping of $\mathbb{R}^n$ into $\mathcal{E}$, then, just as when $\mathcal{E} = \mathbb{C}$, for each point $a$ of $\mathbb{R}^n$ we write $\Phi_{(a)}$ for the translate

$$\Phi_{(a)}(x) = \Phi(x + a), \quad x \in \mathbb{R}^n$$

(cf. Example D). If $\mathcal{E}$ is a separable Banach space and $E$ is a Borel set in $\mathbb{R}^n$ having finite Lebesgue measure, then the translation invariance of Lebesgue measure $\mu_n$ ensures that

$$\int_{\mathbb{R}^n} \chi_E v d\mu_n = \int_{\mathbb{R}^n} (\chi_E v)_{(a)} d\mu_n \quad (= \mu_n(E)v)$$

for each point $a$ of $\mathbb{R}^n$ and every vector $v$ in $\mathcal{E}$. Hence if $\Sigma$ is an arbitrary integrable simple $\mathcal{E}$-valued mapping on $\mathbb{R}^n$, then

$$\int_{\mathbb{R}^n} \Sigma d\mu_n = \int_{\mathbb{R}^n} \Sigma_{
