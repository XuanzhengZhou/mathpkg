(Cauchy Criterion for Infinite Series). If $\{x_n\}_{n=0}^{\infty}$ is a sequence of vectors in a Banach space $\mathcal{E}$, then the infinite series $\sum_{n=0}^{\infty} x_n$ is convergent in $\mathcal{E}$ if and only if, for each given positive number $\varepsilon$, there exists a positive integer $K$ such that $\|\sum_{n=K}^{K+p} x_n\| < \varepsilon$ for every positive integer $p$. (Suitably worded versions of the Cauchy criterion are also valid, of course, for infinite series of the form $\sum_{n=1}^{\infty

Note that this definition can be paraphrased as follows: The family $\{x_{\gamma}\}_{\gamma \in \Gamma}$ has sum $x$ if for every positive number $\varepsilon$ there exists a finite subset $D_{\varepsilon}$ of $\Gamma$ such that $\|s_D - x\| < \varepsilon$ for every finite subset $D$ of $\Gamma$ such that $D \supset D_{\varepsilon}$.
