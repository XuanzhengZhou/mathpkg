---
role: exercise
locale: en
chapter: "1"
section: "Section 01_Section_1"
exercise_number: 10
---

J. (i) Let $f$ be a real-valued function on a closed interval $[a, b]$. We define two nets of nonnegative real numbers associated with $f$ and indexed by the directed set of partitions of $[a, b]$ by writing

$$v_+(\mathcal{P}) = \sum_{i=1}^{n} [f(s_i) - f(s_{i-1})]^+, \quad v_-(\mathcal{P}) = \sum_{i=1}^{n} [f(s_i) - f(s_{i-1})]^-$$

(Ex. A), where $\mathcal{P} = \{s_i\}_{i=0}^{n}$. The numbers $v_+(\mathcal{P})$ and $v_-(\mathcal{P})$ are called the positive and negative variations of $f$ over $\mathcal{P}$, respectively. Show that the nets $\{v_+(\mathcal{P})\}$ and $\{v_-(\mathcal{P})\}$ are both monotone increasing, and that $f$ is of bounded variation on $[a, b]$ if and only if both of the nets $\{v_+(\mathcal{P})\}$ and $\{v_-(\mathcal{P})\}$ are bounded (above). Show also that if $f$ is of bounded variation on $[a, b]$, and if we define $V_+(f; a, b) = \sup_{\mathcal{P}} v_+(\mathcal{P})$ and $V_-(f; a, b) = \sup_{\mathcal{P}} v_-(\mathcal{P})$, then

$$V(f; a, b) = V_+(f; a, b) + V_-(f; a, b)$$

and

$$f(b) - f

in $E$. Show also that the collection of all partitions of $X$ is a directed set with respect to the partial ordering $\leq$.

(ii) If $\{E_1, \ldots, E_n\}$ is a finite collection of subsets of a set $X$, then the partition of $X$ determined by $\{E_1, \ldots, E_n\}$ consists of the collection of all sets of the form

$$A_1 \cap \cdots \cap A_n$$

where each $A_i$ is either $E_i$ or $X \setminus E_i$. (There are $2^n$ such sequences $\{A_1, \ldots, A_n\}$, b
...
