---
role: exercise
locale: en
chapter: "6-7"
section: "Section 08_Section_8"
exercise_number: 8
---

H. Let $\%$ be a collection of subsets of a set $X$, and let $R$ and $S$ denote, respectively, the ring and $\sigma$-ring generated by $\%$. If $\%$ is finite, then so is $R$. What about $S$? If $\%$ is countable, then so is $R$, while for $S$ one has the estimate card $S \leq \aleph$ (where $\aleph$ denotes the cardinal number of the continuum). Indeed, card $\% \leq \aleph$ implies card $S \leq \aleph$. (Hint: If $\%$ consists of $k$ sets, then $R(\%)$ cannot contain more than $2^{2^k}$ sets; if card $\% \leq \aleph$, then card

A of $X$ with the property that if $D \in \mathcal{L}$, then $A \cap D \in \mathbf{Q}(\mathcal{L})$, and let $\mathbf{B}$ denote the collection of all subsets $B$ of $X$ with the property that if $Q \in \mathbf{Q}(\mathcal{L})$, then $B \cap Q \in \mathbf{Q}(\mathcal{L})$. Verify that $\mathbf{A}$ and $\mathbf{B}$ are both $\sigma$-quasirings containing $\mathcal{L}$, and use Problem J.
