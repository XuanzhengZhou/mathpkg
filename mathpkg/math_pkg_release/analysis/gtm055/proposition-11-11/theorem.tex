The direct sum $\mathcal{E}_1 \oplus_1 \cdots \oplus_1 \mathcal{E}_n$ of a finite sequence $\{\mathcal{E}_1, \ldots, \mathcal{E}_n\}$ of normed spaces is complete if and only if each space $\mathcal{E}_i$ is complete, $i = 1, \ldots, n$.

Proof. A sequence $\{(x_1^{(k)}, \ldots, x_n^{(k)})\}_{k=1}^{\infty}$ is Cauchy [convergent] in $\mathcal{E}_1 \oplus_1 \cdots \oplus_1 \mathcal{E}_n$ if and only if each sequence $\{x_i^{(k)}\}_{k=1}^{\infty}$ is Cauchy [convergent] in its respective space $\mathcal{E}_i$.

There is an interesting and frequently useful generalization of the concept of a normed space. The idea is to weaken radically the homogeneity requirement in the definition of a norm. Consider a nonnegative function $| |$ on a linear space $\mathcal{E}$ satisfying the following conditions:

(i) $| x | = 0$ if and only if $x = 0$,
(ii)$| \lambda x | \leq | x |$ for every $x$ in $\mathcal{E}$ and all complex numbers $\lambda$ such that $| \lambda | \leq 1$,
(iii)$| x + y | \leq | x | + | y |$ for all $x, y$ in $\mathcal{E}$.

It follows at once from (ii)$''$ that $| \lambda x | = | x |$ whenever $| \lambda | = 1$, and therefore that the function $

Conversely, if $\rho$ is an invariant metric on a linear space $\mathcal{E}$ that satisfies (9), and if $\alpha_n \to 0$ implies $\rho(\alpha_n x, 0) \to 0$ for every vector $x$ in $\mathcal{E}$, then there exists a unique quasinorm $|\rho|$ on $\mathcal{E}$ such that $\rho$ is defined by $|\rho|$.

It is an immediate consequence of condition (iv) in the definition of a quasinorm that the $\mathcal{E}$-valued mapping $\alpha \to \alpha x$ is continuous on $\mathbb{C}$ for each fixed vector $x$ in $\mathcal{E}$. Much more is true however.
