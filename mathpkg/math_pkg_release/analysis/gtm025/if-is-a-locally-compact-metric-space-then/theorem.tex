If $X$ is a locally compact metric space, then all functions in $\mathfrak{C}_0(X)$ are uniformly continuous. If $X$ is a compact metric space, then all functions in $\mathfrak{C}(X)$ are uniformly continuous.