Let $S$ be a set. Then:

(i) [TARSKI] the set $S$ is finite if and only if each nonvoid family of subsets of $S$ has a minimal member;

(ii) [DEDEKIND] the set $S$ is infinite if and only if $S$ is equivalent to some proper subset of itself.