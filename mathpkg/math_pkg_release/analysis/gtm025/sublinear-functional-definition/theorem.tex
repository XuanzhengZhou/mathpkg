Let $E$ be a real linear space. A function $p : E \to R$ is \textbf{sublinear} if (i) $p(x + y) \leq p(x) + p(y)$ (subadditive); (ii) $p(\alpha x) = \alpha p(x)$ for all $\alpha \geq 0$ (positive homogeneous).
