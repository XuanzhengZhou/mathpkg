Let $A \subset R$. An extended real-valued function $\alpha$ on $A$ is \textbf{nondecreasing} if $\alpha(x) \leq \alpha(y)$ whenever $x < y$ in $A$. It is \textbf{strictly increasing} if $\alpha(x) < \alpha(y)$ whenever $x < y$.
