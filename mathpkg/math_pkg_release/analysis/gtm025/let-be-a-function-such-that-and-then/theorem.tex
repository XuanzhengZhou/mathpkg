Let $f$ be a function such that $\text{dom}f = X$ and $\text{rng}f \subset Y$. Then

The equality (ii) is true because the following statements are pairwise equivalent: $x \in \text{dom} h$; $(x, y) \in h$ for some $y$; $(x, y) \in f$ for some $f \in \mathfrak{F}$; $x \in \text{dom} f$ for some $f \in \mathfrak{F}$.

Let $x \in \text{dom} h \cap \text{dom} f = \text{dom} f$ where $f \in \mathfrak{F}$. Then $(x, f(x)) \in f \subset h$ and $h$ is single-valued so $h(x) = f(x)$. This proves (iii).

The equality (iv) follows from the previous conclusions and (2.15.i) since $\text{rng} h = h (\text{dom} h) = h (\cup \{ \text{dom} f : f \in \mathfrak{F} \}) = \cup \{ h (\text{dom} f) : f \in \mathfrak{F} \} = \cup \{ f (\text{dom} f) : f \in \mathfrak{F} \} = \cup \{ \text{rng} f : f \in \mathfrak{F} \}$.