An orthonormal set $E$ in an inner product space $H$ is \textbf{complete} if the only vector orthogonal to all elements of $E$ is $0$, i.e., $\{x \in H : \langle x, z \rangle = 0 \text{ for all } z \in E\} = \{0\}$.
