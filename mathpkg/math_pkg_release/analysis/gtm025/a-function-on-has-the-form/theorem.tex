A function $f$ on $[a, b]$ has the form

$$f(x) = f(a) + \int_{a}^{x} \varphi(t) dt$$

for some $\varphi \in \mathfrak{L}_1([a, b])$ if and only if $f$ is absolutely continuous on $[a, b]$.

In this case we have $\varphi(x) = f'(x)$ a.e. on $]a, b[$.