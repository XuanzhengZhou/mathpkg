For $1 \leq p < \infty$, a sequence $(f_n)$ in $\mathfrak{L}_p$ \textbf{converges weakly} to $f \in \mathfrak{L}_p$ if $\lim_n \int_X f_n g d\mu = \int_X f g d\mu$ for all $g \in \mathfrak{L}_q$ where $\frac{1}{p} + \frac{1}{q} = 1$.
