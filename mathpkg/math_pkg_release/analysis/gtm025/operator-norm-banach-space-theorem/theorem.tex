Let $A, B$ be normed linear spaces over $F$. Then $\mathfrak{B}(A, B)$, the set of all bounded linear transformations from $A$ to $B$, with the operator norm $\|T\| = \sup\{\|T(x)\| : \|x\| \leq 1\}$, is a normed linear space. It is a Banach space if $B$ is a Banach space.
