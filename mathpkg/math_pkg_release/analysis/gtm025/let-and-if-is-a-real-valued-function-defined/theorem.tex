Let $a \in R$ and $\delta > 0$. If $\varphi$ is a real-valued function defined on $]a, a + \delta[$, define

$$\lim_{h \to a} \varphi(h) = \sup \left\{ \inf

These two extended real numbers are called the lower right limit and the upper right limit of $\varphi$ at $a$ respectively. If $\varphi$ is a real-valued function defined on $]a - \delta, a[$, define the lower left limit and the upper left limit of $\varphi$ at $a$ to be the extended real numbers

$$\lim_{h \uparrow a} \varphi(h) = \sup \left\{ \inf \left\{ \varphi(h) : t < h < a \right\} : a - \delta \leq t < a \right\}$$

and

$$\lim_{h \uparrow a} \varphi(h) = \inf \left\{ \sup \left\{ \varphi(h) : t < h < a \right\} : a - \delta \leq t < a \right\}$$

respectively.