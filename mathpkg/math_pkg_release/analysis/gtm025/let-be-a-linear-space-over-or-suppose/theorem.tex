Let $E$ be a linear space over $K$ [or $R$]. Suppose that there is a function $x \rightarrow \|x\|$ with domain $E$ and range contained in $R$ such that:

(i) $\|0\| = 0$ and $\|x\| > 0$ if $x \neq 0$;

(ii) $\|\alpha x\| = |\alpha| \|x\|$ for all $