Let $\iota$ be any measure on $R$ as in §9 and let $1 \leq p < \infty$. Then the step functions on $R$ form a dense subset of $\mathf

intervals, $s$ is a step function. Also

$$\|\sigma - s\|_p = \left\| \sum_{j=1}^{m} \beta_j \left( \xi_{B_j} - \xi_{V_j} \right) \right\|_p$$

$$\leq \sum_{j=1}^{m} |\beta_j| \cdot \|\xi_{B_j} - \xi_{V_j}\|_p < (2\delta)^{\frac{1}{p}} \left( \sum_{j=1}^{m} |\beta_j| \right) < \frac{\varepsilon}{2},$$

and so

$$\|f - s\|_p \leq \|f - \sigma\|_p + \|\sigma - s\|_p < \varepsilon.$$

The norm in the spaces $\mathfrak{L}_p(R, \mathcal{M}_h, \lambda)$ has an interesting and useful continuity property, which we now establish. Recall the definition (8.14) of the translate $f_h$ of a function $f$ on $R$ by a real number $h$.