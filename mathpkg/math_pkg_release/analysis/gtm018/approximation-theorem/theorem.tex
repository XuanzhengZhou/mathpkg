If $\mu$ is a $\sigma$-finite measure on a ring $\mathbf{R}$, then for every set $E$ in $\mathbf{S}(\mathbf{R})$ of finite measure and for every $\epsilon > 0$, there exists a set $F$ in $\mathbf{R}$ such that $\mu(E \Delta F) < \epsilon$.
