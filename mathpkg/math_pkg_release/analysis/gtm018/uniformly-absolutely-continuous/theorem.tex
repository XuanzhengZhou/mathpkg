A sequence $\{\nu_n\}$ of finite valued set functions defined for all measurable sets has **uniformly absolutely continuous** terms if for every $\epsilon > 0$ there exists $\delta > 0$ such that $|\nu_n(E)| < \epsilon$ for every measurable set $E$ with $\mu(E) < \delta$, and for every positive integer $n$.
