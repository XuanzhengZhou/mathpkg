If $\mu$ and $\nu$ are two measures such that $(X, S, \mu)$ and $(X, S, \nu)$ are measurable groups, and if $E \in S$ is such that $0 < \nu(E) < \infty$, then, for every nonnegative measurable function $f$ on $X$,
$$
\int f(x)\, d\mu(x) = \mu(E) \int \frac{f(y^{-1})}{\nu(Ey)}\, d\nu(y).
$$
