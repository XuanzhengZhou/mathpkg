If $\{f_n\}$ is a non decreasing sequence of non negative, extended real valued, measurable functions converging almost everywhere to a measurable function $f$, then
$$\int f \,d\mu = \lim_n \int f_n \,d\mu.$$
