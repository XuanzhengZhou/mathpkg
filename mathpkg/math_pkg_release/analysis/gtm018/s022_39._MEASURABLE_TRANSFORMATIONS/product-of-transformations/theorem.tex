If $T$ is a transformation from $X$ into $Y$ and $S$ is a transformation from $Y$ into $Z$, the product of $S$ and $T$, in symbols $ST$, is the transformation from $X$ into $Z$ defined by $(ST)(x) = S(T(x))$.
