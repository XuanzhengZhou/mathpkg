If $T$ is a measurable transformation from $(X, \mathbf{S})$ into $(Y, \mathbf{T})$, and if $g$ is an extended real valued measurable function on $Y$, then $gT$ is measurable with respect to $T^{-1}(\mathbf{T})$.
