If $T$ is a measurable transformation from $(X, \mathbf{S})$ into $(Y, \mathbf{T})$, and if a function $f$ on $X$ is measurable with respect to $T^{-1}(\mathbf{T})$, then $f(x_1) = f(x_2)$ whenever $T(x_1) = T(x_2)$.
