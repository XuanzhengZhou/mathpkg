If $\mu$ is a signed measure, if $\{E_n\}$ is a monotone sequence of measurable sets, and if, in case $\{E_n\}$ is a decreasing sequence, $|\mu(E_n)| < \infty$ for at least one value of $n$, then
$$\mu(\lim_n E_n) = \lim_n \mu(E_n).$$
