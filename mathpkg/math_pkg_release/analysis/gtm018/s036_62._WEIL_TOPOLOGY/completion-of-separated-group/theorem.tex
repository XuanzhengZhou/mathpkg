If $X$ is a separated measurable group, then $X$ is dense in its Weil completion $\hat{X}$, a locally compact group. The measure $\mu$ on $X$ extends to a Baire measure $\hat{\mu}$ on $\hat{X}$ defined by $\hat{\mu}(\hat{E}) = \mu(\hat{E} \cap X)$ for every Baire set $\hat{E}$ in $\hat{X}$.
