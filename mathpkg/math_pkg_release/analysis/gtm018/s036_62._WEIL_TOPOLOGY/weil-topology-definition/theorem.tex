Let $X$ be a separated measurable group. The topology on $X$ obtained by taking the class $N = \{\{x : \rho(xE, E) < \epsilon\} : \text{$E$ measurable, $0 < \mu(E) < \infty$, $0 < \epsilon < 2\mu(E)$}\}$ as a base at the identity $e$ is called the \emph{Weil topology}.
