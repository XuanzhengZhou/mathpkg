Since the class of all subsets of $X$ is a monotone class, and the intersection of any collection of monotone classes is a monotone class, we may define the monotone class $\mathbf{M}(\mathbf{E})$ \text{generated by} any class $\mathbf{E}$ of sets as the smallest monotone class containing $\mathbf{E}$.
