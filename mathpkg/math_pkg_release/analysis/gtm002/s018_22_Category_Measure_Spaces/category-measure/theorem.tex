A finite measure $\mu$ defined on the $\sigma$-algebra $S$ of subsets of a topological space $X$ having the property of Baire is called a \textbf{category measure} if $\mu(E) = 0$ if and only if $E$ is of first category. The triple $(X, S, \mu)$ is then called a \textbf{category measure space}.
