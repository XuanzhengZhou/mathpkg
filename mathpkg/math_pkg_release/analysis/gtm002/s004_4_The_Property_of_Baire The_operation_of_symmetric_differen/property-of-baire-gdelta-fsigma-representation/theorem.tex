A set has the property of Baire if and only if it can be represented as a $G_\delta$ set plus a set of first category (or as an $F_\sigma$ set minus a set of first category). More precisely, any set with the property of Baire can be represented as the disjoint union of a $G_\delta$ set and a set of first category.
