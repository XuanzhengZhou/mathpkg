The metric space $(C[a,b], \sigma)$, where $\sigma(f, g) = \int_a^b |f(x) - g(x)| \, dx$ is the integral metric, is not complete. There exists a Cauchy sequence in $(C[a,b], \sigma)$ that does not converge to any continuous function.
