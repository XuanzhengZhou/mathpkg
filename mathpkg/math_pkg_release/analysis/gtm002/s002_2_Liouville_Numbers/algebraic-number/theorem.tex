A complex number $z$ is called \textbf{algebraic} if it satisfies some equation of the form
$$a_0 + a_1 z + a_2 z^2 + \cdots + a_n z^n = 0$$
with integer coefficients $a_i \in \mathbb{Z}$, not all zero.
