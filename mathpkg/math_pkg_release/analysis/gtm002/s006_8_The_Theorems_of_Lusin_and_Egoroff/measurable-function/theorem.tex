A real-valued function $f$ on $\mathbb{R}$ is called \emph{measurable} if $f^{-1}(U)$ is measurable for every open set $U$ in $\mathbb{R}$. Equivalently, $U$ may be restricted to a countable base for the topology of $\mathbb{R}$, or allowed to run over all Borel sets.
