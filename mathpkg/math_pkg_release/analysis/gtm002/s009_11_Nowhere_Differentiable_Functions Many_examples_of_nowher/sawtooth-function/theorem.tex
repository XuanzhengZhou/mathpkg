The saw-tooth function $\phi : \mathbb{R} \to [0, 1/2]$ is defined by

$$\phi(x) = \min(x - [x], [x] + 1 - x),$$

i.e., the distance from $x$ to the nearest integer. Here $[x]$ denotes the greatest integer $\leq x$.
