A set $A$ is \text{nowhere dense} if it is dense in no interval, i.e., if every interval has a subinterval contained in the complement of $A$. Equivalently, $A$ is nowhere dense if and only if $A^\prime$ contains a dense open set, and if and only if $\bar{A}$ has no interior points.
