Any property that is definable in terms of open sets alone is a \textbf{topological property}. For instance, convergence is a topological property, because $x_n \to x$ if and only if every open set that contains $x$ contains all but a finite number of terms of the sequence.
