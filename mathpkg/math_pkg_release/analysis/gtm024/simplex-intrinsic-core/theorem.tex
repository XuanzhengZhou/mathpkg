Let $A$ be an $n$-simplex in $X$ with vertices $\{x_0, x_1, \ldots, x_n\}$. Then $\operatorname{icr}(A)$ consists of all points in $A$ each of whose barycentric coordinates is positive. In particular, $\operatorname{icr}(A) \neq \emptyset$.
