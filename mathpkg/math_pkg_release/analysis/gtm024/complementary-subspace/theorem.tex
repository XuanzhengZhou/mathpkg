A \emph{complementary subspace} to a subspace $M \subset X$ is a subspace $N$ such that $M \oplus N = X$. The associated \emph{projection} $P: X \to M$ along $N$ is defined by $P(m+n) = m$ for $m \in M, n \in N$.
