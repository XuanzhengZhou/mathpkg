Let $A$ be an absorbing set in $X$. (a) $\rho_A$ is positively homogeneous; (b) if $A$ is convex, $\rho_A$ is sublinear; (c) if $A$ is balanced, $\rho_A(\lambda x) = |\lambda|\rho_A(x)$ for all scalars $\lambda$.
