A finite set $\{x_0, x_1, \ldots, x_n\} \subset X$ is \emph{affinely independent} (in general position) if $\{x_1-x_0, \ldots, x_n-x_0\}$ is linearly independent. The convex hull is an \emph{$n$-simplex} with \emph{barycentric coordinates} as the unique convex combination coefficients.
