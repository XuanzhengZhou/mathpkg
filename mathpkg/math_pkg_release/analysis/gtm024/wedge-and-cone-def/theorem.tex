A wedge $P$ in a real linear space $X$ is a convex set closed under multiplication by non-negative scalars. It defines a vector ordering $x \leqslant y \iff y - x \in P$. A wedge $P$ is a cone if $P \cap (-P) = \{\theta\}$, which is equivalent to the induced ordering being anti-symmetric.