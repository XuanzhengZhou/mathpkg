Let $X$ be a linear space over $\mathbb{F}$. The algebraic conjugate $X'$ is the set of all linear maps $\phi: X \to \mathbb{F}$, with $(\phi+\psi)(x) = \phi(x)+\psi(x)$ and $(\alpha\phi)(x) = \alpha\phi(x)$. Elements of $X'$ are called linear functionals on $X$.