Let $\mathbb{C}$ be the family of all convex circled subsets of a linear space $E$, and for each subfamily $A$ of $\mathbb{C}$, let $A_~$ be the family of members of $\mathbb{C}$ which are absorbed by each member of $A$, and $A^~$ the family of members of $\mathbb{C}$ which absorb each member of $A$. Then for subfamilies $A$ and $B$ of $\mathbb{C}$:

(i) if $A \supset B$, then $A_~ \subset B_~$ and $A^~ \subset B^~$;

(ii) $A \subset A^{~_~}$ and $A \subset A_~^~$;

(iii) $A^{~_~^~} = A_~$ and $A^{~_~^~^~} = A^{~_~}$;

(iv) if each member of $A$ is radial at $0$, then $A_~$ covers $E$; if $A$ covers $E$, then each member of $A^~$ is radial at $0$;

(v) each member of $\mathbb{C}$ which is absorbed by a member of $A_~$, each scalar multiple of a member of $A_~$, and the convex extension of the union of any two members of $A_~$, belongs to $A_~$;

(vi) each member of $\mathbb{C}$ which absorbs a member of $A^~$, each non-zero scalar multiple of a member of $A^~$, and the intersection of any two members of $A^~$, belongs to $A^~$.
