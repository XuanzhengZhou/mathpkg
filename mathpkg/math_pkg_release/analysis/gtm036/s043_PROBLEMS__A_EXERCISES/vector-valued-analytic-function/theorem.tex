Let $D$ be a domain in the complex plane and let $E$ be a sequentially complete locally convex Hausdorff space. A mapping $x : D \to E$ is called \textit{analytic} in $D$ if and only if, for each $z \in D$, the limit
$$\lim_{\zeta \to 0} \frac{x(z + \zeta) - x(z)}{\zeta}$$
exists in $E$.
