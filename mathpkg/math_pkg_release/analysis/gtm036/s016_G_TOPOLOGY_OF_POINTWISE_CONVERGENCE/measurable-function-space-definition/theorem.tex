Let $(X, \mathcal{S}, \mu)$ be a finite measure space, and let $S(X, \mu)$ be the space of all real valued measurable functions on $X$. For each positive integer $n$, let
$$
U_n = \{ f : \mu(\{ x : |f(x)| > 1/n \}) < 1/n \}.
$$
