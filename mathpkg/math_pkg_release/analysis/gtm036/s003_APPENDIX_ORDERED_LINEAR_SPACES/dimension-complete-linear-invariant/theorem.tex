Each linear space is characterized, to a linear isomorphism, by a single cardinal number called its dimension. Two linear spaces over the same field are linearly isomorphic if and only if they have the same dimension.
