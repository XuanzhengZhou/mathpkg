If $A$ and $B$ are non-void subsets of a linear space $E$, then
$$
\langle A \cup B \rangle = \bigcup \{[x:y] : x \in \langle A \rangle,\; y \in \langle B \rangle\},
$$
where $[x:y]$ denotes the line segment joining $x$ and $y$.
