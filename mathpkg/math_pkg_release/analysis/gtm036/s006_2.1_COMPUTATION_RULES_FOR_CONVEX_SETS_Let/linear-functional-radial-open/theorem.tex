If $f$ is a linear functional on a linear space $E$ which is not identically zero and $A \subset E$ is radial at $x$, then $f[A]$ is radial at $f(x)$. Moreover, if $A$ is convex and radial at each of its points, then $f[A]$ is open.
