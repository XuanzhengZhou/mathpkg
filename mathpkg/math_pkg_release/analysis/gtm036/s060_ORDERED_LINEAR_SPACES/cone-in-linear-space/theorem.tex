A \textbf{cone} in a real linear space $E$ is a convex subset of $E$ that is closed under multiplication by non-negative scalars. That is, $C \subseteq E$ is a cone if and only if:
\begin{itemize}
  \item $C$ is convex;
  \item For all $x \in C$ and $t \geq 0$, $tx \in C$.
\end{itemize}
