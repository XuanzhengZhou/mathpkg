The null space of a non-identically-zero linear functional $f$ on a real linear space $E$ is a maximal proper subspace of $E$. Each translate of a maximal proper subspace of a linear space is called a hyperplane.

Each hyperplane is a linear manifold, and is contained in no other linear manifold except the space $E$ itself. Each hyperplane is the intersection of two complementary half-spaces.
