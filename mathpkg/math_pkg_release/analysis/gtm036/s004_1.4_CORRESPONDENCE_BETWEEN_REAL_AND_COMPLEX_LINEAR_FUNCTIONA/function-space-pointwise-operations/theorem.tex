If $F$ is a linear space and $X$ is a set, then the family of all functions on $X$ to $F$ is a linear space, with addition and scalar multiplication defined pointwise:
$$
(f + g)(x) = f(x) + g(x), \qquad (a f)(x) = a f(x).
$$
