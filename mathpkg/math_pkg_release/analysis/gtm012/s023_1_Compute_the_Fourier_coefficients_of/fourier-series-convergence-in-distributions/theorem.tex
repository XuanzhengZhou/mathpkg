Let $F \in \mathcal{P}'$ have Fourier coefficients $(a_n)_{n=-\infty}^{\infty}$.
For each $N \in \mathbb{N}$, let $F_N \in \mathcal{P}'$ be the distribution defined by the function
$$x \mapsto \sum_{n=-N}^{N} a_n \exp(inx).$$
Then
$$F_N \to F \quad \text{in } \mathcal{P}'.$$
