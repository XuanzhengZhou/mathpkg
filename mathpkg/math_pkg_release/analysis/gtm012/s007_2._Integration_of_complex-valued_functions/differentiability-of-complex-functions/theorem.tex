\textbf{Proposition 3.2.} The function $f: (a, b) \to \mathbb{C}$ is differentiable at $x \in (a, b)$ if and only if the real and imaginary parts $g = \operatorname{Re} f$ and $h = \operatorname{Im} f$ are differentiable at $x$. If so, then
$$f'(x) = g'(x) + i h'(x).$$
