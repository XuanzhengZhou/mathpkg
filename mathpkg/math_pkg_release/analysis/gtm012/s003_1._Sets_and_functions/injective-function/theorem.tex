A function $f: A \to B$ is said to be injective (or one-to-one, written 1-1) if it assigns distinct elements of $B$ to distinct elements of $A$. Equivalently,
$$\text{if } x, y \in A \text{ and } x \neq y, \text{ then } f(x) \neq f(y).$$
