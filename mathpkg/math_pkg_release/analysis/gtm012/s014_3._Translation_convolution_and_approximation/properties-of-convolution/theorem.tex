If $u, v, w \in \mathcal{C}$ and $a \in \mathbb{C}$, then $u * v \in \mathcal{C}$ and the following hold:
\begin{align}
u * v &= v * u, \\
(au) * v &= a(u * v), \\
(u + v) * w &= u * w + v * w, \\
(u * v) * w &= u * (v * w), \\
T_t(u * v) &= (T_t u) * v = u * (T_t v).
\end{align}
