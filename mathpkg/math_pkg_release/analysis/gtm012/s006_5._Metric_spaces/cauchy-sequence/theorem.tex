Suppose $(S, d)$ is a metric space. A sequence $(x_n)_{n=1}^{\infty}$ in $S$ is a Cauchy sequence if for every $\varepsilon > 0$ there exists a positive integer $N$ such that

$$d(x_n, x_m) < \varepsilon \quad \text{whenever } n, m \geq N.$$
