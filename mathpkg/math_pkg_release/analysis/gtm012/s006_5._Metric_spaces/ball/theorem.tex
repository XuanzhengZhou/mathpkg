Suppose $(S, d)$ is a metric space, $x$ is a point of $S$, and $r$ is a positive real number. The ball of radius $r$ about $x$ is defined to be

$$B_r(x) = \{ y \mid y \in S,\ d(x, y) < r \}.$$

Clearly $x \in B_r(x)$. If $0 < r < s$, then $B_r(x) \subset B_s(x)$.
