For $U \subset X$ open, set $M_U := \hat{\Gamma}(U, \mathcal{S})$. If $U, V \subset X$ are open with $V \subset U$, define the restriction map
$$
r_V^U : M_U \to M_V, \qquad r_V^U(s) := s|_V.
$$
Then $\{M_U, r_V^U\}$ is a pre-sheaf, and the corresponding sheaf is denoted by $W(\mathcal{S})$.
