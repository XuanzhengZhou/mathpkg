Let $p(X_1, \ldots, X_s)$ be a symmetric polynomial with integer coefficients. Then there is exactly one polynomial $Q(Y_1, \ldots, Y_s)$ with integer coefficients such that $p(X_1, \ldots, X_s) = Q(e_1, \ldots, e_s)$ where $e_k$ are the elementary symmetric polynomials.
