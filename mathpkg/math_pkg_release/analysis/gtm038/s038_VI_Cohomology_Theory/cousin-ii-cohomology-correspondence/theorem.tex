\textbf{Theorem 4.7.} Let $(U_t, f_t)_{t \in I}$ be a Cousin II
distribution on $X$, and let $\mathfrak{U} = (U_t)_{t \in I}$ be the
corresponding open covering.

\textit{[Statement incomplete --- the theorem is truncated at the section
boundary. The continuation is expected to state that the transition functions
$h_{t_0 t_1}$ define a cocycle in $Z^1(\mathfrak{U}, \mathcal{O}^*)$, and that
the distribution is solvable if and only if this cocycle is a coboundary.]}
