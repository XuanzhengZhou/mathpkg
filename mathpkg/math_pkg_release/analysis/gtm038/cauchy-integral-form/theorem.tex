Let $g$ be continuously differentiable on a polycylinder $P$, $f := \partial g / \partial \overline{z}$ bounded. Then for $w \in P$, $g(w) = \operatorname{ch}(g|T)(w) + \int_P f(z) K(z,w) dV$ where $K(z,w)$ is the Cauchy kernel.
