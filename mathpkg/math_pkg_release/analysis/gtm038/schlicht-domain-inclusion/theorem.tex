Let $\mathfrak{X}^* = (X_\nu)_{\nu \in N}$ be the finest partition defining an equivalence relation on the union of all domains containing a given base point, compatible with the projection. Then $\tilde{G} := \{X_\nu : \nu \in N\}$ forms the maximal Riemann domain for the given family of functions.
