A domain $\mathfrak{G} = (G, \pi, x_0)$ over $\mathbb{C}^n$ is called a domain of holomorphy if there exists an $f \in A(G)$ (holomorphic on $G$) with $H_f(\mathfrak{G}) = \mathfrak{G}$, i.e., $f$ cannot be extended to any larger domain.
