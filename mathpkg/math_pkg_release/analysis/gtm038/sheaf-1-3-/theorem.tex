Let $(S, \pi)$ be a sheaf over $B$, $W \subset B$ open and $s: W \rightarrow S$ a continuous mapping with $\pi \circ s = \text{id}_W$. Then $s$ is called a section of $S$ over $W$. We denote the set of all sections of $S$ over $W$ by $\Gamma(W, S)$.
