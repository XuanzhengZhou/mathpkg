Let $B \subset \mathbb{C}^n$ be a region, $\mathfrak{z}_0 \in B$ a point. The set
$$C_B(\mathfrak{z}_0) := \{ \mathfrak{z} \in B: \mathfrak{z} \text{ and } \mathfrak{z}_0 \text{ can be joined by a path in } B \}$$
is called the component of $\mathfrak{z}_0$ in $B$.
