Let $M$ be a subset of $\mathbb{C}^n$, $z_0 \in M$, $f$ a complex function on $M$. One says that the power series $\sum_{v=0}^{\infty} a_v(z_1 - z_0)^v$ converges uniformly on $M$ to $f(z)$ if for each $\varepsilon > 0$ there is a finite set $I_0 \subset \Im$ such that
