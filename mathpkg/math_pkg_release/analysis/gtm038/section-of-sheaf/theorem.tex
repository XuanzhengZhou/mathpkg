Let $(S, \pi)$ be a sheaf over $B$, $W \subset B$ open and $s: W \to S$ a continuous mapping with $\pi \circ s = \operatorname{id}_W$. Then $s$ is called a section of $S$ over $W$. We denote $\Gamma(W, S)$ the set of all sections of $S$ over $W$.
