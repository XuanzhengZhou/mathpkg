Let $G \subset \mathbb{C}^n$ be a domain, let $f_1, \ldots, f_{n-k}$ be holomorphic functions in $G$, and set
$$M := \{ \mathfrak{z} \in G : f_1(\mathfrak{z}) = \cdots = f_{n-k}(\mathfrak{z}) = 0 \}.$$
If $M' \subset M$ is an irreducible component, then
$$\dim_{\mathbb{C}}(M') \geq k.$$
