Let $(P, H)$ be a Euclidean Hartogs figure in $\mathbb{C}^n$, $g := (g_1, \ldots, g_n)$: $P \rightarrow \mathbb{C}^n$ be a biholomorphic mapping, and let $\tilde{P} := g(P), \tilde{H} := g(H)$. Then $(\tilde{P}, \tilde{H})$ is called a general Hartogs figure.
