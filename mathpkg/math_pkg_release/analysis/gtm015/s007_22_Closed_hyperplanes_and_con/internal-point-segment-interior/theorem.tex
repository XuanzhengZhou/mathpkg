Let $E$ be a TVS over $\mathbb{K}$ and let $A$ be a convex subset of $E$ with nonempty interior. Suppose $a \in \operatorname{int} A$ and $b \in \overline{A}$. Then every internal point of the segment from $a$ to $b$ is an interior point of $A$.