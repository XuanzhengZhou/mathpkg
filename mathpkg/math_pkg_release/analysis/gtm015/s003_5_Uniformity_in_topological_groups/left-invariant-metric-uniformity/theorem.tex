If $$G$$ is a metrizable topological group and if $$d$$ is a left-invariant compatible metric on $$G$$, then the uniform structure $$(G, \mathcal{U}_d)$$ derived from $$d$$ coincides with the left uniform structure $$(G, \mathcal{U}_s)$$.
