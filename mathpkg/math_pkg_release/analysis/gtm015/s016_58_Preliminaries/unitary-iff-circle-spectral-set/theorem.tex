The following conditions on $T \in \mathcal{L}(H)$ are equivalent:
\begin{enumerate}
\item[(a)] $T$ is unitary ($T^*T = TT^* = I$);
\item[(b)] the unit circle $\mathbb{T} = \{\lambda \in \mathbb{C} : |\lambda| = 1\}$ is a spectral set for $T$.
\end{enumerate}
