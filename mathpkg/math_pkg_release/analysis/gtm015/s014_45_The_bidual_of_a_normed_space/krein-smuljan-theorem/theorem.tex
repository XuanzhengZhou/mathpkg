Let $E$ be a Banach space and let $K$ be a convex subset of $E'$. The following conditions on $K$ are equivalent:
\begin{enumerate}
\item[(a)] $K$ is weak* closed;
\item[(b)] for every $r > 0$, the set $K \cap B_r$ is weak* closed, where
$$
B_r = \{ f \in E' : \|f\| \leq r \}.
$$
\end{enumerate}
