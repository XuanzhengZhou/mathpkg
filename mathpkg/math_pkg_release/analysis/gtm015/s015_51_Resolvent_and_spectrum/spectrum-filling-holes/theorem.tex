If $x \in B \subset A$, where $B$ is a closed subalgebra of $A$ containing $1$, then
$$\sigma_B(x) = \sigma_A(x) \cup W,$$
where $W$ is the union of certain of the holes of $\sigma_A(x)$. Moreover, $\rho_A(x)$ and $\rho_B(x)$ have the same unbounded component.
