\textbf{Theorem 2.6.}

(a) Let $M$ be a maximal ideal in $C(X)$; if $Z(f)$ meets every member of $Z[M]$, then $f \in M$.

(b) Let $\mathcal{A}$ be a $z$-ultrafilter on $X$; if a zero-set $Z$ meets every member of $\mathcal{A}$, then $Z \in \mathcal{A}$.
