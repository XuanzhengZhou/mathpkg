If $\tau$ is a homeomorphism, then $\tau_0$ carries $X_0 - X$ to $Y - \tau[X]$.

In particular, every compactification $Y$ of $X$ is a continuous image of $\beta X$, under a mapping that leaves $X$ pointwise fixed, and carries $\beta X - X$ to $Y - X$.
