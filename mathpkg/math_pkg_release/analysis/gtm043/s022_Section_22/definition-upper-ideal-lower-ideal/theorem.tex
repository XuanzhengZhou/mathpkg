A prime ideal is called an \emph{upper ideal} if the set of all its predecessors (in the partial order under set inclusion) has a maximal element. Briefly, an upper ideal is one that has an immediate predecessor.

Similarly, a \emph{lower ideal} is a prime ideal that has an immediate successor.
