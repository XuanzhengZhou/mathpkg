In a completely regular space, every $G_{\delta}$ containing a compact set $S$ contains a zero-set containing $S$.

In particular, every compact $G_{\delta}$ in a completely regular space is a zero-set. A special case: every $G_{\delta}$-point is a zero-set.
