The mapping $Z$ is one-one from the set of all maximal ideals in $C$ onto the set of all $z$-ultrafilters.

In particular:
\begin{enumerate}
\item[(b)] If $\mathcal{A}$ is a $z$-ultrafilter on $X$, then $Z^{\leftarrow}[\mathcal{A}]$ is a maximal ideal in $C$.
\end{enumerate}
