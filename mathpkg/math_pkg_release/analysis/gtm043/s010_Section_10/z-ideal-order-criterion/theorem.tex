If $I$ is a $z$-ideal in $C(X)$, then $I(f) \geq 0$ if and only if $f$ is nonnegative on some zero-set of $I$.

Moreover, when $M$ is a maximal ideal in $C(X)$,
$$
M(f) \geq 0 \quad [\text{resp. } > 0] \text{ if and only if } f \text{ is nonnegative [resp. positive] on some zero-set of } M.
$$
