Every prime ideal $P$ in $C(X)$ [resp. $C^*(X)$] is absolutely convex, and the residue class ring $C/P$ [resp. $C^*/P$] is totally ordered. Furthermore, the mapping $r \mapsto P(r)$ is an order-preserving isomorphism of the real field $\mathbf{R}$ into the residue class ring.
