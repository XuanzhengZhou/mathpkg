If $I$ is a $z$-ideal in $C(X)$, then $C/I$ is totally ordered if and only if $I$ is prime.

Equivalently, for a $z$-ideal $I$, the residue class ring is totally ordered precisely when, for every $f \in C(X)$, there exists a zero-set of $I$ on which $f$ does not change sign.
