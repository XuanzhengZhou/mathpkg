A mapping $\varphi$ from $X$ into $Y$ is said to be **closed** if for every closed subset $A$ of $X$, $\varphi[A]$ is a closed set in $Y$. (It is not enough that $\varphi[A]$ be closed in $\varphi[X]$.)
