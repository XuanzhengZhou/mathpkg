Every closed set in a metric space is $C^*$-embedded. In particular, every closed set in $\mathbf{R}$ is $C^*$-embedded in $\mathbf{R}$.

This result is \textbf{Tietze's Extension Theorem}. Urysohn's generalization to normal spaces will be discussed in Chapter 3.
