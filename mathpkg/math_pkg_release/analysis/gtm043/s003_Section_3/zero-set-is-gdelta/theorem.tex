Every zero-set is a $G_\delta$, i.e., a countable intersection of open sets:
$$Z(f) = \bigcap_{n \in \mathbb{N}} \{x \in X: |f(x)| < 1/n\}.$$

Conversely, in a normal space, every closed $G_\delta$ is a zero-set. In a metric space, every closed set is a zero-set.
