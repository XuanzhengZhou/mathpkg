Every (ring) homomorphism $t$ from $C(Y)$ or $C^*(Y)$ into $C(X)$ is a lattice homomorphism. That is, for all $g, h$ in the domain:
$$t(g \vee h) = tg \vee th, \qquad t(g \wedge h) = tg \wedge th.$$
In particular, $t$ preserves absolute values: $t|g| = |tg|$.
