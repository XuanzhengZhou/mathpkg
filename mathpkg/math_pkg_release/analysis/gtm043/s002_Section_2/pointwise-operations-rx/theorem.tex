Addition and multiplication on $\mathbf{R}^X$ are defined pointwise:

$$(f + g)(x) = f(x) + g(x), \qquad (fg)(x) = f(x)g(x).$$

Both operations are associative and commutative, and the distributive law holds.
