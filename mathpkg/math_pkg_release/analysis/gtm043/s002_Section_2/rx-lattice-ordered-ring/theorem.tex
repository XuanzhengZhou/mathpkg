The ring $\mathbf{R}^X$ with the pointwise partial ordering is a lattice-ordered ring. The absolute value $|f|$, defined by $|f| = f \vee -f$, satisfies:
$$|f|(x) = |f(x)|.$$
Although $\mathbf{R}$ is totally ordered, $\mathbf{R}^X$ is not totally ordered when $X$ contains at least two points.
