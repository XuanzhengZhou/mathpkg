A topology $\mathfrak{T}$ on a vector space $L$ is called \textbf{translation-invariant} if all translations $x \mapsto x + x_0$ (for each $x_0 \in L$) are homeomorphisms. Such a topology is completely determined by the neighborhood filter of any point $x \in L$, in particular by the neighborhood filter of $0$.
