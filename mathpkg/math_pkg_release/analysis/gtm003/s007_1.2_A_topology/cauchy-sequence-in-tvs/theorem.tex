A sequence $\{x_n : n \in \mathbb{N}\}$ in a subset $A$ of a t.v.s. $L$ is a \textbf{Cauchy sequence} if and only if for each $0$-neighborhood $V$ in $L$ there exists $n_0 \in \mathbb{N}$ such that $x_m - x_n \in V$ whenever $m \geq n_0$ and $n \geq n_0$.
