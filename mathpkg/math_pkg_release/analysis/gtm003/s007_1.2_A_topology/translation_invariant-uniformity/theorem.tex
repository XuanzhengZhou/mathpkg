A uniformity on a vector space $L$ is called \textbf{translation-invariant} if it has a base $\mathfrak{N}$ such that $(x, y) \in N$ is equivalent with $(x + z, y + z) \in N$ for each $z \in L$ and each $N \in \mathfrak{N}$.
