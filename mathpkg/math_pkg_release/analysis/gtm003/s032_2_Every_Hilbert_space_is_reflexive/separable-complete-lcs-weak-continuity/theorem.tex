Let $E$ be a separable, complete locally convex space and let $f$ be a linear form on the dual $E'$. For $f$ to be $\sigma(E', E)$-continuous it suffices that $\lim_n f(x_n') = 0$ whenever $\{x_n'\}$ is a null sequence for $\sigma(E', E)$.