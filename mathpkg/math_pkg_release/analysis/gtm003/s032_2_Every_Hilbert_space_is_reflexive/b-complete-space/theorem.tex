A locally convex space $E$ is defined to be $B$-complete (or a Ptak space) if a subspace $Q \subset E'$ is closed for $\sigma(E', E)$ whenever $Q \cap A$ is $\sigma(E', E)$-closed in $A$ for each equicontinuous set $A \subset E'$.
