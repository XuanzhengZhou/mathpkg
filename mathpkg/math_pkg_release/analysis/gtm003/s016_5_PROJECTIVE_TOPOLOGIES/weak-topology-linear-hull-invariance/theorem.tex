Let $E$ be a vector space over $K$ and let $F$ be a non-empty subset of $E^*$. Then $F$ can be replaced by its linear hull $\operatorname{span}(F)$ in $E^*$ without changing the corresponding projective topology, i.e.,
$$\sigma(E, F) = \sigma(E, \operatorname{span}(F)).$$
