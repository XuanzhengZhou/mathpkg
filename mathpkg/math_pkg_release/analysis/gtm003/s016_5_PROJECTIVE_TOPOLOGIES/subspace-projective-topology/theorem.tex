Let $M$ be a subspace of the locally convex space $(E, \mathfrak{T})$. The topology of $M$ (i.e., the topology induced on $M$ by $\mathfrak{T}$) is the projective topology on $M$ with respect to the canonical imbedding $M \rightarrow E$.
