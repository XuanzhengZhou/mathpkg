For a subset $A$ of a vector space $L$ over $K$, the circled hull of $A$ is the intersection of all circled subsets of $L$ containing $A$. It is the smallest circled set containing $A$, given by $\bigcup_{|\lambda| \leq 1} \lambda A$.
