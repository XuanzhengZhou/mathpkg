Let $E$ be a t.v.s. and $M$ a subspace of $E$. The quotient topology on $E/M$ is the inductive topology with respect to the natural (or canonical) map of $E$ onto $E/M$. This topology is Hausdorff if and only if $M$ is closed in $E$.
