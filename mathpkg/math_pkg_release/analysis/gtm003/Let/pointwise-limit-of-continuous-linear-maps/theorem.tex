Let $E$ be a barreled l.c.s. or a Baire t.v.s., and let $\{u_n\}$ be a sequence in $\mathcal{L}(E, F)$ converging pointwise to a map $u$. Then $u \in \mathcal{L}(E, F)$ and the convergence is uniform on every precompact subset of $E$.
