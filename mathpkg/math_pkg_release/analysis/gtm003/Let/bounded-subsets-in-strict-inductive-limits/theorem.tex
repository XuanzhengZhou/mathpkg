Let $E(\mathfrak{T}) = \varinjlim_{n \to \infty} E_n(\mathfrak{T}_n)$ and let $E_n$ be closed in $E_{n+1}(\mathfrak{T}_{n+1})$ $(n \in N)$. A subset $B \subset E$ is bounded in $E(\mathfrak{T})$ if and only if for some $n \in N$, $B$ is a bounded subset of $E_n(\mathfrak{T}_n)$.
