Let $E$ be a locally convex space over $\mathbb{C}$ such that the closed, convex hull of each compact subset of $E$ is compact (which is, in particular, the case if $E$ is quasi-complete). Then each continuous function $f: \delta \rightarrow E$ on a rectifiable oriented arc $\delta$ is Riemann integrable over $\delta$.
