An algebra $A$ over $K$ ($K = \mathbb{R}$ or $\mathbb{C}$) is called a \emph{locally convex algebra over $K$} if the underlying vector space $A_0$ is a locally convex space and if multiplication $(a, b) \mapsto ab$ is separately continuous.
