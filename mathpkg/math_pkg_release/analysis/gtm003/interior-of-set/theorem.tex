Let $(X, \mathfrak{T})$ be a topological space and $A \subset X$. The \text{interior} of $A$, denoted $\mathring{A}$ (or $\operatorname{int} A$), is the open set which is the union of all open subsets of $A$. An element $x \in \mathring{A}$ is called an \text{interior point} of $A$.
