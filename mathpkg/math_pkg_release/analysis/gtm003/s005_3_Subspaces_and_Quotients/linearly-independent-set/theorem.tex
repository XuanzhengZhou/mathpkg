A subset $A \subset L$ is called \text{linearly independent} if for every non-empty finite subset $\{x_i : i = 1, \ldots, n\}$ of $A$, the relation $\sum_{i=1}^{n} \lambda_i x_i = 0$ implies $\lambda_i = 0$ for $i = 1, \ldots, n$. The empty subset of $L$ is linearly independent.
