Let $f: X \rightarrow Y$ be a mapping and $A \subset X$. The mapping $g: A \rightarrow Y$ defined by $g(x) = f(x)$ for all $x \in A$ is called the \text{restriction} of $f$ to $A$ and is frequently denoted by $f_A$. Conversely, $f$ is called an \text{extension} of $g$ (to $X$ with values in $Y$).
