Let $E, F$ be vector spaces over $K$ with dimensions $d_1 = \dim E^*$, $d_2 = \dim F$. The canonical isomorphism $E \otimes F \to L(E^*, F)$ (from part (a)) is onto $L(E^*, F)$ if and only if at least one of the cardinals $d_1, d_2$ is finite.
