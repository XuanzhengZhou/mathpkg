The dual of $(L, \mathfrak{T}_O)$ is the space $L^b$ of all order-bounded linear forms on $L$. That is, a linear form $f$ on $L$ is continuous for $\mathfrak{T}_O$ if and only if $f$ is bounded on every order interval.
