A linear form $\varphi$ on an ordered vector space $E$ (in particular, on a $C^*$-algebra $A$) is called \textit{order continuous} if for every directed $(\leq)$ set $S \subset E$, $\sup S = z$ implies $\lim_S \varphi(x) = \varphi(z)$.
