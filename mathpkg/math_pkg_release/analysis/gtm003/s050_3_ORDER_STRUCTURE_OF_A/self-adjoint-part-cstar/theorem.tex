Given a $C^*$-algebra $A$, the self-adjoint part of $A$ is defined as
$$A_{sa} = \{x \in A : x = x^*\}.$$
$A_{sa}$ is a closed real vector subspace of $A$, being the kernel of the continuous real linear map $x \mapsto x - x^*$, and we have the decomposition
$$A = A_{sa} + iA_{sa}.$$
