A subset $H \subset L$ is a hyperplane if and only if $H = \{x : f(x) = \alpha\}$ for some $\alpha \in K$ and some non-zero $f \in L^*$. The linear form $f$ and the scalar $\alpha$ are determined by $H$ up to a common non-zero factor $\beta \in K$.
