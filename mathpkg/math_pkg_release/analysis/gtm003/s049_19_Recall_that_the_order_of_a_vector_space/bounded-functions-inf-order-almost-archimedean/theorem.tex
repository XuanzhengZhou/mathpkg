Let $T$ be a set containing at least two elements and let $E$ be the vector space (over $\mathbf{R}$) of all bounded real functions on $T$, ordered by the relation ``$g \leq f$ if either $f = g$ or $\inf\{f(t) - g(t): t \in T\} > 0$''. The order of $E$ is almost Archimedean, but not Archimedean.
