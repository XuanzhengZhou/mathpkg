A locally convex algebra over $\mathbf{C}$ with unit $e$ is a spectral algebra if and only if there exists a real subalgebra $A_1$ containing $e$ such that each $a \in A$ has a unique representation $a = b + ic$ ($b, c \in A_1$), and such that $A_1$ is a spectral algebra over $\mathbf{R}$.
