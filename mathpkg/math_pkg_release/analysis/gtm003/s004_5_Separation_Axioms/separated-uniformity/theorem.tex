A uniformity on a set $X$ is called \textbf{separated} if its vicinity filter $\mathfrak{W}$ satisfies the additional axiom:

\[
\bigcap \{W : W \in \mathfrak{W}\} = \Delta
\]

where $\Delta = \{(x,x) : x \in X\}$ is the diagonal of $X \times X$.
