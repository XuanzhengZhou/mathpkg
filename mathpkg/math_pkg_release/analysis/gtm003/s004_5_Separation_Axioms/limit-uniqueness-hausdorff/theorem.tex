A topological space $X$ is separated if and only if each filter $\mathfrak{F}$ that converges in $X$ converges to exactly one point $x \in X$. This point $x$ is called the \textbf{limit} of $\mathfrak{F}$.
