Let $X$ be a set, $\{X_\alpha : \alpha \in \mathbf{A}\}$ a family of uniform spaces, and $f_\alpha : X \to X_\alpha$ ($\alpha \in \mathbf{A}$) a family of mappings. Then there exists a coarsest uniformity on $X$ for which each mapping $f_\alpha$ ($\alpha \in \mathbf{A}$) is uniformly continuous.
