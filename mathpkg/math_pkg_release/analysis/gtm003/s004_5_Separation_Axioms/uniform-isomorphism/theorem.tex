Two uniform spaces $X, Y$ are called \textbf{isomorphic} if there exists a bijection $f$ of $X$ onto $Y$ such that both $f$ and $f^{-1}$ are uniformly continuous. The mapping $f$ itself is called a \textbf{uniform isomorphism}.
