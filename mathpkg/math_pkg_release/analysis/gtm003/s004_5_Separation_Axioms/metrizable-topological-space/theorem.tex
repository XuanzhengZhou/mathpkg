A topological space $X$ is called \textbf{metrizable} if its topology can be derived from a metric $d$ on $X$ (via the uniformity generated by the sets $W_n = \{(x,y) : d(x,y) < n^{-1}\}$, $n \in \mathbb{N}$).
