A topological space $X$ is Hausdorff (or separated) if for any two distinct points $x, y \in X$, there exist disjoint open sets $U, V$ such that $x \in U$ and $y \in V$. Equivalently, the diagonal $\Delta = \{(x, x) : x \in X\}$ is closed in $X \times X$.
