Let $E$ be a barreled space and $F$ a locally convex space. If a net $\{u_\alpha\}$ in $\mathcal{L}(E, F)$ converges pointwise to a linear map $u: E \rightarrow F$, then $u$ is continuous and the convergence is uniform on precompact subsets of $E$.
