A bilinear map $b: E \times F \rightarrow G$ between t.v.s. is \text{separately continuous} if for each fixed $x_0 \in E$, the map $y \mapsto b(x_0, y)$ is continuous, and for each fixed $y_0 \in F$, the map $x \mapsto b(x, y_0)$ is continuous.
