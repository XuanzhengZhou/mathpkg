Let $E, F, G$ be l.c.s. The space $\mathcal{B}(E, F; G)$ of continuous bilinear maps from $E \times F$ into $G$ is a locally convex space under the topology of uniform convergence on the products $S \times T$ where $S$ and $T$ range over families of bounded subsets of $E$ and $F$, respectively.
