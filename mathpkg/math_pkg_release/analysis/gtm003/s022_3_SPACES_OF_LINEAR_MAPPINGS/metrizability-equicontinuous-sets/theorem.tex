If $E$ is separable and $F$ is metrizable, then every equicontinuous subset of $\mathcal{L}(E, F)$ is metrizable for the topology of precompact convergence. In particular, the topology of simple convergence coincides with the topology of precompact convergence on equicontinuous subsets.
