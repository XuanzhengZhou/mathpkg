A subset $H \subset L$ is a hyperplane if and only if $H = \{x: f(x) = \alpha\}$ for some $\alpha \in K$ and some non-zero $f \in L^*$. $f$ and $\alpha$ are determined by $H$ to within a common factor $\beta$, $0 \neq \beta \in K$.
