A subset $B$ of a vector space $L$ over $K$ is linearly independent if for any finite subset $\{b_1, \ldots, b_n\} \subset B$ and scalars $\lambda_i \in K$, the relation $\sum_{i=1}^n \lambda_i b_i = 0$ implies $\lambda_i = 0$ for all $i$. Otherwise, $B$ is linearly dependent.
