If $f_0$ is a linear form on a subspace $M$ of an ordered t.v.s. $E$, and if $M$ contains an interior point $x_0$ of the positive cone $C$, then $f_0$ can be extended to a continuous positive linear form on $E$.
