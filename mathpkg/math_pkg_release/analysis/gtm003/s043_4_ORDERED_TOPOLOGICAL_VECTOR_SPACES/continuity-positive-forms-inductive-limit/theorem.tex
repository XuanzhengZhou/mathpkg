Let $E$ be an ordered l.c.s. which is the inductive limit of a family $\{E_\alpha : \alpha \in A\}$ of ordered (F)-spaces with respect to a family of positive linear maps, and suppose that $E_\alpha = C_\alpha - C_\alpha$ for all $\alpha \in A$. Then each positive linear form on $E$ is continuous.
