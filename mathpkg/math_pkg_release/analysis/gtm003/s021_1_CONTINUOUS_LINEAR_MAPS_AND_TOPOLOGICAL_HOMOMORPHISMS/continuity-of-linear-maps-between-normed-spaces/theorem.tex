If $u$ is a linear map from a normed space $(L, \|\cdot\|)$ into a normed space $(M, \|\cdot\|)$, then $u$ is continuous if and only if

$$\|u(x)\| \leq c \|x\|$$

for some $c > 0$ and all $x \in L$.
