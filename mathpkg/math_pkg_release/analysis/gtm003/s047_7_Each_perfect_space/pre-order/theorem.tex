A reflexive, transitive binary relation ``$<$'' on a set $S$ is called a \emph{pre-order} on $S$. That is, for all $x, y, z \in S$:
\begin{itemize}
\item (reflexivity) $x < x$;
\item (transitivity) if $x < y$ and $y < z$, then $x < z$.
\end{itemize}
