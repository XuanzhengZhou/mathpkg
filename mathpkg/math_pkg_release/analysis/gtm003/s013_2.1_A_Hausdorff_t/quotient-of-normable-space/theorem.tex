The quotient space of a normable (and complete) topological vector space $L$ over a closed subspace $M$ is normable (and complete). If $(L, \|\cdot\|)$ is a normed space, the norm
$$\|\hat{x}\| = \inf\{\|x\| : x \in \hat{x}\}, \quad \hat{x} \in L/M$$
generates the topology of $L/M$.
