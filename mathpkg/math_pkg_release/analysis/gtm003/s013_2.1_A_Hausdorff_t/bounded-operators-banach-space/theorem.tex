Let $L$, $N$ be normed spaces over $K$. The map $u \mapsto \|u\|$ is a norm on the vector space $\mathcal{L}(L, N)$ of all continuous linear maps from $L$ into $N$. Moreover, $\mathcal{L}(L, N)$ is a Banach space under this norm if $N$ is a Banach space.
