A topological vector space $L$ whose topology can be obtained from a norm $\|\cdot\|$ on $L$ via the metric
$$(x, y) \mapsto \|x - y\|$$
is called a \text{normable space}. Such a norm is, in general, not unique.
