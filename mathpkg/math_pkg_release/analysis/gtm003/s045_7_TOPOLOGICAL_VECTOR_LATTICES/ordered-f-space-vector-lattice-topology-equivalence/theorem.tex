Let $(E, \mathfrak{T})$ be an ordered (F)-space (over $\mathbb{R}$) which is a vector lattice. Then $\mathfrak{T} = \mathfrak{T}_0$ if and only if the positive cone $C$ of $E$ is normal in $(E, \mathfrak{T})$.
