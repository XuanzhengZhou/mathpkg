If $A$ is a subset of a vector lattice $E$, the \textbf{disjoint complement} $A^\perp$ is the set of all $y \in E$ such that $y$ is disjoint from each element of $A$:
$$A^\perp = \{y \in E : x \perp y \text{ for all } x \in A\}.$$
