Two elements $x, y$ of a vector lattice $E$ are \textbf{disjoint} if
$$\inf(|x|, |y|) = 0.$$
This relation is denoted by $x \perp y$.

Two subsets $A \subset E$ and $B \subset E$ are \textbf{lattice disjoint} (or simply \textbf{disjoint}) if $x \in A$ and $y \in B$ implies $\inf(|x|, |y|) = 0$.
