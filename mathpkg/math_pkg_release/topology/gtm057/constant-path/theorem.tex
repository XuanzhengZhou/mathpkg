A \textit{constant path} is one whose image is a single point. An \textit{identity path} $e$ satisfies $e \cdot a = a$ and $b \cdot e = b$ whenever the products are defined. Every identity path is constant, but the converse is false in general.
