For an $m \times n$ matrix $A$ over a commutative ring $R$ and integer $k \geq 0$:
- If $0 < n-k \leq m$, $E_k(A)$ is the ideal generated by determinants of all $(n-k) \times (n-k)$ submatrices of $A$.
- If $n-k > m$, $E_k(A) = 0$.
- If $n-k \leq 0$, $E_k(A) = R$.
