An element of the group ring $JF$ of a free group $F$ is called a \textit{free polynomial}: $f(x) = \sum n_i u_i$, where $u_i \in F$ (the free group on $x_1, x_2, \ldots$). The trivializer gives $t f(x) = f(1)$.
