For a polygonal knot $K$ in regular position, the \textit{under presentation} of $\pi(\mathbb{R}^3 - K, p_0')$ (basepoint below the knot) is
$$(y_1, \ldots, y_n : s_1, \ldots, s_n)_{\phi'},$$
where $s_j = (d_j \cdot u_j \cdot d_j^{-1})^\flat$ records the crossings around the $j$th overpass.
