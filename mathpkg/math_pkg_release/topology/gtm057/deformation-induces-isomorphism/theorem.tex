If $\{h_s\}$, $0 \leq s \leq 1$, is a deformation of $X$, then $(h_1)_*: \pi(X,p) \to \pi(X, h_1p)$ is an isomorphism onto. More precisely, $(h_1)_*(\beta) = \alpha^{-1} \cdot \beta \cdot \alpha$, where $\alpha$ is the class of the path $a(t) = h_t(p)$.
