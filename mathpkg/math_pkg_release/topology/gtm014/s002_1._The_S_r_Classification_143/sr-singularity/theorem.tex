A smooth mapping $f: X \to Y$ has a \textbf{singularity of type $S_r$} at $x \in X$ if the differential $(df)_x$ drops rank by $r$; i.e., if
$$
\operatorname{rank}(df)_x = \min(\dim X, \dim Y) - r.
$$
Denote by $S_r(f)$ the set of singularities of $f$ of type $S_r$.
