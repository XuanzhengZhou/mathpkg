Let $X$ be a differentiable $n$-manifold and let $p \in X$. There exists a unique vector space structure on $T_p X$ such that for every chart $\phi$ on $X$ with $p \in \operatorname{dom} \phi$, the mapping
$$
\lambda_\phi^p \colon \mathbf{R}^n \to T_p X
$$
is a linear isomorphism.
