Let $\alpha: J^k(X, Y) \to X$ be the source map; then the sequence
$$0 \longrightarrow J^k(f^*TY)_p \xrightarrow{\lambda} T_\sigma J^k(X, Y) \xrightarrow{(d\alpha)_\sigma} T_p X \longrightarrow 0$$
is exact, where $\sigma = j^k f(p)$ and $\lambda$ is the map defined in Proposition 5.8.
