A smooth mapping $f: X \to Y$ is locally transverse stable if for every point $p \in X$, there exists a neighborhood $U$ of $p$ such that the restriction $f|_U$ is stable as a mapping into $Y$. Equivalently, the local ring condition for infinitesimal stability holds at every point.
