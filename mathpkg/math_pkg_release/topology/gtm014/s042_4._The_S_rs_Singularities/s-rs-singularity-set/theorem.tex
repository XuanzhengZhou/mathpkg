For a smooth map $f: X \to Y$, the set $S_{r,s}(f)$ is a submanifold of $X$ (when nonempty) with

$$\dim S_{r,s}(f) = \dim X - r^2 - er - \text{codim}(S_{r,s}(f) \text{ in } S_r(f)),$$

where $e = |\dim X - \dim Y|$.
