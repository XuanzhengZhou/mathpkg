% Theorem 1.2: Finite Order Criterion for Infinitesimal Stability
% From GTM014, Chapter V, §1
%
% STATEMENT ONLY — no proof text.

\begin{theorem}
Let $f: X \to Y$ be smooth, let $p \in X$, and let $q = f(p) \in Y$.
Set $m = \dim Y$.
Then the germ $[f]_p$ is infinitesimally stable if and only if the equations $(**)$ can be solved to order $m$.
\end{theorem}
