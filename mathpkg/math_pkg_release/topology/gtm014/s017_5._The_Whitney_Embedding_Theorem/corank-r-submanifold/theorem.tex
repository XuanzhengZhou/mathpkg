$S_r$ is a submanifold of $J^1(X, Y)$ with

$$\operatorname{codim} S_r = (n - q + r)(m - q + r),$$

where $n = \dim X$, $m = \dim Y$, and $q = \min(n, m)$. In fact, $S_r$ is a subfiber-bundle of $J^1(X, Y)$ with fiber $L^r(\mathbf{R}^n, \mathbf{R}^m)$.
