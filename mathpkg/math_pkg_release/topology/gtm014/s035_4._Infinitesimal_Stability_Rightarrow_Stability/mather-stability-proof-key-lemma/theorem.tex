If the local finite-dimensionality condition for stability holds at every point $p \in X$, then $f$ is stable. This reduces a global geometric condition to a pointwise algebraic condition that can be verified using the preparation theorem.
