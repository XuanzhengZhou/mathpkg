A submanifold $A \subset M$ is called a \textbf{neat submanifold} if $\partial A = A \cap \partial M$ and (for $r \geqslant 1$) $A$ is not tangent to $\partial M$ at any point $x \in \partial A$; that is, $A_x \not\subset (\partial M)_x$.
