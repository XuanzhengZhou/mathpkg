Let $(\mathcal{F}, \mathfrak{Y})$ be a nontrivial structure functor on $X$ which is continuous and locally extendable. Then $\mathcal{F}(X) \neq \emptyset$. In fact, if $\mathcal{F}(Y_0) \neq \emptyset$ then the restriction map $\mathcal{F}(X) \to \mathcal{F}(Y_0)$ is surjective.
