Let $M, N$ be manifolds of dimensions $m, n$ and $f: M \to N$ a $C^r$ map. If
$$r > \max\{0, m - n\},$$
then the set of critical values $f(\sum_f)$ has measure zero in $N$. In particular, the set of regular values of $f$ is dense in $N$.
