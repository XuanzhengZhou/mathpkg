Let $X$ be a path-connected space. The maps $v_n: S(\bigwedge_n X) \rightarrow S\Omega S(X)$ collect to define a map

$$v: \bigvee_{1 \leq n} S(\bigwedge_n X) \rightarrow S\Omega S(X)$$

which is a weak homotopy equivalence.
