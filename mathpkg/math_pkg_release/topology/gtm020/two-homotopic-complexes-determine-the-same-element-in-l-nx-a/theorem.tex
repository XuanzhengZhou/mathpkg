We construct an inverse to the isomorphism $L_1(X, A) \rightarrow L_n(X, A)$. For $\xi \in S_n(X, A)$ we can put riemannian metrics on $\xi_i$ and define $\alpha_i^*: \xi_i \rightarrow \xi_{i-1}$. We consider $\eta = (0 \rightarrow \eta_0 \xrightarrow{\beta} \eta_1 \rightarrow 0)$, where $\eta_0 = \sum_i \xi_{2i}$ and $\eta_1 = \sum_i \xi_{2i+1}$ and $\beta(x_0, x_2, x_4, \ldots) = (\alpha_1(x_0), \alpha_2^*(x_2) + \alpha_3(x_2), \alpha_4^*(x_4) + \alpha_5(x_4), \ldots)$. Since $\xi_{2i+1} = \alpha_2i(\xi_{2i}) \oplus \alpha_2i+1(\xi_{2i+2})$ over $A$, the element $\eta$ defines a class in $S_1(X, A)$. If $\xi \in S_1(X, A)$, then $\xi$ equals $\eta$. This is easily seen to define an inverse $L_n(X, A) \rightarrow L_1(X, A)$, by (8.6) and (8.7).

8.8 Products. If $\xi \in S_n(X, A)$ and if $\eta \in S_m(Y, B)$, then the tensor product $\xi \otimes \eta \in S_{n+m}(X \times Y, (X

9.5 Example. Let $U$ be an arbitrary half-exact cofunctor, and let $Z$ be a (finite) cell complex. Then the functions that assign to $X$ the group $U(X \wedge Z)$ and to $f: Y \rightarrow X$ the group morphism $U(f \wedge 1_Z)$ collect to define a half-exact cofunctor.
