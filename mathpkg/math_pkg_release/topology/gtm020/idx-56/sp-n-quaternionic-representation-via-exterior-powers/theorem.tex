The action of $Sp(n)$ on $\mathbf{H}^n$ defines an element of $RSp(Sp(n))$. After restriction of scalars to $\mathbf{C}$, there is an element of $RSp(n)$ whose exterior powers generate $RSp(n)$ as a ring.
