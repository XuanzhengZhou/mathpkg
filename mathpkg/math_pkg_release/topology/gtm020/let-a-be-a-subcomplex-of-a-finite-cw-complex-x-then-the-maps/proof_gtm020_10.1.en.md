---
locale: en
of_concept: let-a-be-a-subcomplex-of-a-finite-cw-complex-x-then-the-maps
role: proof
source_book: gtm020
source_chapter: 10. Relative K-Theory
source_section: '1'
---

Since the composition $A \rightarrow X \rightarrow X/A$ is constant, $\alpha\beta$ is zero. Let $\xi$ be a bundle over $X$ such that $\xi/A$ is $s$-trivial. Then there is an isomorphism $u: (\xi \oplus \theta^k)|A \rightarrow \theta^m$ which, when composed with the projection $\theta^m \rightarrow F^m$, defines a trivialization $t$ of $\xi \oplus \theta^k$ over $A$. Then $(\xi \oplus \theta^k)/t$ is a vector bundle over $X/A$ whose induced bundle over $X$ by the projection $X \rightarrow X/A$ is $s$-equivalent to $\xi$.

For the last statement, if $A$ is contractible, $\beta: \tilde{K}(X/A) \rightarrow \tilde{K}(X)$ is an epimorphism. Let $\xi$ be a vector bundle over $X/A$ such that $p_A^*(\xi)$ is $s$-trivial; that is, $p_A^*(\xi \oplus \theta^k) = p_A^*(\xi) \oplus \theta^k$ is trivial over $X$. Since $\xi \oplus \theta^k$ is isomorphic to $p_A^*(\xi \oplus \theta^k)/t$ for some trivialization $t$ over $A$ and since any two such trivializations are homotopic over $A$, it can be assumed that $t$ is the restriction of a trivialization of $p_A^*(\xi \oplus \theta^k)$ over $X$. Consequently, $\xi \oplus \

We define $u(x)$ to be the class of $(x, 0)$ and $v(y)$ the class of $y$ in $Z_f$. The map $w$ is the projection $Z_f \rightarrow Z_f / u(X) = C_f$, and $a(f)(y)$ is the class of $y$ in $C_f$. We define $b(f)(y) = *$ and $b(f)(x, t) = \langle x, t \rangle$ in $S(X)$. We view $C_{a(f)}$ as $C(X) + C(Y)$ modulo the relation $\langle x, 1 \rangle$ equals $\langle f(x), 1 \rangle$. To define $r$, we require that $r(C(Y)) = *$ and that $r|C(X)$: $C(X) \rightarrow S(X)$ be the natural projection. The homeomorphism $j$ is defined by the relation $j(\langle x, t \rangle) = \langle x, 1 - t \rangle$.
