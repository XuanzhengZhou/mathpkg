(1) $\beta(ax + a'x', y) = a\beta(x, y) + a'\beta(x', y)$
$$\beta(x, by + b'y') = \bar{b}\beta(x, y) + \bar{b}'\beta(x, y

9.3 Example. Let $\theta^k$ be the product $k$-dimensional bundle over $B$. Then $\beta(b, x, x') = (x|x')$, the euclidean inner product, is a riemannian metric in the real case and a hermitian metric in the complex case.

9.4 Example. For $k \leq m \leq +\infty$, the canonical bundle $\gamma_k^m$ over $G_k(F^m)$ has a natural riemannian metric derived from the euclidean metric on $F^m$. For $(V, x, x') \in E(\gamma_k^m \oplus \gamma_k^m)$, let $\beta(V, x, x') = (x|x')$, the euclidean inner product. On each fibre $V$ the function $\beta$ is clearly an inner product.

Using this construction and a Gauss map, we are able to get the next general result.
