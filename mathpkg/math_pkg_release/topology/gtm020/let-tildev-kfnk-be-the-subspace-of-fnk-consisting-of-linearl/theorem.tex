$$\left\| y_j - \sum_{i < j} (y_j|v_i)v_i \right\|$$

For $W \in G_k(F^n)$, let $O(W)$ denote the open set of all $W'$ related to $W$. For $W = \langle v_1, \ldots, v_k \rangle$ we define a linear inner product preserving map

$$f_{(v_1, \ldots, v_k), W'} : W \rightarrow F^n$$

where $f_{(v_1, \ldots, v_k), W'} (W) = W'$, and the map $f$ is continuous in $(v_1, \ldots, v_k) \in V_k(F^n)$ and $W' \in O(W)$. This s done by requiring $f_{(v_1, \ldots, v_k), W'} (v_i) = v_i'$, where $(v_1', \ldots, v_k') = GS(\pi_{W'} (v_1), \ldots, \pi_{W'} (v_k))$. The above definition is well defined in view of (3.3). With these notations we prove the following theorem.
