The characteristic map $c_n: S^{n-1} \to SO(n)$ of the tangent bundle $\tau(S^n)$ is given by
$$c_n(x) = R(x, e_{n-1})^2 = \alpha(x)\alpha(e_{n-1}) = \beta(x)\beta(e_{n-1}),$$
where $e_{n-1} = (0, \ldots, 0, 1) \in S^{n-1}$ and $\alpha, \beta$ are the reflection maps defined in (9.8).
