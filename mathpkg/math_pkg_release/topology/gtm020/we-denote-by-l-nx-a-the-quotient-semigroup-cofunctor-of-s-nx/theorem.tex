Observe that $L_1(X, A)$ is just $L(X, A)$ of Sec. 4. One can prove that $\xi$ defines the zero element in $L_n(X, A)$ if $\xi$ is acyclic on $X$.

By viewing a complex of length $n$ as a complex of length $n + 1$, we define a natural morphism $L_n(X, A) \rightarrow L_{n+

As corollaries of this theorem there are the following results. Recall that two complexes $\xi$ and $\eta$ are homotopic on $(X, A)$ provided there is a complex $\zeta$ on $(X \times I, A \times I)$ such that $\zeta = \zeta|(X \times 0, A \times 0)$ and $\eta = \zeta|(X \times 1, A \times 1)$.
