The fibre of $P\xi^n$ is $FP^{n-1}$, and the bundle is locally trivial. For each $b \in B$, the inclusion $F^n \rightarrow p^{-1}(b) \subset E$ defines a natural inclusion $j_b: FP^{n-1} \rightarrow q^{-1}(b) \subset E'$. A point in $E'$ is a line $L$ in the fibre of $\xi$ over $q(L)$.

The induced bundle $q^*(\xi)$ has a canonical line bundle $\lambda_\xi$ as a subbundle where a point in the total space $E(\lambda_\xi)$ of $\lambda_\xi$ over $L$ is a pair $(L, x)$, where $q(L) = b = p(x)$, or, equivalently, $x \in L$. There is an exact sequence over $E'$ where $\sigma_\xi$ is the quotient of $q^*(\xi)$ by $\lambda_\xi$:

$$0 \rightarrow \lambda

bundle. Let $a_{\xi}$ denote the class $f^*(z)$. Since $f$ is unique up to homotopy, the class $a_{\xi}$ is well defined. The basic properties of the class $a_{\xi}$ are contained in the next theorem.
