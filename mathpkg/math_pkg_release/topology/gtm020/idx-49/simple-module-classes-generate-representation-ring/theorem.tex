The set of $G$-module classes $[M]$, where $M$ is a simple $G$-module, generates the abelian group $R_F(G)$. That is, every element of $R_F(G)$ can be expressed as a finite $\mathbf{Z}$-linear combination of isomorphism classes of simple $G$-modules.
