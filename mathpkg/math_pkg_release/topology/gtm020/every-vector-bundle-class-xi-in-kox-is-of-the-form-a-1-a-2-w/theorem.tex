in $RO(SO(m))$. Let $\xi^m$ and $\eta^n$ be two vector bundles over $X$ with principal bundles $\alpha$ or $\beta$. Using the constructions of (5.4), we have a morphism $\widetilde{\alpha \oplus \beta}$: $R(U(m) \oplus U(n)) \rightarrow K(X)$ such that $(\widetilde{\alpha \oplus \beta})(\rho_m \oplus 0) = \tilde{\alpha}(\rho_m) = \{\xi\}$ and $(\widetilde{\alpha \oplus \beta})(0 \oplus \rho_n) = \tilde{\beta}(\rho_n) = \{\eta\}$ in $K(X)$ for complex vector bundles. For real vector bundles the morphism $\widetilde{\alpha \oplus \beta}$ is defined $RO(SO(m) \oplus SO(n)) \rightarrow KO(X)$.

With these notations we are able to state and prove the main result of this section.
