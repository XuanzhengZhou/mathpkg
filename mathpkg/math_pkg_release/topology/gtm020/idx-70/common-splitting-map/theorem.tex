Let $\xi_1, \ldots, \xi_r$ be $r$ vector bundles over $B$ which are either all real or all complex. Then there exists a map $f: B_1 \rightarrow B$ such that $f$ is a splitting map for each $\xi_i$, where $1 \leq i \leq r$.
