For the multiplicative structure of $K(CP^n)$, one uses the ring morphism $ch: K(CP^n) \rightarrow H^{ev}(CP^n, Q)$ which we consider at length in Chap. 20.

There is a natural map $q: RP^{2n+1} \rightarrow CP^n$. One proves that $q^*(\xi_n(C))$ is the complexification of $\xi_{2n+1}(R)$, where $\xi_k(F)$ denotes the canonical line bundle over $FP^k$.

Again the spectral sequence for $\tilde{K}(RP^{2n+1})$ collapses. Using this fact and the exact sequence

$$Z \leftarrow \tilde{K}(RP^{2n}) \leftarrow \tilde{K}(RP^{2n+1}) \leftarrow 0$$

we have the next proposition, after looking at the spectral sequence.
