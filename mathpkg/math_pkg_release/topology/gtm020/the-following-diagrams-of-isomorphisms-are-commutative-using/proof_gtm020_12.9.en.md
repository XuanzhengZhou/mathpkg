---
locale: en
of_concept: the-following-diagrams-of-isomorphisms-are-commutative-using
role: proof
source_book: gtm020
source_chapter: 12. Clifford Algebras
source_section: '9'
---

For the first square, observe that multiplication by $b$ is an isomorphism $M^0 \rightarrow M^1$ and $M^1 \rightarrow M^0$. The second square is commutative by the definition of $\alpha_b^0$ from $\alpha_b$. For the last square we compute $\alpha_{e_{k+1}}\phi(x_0 + x_1)

The similar diagram in the complex case is also commutative.

Proof. Using the following commutative diagram, we have the result by applying the cofunctor $N$.

$$C_{k-1} \xrightarrow{i} C_k$$
$$C_k^0 \xrightarrow{j} C_{k+1}$$

Here $j(e_i) = e_i$ for $i \leq k-1$ and $j(e_k) = e_{k+1}$. Moreover, we use the following commutative diagram:

$$M_{k+1} \xrightarrow{i*} M_k$$
$$N(C_{k+1}^0) \xrightarrow{j*} N(C_k^0)$$

The proposition is proved by putting these two diagrams together.

6.12. Table of $L_k$ and $L_k^c$

| $k$ | $C_k$ | $N_k$ | $M_k$ | $L_k$ | $a_k$ |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | C(1) | Z | Z | $Z_2$ | 1 |
| 2 | H(1) | Z | Z | $Z_2$ | 2 |
| 3 | H(1) $\oplus$ H(1) | Z $\oplus$ Z | Z | 0 | 4 |
| 4 | H(2) | Z | Z $\oplus$ Z | Z | 4 |
| 5 | C(4) | Z | Z | 0 | 8 |
| 6 | R(8) | Z | Z | 0 | 8 |
| 7 | R(8) $\oplus$ R(8) | Z $\oplus$ Z | Z | 0 | 8 |
| 8 | R(16) | Z | Z $\oplus$ Z | Z | 8 |

Moreover, we have $C_{k+8} = C_k \otimes C_8$, $N_{k+8} = N_k$, $M_{k+8} = M_k$, and $a_{k+8} = 16a_k$.

Moreover, we have $C_{k+2}^c = C_k^c \bigotimes_C C(2)$, $N_{k+2}^c = N_k^c$, $M_{k+2}^c = M_k^c$, $L_{k+2}^c = K_k^c$, and $a_{k+2}^a = 2a_k^c$.

To prove the above statements, for $k = 1, 2$ we have $L_1 \cong \text{coker} (N_1 \rightarrow N_0)$ and $L_2 \cong \text{coker} (N_2 \rightarrow N_1)$, which are defined by the restriction to $R$ of $C$ acting on $C$ and to $C$ of $H$ acting on $H$, respectively. Two components result, and the cokernel is $Z_2$.

For $k = 5, 6$, we have $L_5 \cong \text{coker} (N_5 \rightarrow N_4)$ and $L_6 \cong \text{coker} (N_6 \rightarrow N_5)$, which are defined by restricting $C(4)$ to $H(2)$ acting on $H^2$ and $R(8)$ to $C(4)$ acting on $C^4$. Irreducible modules go into irreducible modules, and $L_5 = L_6 = 0$.

For $k = 3, 7$, we have $L_3 \cong \text{coker} (N_3 \rightarrow N_2)$ and $L_7 \cong \text{coker} (N_7 \rightarrow N_6)$ which are defined by restriction of $H(1) \oplus H(1)$ and of $R(8) \oplus R(8)$ to the first factor. Then $N_3 \rightarrow N_2$ and $N_7 \rightarrow N_6$ are epimorphisms, and $L_3 = L_7 = 0$.

For $k = 4, 8$, we have $L_4 \cong \text{coker} (M_5 \right

(1) $u(vw) = (uv)w$.
(2) $c(uv) = uc(v)$.
(3) For the inclusions $i$: $C_{k-1} \rightarrow C_k$ and $i^*$: $M_k \rightarrow M_{k-1}$, there is the relation $(ui^*(v)) = i^*(uv)$.
(4) $uv = vu$ for $kl$ even and $uv = c(vu)$ for $kl$ odd.

Proof. Statements (1) to (3) follow easily from the definitions. For the last statements, we have the following diagram:

$$\begin{array}{ccc}
C_k & \hat{\otimes} & C_l \\
C_{k+1} & \phi_{l,k} & C_l \hat{\otimes} C_k
\end{array}$$

Here $T(x_p \hat{\otimes} y_q) = (-1)^{pq} y_q \hat{\otimes} x_p$ for $x_p \in C_k^p$ and $y_q \in C_l^q$. Let $\sigma = \phi_{l,k}^{-1} T \phi_{k,l}$ which is an automorphism of $C_{k+l}$. Observe that $\sigma(e_i) = e_{l+i}$ for $1 \leq i \leq k$ and $\sigma(e_i) = e_{i-k}$ for $k + 1 \leq i \leq k + l$. Then $\sigma$ is a composition of $kl$ inner automorphisms $\alpha_b$, where $b \in R^k - 0$. For example, $\alpha_b$ where $b = (e_1 + e_2)/\sqrt{2}$ interchanges $e_1$ and $e_2$. By (6.9) this is $kl$ applications of $c$ when applied to $M_{k+l}$. Since $T^*(N \hat{\otimes} M) = M \hat{\otimes} N$ and since $\sigma^* \phi_{l,k}^* = \phi_{k,l}^* T^*$, we have $\phi_{k,l}^*(N \hat{\otimes} M) = c^{pq} \phi_{

given by complexification is defined by $c(\lambda_1) = 0$, $c(\lambda_4) = 2\mu^1$, and $c(\lambda_8) = \mu^4$, and the group homomorphism $r: L^c_* \rightarrow L_*$ given by restriction from $C^c_k$ to $C_k$ is defined by $r(\mu) = r(\mu^3) = 0$, $r(\mu^2) = \lambda_4$, and $r(\mu^4) = 2\lambda_8$.

Proof. Since $L_1 = Z/2Z$, it is generated by $\lambda_1$ with $2\lambda_1 = 0$. Since $a_1 = 1$, $a_2 = 2$, it follows that $\lambda_1^2$ generates $L_2 = Z/2Z$.

Let $\omega = e_1 \cdots e_k$ in $C_k$. For $k = 2q$ we have $\omega^2 = (-1)^q$ and $k = 4m$ we have $\omega^2 = 1$. If $M$ is an irreducible graded $C_k$-module, $\omega$ acts on $M^0$ as the scalar $\varepsilon = \pm 1$, and the $\varepsilon$-module corresponds to the action of $\omega$ equal to multiplication by $\varepsilon$. Since $e_i\omega = -\omega e_i$, if $M$ is an $\varepsilon$-module, $c(M)$ is a $(-\varepsilon)$-module. If $M$ is an $\varepsilon$-module and if $N$ is an $\varepsilon'$-module, then $M \otimes N$ is an $\varepsilon\varepsilon'$-module.

Let $\lambda_4$ be the class of an irreducible $C_4$-module $M$ in $L_4$ which is a $(-1)$-module. Then $M \otimes M$ is of the type $\varepsilon^2 = +1$ in cases over $C_8$. Let $\lambda_8$ be the class of the $(+1)$-module $W$ of $C_8$. Then we have $M \otimes M
