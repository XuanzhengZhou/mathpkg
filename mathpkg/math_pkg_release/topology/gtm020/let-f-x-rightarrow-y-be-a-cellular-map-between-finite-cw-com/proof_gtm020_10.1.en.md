---
locale: en
of_concept: let-f-x-rightarrow-y-be-a-cellular-map-between-finite-cw-com
role: proof
source_book: gtm020
source_chapter: 10. Relative K-Theory
source_section: '1'
---

The exactness of $\tilde{K}(S(X)) \rightarrow \tilde{K}(C_f) \rightarrow \tilde{K}(Y) \rightarrow \tilde{K}(X)$ follows from (2.5) and (2.6). From (2.7) we have the following commutative diagram where the top row is exact and the vertical morphism is an isomorphism.

$$\tilde{K}(C_a(f)) \xrightarrow{\tilde{K}(S(Y))} \tilde{K}(S(X)) \xrightarrow{\tilde{K}(C_f)}$$

Consequently, the bottom row is exact, and the first sequence in the theorem is exact.

For the second statement, we have the following commutative diagram:

$$A \xrightarrow{j} X \xrightarrow{a(j)} C_j \xrightarrow{b(j)} S(A)$$

Since $q$ is the projection arising from pinching $C(A)$ to a point, $q^j: \tilde{K}(X/A) \rightarrow \tilde{K}(C_j)$ is an isomorphism by (2.1). The top row is exact, and the vertical morphism is an isomorphism in
