(1) $M$ is orientable.

(2) There exists an atlas $A$ of $M$ such that for $(U, \phi), (V, \psi) \in A$ the relation $\det D(\psi \phi^{-1}) > 0$ on $\phi(U \cap V)$ holds.

(3) The tangent bundle $\tau(M)$ has $SO(n)$ as its structure group.

Finally, if $M$ is connected, $M$ has precisely two orientations.
