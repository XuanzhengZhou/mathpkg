Observe that for $f \in \text{Hom}_F(M, N)$ we have $f \in \text{Hom}_G(M, N)$ if and only if $sf = f$ for each $s \in G$. For the action $sa = a$ on $F$, we have the structure of a $G$-module on the dual module $M^+ = \text{Hom}_F(M, \bar{F})$, the module of conjugate linear functionals.
