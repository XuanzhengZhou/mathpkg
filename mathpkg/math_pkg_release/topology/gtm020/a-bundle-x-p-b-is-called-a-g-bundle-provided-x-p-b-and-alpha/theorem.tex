2. Definition and Examples of Principal Bundles

A $G$-space $X$ has the property that the relation $xs = x$ holds only for $s = 1$ if and only if it has the property that the relation $xs = xt$ holds only for $s = t$.
