Let $f: U \to V$ be a homeomorphism of subsets of $\mathbf{R}^n$. Then the \textbf{orientation number} of $f$ at $x$, denoted $O_x(f)$, is defined by the relation
$$f_*(\alpha_x) = O_x(f)\,\alpha_{f(x)}$$
or, equivalently,
$$f^*(\beta_{f(x)}) = O_x(f)\,\beta_x.$$
