The \textbf{supremum} (or \textbf{least upper bound}) of a subset $A$ of an ordered set is an element $s$ such that:
\begin{enumerate}
\item $s$ is an upper bound for $A$: $a \leq s$ for all $a \in A$;
\item if $t$ is any upper bound for $A$, then $s \leq t$.
\end{enumerate}
