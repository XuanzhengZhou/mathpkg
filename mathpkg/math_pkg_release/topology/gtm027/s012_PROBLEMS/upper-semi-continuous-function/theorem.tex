A real-valued function $f$ on a topological space $X$ is called \emph{upper semi-continuous} if and only if the set $\{\, x \in X : f(x) \geq a \,\}$ is closed for each real number $a$. A real-valued function $g$ is called \emph{lower semi-continuous} if and only if $-g$ is upper semi-continuous.
