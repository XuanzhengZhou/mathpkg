Let $(X,d)$ be a pseudo-metric space, and define $e(x,y) = \min[1, d(x,y)]$. Then $(X,e)$ is a pseudo-metric space whose topology is identical with that of $(X,d)$. Consequently, each pseudo-metric space is homeomorphic to a pseudo-metric space of diameter at most one.
