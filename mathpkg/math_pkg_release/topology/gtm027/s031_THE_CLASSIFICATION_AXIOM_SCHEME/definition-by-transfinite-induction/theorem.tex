There is a unique function $f$ on $R$ such that $f(x) = g(f|x)$ for each $x \in R$; if $h$ is any function on an ordinal such that $h(z) = g(h|z)$ for $z$ in $\text{domain } h$, then $h \subset f$, and if $z \in \text{domain } h$, then $f(z) = g(f|z)$.
