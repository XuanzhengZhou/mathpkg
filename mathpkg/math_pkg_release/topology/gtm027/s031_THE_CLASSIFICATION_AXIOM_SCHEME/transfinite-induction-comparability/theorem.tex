Let $f$ be a function such that $\text{domain } f$ is an ordinal and $f(u) = g(f|u)$ for $u$ in $\text{domain } f$. If $h$ is also a function such that $\text{domain } h$ is an ordinal and $h(u) = g(h|u)$ for $u$ in $\text{domain } h$, then $h \subset f$ or $f \subset h$.
