The Sorgenfrey plane $(Y, \mathfrak{U})$ contains a subspace which is not separable. For example, the anti-diagonal $\{(x, y) \in \mathbb{R}^2 : x + y = 1\}$ with the subspace topology is not separable.
