Two sets $A$ and $B$ are \text{disjoint} (or non-intersecting) iff
$$
A \cap B = 0.
$$
The sets $A$ and $B$ \text{intersect} iff $A \cap B \neq 0$.
If $\alpha$ is a family of sets, then $\alpha$ is a \text{disjoint family} iff no two members of $\alpha$ intersect.
