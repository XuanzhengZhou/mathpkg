The \text{identity relation} on a set $X$, denoted $\Delta$ or $\Delta(X)$, is:
$$
\Delta(X) = \{(x,x): x \in X\}.
$$
It satisfies $\Delta \circ R = R \circ \Delta = R$ whenever $R$ is a relation whose range and domain are subsets of $X$. The identity relation is also called the \text{diagonal}.
