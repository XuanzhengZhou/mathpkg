The \text{void set} (empty set) is denoted $0$ and is defined to be:
$$
0 = \{x : x \neq x\}.
$$
(Any proposition which is always false could be used instead of $x \neq x$.)
The void set is a subset of every set $A$ because each member of $0$ (there are none) belongs to $A$.
