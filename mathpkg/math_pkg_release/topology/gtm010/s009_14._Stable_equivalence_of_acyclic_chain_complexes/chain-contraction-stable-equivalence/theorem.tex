Let $C$ be an acyclic $(R, G)$-complex with chain contraction $\delta$. Then $C \sim C_\delta$ where $C_\delta$ is the complex concentrated in two adjacent dimensions given by $(d + \delta)|C_{\text{odd}}: C_{\text{odd}} \to C_{\text{even}}$.
