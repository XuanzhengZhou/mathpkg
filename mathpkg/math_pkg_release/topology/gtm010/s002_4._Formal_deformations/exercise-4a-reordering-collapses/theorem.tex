If $K \searrow L$ then any given sequence of elementary collapses can be reordered to yield a sequence $K = K_0 \searrow K_1 \searrow \ldots \searrow K_q = L$ with $K_i = K_{i+1} \cup e^{n_i} \cup e^{n_i-1}$ where $n_0 \geq n_1 \geq \ldots \geq n_{q-1}$.
