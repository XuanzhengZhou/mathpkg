$K \searrow L$ ($K$ collapses to $L$) and $L \nearrow K$ ($L$ expands to $K$) iff there is a finite sequence (possibly empty) of elementary collapses
$$K = K_0 \searrow K_1 \searrow \dots \searrow K_q = L.$$
