A homomorphism $\varphi: G \to H$ from one group to another is called a \textbf{monomorphism} if it is one-to-one (injective), i.e., the kernel contains only the identity element: $\ker \varphi = \{e_G\}$.
