If $C$ is a set and $f : B \to C$ is any function from $B$ to $C$, then $f \mid A$ denotes the \textbf{restriction} of $f$ to the subset $A \subseteq B$. For any $a \in A$, $(f \mid A)(a) = f(a) \in C$.
