If $C$ is a set, $A$ is a subset of $B$, and $f \colon B \to C$ is any function from $B$ to $C$, then $f \mid A$ denotes the \textbf{restriction} of $f$ to the subset $A$; i.e., for any $a \in A$,
$$
(f \mid A)(a) = f(a) \in C.
$$
