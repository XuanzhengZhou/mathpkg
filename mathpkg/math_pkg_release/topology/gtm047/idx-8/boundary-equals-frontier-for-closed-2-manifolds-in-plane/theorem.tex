Let $M$ be a 2-manifold with boundary, lying in $\mathbf{R}^2$. If $M$ is closed, then $\operatorname{Bd} M = \operatorname{Fr} M$. (In the case $M = |K|$, it then follows that $\operatorname{Fr} M = |\partial K|$.)
