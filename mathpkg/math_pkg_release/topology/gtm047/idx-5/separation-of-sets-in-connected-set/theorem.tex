Let $C$ be a connected set, let $D$ be a subset of $C$, and let $H_1, H_2$ be disjoint sets in $C - D$. If $C - D$ is the union of two separated sets containing $H_1$ and $H_2$ respectively, then we say that $D$ \textbf{separates $H_1$ from $H_2$ in $C$}.
