Let $X$ be a pathwise connected topological space and let $P_0 \in X$. Then

$$\operatorname{CP}(X, P_0) = \{ p: [0,1] \to X \mid p \text{ continuous}, \; p(0) = P_0, \; p(1) = P_0 \}$$

is the set of all closed paths (loops) in $X$ based at $P_0$. The point $P_0$ is called the base point.
