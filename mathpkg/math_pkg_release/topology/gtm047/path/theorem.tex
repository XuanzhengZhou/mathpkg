A path, in a space $[X, \emptyset]$ (or $[X, d]$) is a mapping

$$p: [a, b] \rightarrow X,$$

where $[a, b]$ is a closed interval in $\mathbb{R}$. If $p(a) = P$ and $p(b) = Q$, then $p$ is a path from $P$ to $Q$.
