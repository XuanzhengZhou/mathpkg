Let $[X, \emptyset]$ be a topological space. For each nonempty set $M \subset X$, let

$$\emptyset \mid M = \{M \cap U \mid U \in \emptyset\}.$$

Then $\emptyset \mid M$ is called the subspace topology for $M$, and the pair $[M, \emptyset \mid M]$ is called a subspace of $[X, \emptyset]$.
