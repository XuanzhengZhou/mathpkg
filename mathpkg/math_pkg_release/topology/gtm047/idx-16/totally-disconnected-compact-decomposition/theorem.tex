Let $C$ be a totally disconnected compact set, in a metric space, and let $\varepsilon$ be a positive number. Then $C$ is the union of a finite collection $G_\varepsilon = \{g_1, g_2, \ldots, g_n\}$ of disjoint nonempty closed sets, with $\delta g_i < \varepsilon$ for each $i$.
