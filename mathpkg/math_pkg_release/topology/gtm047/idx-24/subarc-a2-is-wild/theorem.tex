Let $A$ be the Fox-Artin arc constructed by replicating the configuration of Figure 20.3, and let $S$ be a cross-section as in Figure 20.4 that decomposes $A$ into subarcs $A_1$ (from $P$ to $S$) and $A_2$ (from $S$ to $Q$). Then $A_2$ is wild.
