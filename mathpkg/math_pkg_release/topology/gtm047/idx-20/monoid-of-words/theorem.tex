$W(A)$ is the set of all words over alphabet $A$. Words are multiplied by laying sequences end to end. This operation is associative, and $e$ (the empty sequence) is the identity element. Thus $W(A)$ forms a monoid. No nonempty word has an inverse in $W(A)$.
