A \text{word} is a finite sequence of syllables:

$$w = a_{j_1}^{\alpha_1}, a_{j_2}^{\alpha_2}, \ldots, a_{j_m}^{\alpha_m}.$$

We allow the empty sequence $e$ which has no terms. The set of all words over alphabet $A$ is denoted $W(A)$.
