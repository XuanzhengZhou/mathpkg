Let $K$ be a triangulated 3-manifold with boundary. Then $K$ is combinatorially equivalent to a set $N(L)$, where $N(L)$ is the regular neighborhood of a subcomplex $L$ of a triangulated 3-manifold $K'$.
