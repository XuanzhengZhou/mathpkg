**Theorem 1.** Let $U$ be an open set in $\mathbb{R}^n$, and let $P, Q \in U$. If $P$ and $Q$ are in different components of $U$, then $U$ is the union of two disjoint open sets containing $P$ and $Q$ respectively.
