If $K$ is a complex, then $|K|$ denotes the union of the elements of $K$, with the subspace topology induced by the topology of $\mathbb{R}^m$. Such a set is called a polyhedron. If $K$ is a finite complex, then $|K|$ is a finite polyhedron.
