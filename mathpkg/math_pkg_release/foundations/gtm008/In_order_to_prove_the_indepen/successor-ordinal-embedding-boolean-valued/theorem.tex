For any ordinal $\alpha$,
$$[\![\check{\alpha} + 1 = (\alpha + 1)^{\check{}}]\!] = 1.$$
Equivalently, the Boolean truth value of
$$(\forall \gamma)[\gamma \in \check{\alpha} \vee \gamma = \check{\alpha} \leftrightarrow \gamma \in (\alpha + 1)^{\check{}}]$$
is $1$.
