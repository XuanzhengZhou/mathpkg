For $y \in V$, the canonical name $\check{y}$ is defined by $\in$-recursion:
$$\check{y} \triangleq \{ \langle \check{x}, 1 \rangle \mid x \in y \}.$$
Equivalently, $\check{y}$ is the $B$-valued set with domain $\{\check{x} \mid x \in y\}$ and constant value $1$.
