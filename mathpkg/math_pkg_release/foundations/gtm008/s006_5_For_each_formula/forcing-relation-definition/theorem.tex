If $p \in P$ and $\varphi$ is a formula (limited or unlimited), then

$$
p \Vdash \varphi \;\triangleq\; p \in \llbracket \varphi \rrbracket,
$$

where $\llbracket \varphi \rrbracket$ is the Boolean truth value of $\varphi$ in the $B$-valued model.
