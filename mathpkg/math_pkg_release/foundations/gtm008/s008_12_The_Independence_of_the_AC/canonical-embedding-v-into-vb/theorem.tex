For $y \in V$, define $\check{y}$ by recursion with respect to the well-founded $\in$-relation:

$$\check{y} \triangleq \{ \langle \check{x}, 1 \rangle \mid x \in y \}.$$

Obviously, $\check{y} \in V^{(2)}$, where $2 = \{0, 1\}$ is the two-element Boolean algebra.
