1. $$\llbracket (\exists x \in u) \varphi(x) \rrbracket = \sum_{x \in \mathcal{D}(u)} u(x) \cdot \llbracket \varphi(x) \rrbracket.$$
2. $$\llbracket (\forall x \in u) \varphi(x) \rrbracket = \prod_{x \in

Remark. Next we shall show how $V$ can be embedded in $V^{(B)}$. As preparation we prove the following.
