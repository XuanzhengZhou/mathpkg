If $M$ is a standard transitive model constructed as $M = \bigcup_{\alpha \in \mathrm{On}} M_\alpha$ where $M_0$ is transitive and $M_{\alpha+1}$ consists of definable subsets of $M_\alpha$, then $M$ satisfies all axioms of ZF.
