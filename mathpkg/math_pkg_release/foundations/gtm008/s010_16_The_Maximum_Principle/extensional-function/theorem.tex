Let $d \subseteq V^{(\mathbf{B})}$. A function $g: d \rightarrow V^{(\mathbf{B})}$ is called **extensional** iff

$$(\forall x, x' \in d)[\llbracket x = x' \rrbracket \leq \llbracket g(x) = g(x') \rrbracket].$$

In other words, Boolean-equal inputs map to Boolean-equal outputs.
