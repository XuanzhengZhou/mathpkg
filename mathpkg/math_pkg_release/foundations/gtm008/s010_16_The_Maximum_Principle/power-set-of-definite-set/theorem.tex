If $u \in V^{(\mathbf{B})}$ is definite, then

$$\llbracket \mathcal{P}(u) = B^{\mathcal{D}(u)} \times \{1\} \rrbracket = 1,$$

where $B^{\mathcal{D}(u)}$ denotes the set of all functions from $\mathcal{D}(u)$ into $B$.
