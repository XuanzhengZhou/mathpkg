Let $u \in V^{(\mathbf{B})}$. Then $u$ is \emph{definite} iff
$$(\forall x \in \mathcal{D}(u))\; u(x) = 1.$$
That is, $u$ assigns Boolean value $1$ to every element in its domain.

\text{Example.} $\check{k}$ (the canonical embedding of a set $k$) is definite.
