If $u$ is definite, then
$$\llbracket \mathcal{P}(u) = B^{\mathcal{D}(u)} \times \{1\} \rrbracket = 1.$$
That is, the power set of a definite set $u$ in $V^{(\mathbf{B})}$ is (with truth value $1$) the set of all functions from $\mathcal{D}(u)$ into $B$, each embedded with truth value $1$.
