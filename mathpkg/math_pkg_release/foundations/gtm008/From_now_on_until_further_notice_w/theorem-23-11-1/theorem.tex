1. $(\forall \alpha)(\forall

Then

1. $b_\alpha \leq b$.
2. $\alpha < \beta \rightarrow b_\alpha \leq b_\beta$ and $b_\alpha = \prod \{b_\beta \mid \beta \geq \alpha\}$.
3. $\sum_{\alpha \in On} b_\alpha = b$.
