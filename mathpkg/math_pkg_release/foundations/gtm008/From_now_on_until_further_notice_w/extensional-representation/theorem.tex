$$(\forall v \in V^{(\mathbf{B})}) (\forall d \subseteq V^{(\mathbf{B})})\; [\mathcal{D}(v) \subseteq d \to$$
$$(\exists u)\; [u \text{ is extensional } \land d = \mathcal{D}(u) \land \llbracket u = v \rrbracket = 1]].$$

In particular, each $v \in V^{(\mathbf{B})}$ can be represented by an extensional set.
