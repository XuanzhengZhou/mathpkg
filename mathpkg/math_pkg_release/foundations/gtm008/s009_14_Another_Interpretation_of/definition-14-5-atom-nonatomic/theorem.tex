An element $b$ of a Boolean algebra $B$ is called an \textbf{atom} iff $b \neq 0$ and
$$(\forall b' \in |B|) \; [b' \leq b \rightarrow b' = 0 \vee b' = b],$$
i.e., $b$ is a minimal element of $B \setminus \{0\}$.

A Boolean algebra $B$ is called \textbf{nonatomic} iff $B$ has no atoms.