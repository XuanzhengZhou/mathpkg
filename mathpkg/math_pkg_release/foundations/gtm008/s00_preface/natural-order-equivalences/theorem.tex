If $\langle B, +, \cdot, ^-, 0, 1 \rangle$ is a Boolean algebra with natural order $\leq$ then $\forall a, b \in B$:
\begin{enumerate}
\item $a \leq b \leftrightarrow -b \leq -a$.
\item $a \leq b \leftrightarrow a - b = 0$.
\item $a \leq b \leftrightarrow (a \Rightarrow b) = 1$.
\end{enumerate}
