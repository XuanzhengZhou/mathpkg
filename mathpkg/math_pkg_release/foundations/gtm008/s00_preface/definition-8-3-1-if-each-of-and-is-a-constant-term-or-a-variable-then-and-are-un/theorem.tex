1. If each of $t_1$ and $t_2$ is a constant term or a variable, then $K(t_1), F(t_1)$, and $t_1 \in t_2$ are unlimited formulas.
2. If $\varphi$ and $\psi$ are unlimited formulas, then $\neg \varphi$ and $\varphi \wedge \psi$ are unlimited formulas.
3. If $\varphi(x)$ is an unlimited formula in which $x$ is a variable, ranked or unranked, that does not

4. Ord³ ($\varphi$) is the length of $\varphi$ i.e., the number of logical symbols in $\varphi$ where atomic formulas $t_1 \in t_2$, $K(t)$, $F(t)$ are assigned length 1.

5. Ord ($\varphi$) = $\omega^2 \cdot \text{Ord}^1 (\varphi) + \omega \cdot \text{Ord}^2 (\varphi) + \text{Ord}^3 (\varphi)$.

Remark. Note that Ord² ($\varphi$) and Ord³ ($\varphi$) are natural numbers. Proof by induction on Ord ($\varphi$) is illustrated by the following in which $P(\cdot)$ stands for $K(\cdot)$ or $F(\cdot)$.
