If $T$ is a topology on $X$ and $A \subseteq X$ then:
\begin{enumerate}
\item $A^0 \triangleq \{x \in A \mid (\exists N(x))[N(x) \subseteq A]\}$.
\item $A^- \triangleq \{x \in X \mid (\forall N(x))[N(x) \cap A \neq 0]\}$.
\end{enumerate}
