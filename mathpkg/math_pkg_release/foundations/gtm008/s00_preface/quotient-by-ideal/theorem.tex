If $I$ is a proper ideal in the Boolean algebra $B$ then:
\begin{enumerate}
\item $(\forall a \in |B|)[a/I \triangleq \{x \in |B| \mid a(-x) + x(-a) \in I\}]$.
\item $|B|/I \triangleq \{a/I \mid a \in |B|\}$.
\end{enumerate}
