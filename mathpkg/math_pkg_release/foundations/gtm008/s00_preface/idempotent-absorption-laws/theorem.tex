If $\langle B, +, \cdot, ^-, 0, 1 \rangle$ is a Boolean algebra then $\forall a, b \in B$:
\begin{enumerate}
\item $a + a = a$, \quad $aa = a$ \qquad \text{Idempotent Laws.}
\item $a + ab = a$, \quad $a(a + b) = a$ \qquad \text{Absorption Laws.}
\end{enumerate}
