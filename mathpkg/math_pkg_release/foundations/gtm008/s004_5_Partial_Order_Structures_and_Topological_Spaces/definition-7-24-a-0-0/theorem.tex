$$A_0 \triangleq 0$$
$$\bar{K}_{\alpha} \triangleq A_{\alpha} \cap K$$
$$A_{\alpha+1} \triangleq Df(A_{\alpha})$$
$$A_{\alpha} \triangleq \bigcup_{\beta < \alpha} A_{\beta}, \alpha \in K_{\Pi},$$

then

$$L_K \triangleq \bigcup_{\alpha \in On} A_{\alpha}.$$

Remark. It can then be easily shown that $A_{\alpha}$ satisfies the conditions 1-3 of page 68. From this, as before, we can prove the following theorem.
