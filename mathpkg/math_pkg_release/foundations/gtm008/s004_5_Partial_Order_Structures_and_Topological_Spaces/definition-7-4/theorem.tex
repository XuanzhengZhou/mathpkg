**

$$\mathbf{A} \models \varphi \iff (\forall f)[\psi_0(f, \mathbf{A}) \rightarrow f(\Gamma\varphi) = 1]$$

$$\iff (\exists f)[\psi_0(f, \mathbf{A}) \wedge f(\Gamma\varphi) = 1]$$

**Remark.** Thus if

$$

Remark. As mentioned above, in the general case where $A$ may be a proper class we do not have a single formula $\psi$ of $\mathcal{L}_0$ such that Theorem 7.7 holds simultaneously for all formulas of $\mathcal{L}$, ($\psi$ having a further argument $[\varphi]$). This result can however be obtained if we restrict ourselves to formulas $\varphi$ with less than $n$ quantifiers, where $n$ is a fixed natural number.
