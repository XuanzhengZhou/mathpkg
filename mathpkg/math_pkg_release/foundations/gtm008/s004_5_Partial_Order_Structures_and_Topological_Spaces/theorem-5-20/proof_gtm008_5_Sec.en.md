---
role: proof
locale: en
of_concept: theorem-5-20
source_book: gtm008
source_chapter: "5"
source_section: "5 Partial Order Structures and Topological Spaces

In the work"
---
For a given $i \in I$ and an element $a_i \in P_i$, let $\hat{a}_i$ be the element of $P$ whose $i$th projection is $a_i$ and whose $j$th projection is $1_j$ for $j \neq i$, i.e.,

$$\hat{a}_i(i) = a_i, \hat{a}_i(j) = 1_j \quad \text{for} \quad j \neq i.$$

For each $F \in F$, let $F_i = \{ a_i \in P_i \mid \hat{a}_i \in F \}.$ Then $F_i$ is a filter for $P_i$ since $a_i \leq b_i$ implies $\hat{a}_i \leq \hat{b}_i$. Furthermore $F_i$ is maximal: If $G_i$ is a

Remark. When we consider regular open sets of a partial order structure $P$, the following notion is very useful.
