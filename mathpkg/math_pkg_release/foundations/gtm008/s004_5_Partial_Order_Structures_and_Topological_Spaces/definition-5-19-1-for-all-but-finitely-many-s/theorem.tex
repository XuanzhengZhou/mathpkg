1. $P \triangleq \{ p \in \prod_{i \in I} P_i \mid p(i) = 1_i \}$ for all but finitely many $i$’s.

2. $(\forall p, q \in P)[p \leq q \leftrightarrow (\forall i \in I)[p(i) \leq q(i)]]$.

3. $1 \triangleq$ the unique $p \in P$ such that $(\forall i \in I)[p(i) = 1_i]$.
