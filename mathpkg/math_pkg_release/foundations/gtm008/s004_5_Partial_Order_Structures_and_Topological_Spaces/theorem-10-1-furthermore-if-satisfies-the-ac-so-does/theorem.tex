Furthermore if $M$ satisfies the AC so does $M[G]$.

Remark. $[\varphi] = b$ is definable in $\mathcal{L}_0$ from $\mathbf{P}$ and $[\varphi]$ (uniformly in $[\varphi]$ if $\varphi$ ranges over limited formulas only).
