$$x \neq y \rightarrow (\exists N(x))[y \notin N(x)] \wedge (\exists N(y))[x \notin N(y)].$$

Remark. For the results we wish to prove we first define filter and ultrafilter for partial order structures.
