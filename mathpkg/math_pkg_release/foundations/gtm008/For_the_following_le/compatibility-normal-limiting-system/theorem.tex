Let $P_0 \subseteq P_1 \subseteq \cdots \subseteq P_\alpha \subseteq \cdots$ ($\alpha < \kappa$) be a normal limiting system. If $x \in P_\alpha$, $y \in P_\beta$, $\alpha < \beta < \kappa$, then
$$\text{Comp}(x, p_{\alpha\beta}(y)) \leftrightarrow \text{Comp}(x, y).$$
