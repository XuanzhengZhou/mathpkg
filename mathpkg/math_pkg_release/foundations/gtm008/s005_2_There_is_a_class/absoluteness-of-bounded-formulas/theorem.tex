If $\varphi$ is a bounded formula of set theory and $B \subseteq A$ are transitive sets (or classes) with appropriate Skolem functions $F$, then for all $b_1, \ldots, b_n \in B$,
$$B \models \varphi(b_1, \ldots, b_n) \leftrightarrow A \models \varphi(b_1, \ldots, b_n).$$
