If $M$ is a $B$-valued structure for $L$ and $M' \subseteq M$, then an element $b \in M$ is defined over $M'$ iff
$$(\forall x \in M)[\llbracket x = b \rrbracket_M = \bigvee_{a \in M'} \llbracket x = a \rrbracket_M].$$
