of Powers. Moreover, $K$ is definable in $M[F]$ and $M[F]$ satisfies the Axiom of Replacement, so $\mathcal{P}^{M[F]}(D(u)) \in M[F]$ by (iv).

We prove (iv) in the following way.

1. $K$ is a function.

Let $S \subseteq B_0 \times I$ and $S \in M$. We have to show that $K(S)$ is uniquely determined by the above requirements, i.e., assuming that conditions (i)–(iii) are satisfied by $w_0, \bar{r}_0$ and by $w_1, \bar{r}_1$ for the same $S$, we have to show that $D(w_0)

Define

$$S = \{ \langle p, v \rangle \mid 0 < p \wedge p \in B_0 \wedge v \in I \wedge 0 < p \cdot \bar{r} \leq \llbracket v \in w \rrbracket \}.$$

Then $K(S) = D(w)$ and $S \in \mathcal{P}^{\mathbf{M}}(B_0 \times I)$.
