(1) $\Gamma \in \mathcal{E} = J(\langle 0, 0 \rangle)$,
(2) $\Gamma = J(\langle 0, 1 \rangle)$,
(3) $\Gamma \neg \Gamma = J(\langle 0, 2 \rangle)$,
(4) $\Gamma \vee \Gamma = J(\langle 0, 3 \rangle)$,
(5) $\Gamma (\neg) = J(\langle 0, 4 \rangle)$,
(6) $\Gamma) \neg = J(\langle 0, 5 \rangle)$,
(7) $\Gamma x_i \neg = J(\langle 0, 6 + i \rangle)$,
(8) $\Gamma \exists^\alpha \Gamma = J(\langle 0, \omega + \alpha \rangle)$,

and

(9) $\Gamma^{\wedge \alpha} \Gamma = J(\langle 0, \omega + \alpha, \omega + \alpha \rangle)$.

For any finite sequence $s_1, \ldots, s_n$ of symbols of $L$,

(10) $\Gamma s_1, \ldots, s_n \Gamma = J(\langle 1, \Gamma s_1, \ldots, \Gamma s_n \rangle)$.
