$a, b, c$ are metavariables whose domain is free variables $a_0, a_1, \ldots$.
$x, y, z$ are metavariables whose domain is bound variables $x_0, x_1, \ldots$.
$\varphi, \psi, \eta$ are metavariables that range over all well-formed formulas (wffs).
