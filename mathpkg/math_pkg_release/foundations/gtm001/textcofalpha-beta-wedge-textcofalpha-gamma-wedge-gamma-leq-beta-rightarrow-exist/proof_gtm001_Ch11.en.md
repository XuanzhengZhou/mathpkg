---
role: proof
locale: en
of_concept: textcofalpha-beta-wedge-textcofalpha-gamma-wedge-gamma-leq-beta-rightarrow-exist
source_book: gtm001
source_chapter: "11"
source_section: ""
---

** If $\alpha$ is cofinal with $\beta$ and with $\gamma$, then

$$(\exists f)[f: \beta \rightarrow \alpha \wedge (\forall \tau < \alpha)(\exists \delta < \beta)[f' \delta \geq \tau] \wedge \text{Smo}(f)]$$

$$(\exists g)[g: \gamma \rightarrow \alpha \wedge (\forall \tau < \alpha)(\exists \delta < \gamma)[g' \delta \geq \tau] \wedge \text{Smo}(g)].$$

In particular if $\delta < \gamma$, then $g' \delta < \alpha$ and so $(\exists \tau < \beta)[f' \tau \geq g' \

Remark. From the results we have now proven, it is easy to show that the character of cofinality of any ordinal $\alpha$, is a cardinal. That is, the smallest ordinal with which $\alpha$ is cofinal is a cardinal.
