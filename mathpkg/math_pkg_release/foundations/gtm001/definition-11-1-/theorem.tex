$$\text{cof}(\alpha, \beta) \leftrightarrow \beta \leq \alpha \wedge (\exists f)[\text{Smo}(f) \wedge f: \beta \rightarrow \alpha \wedge (\forall \gamma < \alpha)(\exists \delta < \beta)[f' \delta \geq \gamma]].$$
