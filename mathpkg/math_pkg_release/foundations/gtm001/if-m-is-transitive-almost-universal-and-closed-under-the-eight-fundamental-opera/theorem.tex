(1) $\cup(a)$ is $M$-constructible,

(2) $\mathcal{P}(a) \cap M$ is $M$-constructible,

(3) If all of the free variables of $\varphi(c, d)$ are among $c, d, b_1, \ldots, b_n$, if $b_1 \in M \land \cdots \land b_n \in M$ and

$$(\forall x, y, z \in M) [\varphi^M(x, y) \land \varphi^M(x, z)
