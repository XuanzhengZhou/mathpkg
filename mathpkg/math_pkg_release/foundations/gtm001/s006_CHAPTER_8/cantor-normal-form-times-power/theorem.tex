Let $\alpha > 1$, $\beta_0 < \beta_1 < \cdots < \beta_n$, $0 < \gamma_0 < \alpha, \ldots, 0 < \gamma_n < \alpha$, and $\delta \ge \omega$. Then
$$(\alpha^{\beta_n} \cdot \gamma_n + \cdots + \alpha^{\beta_0} \cdot \gamma_0) \cdot \alpha^\delta = \alpha^{\beta_n + \delta}.$$
