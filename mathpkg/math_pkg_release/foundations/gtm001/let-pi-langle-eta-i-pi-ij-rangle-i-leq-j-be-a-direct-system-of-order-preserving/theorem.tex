$$M \triangleq \bigcup_{i \in I} \{i\} \times \eta_i.$$

On $M$ we define an equivalence relation $\equiv$:

$$\langle i, \alpha \rangle \triangleq \langle j, \beta \rangle \quad \text{iff} \quad (\exists k \in I)[i \leq k \
