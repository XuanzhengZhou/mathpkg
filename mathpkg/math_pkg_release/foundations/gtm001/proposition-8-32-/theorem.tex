If $\beta \in K_{\text{II}}$ then since $\gamma \neq 0$

$$\gamma^{\beta} = \bigcup_{\delta < \beta} \gamma^{\delta}.$$

Furthermore, if $\alpha < \beta$ then $\alpha + 1 < \beta$ and hence

$$\gamma^{\alpha} < \bigcup_{\delta < \beta} \gamma^{\delta} = \gamma^{\beta}.$$
