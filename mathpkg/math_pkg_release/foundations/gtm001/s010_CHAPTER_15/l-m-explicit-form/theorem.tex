If $M$ is a standard transitive model of ZF then
\begin{enumerate}
\item $L^M = \{x \mid (\exists \alpha \in M)[x = F' \alpha]\}$,
\item $a \subseteq \omega \land a \in M \rightarrow L_a^M = \{x \mid (\exists \alpha \in M)[x = F_a' \alpha]\}$.
\end{enumerate}
