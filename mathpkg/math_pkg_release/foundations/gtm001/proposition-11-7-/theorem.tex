$$\beta \leq \alpha \land (\exists f)[f: \beta \rightarrow \alpha \land (\forall \gamma < \alpha)(\exists \delta < \beta)[f‘\delta \geq \gamma

Since by hypothesis $(\forall \gamma < \alpha)(\exists \delta < \beta)[f' \delta \geq \gamma]$ it follows that there is a smallest such $\delta$. This smallest $\delta$ is an element of $a$ and so $(\exists v < \eta)[h'v = \delta]$. Consequently

$$g'v = f'h'v = f' \delta \geq \gamma.$$

Thus $\alpha$ is cofinal with $\eta$.
