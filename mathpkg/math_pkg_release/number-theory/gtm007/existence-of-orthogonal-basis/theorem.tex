\textbf{Theorem 1.} Every quadratic module $(V,Q)$ has an orthogonal basis. If $(e_i)$ is such a basis, the matrix of $Q$ is diagonal: $Q(\sum x_i e_i) = a_1 x_1^2 + \cdots + a_n x_n^2$ where $a_i = e_i \cdot e_i$.
