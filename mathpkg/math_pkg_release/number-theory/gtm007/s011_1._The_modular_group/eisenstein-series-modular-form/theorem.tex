Let $k$ be an integer $> 1$. The Eisenstein series
$$G_k(z) = \sum_{m,n} {}' \frac{1}{(mz + n)^{2k}},$$
where the sum runs over all pairs of integers $(m, n) \neq (0, 0)$, is a modular form of weight $2k$.
Moreover,
$$G_k(\infty) = 2\zeta(2k),$$
where $\zeta$ denotes the Riemann zeta function.
