An element $x$ of a quadratic module $(V, Q)$ is called isotropic if $Q(x) = 0$. A subspace $U$ of $V$ is called isotropic if all its elements are isotropic, i.e. $U \subset U^0$ or equivalently $Q|_U = 0$.
