Suppose $p \neq 2$. Let $f(X) = \sum a_{ij} X_i X_j$ with $a_{ij} = a_{ji}$ be a quadratic form with coefficients in $\mathbb{Z}_p$ whose discriminant $\det(a_{ij})$ is invertible in $\mathbb{Z}_p$. Then every element of $\mathbb{Q}_p$ (resp.\ of $\mathbb{Z}_p$) is represented by $f$.
