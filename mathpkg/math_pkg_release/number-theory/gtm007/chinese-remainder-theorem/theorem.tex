\textbf{Lemma 1 (Chinese remainder theorem).} Let $a_1, \ldots, a_n, m_1, \ldots, m_n$ be integers with the $m_i$ being pairwise relatively prime. There exists an integer $a$ such that $a \equiv a_i \pmod{m_i}$ for all $i$.
