Let $u$ be an integer $\geq 0$. The sum
$$S(X^u) = \sum_{x \in K} x^u$$
is equal to $-1$ if $u \geq 1$ and $u$ is divisible by $q-1$; it is equal to $0$ otherwise.

(We agree that $x^0 = 1$ even if $x = 0$.)
