Let $(V,Q)$ be a quadratic module over $k$. Two elements $x,y$ of $V$ are called \textbf{orthogonal} if $x \cdot y = 0$. The set of elements orthogonal to a subset $H$ of $V$ is denoted by $H^0$; it is a vector subspace of $V$.
