We say that a form $f(X_1, \ldots, X_n)$ \textbf{represents} an element $a$ of $k$ if there exists $x \in k^n$, $x \neq 0$, such that $f(x) = a$. In particular, $f$ represents $0$ if and only if the corresponding quadratic module contains a nonzero isotropic element.
