If $(V,Q)$ and $(V',Q')$ are two quadratic modules, a linear map $f: V \to V'$ such that $Q' \circ f = Q$ is called a \textbf{morphism} (or \textbf{metric morphism}) of $(V,Q)$ into $(V',Q')$. Then $f(x) \cdot f(y) = x \cdot y$ for all $x,y \in V$.
