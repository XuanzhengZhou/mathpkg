Two orthogonal bases

$$e = (e_1, \dots, e_n) \quad\text{and}\quad e' = (e'_1, \dots, e'_n)$$

of $V$ are called \textbf{contiguous} if they have an element in common (i.e., if there exist $i$ and $j$ such that $e_i = e'_j$).
