Let $f$ and $f'$ be two nondegenerate quadratic forms over $\mathbf{Q}$. Then $f$ and $f'$ are equivalent over $\mathbf{Q}$ if and only if they are equivalent over $\mathbf{Q}_v$ for every place $v \in V$ (i.e., over each $\mathbf{Q}_p$ and over $\mathbf{R}$).
