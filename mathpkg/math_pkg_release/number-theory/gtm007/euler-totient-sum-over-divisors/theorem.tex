\textbf{Lemma 1.} If $n$ is an integer $\geq 1$, then
$$n = \sum_{d \mid n} \phi(d)$$
where $\phi(d)$ is the Euler totient function, i.e., the number of integers $x$ with $1 \leq x \leq d$ which are relatively prime to $d$.
