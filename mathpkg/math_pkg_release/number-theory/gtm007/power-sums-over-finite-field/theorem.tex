Let $K$ be a finite field with $q$ elements. For an integer $u \geq 0$, let $S(X^u) = \sum_{x \in K} x^u$ (with the convention $0^0 = 1$). Then:
$$S(X^u) = \begin{cases} -1 & \text{if } u \geq 1 \text{ and } u \equiv 0 \pmod{q-1}, \\ 0 & \text{otherwise.} \end{cases}$$
