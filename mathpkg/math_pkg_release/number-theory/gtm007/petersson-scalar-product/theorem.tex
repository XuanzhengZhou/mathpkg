For cusp forms $f, g$ of weight $2k$ ($k > 0$), the Petersson scalar product is
$$\langle f, g \rangle = \int_{H/G} f(z)\overline{g(z)} y^{2k} \frac{dx\,dy}{y^2} = \int_D f(z)\overline{g(z)} y^{2k-2} dx\,dy,$$
where $z = x + iy$. This defines a positive definite Hermitian inner product on $M_k^0$.
