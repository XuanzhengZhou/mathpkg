\textbf{Proposition 12.} If $f(z) = \sum_{m} c(m) q^m$, then $T(n)f(z) = \sum_{m} \gamma(m) q^m$ with
$$\gamma(m) = \sum_{\substack{a \mid \gcd(n,m) \\ a \geq 1}} a^{2k-1} c\left(\frac{mn}{a^2}\right).$$
In particular, $\gamma(0) = \sigma_{2k-1}(n) c(0)$ and $\gamma(1) = c(n)$.
