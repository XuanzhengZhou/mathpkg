A hBadic distribution $\mu$ on a compact-open  \subset \mathbb{Q}_p$ is an additive map from the set of compact-open subsets of $ to $\mathbb{Q}_p$, i.e., if  = U_1 \cup U_2$ is a disjoint union of compact-open sets, then $\mu(U) = \mu(U_1) + \mu(U_2)$. Equivalently, a hBadic distribution is a $\mathbb{Q}_phBlinear map from the space of locally constant functions on $ to $\mathbb{Q}_p$.
