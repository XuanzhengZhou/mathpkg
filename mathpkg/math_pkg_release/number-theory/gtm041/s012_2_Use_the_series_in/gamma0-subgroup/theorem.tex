If $q$ is any positive integer we define $\Gamma_0(q)$ to be the set of all matrices $\begin{pmatrix} a & b \\ c & d \end{pmatrix}$ in $\Gamma$ with $c \equiv 0 \pmod{q}$. It is easy to verify that $\Gamma_0(q)$ is a subgroup of $\Gamma$.
