A nonzero function $f \in M_k$ is called an \text{eigenfunction} (or \text{eigenform}) of the Hecke operator $T_n$ if it satisfies a relation of the form
$$T_n f = \lambda(n) f$$
for some complex scalar $\lambda(n)$.
