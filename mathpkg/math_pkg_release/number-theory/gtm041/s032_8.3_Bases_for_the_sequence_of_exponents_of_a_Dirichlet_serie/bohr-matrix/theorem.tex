A \textit{Bohr matrix} is a finite or infinite square matrix $R = (r_{ij})$ with rational entries. If $R$ is infinite we require that all but a finite number of entries in each row be zero.

The sum and product of two Bohr matrices (defined as for finite matrices) is again a Bohr matrix.
