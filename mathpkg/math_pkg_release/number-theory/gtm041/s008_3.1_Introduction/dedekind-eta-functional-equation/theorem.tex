If $\tau \in H = \{\tau \in \mathbb{C} : \text{Im}(\tau) > 0\}$, then
$$\eta\left(\frac{-1}{\tau}\right) = (-i\tau)^{1/2} \eta(\tau),$$
where the branch of $z^{1/2}$ is chosen to be positive when $z > 0$.
