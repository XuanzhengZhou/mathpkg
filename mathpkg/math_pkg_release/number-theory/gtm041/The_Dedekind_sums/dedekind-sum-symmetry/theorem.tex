Let $k$ be a positive integer and $(h, k) = 1$.

(a) If $h' \equiv \pm h \pmod{k}$, then $s(h', k) = \pm s(h, k)$, with the same sign as in the congruence.

(b) If $h\bar{h} \equiv \pm 1 \pmod{k}$, then $s(\bar{h}, k) = \pm s(h, k)$.

(c) If $h^2 + 1 \equiv 0 \pmod{k}$, then $s(h, k) = 0$.
