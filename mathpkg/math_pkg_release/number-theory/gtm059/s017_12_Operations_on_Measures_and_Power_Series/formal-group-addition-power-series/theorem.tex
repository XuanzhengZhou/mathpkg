For power series in the variable $X$, define the formal group addition operator $[+]$ by
$$
X [+] x = (1+X)(1+x) - 1 = X + x + Xx,
$$
where $x$ is a constant (or an element of the coefficient ring).
More generally, the formal group addition is given by
$$
X [+] Y = (1+X)(1+Y) - 1 = X + Y + XY.
$$
