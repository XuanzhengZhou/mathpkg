Let $K_i$ be a finite extension of the local field $K$. The trace defines a non-degenerate $K$-bilinear pairing

$$
K_i \times K_i \longrightarrow K
$$

given by

$$
(x, y) \longmapsto T_i(xy)
$$

where $T_i$ denotes the trace map from $K_i$ to $K$.
