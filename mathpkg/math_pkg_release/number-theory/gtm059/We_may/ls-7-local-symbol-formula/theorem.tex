Let $A(b_0)^{g+1}A(b_1)$ and $A_{\mathfrak{a}}$ be Lubin-Tate formal modules. The local symbol pairing

$$
A(b_0)^{g+1}A(b_1) \times K_2 \longrightarrow A_{\mathfrak{a}}
$$

induces, via the logarithm map, a pairing

$$
A(b_0)^{g+1}A(b_1) \times A_{\mathfrak{a}} \longrightarrow A_{\mathfrak{a}}.
$$

In light of the exact duality

$$
\mathfrak{a}^{g+1}\mathfrak{a}^* \times \mathfrak{a}^* \longrightarrow \mathfrak{a}^* \longrightarrow \mathfrak{a}^* \times \mathfrak{a}^*
$$

there exists a unique group homomorphism

$$
A_{\mathfrak{a}} \longrightarrow A(b_0)^{g+1}A(b_1)
$$

such that for $x \in A(b_0)$ and $y \in A(b_1)$, the local symbol is given by

$$
\langle x, y \rangle = \cdots
$$
