The \textbf{formal multiplicative group}, denoted $G_m$, is the formal group defined over any commutative ring by the power series
$$
F(X,Y) = X + Y + XY = (1+X)(1+Y) - 1.
$$
If $n$ is a positive integer, then $[n]_{G_m}(X) = (1+X)^n - 1$.
