For any function $g(N)$ define:
$$S_a(h) = \sum_{n} g_a(n) h(n)$$
with the sum taken over $a \in \mathbb{Z}(N)^k$. If $f$ is defined on $\mathbb{Z}(N)$ to be $0$ outside $G(N)$, then
$$S_a(h) = \int dh.$$
