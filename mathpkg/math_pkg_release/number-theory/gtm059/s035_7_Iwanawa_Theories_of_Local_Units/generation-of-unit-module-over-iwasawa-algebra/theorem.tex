Let $x \in \mathbb{Z}_p^\times$. One can choose $i \in \{1, 2, \dots, p-1\}$ such that the element
$$U_i^c = U_i^c(1 + x)$$
generates $U_i^c$ over the Iwasawa algebra $A$, i.e.
$$U_i^c = A \cdot U_i^c.$$
