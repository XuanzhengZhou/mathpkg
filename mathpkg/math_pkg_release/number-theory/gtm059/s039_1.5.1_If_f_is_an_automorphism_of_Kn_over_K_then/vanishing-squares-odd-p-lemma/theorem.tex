Let $p$ be an odd prime. Then:

(i) $(v_k v_k)^2 = 0$.

Whether $p$ is odd or even, we have:

(ii) $(v_k - v_k)^2 = 0$,

(iii) $(v_k - 1)^2 = 0$ if $\operatorname{ord}_\pi x \geq e + 1$,

and so

(iv) $(v_k v_k)^2 = 0$ if $\operatorname{ord}_\pi x \geq e + 1$.
