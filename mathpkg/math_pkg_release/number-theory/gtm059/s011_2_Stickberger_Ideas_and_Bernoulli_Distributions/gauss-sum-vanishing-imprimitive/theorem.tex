Let $\chi$ be a character on $\mathbb{Z}/m\mathbb{Z}$ that is primitive of conductor $d$, where $d \mid m$. If $n$ is an integer not prime to $m$, then
$$S(\chi, n) = \sum_{x \in \mathbb{Z}/m\mathbb{Z}} \chi(x) \zeta_m^{nx} = 0$$
where $\zeta_m$ is a primitive $m$-th root of unity.
