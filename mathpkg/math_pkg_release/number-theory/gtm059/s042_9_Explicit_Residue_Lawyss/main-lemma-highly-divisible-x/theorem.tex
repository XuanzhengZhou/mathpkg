Let $x \in \{x_0, x_1\}$. Suppose that
$$\operatorname{ord}_p x \geq |x_2| + 1 + 2e$$
where $e$ is the ramification index of $K$ over $\mathbb{Q}_p$. Then
$$\bigl(x_n, x_n\bigr) = \Bigl[x_n, \tfrac{1}{2} |x_n|\Bigr).$$
