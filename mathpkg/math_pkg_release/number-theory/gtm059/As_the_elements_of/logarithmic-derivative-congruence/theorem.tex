\text{Let } m \geq 2 \text{ and let } u \text{ be a unit in the formal group. Then}
$$
\lambda_i(u) \equiv \lambda_i(u) \pmod{\tau_{u_m}},
$$
\text{where } \lambda_i \text{ is the logarithmic derivative and } \tau_{u_m} \text{ denotes the trace-like congruence modulus.}
