Assume that $m = p^n$ is a prime power. Then
$$
(R^* : \mathcal{S}) = \prod_{i} R_{i,i}
$$
where $\mathcal{S} = R \cap R_1$ is the Stickelberger ideal, $R^*$ is the full group ring, and the $R_{i,i}$ are certain determinants arising from the group ring decomposition.
