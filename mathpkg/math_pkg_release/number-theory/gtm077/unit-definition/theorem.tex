An algebraic integer $\varepsilon$ is called a unit if $1/\varepsilon$ is also an algebraic integer. Equivalently, $\varepsilon$ divides $1$ in the ring of integers of its field. The conjugates of a unit are also units, and each divisor of a unit and each product of units is a unit.
