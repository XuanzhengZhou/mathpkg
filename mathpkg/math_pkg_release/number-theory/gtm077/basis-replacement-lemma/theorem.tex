If $B_1, \ldots, B_r$ is a maximal independent system in an Abelian $p$-group, and
$$A = B_m^u B_{m+1}^{x_{m+1}} \cdots B_r^{x_r}$$
with $u \not\equiv 0 \pmod{p}$, then replacing $B_m$ by $A$ yields another maximal independent system of the same rank.
