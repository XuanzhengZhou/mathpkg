If $h$ is the order of a finite group $\mathfrak{G}$, then $A^h = E$ for every element $A$ of $\mathfrak{G}$. Moreover, for any integers $m,n$, $A^m = A^n$ implies $m \equiv n \pmod{a}$ where $a$ is the order of $A$.
