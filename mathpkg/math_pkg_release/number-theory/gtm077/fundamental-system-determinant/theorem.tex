In order that the numbers

$$\omega^{(i)} = \sum_{k=1}^{n} c_{ik} \theta^{k-1} \quad (c_{ik} \text{ numbers in } k)$$

form a fundamental system of $K(\theta)$, it is necessary and sufficient that the determinant $\|c_{ik}\| \neq 0$.

Obviously we need only investigate when the numbers $1, \theta, \ldots, \theta^{n-1}$ can be represented in terms of the $\omega^{(i)}$ as

$$\theta^{p-1} = \sum_{i=1}^{n} a_{

However since no linear homogeneous relation holds between 1, $\theta$, ..., $\theta^{n-1}$ with coefficients in $k$, unless all coefficients are 0, we have

$$\delta_{kp} = \sum_{i=1}^{n} a_{ki} c_{ip} = \begin{cases} 0 & \text{if } p \neq k, \\ 1 & \text{if } p = k. \end{cases}$$

Thus the determinant $\|\delta_{kp}\|$ is $= 1$ and on the other hand it is equal to the product $\|a_{ki}\| \cdot \|c_{ip}\|$; hence the determinant of the $c_{ip} \neq 0$.
