If $\alpha$ has degree $m$ over $k$ and $\beta$ has degree $q$ over $K(\alpha;k)$, then $K(\alpha,\beta;k)$ has degree $mq$ over $k$. The $n=mq$ conjugates decompose into $m$ blocks of $q$ elements each, corresponding to relative conjugates.
