A set $S$ of integers which along with two numbers $m$ and $n$ always contains $m \pm n$ and is not equal to $\{0\}$ (a module) consists exactly of all multiples of a uniquely determined positive integer $d$. In particular, every subgroup of the additive group of integers is cyclic.
