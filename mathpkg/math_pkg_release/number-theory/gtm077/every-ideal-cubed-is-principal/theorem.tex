If $h$ is the class number of $K$, then for every ideal $\mathfrak{a}$ in $K$, $\mathfrak{a}^h$ is a principal ideal. Equivalently, the ideal class group has exponent dividing $h$ (in fact, it is annihilated by $h$).
