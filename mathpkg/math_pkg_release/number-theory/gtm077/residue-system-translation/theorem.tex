If $x_1, x_2, \ldots, x_n$ forms a complete system of residues mod $n(n > 0)$, then $ax_1 + b, \ldots, ax_n + b$ is also such a system, as long as $a$ and $b$ are integers and $(a, n) = 1$.

For by Theorem 6 the $n$ numbers $ax_i + b$ ($i = 1, 2, \ldots, n$) are likewise incongruent numbers modulo $n$.

A representation of a residue system with respect to a composite modulus, which is often useful, is given by the following:
