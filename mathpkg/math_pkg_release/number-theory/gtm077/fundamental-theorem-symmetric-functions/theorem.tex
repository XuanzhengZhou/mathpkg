Every polynomial symmetric in $\alpha_1,\ldots,\alpha_n$ with coefficients in $k$ can be expressed as a polynomial in the elementary symmetric functions of the $\alpha_i$, using only addition, subtraction, and multiplication.
