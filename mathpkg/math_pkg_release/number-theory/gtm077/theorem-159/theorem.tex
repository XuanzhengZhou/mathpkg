The theta-series defined by (175) also admits the representation

$$\theta(t, z; \mathfrak{a})$$

$$= \frac{1}{N(\mathfrak{a})|\sqrt{d}| \sqrt{t_1 \cdot t_2 \cdots t_n}} \sum_{\lambda \text{ in } 1/\mathfrak{a}\mathfrak{b}} \exp \left\{ -\pi \sum_{p=1}^{n} \frac{\lambda^{(p)}^2}{t_p} - 2\pi i \sum_{p=1}^{n} \lambda

holds for the functions of $t_1, \ldots, t_n$

$$\theta(t; \mathfrak{f}) = \theta(t, 0; \mathfrak{f}) = \sum_{\mu \in \mathfrak{f}} \exp \left\{ -\pi \sum_{p=1}^{n} t_p \mu^{(p)^2} \right\}.$$

Moreover, we deduce from Theorem 159
