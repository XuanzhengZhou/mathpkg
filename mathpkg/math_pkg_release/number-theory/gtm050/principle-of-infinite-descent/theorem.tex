Suppose that the assumption that a given positive integer has a given set of properties implies that there is a smaller positive integer with the same set of properties. Then no positive integer can have this set of properties.
