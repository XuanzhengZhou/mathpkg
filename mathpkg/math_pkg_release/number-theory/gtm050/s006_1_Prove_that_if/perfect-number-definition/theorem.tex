A positive integer $n$ is called \textit{perfect} if it is equal to the sum of its proper divisors (i.e., all positive divisors of $n$ other than $n$ itself). Equivalently, $\sigma(n) = 2n$ where $\sigma(n)$ is the sum-of-divisors function.

For example, $6 = 1 + 2 + 3$ is perfect.
