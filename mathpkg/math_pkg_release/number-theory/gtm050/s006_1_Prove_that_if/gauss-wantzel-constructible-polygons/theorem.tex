A regular $n$-gon can be constructed with ruler and compass if and only if
$$n = 2^k \, p_1 p_2 \ldots p_m$$
where the $p_i$ are distinct prime Fermat numbers (i.e., primes of the form $2^{2^j} + 1$) and $k \geq 0$.

Gauss proved the sufficiency; Wantzel proved the necessity.
