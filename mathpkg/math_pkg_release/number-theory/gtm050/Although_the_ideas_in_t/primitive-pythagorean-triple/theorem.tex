A Pythagorean triple $(x, y, z)$ with $x, y, z$ positive integers satisfying
$$x^2 + y^2 = z^2$$
is called \textbf{primitive} if $x, y, z$ have no common divisor other than $1$, i.e.
$$\gcd(x, y, z) = 1.$$
