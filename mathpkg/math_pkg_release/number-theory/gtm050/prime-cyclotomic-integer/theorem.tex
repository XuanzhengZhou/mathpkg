A cyclotomic integer $h(\alpha)$ is called \text{prime} if it is not a unit and has the property that whenever $h(\alpha) \mid f(\alpha)g(\alpha)$, then $h(\alpha) \mid f(\alpha)$ or $h(\alpha) \mid g(\alpha)$.
