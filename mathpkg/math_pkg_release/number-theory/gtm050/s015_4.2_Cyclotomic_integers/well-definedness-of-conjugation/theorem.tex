Let $f(\alpha)$ and $g(\alpha)$ be cyclotomic integers with respect to a primitive $\lambda$th root of unity $\alpha$. If $f(\alpha) = g(\alpha)$ as complex numbers, then for each $j = 2, 3, \ldots, \lambda-1$,
$$f(\alpha^j) = g(\alpha^j).$$
