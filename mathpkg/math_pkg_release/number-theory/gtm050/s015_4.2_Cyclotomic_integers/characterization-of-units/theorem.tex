Let $f(\alpha)$ and $g(\alpha)$ be cyclotomic integers. If $f(\alpha)g(\alpha) = 1$, then $f(\alpha)$ is a unit (i.e., $Nf(\alpha) = 1$) and
$$g(\alpha) = f(\alpha^2)f(\alpha^3)\cdots f(\alpha^{\lambda-1}).$$
