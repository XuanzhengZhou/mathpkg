A cyclotomic integer $f(\alpha)$ is called a \textbf{unit} if its norm equals $1$:
$$Nf(\alpha) = 1.$$
If $f(\alpha)$ is a unit, then its inverse is given by
$$f(\alpha)^{-1} = f(\alpha^2)f(\alpha^3)\cdots f(\alpha^{\lambda-1}),$$
which is itself a cyclotomic integer.
