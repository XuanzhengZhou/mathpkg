If $x, y, z$ are positive integers such that $x^5 + y^5 = z^5$, then one of the numbers $x, y, z$ must be divisible by 5.

Equivalently, if $x^5 + y^5 + z^5 = 0$ in nonzero integers, then $5 \mid xyz$.
