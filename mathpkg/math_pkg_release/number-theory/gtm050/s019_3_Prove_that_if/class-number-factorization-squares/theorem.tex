The class number $h$ of quadratic integers with determinant $D$ can be factored in two ways:
$$h = gn$$
where $g$ is the number of genera and $n$ is the number of classes per genus; and
$$h = sa$$
where $s$ is the number of distinct classes that are squares of other classes and $a$ is the number of ambiguous classes.
