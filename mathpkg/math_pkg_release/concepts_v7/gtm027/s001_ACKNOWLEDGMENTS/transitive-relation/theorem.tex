A relation $R$ is \text{transitive} iff whenever $xRy$ and $yRz$, then $xRz$. In terms of composition:
$$
R \circ R \subset R.
$$
If $R$ is both transitive and reflexive, then $R \circ R = R$ (idempotence under composition). The inverse of a transitive relation is transitive.
