A \text{set} is a class. Two sets are \text{identical} iff they have the same members:
$$
A = B \iff (\forall x)(x \in A \iff x \in B).
$$
The symbol $\in$ denotes \text{membership}: $x \in A$ iff $x$ is a member (an element, a point) of $A$.
