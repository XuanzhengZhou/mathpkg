A topological space $X$ is a \textbf{Boolean space} (or Stone space) if and only if $X$ is a compact Hausdorff space and the family of all subsets of $X$ that are both open and closed (clopen) forms a base for the topology of $X$.
