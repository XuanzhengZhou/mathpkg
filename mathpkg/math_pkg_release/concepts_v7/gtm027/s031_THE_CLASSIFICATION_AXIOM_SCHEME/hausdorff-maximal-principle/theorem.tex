\textbf{(Hausdorff Maximal Principle)} If $x$ is a set, then there is a nest $n \subset x$ such that if $m$ is a nest with $m \subset x$ and $n \subset m$, then $m = n$. That is, every set contains a maximal nest (with respect to inclusion).
