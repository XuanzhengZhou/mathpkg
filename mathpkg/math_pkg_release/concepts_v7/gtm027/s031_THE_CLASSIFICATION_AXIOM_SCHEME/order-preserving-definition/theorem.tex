$f$ is $r$-$s$ order preserving if and only if $f$ is a function, $r$ well-orders $\text{domain } f$, $s$ well-orders $\text{range } f$, and $f(u) \, s \, f(v)$ whenever $u$ and $v$ are members of $\text{domain } f$ such that $u \, r \, v$.
