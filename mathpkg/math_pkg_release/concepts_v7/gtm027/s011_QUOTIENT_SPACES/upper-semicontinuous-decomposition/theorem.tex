A decomposition $\mathfrak{D}$ of a topological space $X$ is \text{upper semi-continuous} if and only if for each $D \in \mathfrak{D}$ and each open set $U$ containing $D$, there exists an open set $V$ such that
$$D \subseteq V \subseteq U$$
and $V$ is a union of members of $\mathfrak{D}$.
