Let $Y$ be a subset of a linearly ordered set $(X, <)$. Then $Y$ inherits the linear order $<$ from $X$, but the order topology on $Y$ induced by this inherited order need not coincide with the subspace topology that $Y$ inherits from the order topology on $X$.
