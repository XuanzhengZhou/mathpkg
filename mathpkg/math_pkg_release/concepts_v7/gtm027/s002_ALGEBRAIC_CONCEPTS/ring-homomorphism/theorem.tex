A \textbf{ring homomorphism} (or \textbf{representation}) is a function $f$ on a ring to another ring such that
$$f(x + y) = f(x) + f(y) \quad \text{and} \quad f(x \cdot y) = f(x) \cdot f(y)$$
for all members $x$ and $y$ of the domain.
