Let $b$ be an integer greater than $1$ and let $\omega_{b-1} = \{q : q \in \omega \text{ and } q \leq b-1\}$.

A \textbf{$b$-adic expansion} $a$ is a function on the integers to $\omega_{b-1}$ such that there is a smallest integer $p$ for which $a_p = a(p)$ is not zero.
