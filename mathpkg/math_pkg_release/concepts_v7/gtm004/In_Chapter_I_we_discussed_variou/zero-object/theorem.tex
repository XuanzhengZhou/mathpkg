An object $0$ in a category $\mathfrak{C}$ is called a \text{zero object} if, for any object $X$ in $\mathfrak{C}$, the sets $\mathfrak{C}(X, 0)$ and $\mathfrak{C}(0, X)$ both consist of precisely one element. If $\mathfrak{C}$ possesses a zero object, then any two such objects are isomorphic.
