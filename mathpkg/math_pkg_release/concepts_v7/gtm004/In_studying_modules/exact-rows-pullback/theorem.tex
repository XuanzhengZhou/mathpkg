\text{Let}
$$
\begin{CD}
B @>{\kappa'}>> E' @>{\nu'}>> A' \\
@| @VVV @VVV \\
B @>>{\kappa}> E @>>{\nu}> A
\end{CD}
$$
\text{be a commutative diagram with exact rows, where the left-hand vertical map is the identity on $B$. Then the right-hand square is a pull-back diagram.}
