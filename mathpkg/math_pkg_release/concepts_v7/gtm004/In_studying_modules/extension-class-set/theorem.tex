\text{For $\Lambda$-modules $A$ and $B$, let $E(A, B)$ denote the set of equivalence classes of extensions of $A$ by $B$. The direct sum yields the distinguished element}
$$
B \xrightarrow{\iota_B} A \oplus B \xrightarrow{\pi_A} A
$$
\text{so $E(A, B)$ is always non-empty.}
