In a filtered (graded) differential group $C$, an element of $H_q(C^{(p)})$ is said to be \textbf{stable} if it is non-zero in every $H_q(C^{(r)})$ for $r \geq p$.

Similarly, an element of $H_q(C/C^{(p)})$ is said to be stable if it is non-zero in every $H_q(C/C^{(r)})$ for $r \geq p$.
