If $\mathfrak{U}$ is a subgroup of $\mathfrak{G}$ and $\mathfrak{G}/\mathfrak{U}$ has $r_0$ basis elements, then among the $h$ characters there exist $r_0$ characters $\chi_i$ of prime power orders such that $A \in \mathfrak{U}$ if and only if $\chi_i(A) = 1$ for $i = 1,\ldots,r_0$.
