For $s > 1$,
$$\sum_{\mathfrak{p}_1} \frac{1}{N(\mathfrak{p}_1)^s} = \log\frac{1}{s-1} + \text{bounded as } s \to 1^+,$$
where $\mathfrak{p}_1$ runs through all prime ideals of degree 1 in $K$. In particular, there are infinitely many prime ideals of degree 1 in every number field.
