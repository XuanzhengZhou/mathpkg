Suppose we have already defined the element $A_1 \cdot A_2 \cdots A_n$ of $S$ which is to denote the product of $n$ arbitrary elements $A_1, A_2, \ldots, A_n$. Then we define the product of $n + 1$ arbitrary elements $A_1, \ldots, A_{n+1}$ of $\mathfrak{G}$ by the equation

$$A_1 \cdot A_2 \cdots A_{n+1} = (A_1 \cdot A_2 \cdots A_n) \cdot A_{n+1}.$$

We now prove the
