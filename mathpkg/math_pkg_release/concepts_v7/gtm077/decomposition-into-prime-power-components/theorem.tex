Let $\mathfrak{G}$ be a finite Abelian group of order $h = a_1 a_2 \cdots a_r$, where the $a_i$ are pairwise relatively prime. Then each element $C$ of $\mathfrak{G}$ can be represented in one and only one way in the form
$$C = A_1 A_2 \cdots A_r$$
with $A_i^{a_i} = E$ for $i = 1, 2, \ldots, r$.
