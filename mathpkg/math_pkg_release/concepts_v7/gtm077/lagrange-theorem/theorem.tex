In a finite group $\mathfrak{G}$ of order $h$, the order $N$ of every subgroup $\mathfrak{U}$ is a divisor of $h$:
$$h = j \cdot N$$
where $j$ is the number of distinct cosets of $\mathfrak{U}$ in $\mathfrak{G}$, called the index of $\mathfrak{U}$.
