The congruence $ax + b \equiv 0 \pmod{n}$ has exactly one solution modulo $n$ if $(a,n) = 1$. If $(a,n) = d > 1$, then the congruence is solvable if and only if $d \mid b$, and in this case it has exactly $d$ distinct solutions modulo $n$.
