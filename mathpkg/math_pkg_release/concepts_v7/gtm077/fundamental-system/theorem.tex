$n$ numbers $\omega^{(1)},\ldots,\omega^{(n)}$ form a fundamental system of $K(\theta)$ over $k$ if every $\alpha \in K(\theta)$ can be uniquely expressed as $\alpha = \sum x_i\omega^{(i)}$ with $x_i \in k$.
