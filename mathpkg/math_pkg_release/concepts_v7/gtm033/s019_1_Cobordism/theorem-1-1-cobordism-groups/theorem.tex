The operation of disjoint union makes $\mathfrak{N}^n$ and $\Omega^n$ into abelian groups. The zero element is the class of any manifold which is a boundary. The inverse of $[M]$ is $[M]$ itself (in $\mathfrak{N}^n$), and the inverse of $[M, \omega]$ is $[M, -\omega]$ (in $\Omega^n$).
