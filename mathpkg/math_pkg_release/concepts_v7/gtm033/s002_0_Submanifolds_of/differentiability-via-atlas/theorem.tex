In terms of the atlas $\{(\varphi_i, U_i)\}$ for $S^n$, a map $f: S^n \to \mathbb{R}^k$ is \textit{differentiable of class $C^r$} if each composite map

$$f \circ \varphi_i^{-1} : B \to \mathbb{R}^k$$

is $C^r$, where $B = \{y \in \mathbb{R}^n : |y| < 1\}$ is the open $n$-disk.
