For an atlas $\{(\varphi_i, U_i)\}$ on a manifold $M$, the coordinate changes (or transition maps)

$$\varphi_j \circ \varphi_i^{-1}: \varphi_i(U_i \cap U_j) \to \varphi_j(U_i \cap U_j)$$

are $C^r$ maps between open subsets of $\mathbb{R}^n$.
