Starting from two connected surfaces $M, N$ without boundary, the \text{connected sum} $M \# N$ is constructed by taking $M$ and $N$ to be disjoint, choosing an embedding $f: S^0 \times D^2 \to M \cup N$ with one disk in each, and forming

$$M \# N = (M \cup N)[f].$$
