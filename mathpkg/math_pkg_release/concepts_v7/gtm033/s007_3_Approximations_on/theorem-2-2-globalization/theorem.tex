Let $\mathcal{X}$ be a rich $C^r$ mapping class on $(M,N)$, $0 \leqslant r \leqslant \infty$. For every closed set $L \subset M$:

(a) $\mathcal{X}_L(M,N)$ is dense and open in $C^r_S(M,N)$;

(b) If $L$ is compact, $\mathcal{X}_L(M,N)$ is also dense and open in $C^r_W(M,N)$.
