If $F$ is a periodic distribution and $u$ is a smooth periodic function, the \textbf{convolution} $F * u$ is the function defined by
$$(F * u)(x) = F(T_x \tilde{u}),$$
where $\tilde{u}(x) = u(-x)$ and $(T_x \tilde{u})(y) = \tilde{u}(y - x)$.
