Suppose $X$ and $Y$ are vector spaces over the same scalar field. A function $T: X \to Y$ is said to be \textbf{linear} if for all vectors $x, x' \in X$ and all scalars $a$:
$$T(x + x') = T(x) + T(x'), \qquad T(ax) = a T(x).$$
