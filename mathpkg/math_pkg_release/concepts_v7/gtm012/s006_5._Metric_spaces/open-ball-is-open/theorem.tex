Let $(S, d)$ be a metric space. For any $x \in S$ and any $r > 0$, the ball $B_r(x)$ is an open set.

If $y \in B_r(x)$, then $B_s(y) \subset B_r(x)$ with $s = r - d(y, x)$. Since $y \in B_r(x)$, we have $s > 0$.
