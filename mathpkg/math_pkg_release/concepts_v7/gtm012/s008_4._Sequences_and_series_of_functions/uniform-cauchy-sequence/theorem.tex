A sequence of bounded functions $(f_n)_{n=1}^{\infty}$ from $S$ to $\mathbb{C}$ is said to be a \emph{uniform Cauchy sequence} if for each $\varepsilon > 0$ there is an integer $N$ so that
$$
\|f_n - f_m\| < \varepsilon \quad \text{if } n, m \geq N,
$$
where $\|f\| = \sup \{|f(x)| \mid x \in S\}$.
