An ordered structure $(M, <, \ldots)$ is called \text{o-minimal} (order-minimal) if every definable (with parameters) subset of $M$ is a finite union of open intervals and points. Equivalently, every definable subset of $M$ is a finite Boolean combination of intervals with endpoints in $M \cup \{\pm \infty\}$.
