Let $N = (N_t)_{t \geq 0}$ be a renewal process with inter-arrival distribution $F$. The \textbf{renewal function} $m(t) = E N_t$ satisfies the \textbf{renewal equation}

$$m(t) = F(t) + \int_0^t m(t - x) \, dF(x).$$
