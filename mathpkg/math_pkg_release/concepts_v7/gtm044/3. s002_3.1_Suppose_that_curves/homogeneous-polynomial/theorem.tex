A nonzero polynomial $q = q(X_1, \ldots, X_n) \in k[X_1, \ldots, X_n]$ is homogeneous (and $q$ is called a form) if all its terms have the same total degree; if this degree is $d$, then $q$ is homogeneous of degree $d$.
