If $$G$$ is a topological group, then the uniform topology deduced from its right uniform structure $$(G, \mathcal{U}_r)$$ coincides with the given topology on $$G$$. For each $$x \in G$$, $$V_r(x) = \{y : (x, y) \in V_r\} = \{y : yx^{-1} \in V\} = Vx$$.
