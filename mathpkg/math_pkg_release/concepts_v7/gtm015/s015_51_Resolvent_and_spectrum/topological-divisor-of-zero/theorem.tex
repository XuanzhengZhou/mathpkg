An element $x \in A$ is called a left topological divisor of zero (left TDZ) if there exists a sequence $y_n \in A$ with $\|y_n\| = 1$ such that $x y_n \to 0$; it is a right TDZ if $y_n x \to 0$; it is a two-sided TDZ if both $x y_n \to 0$ and $y_n x \to 0$.
