Let $A$ be a normed algebra (with or without unity element), let $A_1$ be the algebra unitification of $A$, and define
$$
\|x + \lambda u\| = \|x\| + |\lambda|.
$$
Then $A_1$ is a normed algebra with $\|u\| = 1$, and the natural embedding $x \mapsto (x, 0)$ of $A$ into $A_1$ is isometric.
