If $f: E \rightarrow F$ is a linear mapping, where $E$ is a vector space over $\mathbb{K}$ and $F$ is a locally convex TVS over $\mathbb{K}$, then the initial topology on $E$ for $f$ is locally convex.