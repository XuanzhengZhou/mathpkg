Let $E$ be a TVS over $\mathbb{C}$. A subset $M$ of $E$ is a closed, maximal $\mathbb{C}$-linear subspace if and only if $M = M_0 \cap (iM_0)$ with $M_0$ a closed, maximal $\mathbb{R}$-linear subspace.