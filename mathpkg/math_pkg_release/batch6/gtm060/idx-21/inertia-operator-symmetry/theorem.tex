The operator $A: K \to K$ defined by $A\Omega = \mathbf{M}$, where

$$\mathbf{M} = m[\mathbf{Q}, [\Omega, \mathbf{Q}]],$$

is symmetric: $(A\mathbf{X}, \mathbf{Y}) = (\mathbf{X}, A\mathbf{Y})$ for all $\mathbf{X}, \mathbf{Y} \in K$.
