For a two-dimensional velocity field $v$ with stream function $\psi$, the vorticity $r$ is expressed in terms of the stream function by the negative Laplacian:
$$r = -\Delta \psi,$$
where $\Delta = \operatorname{div} \operatorname{grad}$ is the Laplacian operator.
