Let $c: I \to \mathbb{R}^n$ ($n \geq 3$) be a closed space curve parametrized by arc length, with curvature $\kappa(t) > 0$. The \emph{total curvature} of $c$ is defined as
$$K(c) = \int_0^L |\kappa(t)|\,dt,$$
where $L$ is the length of $c$.
