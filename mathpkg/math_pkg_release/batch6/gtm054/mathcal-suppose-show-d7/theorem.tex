$\mu(S, T) = (-1)^{|S + T|}[S, T]$ for all $S, T \in \mathcal{P}(X)$.

By D2 and D3, $\mu(S, S + \{x\}) = -1$ for all $x \in U + S$. Now suppose $\mu(S, R)

We show that $\mu = \bar{\mu}$. By D9 and D5, for all $x, y \in U$,

$$\mu(x, y) = \sum_{w \in U} \mu(x, w) \delta_{w,y} = \sum_{w \in U} \mu(x, w) \sum_{z \in U} [w, z] \bar{\mu}(z, y)$$

$$= \sum_{z \in U} \bar{\mu}(z, y) \sum_{w \in U} \mu(x, w)[w, z] = \sum_{z \in U} \bar{\mu}(z, y) \delta_{x,z} = \bar{\mu}(x, y).$$

The following is the main result of this section.
