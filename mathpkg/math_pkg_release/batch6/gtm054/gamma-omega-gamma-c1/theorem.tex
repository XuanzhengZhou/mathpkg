$n(q_1, q_2) = 1 + \max\{\nu_0(\Gamma): \omega(\Gamma) < q_1 \text{ and } \alpha_0(\Gamma) < q_2\}$, if such a maximum exists. Were C1 taken as a definition, it would make sense in spite of B1 to allow $q_1$ or $q_2$ to equal 1. For the sake of ease in proving the next proposition it is convenient to do so, and we have immediately that
