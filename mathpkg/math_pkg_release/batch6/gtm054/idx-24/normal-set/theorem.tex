A subset $U \subseteq V$ of a graph $\Gamma = (V, \mathcal{E})$ is said to be \textbf{normal} if it is extremal, $V = U + N(U)$, and every component of $\Gamma_U$ is itself almost factorable (and hence odd).
