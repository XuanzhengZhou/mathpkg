A multigraph that is also a set system is called a \textbf{graph}. Equivalently, a graph is a pair $(V, \mathcal{E})$ where $\mathcal{E} \subseteq \mathcal{P}_2(V)$, i.e., every edge is a 2-element subset of $V$.
