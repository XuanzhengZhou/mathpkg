A system $\Lambda$ \textbf{distinguishes vertices} if for every two distinct vertices there is a block which contains exactly one of them.

Equivalently, $\Lambda^*$ is a set system if and only if $\Lambda$ distinguishes vertices.
