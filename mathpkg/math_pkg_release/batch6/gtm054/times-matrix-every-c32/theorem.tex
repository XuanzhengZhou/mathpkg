$H_1H_1^* = 4qI - J$.

Let $H$ be the $4q \times 4q$ matrix such that every entry in the first row and first column is 1, and when these two lines are suppressed, the remaining matrix is $H_1$. One easily verifies that $H$ is a $4q \times 4q$ matrix satisfying C26 if and only if $H_1$ is a $(4q - 1) \times (4q - 1)$ matrix whose entries are $\pm 1$, whose line sums are $-1$, and which satisfies C32. Finally, we observe that permuting the rows or the columns of $M$ has an identical effect on $H$ and vice versa, while the multiplication of any line of $M$ by $-1$ yields a matrix without constant row sums while the same operation on $H$ yields an equivalent Hadamard matrix.

The above symmetric block designs as well as those obtainable from them by the procedure of Proposition C2 are known as Hadam

PROOF. Let $H_i$ be an $m_i \times m_i$ Hadamard matrix ($i = 1, 2$). Then

$$\begin{align*}
(H_1 \times H_2)(H_1 \times H_2)^* &= (H_1 \times H_2)(H_1^* \times H_2^*) \\
&= H_1 H_1^* \times H_2 H_2^* \\
&= m_1 I_{m_1 \times m_1} \times m_2 I_{m_2 \times m_2} \\
&= m_1 m_2 I_{m_1 m_2 \times m_1 m_2}.
\end{align*}$$

by C34

by C35

by C26
