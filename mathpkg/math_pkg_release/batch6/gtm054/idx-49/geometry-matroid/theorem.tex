A matroid $\Lambda = (V, \mathcal{M})$ is called a **geometry** if $\mathcal{P}_0(V) \setminus \mathcal{P}_1(V) \subseteq \mathcal{C}(\Lambda)$; that is, every subset of $V$ of size 0 or 1 (excluding loops) is a closed set.

(Crapo and Rota use the term "pregeometry" for a matroid.)
