A graph is color-perfect if and only if its complement is color perfect.

It is immediate that if $\Gamma = (V, \mathcal{E})$ is color-perfect (respectively: perfect), then so is $\Gamma_s$ for all $S \in \mathcal{P}(V)$.
