\text{A cycle cover of a graph } $\Gamma$ \text{ is a sequence } $Z_1, \ldots, Z_m \in \mathcal{X}(\Gamma) + \{\varnothing\}$ \text{ such that each edge of } $\Gamma$ \text{ belongs to } $Z_i$ \text{ for at most two indices } $i$.
