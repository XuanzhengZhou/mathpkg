For each vertex $x \in V$, define $g_x \in \mathbb{Q}^W$ by
$$
g_x(u, v) = \begin{cases}
1 & \text{if } u = x; \\
-1 & \text{if } v = x; \\
0 & \text{otherwise}.
\end{cases}
$$
Clearly $\sigma(g_x) = f^*(x)$.
