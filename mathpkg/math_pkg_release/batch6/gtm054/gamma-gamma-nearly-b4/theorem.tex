$$\Gamma = (V, D)$$

$$\Gamma' = (V', D')$$

Hence $|S'| \geq |S|$, with a nearly identical argument from here on if $\Gamma$ is a directed graph.

If $S$ did not separate $Z$ from $A$ in $\Gamma$, there would exist an AZ-path $\Pi$ in $\Gamma_{(s)}$ whose initial vertex is some $a \in A + (A \cap S)$. This would imply that neither $a$ nor $\{c, a\}$ (respectively, $(c, a)$) belongs to $S'$. By adjoining $c$ and $\{c, a\}$ (respectively, $(c, a)$) to $\Pi$, one obtains a cZ-path in $\Gamma'_{(s')}$, and so $S'$ fails to separate $Z$ from $c$ in $\Gamma'$ as assumed. Hence $S$ separates $Z$ from $A$ in $\Gamma$. By hypothesis, $|S| \
