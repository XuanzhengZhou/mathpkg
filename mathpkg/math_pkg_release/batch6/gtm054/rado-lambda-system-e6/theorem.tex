(R. Rado [r.1]). Let $\Lambda = (V, f, E)$ be a system and let $\Theta = (V, \mathcal{M})$ be a matroid with rank function $r$. A necessary and sufficient condition for there to exist an LDR $\lambda$ of $\Lambda$ such that $\lambda[E] \in \mathcal{I}(\Theta)$ is that

$$r(\bigcup_{e \in F} f(e)) \geq |F| \quad \text{for all } F \in \mathcal{P}(E).$$

Proof: Necessity. Let $\lambda$ be an LDR of $\Lambda$ and suppose that $\lambda[E] \in \mathcal{I}(\Theta)$. If $F \subseteq E$, then certainly $\lambda[F] \in \mathcal{I}(\Theta
