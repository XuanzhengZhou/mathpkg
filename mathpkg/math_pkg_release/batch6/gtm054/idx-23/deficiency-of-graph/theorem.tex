The deficiency of a graph $\Gamma = (V, \mathcal{E})$ is defined as

$$\delta(\Gamma) = \max\{\delta(U) : U \in \mathcal{P}(V)\}$$

where $\delta(U) = \|U\| - |N(U)|$. It always holds that $\delta(\Gamma) \geq 0$ since $\delta(\varnothing) = 0$.
