If $a \in \mathbb{C}^N$, then $a$ is a unit if and only if $a_0 \neq 0$.

If $a$ is a unit of $\mathbb{C}^N$, its inverse will be denoted by $1/a$. If $a$ and $b$ are polynomials and $a_0 \neq 0$, then $b/a = b(1/a)$ is a rational function.
