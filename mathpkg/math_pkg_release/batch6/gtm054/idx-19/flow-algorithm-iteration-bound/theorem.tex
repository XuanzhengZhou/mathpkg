Let $(V, k)$ be an integral network, let $e_0 \in W$, and let $h$ be a feasible flow. With $h$ as initial feasible flow, the Flow Algorithm yields a maximum flow through $e_0$ within at most $k(e_0) - h(e_0)$ iterations.
