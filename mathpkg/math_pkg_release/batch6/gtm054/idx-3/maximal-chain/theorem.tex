The collection of all chains on $(U, \leq)$ is partially-ordered by set-inclusion. A maximal chain is a maximal element of this ordered collection. A maximal chain of $(U, \leq)$ contains both a unique maximal element of $(U, \leq)$ and a unique minimal element of $(U, \leq)$.
