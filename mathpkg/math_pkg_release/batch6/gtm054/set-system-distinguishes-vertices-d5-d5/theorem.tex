$\sum_{u \in U} \mu(x, u)[u, y] = \sum_{x \leq u \leq y} \mu(x, u) = \delta_{x,y}$ for all $x, y \in U$.

Example. Let $X$ be a finite set, and let us determine the Möbius function for $(\mathcal{P}(X), \subseteq)$. Let $S, T \in \mathcal{P}(X)$. It should quickly become apparent that if $S \subseteq T$, then $\mu(S, T)$ is dependent only upon $|S + T|$, the length of a chain from $S$ to $T$ in the Boolean lattice $(\mathcal{P}(X), \subseteq)$. We prove inductively that
