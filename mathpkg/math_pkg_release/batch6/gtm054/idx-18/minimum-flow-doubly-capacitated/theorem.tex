A feasible flow $h$ in a doubly-capacitated network $(V, k_1, k_2)$ is a \text{minimum flow through $e_0$} if $h(e_0) \leq h'(e_0)$ for any feasible flow $h'$. If $k_1$ itself is a flow, then it is a minimum flow through each edge.
