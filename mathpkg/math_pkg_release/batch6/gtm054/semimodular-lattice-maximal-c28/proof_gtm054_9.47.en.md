---
role: proof
locale: en
of_concept: semimodular-lattice-maximal-c28
source_book: gtm054
source_chapter: "9"
source_section: "XD"
---

(a) We proceed by induction on $|U|$. The result holds if $|U| \leq 2$. If it is not the case that $x = 0$ and $y = 1$, then since $L_{[x,y]}$ is semimodular, all maximal chains in $L_{[x,y]}$ have the same length. Now consider maximal chains $0 = x_0, \ldots, x_h = 1$ and $0 = y_0, \ldots, y_k = 1$ in $L$. If $x_1 \neq y_1$, let $z = x_1 \vee y_1$. Since both $x_1$ and $y_1$ are successors of 0, $z$ must be a successor of both $x

is in fact a lattice, although it need not be a sublattice of $(\mathcal{P}(V), \subseteq)$. If $U_1, U_2 \in \mathcal{C}$, their meet in $(\mathcal{P}(U), \subseteq)$ is of course $U_1 \cap U_2$, and by C12d, $U_1 \cap U_2 = c(U_1 \cap U_2) \in \mathcal{C}$. The join of $U_1$ and $U_2$ in $\mathcal{P}(V)$ is, of course $U_1 \cup U_2$, but in $(\mathcal{C}, \subseteq)$ it is $c(U_1 \cup U_2)$ which, as we already know, need not equal $U_1 \cup U_2$. Since $(\mathcal{C}, \subseteq)$ characterizes a closure structure, it characterizes the matroid derivable from it; the matroid is unique up to isomorphism.
