For a knot group, the 0th elementary ideal and 0th knot polynomial are trivial: $E_0 = \Delta_0 = 0$. This holds because a knot group has a presentation whose Alexander matrix is $(n-1) \times n$ (with one more generator than relator).
