If matrices $A$ and $A'$ are equivalent under operations (i)-(v) (row/column permutations, adding zero rows, row/column linear combinations, adjoining unit row and column), then $E_k(A) = E_k(A')$ for all $k$.
