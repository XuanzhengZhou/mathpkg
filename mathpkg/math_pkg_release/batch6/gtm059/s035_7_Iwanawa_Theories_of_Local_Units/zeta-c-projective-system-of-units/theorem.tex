Given $x = z \neq 1$, there exists $i \leq k$ such that if we let $b = i - 1$, and
$$\zeta_c = \zeta^0 \alpha b^0 \alpha b^0 - \zeta_c$$
then
$$(\alpha b) \alpha(\theta) \neq 0 \bmod p;$$
$$(\alpha b) \alpha(\theta) = \alpha b \gamma \text{ generates } U_c(\theta) \text{ over } U_c.$$
