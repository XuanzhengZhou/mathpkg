\text{The Dedekind determinant factorizes as}
$$\det\left( f(ab^{-1}) \right)_{a,b \in G} = \left( \sum_{a \in G} f(a) \right) \cdot \prod_{\chi \neq \chi_0} \sum_{a \in G} \chi(a) f(a),$$
\text{where } \chi_0 \text{ is the trivial character of } G.
