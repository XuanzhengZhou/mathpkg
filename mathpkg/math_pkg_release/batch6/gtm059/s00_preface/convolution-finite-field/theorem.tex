Let $F = \mathbb{F}_q$ be a finite field. For functions $f, g: F \to \mathbb{C}$, their convolution $f * g: F \to \mathbb{C}$ is defined by

$$(f * g)(x) = \sum_{y \in F} f(y) g(x - y).$$

Convolution is commutative: $f * g = g * f$.
