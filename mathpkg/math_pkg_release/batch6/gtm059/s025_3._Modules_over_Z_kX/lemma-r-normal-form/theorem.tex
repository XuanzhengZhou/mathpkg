Let $A = \mathbb{Z}_p[[T]]$. Suppose that $R$ is a relation matrix in $(r-1)$-normal form over $A$, with its first $r-1$ diagonal entries being distinguished polynomials. Then $R$ can be transformed by admissible operations into a matrix which is in $r$-normal form and has the same dimension as $R$.
