The uniformizer $\pi$ is a norm from every extension $K(A_\pi)$, where $A_\pi = \bigcup_n A_n$ is the union of all $\pi$-power torsion points. More precisely, for each $n \ge 1$, $\pi$ is a norm from $K(A_n)$ to $K$.
