If $X$ is complete, there is a natural group homomorphism, the \text{degree}, from $A^n(X)$ to $\mathbf{Z}$, defined by
$$\deg\left(\sum n_i P_i\right) = \sum n_i,$$
where the $P_i$ are points. This is well-defined on rational equivalence classes.
