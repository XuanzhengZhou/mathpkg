Let $P_1, \ldots, P_r$ be points in $\mathbf{P}^2$, no three collinear, and let $\mathfrak{d}$ be the linear system of all conics passing through $P_1, \ldots, P_r$. If $r \leqslant 4$, then $\mathfrak{d}$ has no unassigned base points. This remains true if $P_2$ is infinitely near $P_1$.
