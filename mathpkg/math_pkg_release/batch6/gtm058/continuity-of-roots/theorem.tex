Let $ be a finite extension of $\mathbb{Q}_p$, and let (X) \in K[X]$ have distinct roots in $\bar{\mathbb{Q}}_p$. For every $\varepsilon > 0$ there exists $\delta > 0$ such that if $\|f-g\|_p < \delta$ for another degree $ polynomial $, then for every root $\alpha_i$ of $ there is precisely one root $\beta_i$ of $ with $|\alpha_i - \beta_i|_p < \varepsilon$.
