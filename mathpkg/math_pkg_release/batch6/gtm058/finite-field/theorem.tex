A field with finitely many elements is called a \text{finite field}. Its characteristic must be a prime $. The set of residue classes modulo a prime $ forms a field $\mathbb{F}_p$ of $ elements. Every finite field contains $\mathbb{F}_p$ as a subfield (its prime field).
