The field $\Omega$ is defined as the completion of $\bar{\mathbb{Q}}_p$ with respect to the extended hBadic absolute value. The norm extends by $|x|_p = \lim_{i\to\infty}|x_i|_p$ for Cauchy sequences $\{x_i\}$ representing $. The ring of integers is v8-full-extract = \{x \in \Omega \mid |x|_p \leq 1\}$ with maximal ideal  = \{x \mid |x|_p < 1\}$.
