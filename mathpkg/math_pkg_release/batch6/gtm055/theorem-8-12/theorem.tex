(Lebesgue). If $f$ is a continuous monotone function defined on an interval $[a, b]$, then the derivative $f'$ exists at every point of $(a, b) \setminus Z$, where $Z$ is a set of Lebesgue measure zero (briefly: $f$ is differentiable a.e. $[\mu_1]$).
