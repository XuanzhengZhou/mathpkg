If $(X, S, \mu)$ is a measure space, then the collection $\mathcal{L}_\infty(X) = \mathcal{L}_\infty(X, S, \mu)$ of all essentially bounded, measurable, complex-valued functions on $X$ is a linear space (with respect to pointwise linear operations), and setting

$$\| f \|_\infty = \text{ess sup}_X |f|$$

defines a pseudonorm on $\mathcal{L}_\infty(X)$.

Note. The spaces $\mathcal{L}_p(X)$ really are generalizations of the spaces $(f_p)$, $1 \leq p \leq +\infty$. Indeed, $\mathcal{L}_p(X, S, \mu)$ coincides with $(f_p)$ if we take $\mathbb{N}_0$ for $X$ and let $\mu$ be the counting measure on $\mathbb{N}_0$ (Ex. 7J).

If $(X, S, \mu)$ is a measure space, and if $\mathcal{L}$ denotes the linear space of all those measurable complex-valued functions on $X$ that vanish outside of some set of measure zero $[\mu]$, then $\mathcal{L}$ is a linear manifold in $\mathcal{L}_p(X)$ for $1 \leq p \leq +\infty$; indeed, $\mathcal{L}$ is the zero space of the pseudonorm $\| \|_p$ for all $1 \leq p \leq +\infty$. Thus the elements of the associated space $\dot{\mathcal{L}}_p(X)$ (Prop. 11.17) are, in all cases, equivalence classes of functions equal a.e. $[\mu]$, and we confront a dilemma. The elements of $\mathcal{L}_p$ are familiar objects, viz., functions, but $\| \|_p$ is only a pseudonorm; the associated function $\| \|_p$ on $\dot{\mathcal{L

regarded as an element of $\mathcal{L}_p(X, \mathbf{S}, \mu)$, $1 \leq p < +\infty$, if there exists a set $D \subset A$ with $\mu(X \setminus D) = 0$ such that $f$ is measurable on $D$ and such that $\int_D |f|^p d\mu < +\infty$. Likewise, if $\tilde{f}$ is a function defined on a subset $\tilde{A}$ of $X$, and if $\tilde{f}$ is measurable on a set $\tilde{D} \subset \tilde{A}$ such that $\mu(X \setminus \tilde{D}) = 0$, then $f$ and $\tilde{f}$ will be regarded as the same element of $\mathcal{L}_p(X)$ if $f = \tilde{f}$ a.e. $[\mu]$ on $D \cap \tilde{D}$. Similar conventions are also observed concerning $\mathcal{L}_\alpha(X, \mathbf{S}, \mu)$.
