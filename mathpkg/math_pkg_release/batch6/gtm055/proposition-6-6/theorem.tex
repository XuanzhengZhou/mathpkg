Let $X$ be a measurable space and let $f$ be a real-valued function defined on $X$. Then the following three conditions are equivalent:

(i) $f$ is measurable,
(ii) There exists a sequence $\{s_n\}_{n=1}^{\infty}$ of measurable simple real-valued functions converging pointwise to $f$,
(iii) There exists a sequence $\{s_n\}_{n=1}^{\infty}$ of measurable simple real-valued functions converging pointwise to $f$ and satisfying the following additional conditions:

(a) $|s_n(x)| \leq |f(x)|$ for all $n$ in $\mathbb{N}$ and $x$ in $X$,
(b) At each point $x$ of $X$, either $0 \leq s_1(x) \leq \cdots \leq s_n(x) \leq \cdots$, or $0 \geq s_1(x) \geq \cdots \geq s_n(x) \geq \cdots$,
(c) If $N$ is a positive integer and if $|f(x)| \leq N$, then $|f(x) - s_n(x)| \leq 1/2^n$ for every $n \geq N$. In particular, the convergence of $\{s_n\}$ to $f$ is uniform on every set on which $f$ is bounded.

PROBLEMS

A. Let $X$ and $Y$ be sets, and let $\Phi: X \rightarrow Y$ be a mapping of $X$ into $Y$. Then the induced mapping $\Phi^{-1}$ of the power class on $Y$ into the power class on $X$ preserves arbitrary unions and intersections as well as complements, differences, and symmetric differences. (Thus, for example, $\Phi^{-1}(A \nabla B) = \Phi^{-1}(A)

the measurability of extended real-valued functions on $X$, and use your findings to show that if $\{f_n\}$ is an arbitrary sequence of measurable extended real-valued functions defined on $X$ then $\sup_n f_n$ and $\inf_n f_n$ are measurable. Show also that an arbitrary monotone real-valued function defined on an arbitrary subset $A$ of $\mathbb{R}$ is Borel measurable on $A$.

D. If $\mathcal{C}$ is a collection of subsets of $X$, and if $M$ is a fixed subset of $X$, then the collection $\mathcal{C}_M = \{C \cap M : C \in \mathcal{C}\}$ is called the trace of $\mathcal{C}$ on $M$. Show that the $\sigma$-ring generated by the trace $\mathcal{C}_M$ coincides with the trace on $M$ of the $\sigma$-ring $\mathbf{S}(\mathcal{C})$ generated by $\mathcal{C}$. (Hint: It is readily verified that the trace of $\mathbf{S}(\mathcal{C})$ on $M$ is a $\sigma$-ring of subsets of $M$ that contains $\mathcal{C}_M$ and therefore contains $\mathbf{S}(\mathcal{C}_M)$; to go the other way, let $\mathbf{T}$ denote the collection of subsets $A$ of $X$ such that $A \cap M \in \mathbf{S}(\mathcal{C}_M)$, and show that $\mathbf{T}$ is a $\sigma$-ring.)

E. (i) Let $\{E_1, \ldots, E_n\}$ be a finite collection of subsets of a set $X$, and let $\mathcal{P}_0$ denote the collection of sets obtained by deleting from the partition of $X$ determined by $\{E_1, \ldots, E_n\}$ the one set $X \setminus (E_1 \cup \cdots \cup E_n)$. (See Problem 1L for an explanation of this terminology; if the sets $E_1, \ldots, E_n$ cover $X$, the set $X \setminus (E_1 \cup \cdots \cup E_n) = \emptyset$ need not be excluded.)

G. For an arbitrary nonempty collection $\%$ of subsets of a set $X$ let $\% \neq \%$ denote the collection of all differences $E \setminus F$, where $E$ and $F$ belong to $\%$. Furthermore, let $\%_\sigma$ denote the collection of all unions of countable subcollections of $\%$, and let $\%^* = (\% \neq \%)_\sigma$. In terms of this operation it is possible to describe the $\sigma$-ring $S(\%)$ generated by $\%$. We suppose, without loss of generality, that $\emptyset \in \%$. Let $\%^{(0)} = \%$, and, for a given countable ordinal number $\alpha$, suppose $\%^{(\xi)}$ already defined for every ordinal number $\xi$ less than $\alpha$. Then if $\alpha$ is not a limit ordinal we set $\%^{(\alpha)} = (\%^{(\xi)})^*$, where $\alpha = \xi + 1$, while if $\alpha$ is a limit ordinal we set $\%^{(\alpha)} = \bigcup_{\xi < \alpha} \%^{(\xi)}$. (See Problem 1V for a discussion of transfinite definitions.) Show that if $\Omega$ denotes, as usual, the smallest uncountable ordinal number (Prob. 1W), then $S(\%) = \bigcup_{\xi < \Omega} \%^{(\xi)}$.

H. Let $\%$ be a collection of subsets of a set $X$, and let $R$ and $S$ denote, respectively, the ring and $\sigma$-ring generated by $\%$. If $\%$ is finite, then so is $R$. What about $S$? If $\%$ is countable, then so is $R$, while for $S$ one has the estimate card $S \leq \aleph$ (where $\aleph$ denotes the cardinal number of the continuum). Indeed, card $\% \leq \aleph$ implies card $S \leq \aleph$. (Hint: If $\%$ consists of $k$ sets, then $R(\%)$ cannot contain more than $2^{2^k}$ sets; if card $\% \leq \aleph$, then card

A of $X$ with the property that if $D \in \mathcal{L}$, then $A \cap D \in \mathbf{Q}(\mathcal{L})$, and let $\mathbf{B}$ denote the collection of all subsets $B$ of $X$ with the property that if $Q \in \mathbf{Q}(\mathcal{L})$, then $B \cap Q \in \mathbf{Q}(\mathcal{L})$. Verify that $\mathbf{A}$ and $\mathbf{B}$ are both $\sigma$-quasirings containing $\mathcal{L}$, and use Problem J.

L. Let $X$ be a topological space and let $A$ be an arbitrary subset of $X$. If $\mathcal{T}$ denotes the topology of $X$, then the trace $\mathcal{T}_A$ of $\m
...
