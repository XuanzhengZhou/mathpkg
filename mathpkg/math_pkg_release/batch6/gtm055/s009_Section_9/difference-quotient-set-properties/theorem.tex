For an integrable function $ on a measure space, the sets where the difference quotients exceed a given threshold satisfy a covering lemma. Specifically, the sets where the upper derivate exceeds $lpha$ can be covered by intervals (or measurable sets) whose total measure is controlled by $rac{1}{lpha} \int |f| d\mu$.
