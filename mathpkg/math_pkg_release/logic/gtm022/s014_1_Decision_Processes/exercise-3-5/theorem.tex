A subset $U$ of $\mathbb{N}$ is called \textbf{recursively enumerable} if it is the range of a recursive function $f: \mathbb{N} \to \mathbb{N}$, or else is empty. Show that $U \subseteq \mathbb{N}$ is recursively enumerable if and only if it is the domain of a partial recursive function.
