The reverse substitution, where the first member of a relation is substituted for the second, is called a \text{backward step}. We write $w_2 \leftarrow w_1$ to denote that $w_1$ is obtainable from $w_2$ by a backward step.
