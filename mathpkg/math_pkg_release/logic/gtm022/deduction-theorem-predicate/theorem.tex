Let $A \subseteq P(V, \mathcal{R})$ and $p, q \in P(V, \mathcal{R})$. If $A \cup \{p\} \vdash q$ and the proof does not use Generalisation on any variable free in $p$, then $A \vdash p \Rightarrow q$.
