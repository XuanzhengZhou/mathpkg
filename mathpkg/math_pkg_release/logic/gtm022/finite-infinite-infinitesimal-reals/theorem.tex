An element $a \in *\mathbb{R}$ is called \emph{finite} if there exists a standard real number $b$ such that $|a| < b$. Otherwise $a$ is called \emph{infinite}. A finite element $a$ is called \emph{infinitesimal} if $|a| < b$ for all standard real numbers $b > 0$.
