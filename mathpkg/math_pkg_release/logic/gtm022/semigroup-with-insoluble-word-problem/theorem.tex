Let $E$ be a recursively enumerable but non-recursive subset of $\mathbb{N}$, and let $M$ be a Turing machine which stops on input $n$ iff $n \in E$. Then the semigroup presentation associated with $M$ has insoluble word problem.
