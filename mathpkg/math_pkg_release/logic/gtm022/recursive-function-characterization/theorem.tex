The set of recursive functions coincides with the set of functions obtainable from the set $I$ of initial functions (zero, successor, projection) by finite iterations of composition, primitive recursion, and minimalisation.
