For each set $a$, there exists a set $b$ such that, if $x \subseteq a$, then $x \in b$. Using (ZF2), we obtain the power set $\operatorname{Pow}(a) = \{x \mid x \subseteq a\}$. This allows formation of the Cartesian product $a \times b$.
