The set of recursive functions coincides with the set of functions obtainable from the initial functions (zero function $c(x) = 0$, successor function $s(x) = x+1$, projection functions $U_i^n$) by finite iterations of the operations of composition, primitive recursion, and minimalisation.
