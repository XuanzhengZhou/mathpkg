If $\pi$ is a property that is true of some set, then there exists a set $a$ such that $\pi(a)$ holds but for every $b \in a$, $\pi(b)$ fails. Equivalently, every non-empty set has an $\in$-minimal element, which implies there are no infinite descending $\in$-chains and $a \notin a$ for all $a$.
