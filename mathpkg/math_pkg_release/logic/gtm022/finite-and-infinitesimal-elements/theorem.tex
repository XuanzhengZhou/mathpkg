An element $a \in *\mathbb{R}$ is called finite if there exists a standard real number $b$ such that $|a| < b$. Otherwise, $a$ is called infinite. A finite element $a$ is called infinitesimal if $|a| < b$ for all standard real numbers $b > 0$. We write $a \cong b$ if $a - b$ is infinitesimal.
