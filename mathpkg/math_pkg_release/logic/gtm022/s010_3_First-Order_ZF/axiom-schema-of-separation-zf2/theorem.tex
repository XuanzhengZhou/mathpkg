$$
(\forall a)(\exists b)(\forall x)\bigl(x \in b \Leftrightarrow (x \in a \land \pi(x))\bigr),
$$
$$
\text{where } \pi(x) \in P(V, \mathcal{R}) \text{ is any predicate with one distinguished free variable } x.
$$
