Let $M_n$ denote the $n$th Turing machine. The stopping problem---determining for all integers $n, r$ whether or not there is a computation of $M_n$ beginning with state $q_0 \operatorname{code}(r)$---is recursively insoluble.
