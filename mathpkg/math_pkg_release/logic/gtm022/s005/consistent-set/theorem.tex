Let  \subseteq P(X)$. We say that $ is consistent if  \notin \text{Ded}(A)$. $ is called a maximal consistent subset if $ is consistent and if every subset  \subseteq P(X)$ which properly contains $ is inconsistent.
