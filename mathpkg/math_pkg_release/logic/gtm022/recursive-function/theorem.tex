A function $f: U \to \mathbb{N}^t$ with domain $U \subseteq \mathbb{N}^k$ is called \emph{(partial) recursive} if there exists a Turing machine $M$ such that $\Psi_M^{(k, t)} = f$, where $\Psi_M^{(k,t)}$ is the function computed by $M$ on inputs coded as $s_1^{n_1}s_0 s_1^{n_2}s_0 \cdots$.
