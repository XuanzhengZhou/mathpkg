Let $r$ be a binary predicate symbol. Then $\operatorname{Pred}(V, \{r\})$ is undecidable. Consequently, if $\mathcal{R}$ contains at least one $n$-ary relation symbol with $n \geqslant 2$, then $\operatorname{Pred}(V, \mathcal{R})$ is undecidable.
