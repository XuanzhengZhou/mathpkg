Let $(\alpha, \beta) : (P_1, V_1) \rightarrow (P_2, V_2)$ be a semi-homomorphism. Let $A \subseteq P_1$, $p \in P_1$.

\begin{enumerate}
\item[(a)] If $A \vdash p$, then $\alpha(A) \vdash \alpha(p)$.
\item[(b)] If $A \vDash p$, then $\alpha(A) \vDash \alpha(p)$.
\end{enumerate}
