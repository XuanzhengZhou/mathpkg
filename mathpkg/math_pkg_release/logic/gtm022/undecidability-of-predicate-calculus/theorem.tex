If $\mathcal{R}$ contains at least one $n$-ary relation symbol with $n \geqslant 2$, then $\operatorname{Pred}(V, \mathcal{R})$ is undecidable. Moreover (Kalmar's Theorem), $\operatorname{Pred}(V, \{r\})$ is undecidable for any binary predicate symbol $r$.
