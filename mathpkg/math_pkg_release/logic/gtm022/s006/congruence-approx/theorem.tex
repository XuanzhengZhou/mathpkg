We define the congruence $\approx$ on $\tilde{P}(V, \mathcal{R})$ identifying expressions differing only by renaming of bound variables ($\alpha$-conversion). The reduced predicate algebra is $P(V, \mathcal{R}) = \tilde{P}(V, \mathcal{R})/{\approx}$.
