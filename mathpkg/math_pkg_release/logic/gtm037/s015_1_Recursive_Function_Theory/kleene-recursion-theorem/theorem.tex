If $m > 1$ and $f$ is an $m$-ary partial recursive function, then there is an $e \in \omega$ such that for all $x_0, \dots, x_{m-2} \in \omega$,

$$\varphi^{m-1}_e(x_0, \dots, x_{m-2}) \simeq f(e, x_0, \dots, x_{m-2}).$$
