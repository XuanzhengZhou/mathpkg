Suppose $\mathfrak{A} \subseteq \mathfrak{B}$, $x \in {}^\omega A$, and $\sigma$ is a term. Then
$$\sigma^{\mathfrak{A}}(x) = \sigma^{\mathfrak{B}}(x).$$

\text{[Statement truncated in source — the full proposition asserts that the evaluation of a term in a substructure coincides with its evaluation in the larger structure, as the operations are the restrictions.]}
