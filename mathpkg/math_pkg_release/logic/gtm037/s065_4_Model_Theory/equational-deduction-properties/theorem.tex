(i) $\Gamma \vdash_{\text{eq}} \sigma = \sigma$;
(ii) if $\Gamma \vdash_{\text{eq}} \sigma = \tau$, then $\Gamma \vdash_{\text{eq}} \tau = \sigma$;
(iii) if $\Gamma \vdash_{\text{eq}} \sigma = \tau$ and $\Gamma \vdash_{\text{eq}} \tau = \rho$, then $\Gamma \vdash_{\text{eq}} \sigma = \rho$;
(iv) if $O$ is an operation symbol of rank $m$ and $\Gamma \vdash_{\text{eq}} \sigma_i = \tau_i$ for each $i < m$, then $\Gamma \vdash_{\text{eq}} O\sigma_0 \cdots \sigma_{m-1} = O\tau_0 \cdots \tau_{m-1}$.
