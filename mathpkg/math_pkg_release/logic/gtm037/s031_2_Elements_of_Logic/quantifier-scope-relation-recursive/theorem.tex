Let $R = \{(m, i, x, y) : m = g\alpha$ for some variable $\alpha$, $x = g^+\varphi$ and $y = g^+\psi$ for some formulas $\varphi, \psi$, $i < \text{Dmn }\varphi$, and $\forall$ is a quantifier on $\alpha$ at the $i$th place in $\varphi$ with scope $\psi\}$. Then $R$ is recursive.
