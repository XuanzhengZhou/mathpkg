An ultrafilter $F$ over $I$ is \text{principal} (or \text{fixed}) if there is an $i \in I$ such that
$$F = \{a \subseteq I : i \in a\}.$$
An ultrafilter that is not principal is called \text{nonprincipal} (or \text{free}).
