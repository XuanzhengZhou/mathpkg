Let $\mathfrak{A}$ be an $\mathfrak{L}$-structure, $I$ a non-empty set, and $F$ an ultrafilter over $I$. For each $a \in A$, set

$$fa = [\langle a : i \in I \rangle]_{\overline{F}}.$$

Then $f$ is an elementary embedding of $\mathfrak{A}$ into $I_{\mathfrak{A}/\overline{F}}$.
