Let $\mathcal{L}$ be a language with at least two unary operation symbols. Then the set
$$
\{\varphi \in \text{Sent}_{\mathcal{L}} : \models \varphi\}
$$
of logically valid sentences of $\mathcal{L}$ is finitely inseparable, and hence undecidable.
