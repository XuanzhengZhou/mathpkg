We extend $g$ and $g^+$ further to finite sequences of expressions. If $\varphi = \langle \varphi_0, \ldots, \varphi_{m-1} \rangle$ is a finite sequence of expressions, let

$$g^{++}(\varphi) = \prod_{i < m} p_i^{g^{+}(\varphi_i)}.$$
