The set $\{ \varphi : \varphi \text{ is a sentence of } \mathcal{L} \text{ and } \models_f \varphi \}$ is recursively enumerable (if the language is effective), where $\models_f \varphi$ means $\varphi$ holds in every $\mathcal{L}$-structure with every frame.
