The set $\{ \varphi : \varphi \text{ is a monadic second-order sentence and } Q^m \models \varphi \}$ is not recursively enumerable, where $Q^m$ is the theory $Q$ plus the second-order induction axiom.
