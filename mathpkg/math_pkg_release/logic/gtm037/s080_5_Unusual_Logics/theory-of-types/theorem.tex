If we imagine iterating the process which led from first-order logic to second-order logic, we arrive at third-, fourth-, etc., order logic. The union of them all is the \textbf{theory of types}. In this theory, variables are assigned types built up inductively from a base type $0$, and quantification over higher-type objects is allowed.
