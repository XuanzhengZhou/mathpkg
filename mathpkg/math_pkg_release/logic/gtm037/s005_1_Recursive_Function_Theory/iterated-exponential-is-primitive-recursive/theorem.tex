The **iterated exponential** function $a : \omega^2 \to \omega$ is defined by:

$$a(m, 0) = m,$$
$$a(m, n + 1) = m^{a(m, n)}.$$

Thus $a(m, n)$ is $m$ raised to the $m$ power $n$ times: $a(m, n) = m^{m^{\cdot^{\cdot^{m}}}}$ ($n$ iterations). This function is primitive recursive.
