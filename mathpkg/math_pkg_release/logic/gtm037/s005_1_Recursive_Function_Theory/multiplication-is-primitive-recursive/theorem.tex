The binary function $\cdot$ on $\omega$ is primitive recursive, defined by the primitive recursion:

$$x \cdot 0 = C_0^1 x,$$
$$x \cdot \delta y = x \cdot y + x = +(U_2^3(x, y, x \cdot y), U_0^3(x, y, x \cdot y)).$$
