$$\text{Start} \rightarrow T_{l\text{seek} 0} \rightarrow T_{\text{right}} \xrightarrow{\text{if 1}} T_{\text{left}}$$

$T_{lend}$ moves the tape to the right until finding $00$, and stops on the right-most of these two zeros. $T_{lend}$ does not start counting zeros until moving the tape.
