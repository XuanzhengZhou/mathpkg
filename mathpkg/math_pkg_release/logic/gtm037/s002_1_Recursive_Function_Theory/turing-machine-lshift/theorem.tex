$$\text{Start} \rightarrow T_{\text{seek 0}} \rightarrow T_{\text{right}} \xrightarrow{\text{if 1}} T_0 \rightarrow T_{l\text{trans}}$$

$T_{l\text{shift}}$ acts as follows in the case of interest:
\[
\begin{array}{c}
0 & 1^{(x+1)} & 0 & 1^{(y+1)} & 0 \\
\vdots \\
0 & 1^{(y+1)} & 0 & 0^{(x+2)}
\end{array}
\]

The tape to the left and right of this portion of $x + y + 5$ symbols is unchanged.
