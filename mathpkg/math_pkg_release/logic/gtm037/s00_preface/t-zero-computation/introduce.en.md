---
role: introduce
locale: en
content_hash: ""
written_against: ""
---

This proposition characterizes the computation performed by the Turing machine $T_0$. The behavior depends on the symbol currently scanned. If the scanned square already contains $0$, the machine stops immediately -- a single-configuration computation. If the scanned square contains $1$, the machine executes one computation step: it writes $0$ (action $v_j = 0$), stays in state $0$, and then in the next step finds $0$ and stops. Note: the original source text is truncated; the statement has been reconstructed from the definition of $T_0$ using mathematical reasoning.
