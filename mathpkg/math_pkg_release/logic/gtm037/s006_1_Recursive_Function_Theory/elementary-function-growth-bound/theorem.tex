If $g$ is a $k$-ary elementary function then there exists an $m \in \omega$ such that for all $x_0, \ldots, x_{k-1} \in \omega$, if $\max(x_0, \ldots, x_{k-1}) > 1$ then
$$g(x_0, \ldots, x_{k-1}) < a(\max(x_0, \ldots, x_{k-1}), m).$$