Let $f$ be a unary partial function. Then the following conditions are equivalent:

\begin{enumerate}
\item[(i)] $f$ is partial recursive;
\item[(ii)] $\{(x, fx) : x \in \operatorname{Dmn} f\}$ is recursively enumerable.
\end{enumerate}
