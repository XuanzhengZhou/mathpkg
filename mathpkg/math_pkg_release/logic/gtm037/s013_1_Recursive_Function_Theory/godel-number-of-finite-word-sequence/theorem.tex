If $\langle d_0, \ldots, d_m \rangle$ is a finite sequence of words, its Gödel number is

$$\prod_{i < m} p_i^{g_i d_i}.$$

Also let $R_4 = \{(m, n) : m \text{ is the Gödel number of a Markov algorithm } A\text{, and } n \text{ is the Gödel number of a computation under } A\}$.
