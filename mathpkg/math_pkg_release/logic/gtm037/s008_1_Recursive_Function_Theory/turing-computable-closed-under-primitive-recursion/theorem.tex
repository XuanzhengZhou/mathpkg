If $f$ is a binary operation on $\omega$ computed by a Turing machine, and $a \in \omega$, then the function $g$ defined from $f$ and $a$ by primitive recursion without parameters is also Turing computable.
