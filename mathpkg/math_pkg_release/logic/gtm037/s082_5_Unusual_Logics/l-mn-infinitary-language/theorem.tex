An L_{mn}-language allows conjunction and disjunction of fewer than m formulas, and universal quantification on blocks of fewer than n variables. Formation rules: if <phi_alpha : alpha < p> is a system of formulas with p < m, then bigwedge and bigvee are formulas; universal quantification over fewer than n variables is allowed.
