Let $G$ be a function of sets whose values are sets. Then there exists a unique function $F$ on the ordinals such that
$$F(\alpha) = G(\text{the set of values of } F \text{ on the elements of } \alpha).$$
In particular, $F(0) = G(\varnothing)$, $F(1) = G(\{F(0)\})$, etc.
