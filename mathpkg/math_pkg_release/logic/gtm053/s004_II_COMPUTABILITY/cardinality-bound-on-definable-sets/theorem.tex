The cardinality of the class of $\phi$-definable sets does not exceed
$$\operatorname{card}(\text{alphabet of } L) + \aleph_0,$$
where $\operatorname{card}(\text{alphabet of } L)$ means the cardinality of the alphabet of $L$ without the set of variables.
