If $M$ is infinite and $\operatorname{card}(\text{alphabet of } L) < 2^{\operatorname{card} M}$, then "almost all" subsets of $M$ are undefinable. More precisely, the cardinality of the set of $\phi$-definable subsets is strictly less than the cardinality of the power set of $M$.
