For an $L$-structure $A$, define its 	ext{diagram} $\mathrm{Diag}(A) = \{S : 	ext{atomic or negation of atomic } L_A	ext{-sentence, s.t. } A_A \models S\}$ and its 	ext{complete diagram} $\mathrm{CDiag}(A) = \{S : L_A	ext{-sentence s.t. } A_A \models S\}$.
For an $L$-structure $B$:
egin{enumerate}
\item[(i)] $B_A \models \mathrm{Diag}(A)$ iff $A \subseteq B$ (there is an embedding).
\item[(ii)] $B_A \models \mathrm{CDiag}(A)$ iff $A \preceq B$ (there is an elementary embedding).
nd{enumerate}
