$$\forall x (\neg x = \varnothing \Rightarrow \exists y (\text{"$y$ is a function with domain $x$"} \land \forall u (u \in x \land \neg u = \varnothing \Rightarrow \exists w (w \in u \land \langle u, w \rangle \in y))))$$
That is, $y$ chooses one element from each nonempty element $u \in x$.
