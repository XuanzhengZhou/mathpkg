Let $U$ be an ultrafilter on an index set $I$, and $\{A_i\}_{i \in I}$ a family of $L$-structures. The 	ext{ultraproduct} $\prod_{i \in I} A_i / U$ has domain the quotient of $\prod_i A_i$ by the equivalence $arphi pprox_U \psi$ if $\{i : arphi(i) = \psi(i)\} \in U$. Relations and operations are defined pointwise modulo $U$:
$$p(	ilde{arphi}_1,\ldots,	ilde{arphi}_n) 	ext{ holds iff } \{i : A_i \models p(arphi_1(i),\ldots,arphi_n(i))\} \in U.$$
When $A_i = A$ for all $i$, it is called an 	ext{ultrapower} $A^I/U$.
