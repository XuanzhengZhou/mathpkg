Given a saturated $L$-structure $\mathbf{A}$ and two $n$-tuples $\bar{a}$ and $\bar{b}$ in $\mathbf{A}$,

$$\text{tp}(\bar{a}) = \text{tp}(\bar{b}) \quad \text{iff there is } \pi \in \text{Aut}(\mathbf{A}) \text{ such that } \pi : \bar{a} \mapsto \bar{b}.$$
