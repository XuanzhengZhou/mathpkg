Let $M \subset N$ be two sets with interpretations $\phi_M$ and $\phi_N$. A formula $P$ in $L$ is called \text{$(M,N)$-absolute} if for all $\xi \in \overline{M}$ we have
$$|P|_M(\xi) = |P|_N(\xi).$$
