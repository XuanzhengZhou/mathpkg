There exists a finite graph $\Gamma_2$ with 117 vertices that can be realized on the unit sphere $S^2$ such that adjacent vertices correspond to orthogonal directions. Any mapping $k: \Gamma_2 \to \{0,1\}$ fails to satisfy the condition that on every triple of pairwise orthogonal vertices, exactly one maps to $0$ and two map to $1$.
