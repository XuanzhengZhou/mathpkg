A set $S \subset M^r$, $r \geqslant 1$, is called \text{$\phi$-definable} (by the formula $P$ in $L$ with the interpretation $\phi$) if there exist variables $x_1, \ldots, x_r$ such that for all $\xi$ in $\overline{M}$,
$$|P|_\phi(\xi) = 1 \iff \langle x_1^\xi, \ldots, x_r^\xi \rangle \in S.$$
