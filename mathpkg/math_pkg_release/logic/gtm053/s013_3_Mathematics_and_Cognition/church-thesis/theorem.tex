Church's Thesis (usual form):
(a) A function $f$ is semicomputable if and only if it is partial recursive.
(b) A function $f$ is computable if and only if both $f$ and $\chi_{D(f)}$ are partial recursive.

Everywhere defined partial recursive functions are called general recursive functions.