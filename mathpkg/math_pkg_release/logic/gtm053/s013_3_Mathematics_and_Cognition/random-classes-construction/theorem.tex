Let $P(x, y_1, \ldots, y_n)$ be a formula that does not contain any free variables besides $x, y_1, \ldots, y_n$. Let $Y_1, \ldots, Y_n \in V^B$ be fixed. Then the function
$$X \mapsto W(X) = \|P(X, Y_1, \ldots, Y_n)\|$$
is a random class.