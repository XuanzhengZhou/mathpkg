An $n$-type $\tau$ in a complete theory $T$ is called principal if there exists a formula $P(\bar{x})$ such that $T \models \exists \bar{x}\, P(\bar{x})$ and for every $Q(\bar{x}) \in \tau$,

$$T \models \forall \bar{x}\, \big(P(\bar{x}) \to Q(\bar{x})\big).$$
