Let $(\mathcal{S}, \pi)$ be a sheaf over $B$, $W \subset B$ open and $s: W \to \mathcal{S}$ a mapping with $\pi \circ s = \text{id}_W$. Then $s \in \Gamma(W, \mathcal{S})$ if and only if $s(W)$ is open in $\mathcal{S}$.
