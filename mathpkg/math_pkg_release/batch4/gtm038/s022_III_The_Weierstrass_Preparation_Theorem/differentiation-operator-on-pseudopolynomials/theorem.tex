Let a mapping $D: A[u] \to A[u]$ be defined by
$$D\left( \sum_{v=0}^{s} A_v(\zeta) u^v \right) := \sum_{v=1}^{s} v \cdot A_v(\zeta) u^{v-1}.$$
If $\omega \in A[u]$, then $D(\omega) \in A[u]$ is called the \text{formal derivative} of $\omega$.
