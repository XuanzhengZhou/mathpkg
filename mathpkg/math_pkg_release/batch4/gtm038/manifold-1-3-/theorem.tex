Let $(X, \mathcal{H})$ be a complex manifold, $W \subset X$ an open subset. Then the mapping $\Gamma(W, \mathcal{H}) \to \mathcal{F}(W, \mathbb{C})$ given by $f \mapsto [f]$ is injective, and for every $f \in \Gamma(W, \mathcal{H})$, $[f]$ is continuous.
