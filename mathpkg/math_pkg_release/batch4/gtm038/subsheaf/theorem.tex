Let $(S, \pi)$ be a sheaf over $B$, $S^* \subset S$ open and $\pi^* := \pi|S^*$. Then $(S^*, \pi^*)$ is called a subsheaf of $S$ if for each $z \in B$, $S^*_z = S^* \cap S_z$ is a subgroup (resp. submodule) of $S_z$.
