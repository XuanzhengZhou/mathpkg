1. $d'$ and $d''$ are $\mathbb{C}$-linear operators with $d' + d'' = d$.
2. $d' d' = 0, d'' d'' = 0$ and $d' d'' + d'' d' = 0$.
3. $d'$, $d''$ are not real. Moreover $d' \varphi = d'' \varphi$ and $d'' \varphi = d' \varphi$.
4. If $\varphi$ is an $\ell$-form, $\psi$ arbitrary, then
