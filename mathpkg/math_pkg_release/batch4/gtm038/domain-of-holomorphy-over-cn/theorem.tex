A domain $\mathfrak{G} = (G, \pi, x_0)$ is called a domain of holomorphy if there exists an $f \in A(G)$ with $H_f(\mathfrak{G}) = \mathfrak{G}$. In the schlicht case this definition agrees with the old one.
