A sheaf $(S, \pi)$ over $B$ is called a $\mathbb{C}$-algebra sheaf if each stalk $S_z$ is a $\mathbb{C}$-algebra and the algebraic operations are continuous. A sheaf of modules over an algebra sheaf is defined analogously.
