Let $\mathfrak{G}_i = (G_i, \pi_i, x_i)$, $i = 1, 2$ be domains with base points and $\mathfrak{G}_1 < \mathfrak{G}_2$ by virtue of a continuous mapping $\varphi: G_1 \rightarrow G_2$. If $f$ is a complex valued function on $G_2$, then we define $f|G_1 := f \circ \varphi$.
