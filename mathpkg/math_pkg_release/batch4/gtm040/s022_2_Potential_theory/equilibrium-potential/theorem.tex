An equilibrium potential for a closed set $E \subset \mathbb{R}^3$ is a potential $g$ such that:

\begin{enumerate}
  \item $g(x) = 1$ for all $x \in E$,
  \item $g$ arises from a charge distribution $\mu$ whose support is contained in $E$.
\end{enumerate}
