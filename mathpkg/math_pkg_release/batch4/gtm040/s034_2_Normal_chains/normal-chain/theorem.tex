A chain is \textbf{normal} if for some fixed state 0 and for all $j$, the quantities $^0\lambda_j$ and $^0\hat{\lambda}_j$ both exist, where $^0\lambda_j = \lim_n \sum_k (P^n)_{mk} \, ^0H_{kj}$ is the probability that $j$ is entered in the long run before reaching 0.