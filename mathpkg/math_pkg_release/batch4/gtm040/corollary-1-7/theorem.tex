If $A$ and $B$ are non-negative matrices and $f$ is a column vector such that $A(Bf)$ and $(AB)f$ are both well defined, then $A(Bf) = (AB)f$. In particular, if $C$ is a non-negative matrix, if $n > 0$, and if $C^n f$ and $C(C^{n-1}f)$ are well defined, then $C^n f = C(C^{n-1}f)$.
