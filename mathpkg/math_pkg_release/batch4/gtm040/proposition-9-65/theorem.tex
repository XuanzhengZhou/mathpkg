If

Lemma 9-66: For any two distinct states $i$ and $j$, let $t_{i,j}(\omega) = t_j(\omega_{t_i})$ be the additional time needed to reach $j$ after $i$. Then for any powers $r$ and $s$

$$M_\pi[t_i^r t_{i,j}^s] = M_\pi[t_i^r] M_i[t_j^s].$$
