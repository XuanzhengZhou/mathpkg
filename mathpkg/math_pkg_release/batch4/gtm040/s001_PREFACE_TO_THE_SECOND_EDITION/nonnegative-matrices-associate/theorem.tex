Let $A$ and $B$ be non-negative matrices, let $\pi \geq 0$ be a row vector, and let $f \geq 0$ be a column vector, with index sets that are subsets of the non-negative integers. Then
$$\pi(Af) = (\pi A)f.$$
More generally, for any non-negative matrices where the products are defined, multiplication is associative.
