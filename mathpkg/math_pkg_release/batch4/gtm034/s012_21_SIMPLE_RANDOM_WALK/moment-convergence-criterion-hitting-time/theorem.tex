If for each integer $p \geq 0$,
$$\lim_{N \to \infty} \mathbf{E}_0 \left[ \left( \frac{\sigma^2 \mathbf{T}_N}{N^2} \right)^p \right] = m_p,$$
where $m_p$ are the moments of the distribution $F(x)$ given in P3, then $\mathbf{T}_N$ satisfies the limit theorem T2.
