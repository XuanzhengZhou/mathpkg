$\sum_{t \in B} A(x,t) K_B(t,y) = \delta(x,y)$, for $x,y$ in $B$.

This is just the definition of a matrix-inverse, and according to a standard theorem of algebra, this inverse is unique, if it exists.