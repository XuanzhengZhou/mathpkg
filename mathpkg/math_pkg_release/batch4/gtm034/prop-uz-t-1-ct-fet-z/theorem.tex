$U(z) = \lim_{t \to 1} \left\{ \sqrt{c(t)} f_e(t; z^{-1}) \right\}^{-1}, \quad |z| < 1,$

$V(z) = \lim_{t \to 1} \left\{ \sqrt{c(t)} f_i(t; z) \right\}^{-1}, \quad |z| < 1.$

The proof of P6 is immediate from D2 above and D17.3.

Now we shall connect $U(z)$ and $V(z)$ with the ladder random variables, but we will of course discuss $Z$ only when $T < \infty$ and $\bar{Z}$ only when $T^* < \infty$.