All aperiodic recurrent random walks have the property that \(a(x) > 0\) for every \(x \neq 0\) with one single exception. This exception is left- or right-continuous random walk whose variance \(\sigma^2\) is infinite.
