A particular case of a product is that in which all coordinate spaces are identical. Then the product $\{X: t \in A\}$ is simply the set of all functions on $A$ to $X$. In this case the product topology is frequently called the topology of pointwise convergence (the simple topology, or the topology of simple convergence).
