A point $x$ of a convex set $A$ is an \textbf{extreme point} of $A$ if and only if $x$ is not an interior point of any line segment whose endpoints belong to $A$. Equivalently, if $x = \lambda y + (1 - \lambda)z$ with $y, z \in A$ and $0 < \lambda < 1$, then $y = z = x$.
