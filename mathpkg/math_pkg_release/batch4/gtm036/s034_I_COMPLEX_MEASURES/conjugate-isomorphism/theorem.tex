By a \textbf{conjugate isomorphism} $T$ is meant a one-to-one mapping of one complex vector space onto another such that
$$T(ax + by) = \overline{a} T(x) + \overline{b} T(y)$$
for all scalars $a, b$ and vectors $x, y$, and $\|T(x)\| = \|x\|$ for all $x$ (in the case of normed spaces).
