Let $g$ be a bounded linear functional on a product space such that for each subset $C$ of the index set $A$, the restriction $g_*C$ is either $g$ or $0$. Define $m(C) = 1$ if $g_*C = g$ and $m(C) = 0$ if $g_*C = 0$. Then $m$ is a countably additive (simple) measure on $A$.
