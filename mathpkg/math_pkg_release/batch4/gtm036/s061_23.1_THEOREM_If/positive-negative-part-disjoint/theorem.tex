In a vector lattice, the positive part $x^+$ and negative part $x^-$ of any element $x$ are always disjoint: $x^+ \wedge x^- = 0$. Moreover, if $x$ and $y$ are arbitrary disjoint positive elements, then $(x - y)^+ = x$ and $(x - y)^- = y$.
