If $U$ is a subset of a linear space $E$ which is radial at $0$, the Minkowski functional (or gauge) for $U$ is the real-valued function $p$ defined on $E$ by
$$
p(x) = \inf\left\{a : \frac{1}{a}x \in U,\; a > 0\right\}.
$$
