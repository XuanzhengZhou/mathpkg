Given a vector ordering $\geq$ on a real linear space $E$, the \textbf{positive cone} of the ordering is the set
$$
\{x \in E : x \geq 0\}.
$$
The ordering is completely determined by its positive cone.
