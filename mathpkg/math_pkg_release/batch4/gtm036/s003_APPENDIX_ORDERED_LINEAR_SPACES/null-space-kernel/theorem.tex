The null space (or kernel) of a linear functional $ on a linear space $ is the set $\{x \in E : f(x) = 0\}$. The null space is a subspace of $. If $ is not identically zero, its null space has codimension one.
