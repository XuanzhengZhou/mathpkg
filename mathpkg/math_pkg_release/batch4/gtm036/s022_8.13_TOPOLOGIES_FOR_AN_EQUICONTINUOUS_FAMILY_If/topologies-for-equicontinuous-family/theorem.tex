If $M$ is an equicontinuous family of functions on $S$ to a linear topological space $E$, then the topology for $M$ of pointwise convergence on a subset $A$ of $S$ is identical with the topology of pointwise convergence on the closure $A^{-}$ of $A$.
