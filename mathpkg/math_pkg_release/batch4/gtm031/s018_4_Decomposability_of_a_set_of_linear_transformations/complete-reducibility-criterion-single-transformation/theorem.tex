A linear transformation $A$ in a finite-dimensional vector space $\mathfrak{R}$ over a field $\Phi$ is completely reducible if and only if its minimum polynomial $\mu(\lambda)$ is a product of distinct prime (irreducible) polynomials.
