Let $\mathfrak{R}$ be a vector space over a field $\Phi$ and let $\mathfrak{R}'$ be a total subspace of the conjugate space $\mathfrak{R}^*$. Then every automorphism of the ring $\mathfrak{L}(\mathfrak{R}' \mid \mathfrak{R})$ which leaves the elements of the center fixed (elementwise) is inner.
