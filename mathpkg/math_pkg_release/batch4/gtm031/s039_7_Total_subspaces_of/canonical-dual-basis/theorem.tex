\textbf{Definition.} For each $i$, define a linear function $e_i^*$ by
$$e_i^*(e_k) = \delta_{ik}.$$
Then $e_i^*$ is the linear function whose matrix relative to $(e_i)$ has a $1$ in the $i$ place and $0$'s elsewhere. The $e_i^*$ form a basis for $\mathfrak{R}_0^*$.
