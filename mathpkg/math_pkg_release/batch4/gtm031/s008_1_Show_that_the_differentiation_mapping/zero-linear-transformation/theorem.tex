The zero mapping $0$ is the linear transformation of $\mathfrak{R}_1$ into $\mathfrak{R}_2$ defined by

$$x0 = 0$$

for all $x \in \mathfrak{R}_1$, where the $0$ on the right is the zero vector in $\mathfrak{R}_2$.
