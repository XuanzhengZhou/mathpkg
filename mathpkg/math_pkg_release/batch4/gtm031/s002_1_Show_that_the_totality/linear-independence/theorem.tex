If $x_1, \cdots, x_m$ are not linearly dependent, then these vectors are said to be \textbf{linearly independent}.

Any non-vacuous subset of a linearly independent set is a linearly independent set. In particular, every vector in a linearly independent set must be $\neq 0$.
