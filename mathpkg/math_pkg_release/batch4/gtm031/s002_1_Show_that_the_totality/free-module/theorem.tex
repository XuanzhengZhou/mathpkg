If there exists a set of generators $S$ such that every $x$ can be written in one and only one way in the form $\sum \xi_i e_{\alpha_i}$ with $e_{\alpha_i} \in S$, then $\mathfrak{R}$ is called a \textbf{free module} and the set $S$ is called a \textbf{basis}.
