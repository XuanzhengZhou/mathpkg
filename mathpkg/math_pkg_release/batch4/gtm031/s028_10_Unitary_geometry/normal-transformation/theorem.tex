A linear transformation $A$ on a unitary space is called normal if $AA' = A'A$. Equivalently, in a unitary basis, the matrix $(\alpha)$ of $A$ satisfies $(\alpha)(\bar{\alpha})' = (\bar{\alpha})'(\alpha)$.
