A matrix $(\alpha)$ is called \textbf{normal} if it commutes with its transpose:
$$(\alpha)(\alpha)' = (\alpha)'(\alpha).$$

A linear transformation $A$ is normal if and only if its matrix relative to a Cartesian basis is normal.
