A square matrix $(\sigma)$ is called an \textbf{orthogonal matrix} if it satisfies

$$(\sigma)(\sigma)' = 1$$

where $(\sigma)'$ denotes the transpose of $(\sigma)$ and $1$ denotes the identity matrix. Equivalently, the rows (and columns) of $(\sigma)$ form an orthonormal set with respect to the standard dot product.
