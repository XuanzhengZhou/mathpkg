\text{Use Exercise 2 to prove that, if } \mathbb{C} \text{ is the field of complex numbers and } \zeta_1, \zeta_2, \dots, \zeta_n \text{ are the } n \text{ distinct } n\text{-th roots of unity, then the diagonal matrix } \operatorname{diag}\{\zeta_1, \zeta_2, \dots, \zeta_n\} \text{ and the companion matrix of } \lambda^n - 1 \text{ are similar in } \mathbb{C}_n.
