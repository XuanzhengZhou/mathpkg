\text{Show that, if } \mu(\lambda) \text{ is any polynomial with leading coefficient } 1, \text{ then there exists a matrix } (\alpha) \text{ that has } \mu(\lambda) \text{ as its minimum polynomial.}
