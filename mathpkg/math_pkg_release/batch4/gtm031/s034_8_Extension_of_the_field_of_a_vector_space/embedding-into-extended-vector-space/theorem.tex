For each $x \in \mathfrak{R}$, set $\bar{x} = 1 \times x$. Then the correspondence $x \mapsto \bar{x}$ is an equivalence between the vector space $\mathfrak{R}$ over $\Phi$ and a subspace $\bar{\mathfrak{R}}$ of $\mathfrak{R}_P$ over $\Phi$.
