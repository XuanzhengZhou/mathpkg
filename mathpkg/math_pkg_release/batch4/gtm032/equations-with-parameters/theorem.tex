Consider polynomial equations $f(x, y_1, \ldots, y_m) = 0$ with parameters $y_i$ over a real closed field $R$. The Tarski-Seidenberg theorem implies that the set of parameters for which the equation has a solution is a semi-algebraic set, describable by polynomial inequalities. The decision method effectively eliminates the quantifier $\exists x$.
