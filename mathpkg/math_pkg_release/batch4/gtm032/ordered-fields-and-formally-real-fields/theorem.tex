A field $\Phi$ is formally real if $-1$ is not a sum of squares in $\Phi$. An ordered field is a field with a total order compatible with addition and multiplication. Artin-Schreier: A field is formally real if and only if it admits an ordering. A field is real closed if it is formally real and has no proper formally real algebraic extension.
