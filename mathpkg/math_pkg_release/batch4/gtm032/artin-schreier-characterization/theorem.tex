A field $\Phi$ can be ordered (i.e., admits a total order compatible with field operations) if and only if $-1$ is not a sum of squares in $\Phi$. A field is real closed if and only if it is ordered, every positive element is a square, and every polynomial of odd degree has a root.
