A field $R$ is called real closed if it is formally real and no proper algebraic extension is formally real. Equivalently: $R$ is not algebraically closed but $R(\sqrt{-1})$ is algebraically closed. In a real closed field, every positive element is a square, and every polynomial of odd degree has a root.
