---
role: proof
locale: en
of_concept: type-realization-in-ultraproduct
source_book: gtm037
source_chapter: "26"
source_section: "Elementary Classes and Elementary Equivalence"
---

Let $A = \{c_\gamma : \gamma < \rho\}$ where $\rho < n^\partial$. For each $\nu < \mu$ let $B_\nu = \{\delta < n : \mathfrak{A} \models (\exists v_0 \varphi_\nu)[\langle a_{v_j\delta} : j < \omega \rangle]\}$. By hypothesis $B_\nu \in D$. Choose witnessing functions $g_\nu$ and let $G = \{g_\nu : \nu < \mu\}$. By Lemma 26.38, choose $F_1 \subseteq F$ with $|F_1| \leq m$ such that $(F \sim F_1, G, D)$ is $m$-consistent. Fix $f \in F \sim F_1$ and define $b_\delta = c_{f\delta}$ (or $c_0$ if out of range). Then for each $\nu$, the set $C_\nu = \{\delta < n : \mathfrak{A} \models \varphi_\nu[\ldots, b_\delta, \ldots]\}$ contains a set in $D$, so the $b_\delta$'s realize all $\varphi_\nu$ in the ultraproduct.
