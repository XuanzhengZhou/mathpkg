A relation $R \subseteq {}^m \omega$ is recursively enumerable (for brevity r.e.) if $R = \emptyset$ or there exist $m$ recursive functions $f_0, \ldots, f_{m-1}$ such that

$$R = \{(f_0 x, \ldots, f_{m-1} x) : x \in \omega\}.$$
