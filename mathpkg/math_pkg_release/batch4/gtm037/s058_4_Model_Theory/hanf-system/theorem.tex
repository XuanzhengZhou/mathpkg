A \textbf{Hanf system} is a ternary relation $R$ with the following property:

\begin{enumerate}
\item[(i)] $\mathrm{pr}_0^* R$ is a set (not a proper class). Recall that
$$\mathrm{pr}_0^* R = \{x : \exists y, z\; (x, y, z) \in R\}.$$
\end{enumerate}
