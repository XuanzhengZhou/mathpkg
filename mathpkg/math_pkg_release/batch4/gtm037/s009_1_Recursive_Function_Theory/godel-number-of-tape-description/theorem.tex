If $F$ is a tape description, then the G\"{o}del number of $F$, $gF$, is the number
$$\prod_{i = 0}^{\infty} p_i^{k_i},$$
where
$$k_i = \begin{cases} F(i/2) & \text{if } i \text{ is even}, \\ F(-(i + 1)/2) & \text{if } i \text{ is odd}. \end{cases}$$
