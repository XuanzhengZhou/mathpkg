For any set $\Gamma$ of sentences, $\text{Mod } \Gamma$ is the class of all models of $\Gamma$. A class $K$ is a universal class if $K = \text{Mod } \Gamma$ for some set $\Gamma$ of universal sentences. $K$ is an elementary class if $K = \text{Mod } \Gamma$ for some set $\Gamma$ of sentences.
