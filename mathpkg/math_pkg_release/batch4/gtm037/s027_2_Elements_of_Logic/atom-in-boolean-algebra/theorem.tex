An element $x \in A$ is an atom of $\mathfrak{A}$ if $x \neq 0$, while for all $y$, $0 \leq y \leq x$ implies $0 = y$ or $y = x$. $\mathfrak{A}$ is atomic if for every nonzero $x \in A$ there is an atom $a \leq x$. $\mathfrak{A}$ is atomless if $\mathfrak{A}$ has no atoms.
