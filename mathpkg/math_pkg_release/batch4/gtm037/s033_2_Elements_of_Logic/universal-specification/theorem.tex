If no free occurrence in $\varphi$ of the variable $\alpha$ is within the scope of a quantifier on a variable occurring in $\sigma$, then

$$\vdash \forall \alpha \varphi \rightarrow \text{Subf}_{\sigma}^{\alpha} \varphi.$$
