$T_{\text{left}}$ is the following Turing machine:
\[
\begin{array}{cccc}
0 & 0 & 3 & 1 \\
0 & 1 & 3 & 1 \\
1 & 0 & 4 & 1 \\
1 & 1 & 4 & 1
\end{array}
\]
That is, $T_{\text{left}}$ has states $0$ and $1$, with $0$ as the initial state. In state $0$, regardless of the scanned symbol ($0$ or $1$), the machine performs action $3$ (move tape one square to the left) and goes to state $1$. In state $1$, regardless of the scanned symbol, the machine performs action $4$ (stop).
