A theory $\Gamma$ in $\mathcal{L}_{\text{nos}}$ is $\omega$-consistent iff for every formula $\varphi$ with free variables among $\{v_0\}$, if $\varphi(\mathbf{x}) \in \Gamma$ for each $x \in \omega$, then $\exists v_0 \neg \varphi(v_0) \notin \Gamma$.
