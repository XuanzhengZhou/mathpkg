Let $R \subseteq {}^m\omega$ and let $\Gamma$ be a consistent recursively axiomatizable theory in $\mathcal{L}_{\text{nos}}$ such that $\neg(\mathbf{n} = \mathbf{p}) \in \Gamma$ whenever $n \neq p$. If $R$ is weakly syntactically definable in $\Gamma$ then $R$ is recursively enumerable.
