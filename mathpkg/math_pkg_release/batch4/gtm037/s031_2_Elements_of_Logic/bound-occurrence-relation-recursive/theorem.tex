Let $S = \{(m, k, x) : m = g\alpha$ for some variable $\alpha$, $x = g^+\varphi$ for some formula $\varphi$, $k < \text{Dmn }\varphi$, and $\alpha$ occurs bound at the $k$th place of $\varphi\}$. Then $S$ is recursive.
