In monadic second-order logic, the set $\{g^{+}\psi : \psi \text{ is a sentence and } \models \psi\}$ is not recursively enumerable. The theory $Q^m$ (arithmetic $Q$ plus the full second-order induction axiom) has, up to isomorphism, $(\omega, +, \cdot, 0, s)$ as its only model.
