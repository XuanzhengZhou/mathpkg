A representation $\rho: G \to \mathrm{GL}(V)$ is called irreducible (or simple) if $V \neq 0$ and no proper nonzero vector subspace of $V$ is stable under $G$.
Equivalently, $V$ has no subrepresentations other than $\{0\}$ and $V$ itself.
