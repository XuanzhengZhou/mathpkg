% Extracted from GTM042 Section 12.1. Source statement truncated by page break in OCR;
% reconstructed from standard Serre text.

Let $K$ be a field of characteristic zero, $\mathbb{C}$ an algebraic closure of $K$, and $G$ a finite group. A linear representation of $G$ over $\mathbb{C}$ is realizable over $K$ if and only if its character takes values in $K$.
