Let $X$ be the set of all $\Gamma_K$-elementary subgroups of $G$ (cf. 12.6). The homomorphisms

$$Ind: \bigoplus_{H \in X} R_k(H) \rightarrow R_k(G)$$

and

$$Ind: \bigoplus_{H \in X} P_k(H) \rightarrow P_k(G)$$

defined by the $Ind_H^G$, for $H \in X$, are surjective.

(In other words, th. 27 holds for $R_k$ and $P_k$.)

Let $l_K$ (resp. $l_k$) denote the identity element of the ring $R_K(G)$ (resp. $R_k(G)$). We have $d(l_K) = l_k$. By th. 27 we can write $l_K$ in the form

$$l_K = \sum_{H \in X} Ind_H(x_H) \text{ with } x_H \in R_K(H).$$

Applying $d$, and using the fact that $d$ commutes with $Ind_H^G$, we obtain an analogous formula for $l_k$:

$$l_k = \sum_{H \in X} Ind_H(x_H') \text{ with } x_H' = d(x_H) \in R_k(H).$$

For $y \in R_k(G)$ (resp. $P_k(G)$), we get by multiplication:

$$y = l_k \cdot y = \sum_{H \in X} Ind_H(x_H') \cdot y =

Chapter 17: Proofs

Remark. The argument used in the proof of th. 39 applies to many other situations (cf. Swan [21], §§ 3,4.) For example it gives the following analogue of Artin’s theorem (cf. th. 26):
