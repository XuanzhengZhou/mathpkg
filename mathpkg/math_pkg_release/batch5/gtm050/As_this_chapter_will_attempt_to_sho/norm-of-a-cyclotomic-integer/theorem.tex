Let $f(\alpha)$ be a cyclotomic integer. Its norm, denoted $Nf(\alpha)$, is the product of all its conjugates:
$$Nf(\alpha) = f(\alpha) f(\alpha^2) f(\alpha^3) \cdots f(\alpha^{\lambda-1}).$$
The norm is always an ordinary integer.
