A cyclotomic integer $f(\alpha)$ is called a unit if its norm equals $1$:
$$Nf(\alpha) = 1.$$
Equivalently, $f(\alpha)$ is a unit if there exists a cyclotomic integer $g(\alpha)$ such that $f(\alpha)g(\alpha) = 1$.
