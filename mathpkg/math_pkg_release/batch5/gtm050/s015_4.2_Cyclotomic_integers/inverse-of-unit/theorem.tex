Let $f(\alpha)$ be a cyclotomic integer with norm $Nf(\alpha) = 1$ (i.e., a unit). Then the product of the non-trivial conjugates of $f(\alpha)$ is the inverse of $f(\alpha)$:
$$f(\alpha)^{-1} = f(\alpha^2)f(\alpha^3)\cdots f(\alpha^{\lambda-1}).$$
