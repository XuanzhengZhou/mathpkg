If $Nh(\alpha)$ is a prime integer, then

$$Nh(\alpha) \equiv 0 \text{ or } 1 \pmod{\lambda}.$$

Equivalently, every prime cyclotomic integer has norm equal either to $\lambda$ or to a rational prime $p \equiv 1 \pmod{\lambda}$.
