A \text{Mersenne prime} is a prime number of the form
$$M_p = 2^p - 1$$
where $p$ is itself a prime. If $n$ is composite, say $n = km$, then $2^n - 1 = 2^{km} - 1 = (2^k - 1)(2^{k(m-1)} + 2^{k(m-2)} + \cdots + 2^k + 1)$ is composite.
