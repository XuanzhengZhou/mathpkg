The \text{norm} of a cyclotomic integer $f(\alpha)$ is the product of its $\lambda-1$ conjugates:
$$N(f(\alpha)) = f(\alpha)f(\alpha^2)f(\alpha^3) \cdots f(\alpha^{\lambda-1}).$$
The norm is always an ordinary integer. It is multiplicative: $N(f(\alpha)g(\alpha)) = N(f(\alpha))N(g(\alpha))$.
