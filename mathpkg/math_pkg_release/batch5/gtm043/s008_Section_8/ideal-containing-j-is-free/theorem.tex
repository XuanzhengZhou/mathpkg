Any ideal $I$ in $C^*(\mathbf{N})$ that contains the function $j$ (where $j(n) = 1/n$ for $n \in \mathbf{N}$) is a free ideal. In particular, every maximal ideal in $C^*(\mathbf{N})$ that contains $j$ is free.
