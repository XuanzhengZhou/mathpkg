Let $X$ be compact, and let $A$ be a sublattice of $C(X)$. Then $\operatorname{cl} A$ contains every function $f \in C(X)$ that can be approximated at each pair of points in $X$ by a function from $A$.
