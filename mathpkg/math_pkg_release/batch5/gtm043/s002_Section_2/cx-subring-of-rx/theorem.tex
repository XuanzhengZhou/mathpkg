$C(X)$ is a commutative ring with unity element $1$, and is a subring of $\mathbf{R}^X$.
The sum and product of two continuous functions are continuous; if $f$ is continuous, then $-f$ is continuous.
The constant function $1$ belongs to $C(X)$ and is its unity element.
