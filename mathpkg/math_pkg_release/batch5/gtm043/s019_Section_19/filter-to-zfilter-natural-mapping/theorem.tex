Let $D$ denote the discrete space whose points are those of $X$, and let $\delta$ be the identity map of $D$ onto $X$. The natural mapping $\delta^\#$ from the set of all filters $F$ on $X$ onto the set of all $z$-filters is defined by

$$\delta^\# F = F \cap Z(X).$$

This mapping preserves convergence.
