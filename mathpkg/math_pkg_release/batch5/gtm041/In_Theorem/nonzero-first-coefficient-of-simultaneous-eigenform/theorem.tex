Assume $k$ is even, $k \geq 4$. If the space $M_k$ contains a simultaneous eigenform $f$ with Fourier expansion
$$f(\tau) = \sum_{m=0}^{\infty} c(m)x^m, \quad x = e^{2\pi i\tau},$$
then $c(1) \neq 0$.
