Let $f \in M_{2k}$ be a modular form of weight $2k$ that is not a cusp form, with Fourier expansion
$$
f(\tau) = \sum_{n=0}^{\infty} c(n) x^n, \qquad x = e^{2\pi i \tau}.
$$
Then the Fourier coefficients satisfy
$$
c(n) = O(n^{2k-1}).
$$
