Let $f \in M_{2k,0}$ be a cusp form of weight $2k$ with Fourier expansion
$$
f(\tau) = \sum_{n=1}^{\infty} c(n) x^n, \qquad x = e^{2\pi i \tau}.
$$
Then the Fourier coefficients satisfy
$$
c(n) = O(n^k).
$$
