If $f$ is an eigenform for every Hecke operator $T_n$, $n \geq 1$, then $f$ is called a \text{simultaneous eigenform}. That is, there exist complex scalars $\lambda(n)$ such that
$$T_n f = \lambda(n) f$$
for all $n \geq 1$.
