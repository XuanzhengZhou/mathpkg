Each $(x, Wx)$ for $x \in N$ is called an \textbf{instantaneous stationary observer}, where $(N, g)$ is the normal Schwarzschild spacetime and $W$ is the future-pointing timelike unit vector field everywhere proportional to $\partial/\partial t$.
