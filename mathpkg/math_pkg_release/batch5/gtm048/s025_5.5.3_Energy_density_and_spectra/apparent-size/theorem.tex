Suppose there exists a smallest integration region $K \subset \mathcal{S}_Z$ with the property that an observed photon comes from a particular astronomical object only if the corresponding spatial direction $U$ is in $K$. Then the solid angle subtended by $K$ is defined as the apparent size of the object for the instantaneous observer $(z, Z)$.
