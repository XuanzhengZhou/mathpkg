Two functions $\phi, \hat{\phi}: \mathbb{R}^3 \to \mathbb{R}$ describe the same gravitational effect if and only if
$$\vec{\nabla}\phi = \vec{\nabla}\hat{\phi}.$$
That is, gravitational potentials that differ by a constant (or more generally by any function with vanishing gradient on the domain) are physically indistinguishable.
