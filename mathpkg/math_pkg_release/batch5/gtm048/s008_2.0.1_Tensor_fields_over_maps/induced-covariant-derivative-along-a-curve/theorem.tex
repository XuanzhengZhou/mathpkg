Let $(M, g, D)$ be a spacetime and let $\gamma: \mathcal{E} \rightarrow M$ be a curve. For a tensor field $A$ on $M$, we write
$$D_{\gamma_*} A := (\gamma^* D)_{d/du} (A \circ \gamma).$$
For a vector field $X$ over $\gamma$, we write
$$D_{\gamma_*} X := (\gamma^* D)_{d/du} X.$$
