Let $\phi: N \rightarrow M$ be a $C^\infty$ map and let $T_s^r M$ be the bundle of $(r, s)$ tensors over $M$ with projection $P: T_s^r M \rightarrow M$. An $(r, s)$ tensor field over $\phi$ is a map $A: N \rightarrow T_s^r M$ such that $P \circ A = \phi$.
