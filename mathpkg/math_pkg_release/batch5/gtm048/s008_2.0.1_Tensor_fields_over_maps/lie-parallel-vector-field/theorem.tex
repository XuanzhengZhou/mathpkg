Let $X$ be a vector field on $M$ with flow $\mu$, and let $\gamma: \mathcal{E} \rightarrow M$ be an integral curve of $X$. A vector field $W$ over $\gamma$ is Lie parallel (with respect to $X$) iff
$$W(u + s) = (\mu_s)_* W(u)$$
whenever $u \in \mathcal{E}$ and $u + s \in \mathcal{E}$.
