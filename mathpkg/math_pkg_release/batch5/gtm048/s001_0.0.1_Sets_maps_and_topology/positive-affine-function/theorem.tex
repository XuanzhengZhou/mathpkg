Let $\mathcal{E} \subset \mathbb{R}$ be connected and open. A function $f: \mathcal{E} \rightarrow \mathbb{R}$ is called \textbf{positive affine} iff $f(u) = cu + d$ for some $c > 0$ and $d \in \mathbb{R}$.
