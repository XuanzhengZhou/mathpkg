For any collection $\{A_t, t \in T\}$ of sets,

$$\left(\bigcup_{t \in T} A_t\right)^c = \bigcap_{t \in T} A_t^c, \quad \left(\bigcap_{t \in T} A_t\right)^c = \bigcup_{t \in T} A_t^c.$$

By convention, $\bigcup_{t \in \emptyset} A_t = \emptyset$ and $\bigcap_{t \in \emptyset} A_t = \Omega$.
