The \textit{convolution} $F = F_1 * F_2$ of two d.f.'s is the d.f. of $X_1 + X_2$ where $X_1, X_2$ are independent with d.f.'s $F_1, F_2$:

$$F(x) = \int_{\mathbb{R}} F_1(x - y) \, dF_2(y).$$

Characteristic functions multiply: $\varphi_{X_1+X_2}(u) = \varphi_{X_1}(u) \varphi_{X_2}(u)$.
