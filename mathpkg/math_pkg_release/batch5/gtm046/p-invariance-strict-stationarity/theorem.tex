A sequence $X_1, X_2, \cdots$ of random variables is strictly stationary if, for every $n$ and every $k$, the joint distribution of $(X_k, \cdots, X_{k+n})$ is the same as that of $(X_1, \cdots, X_{n+1})$. Equivalently, the shift transformation on the sequence is measure-preserving.
