If $K$ is a submodule of an $R$-module $M$, the set of cosets $M/K$ can be made into an $R$-module with operations
$$(a+K) + (b+K) = (a+b) + K, \quad x(a+K) = xa + K$$
for $a, b \in M$ and $x \in R$. This is called the \emph{quotient module} (or \emph{factor module}) of $M$ by $K$.
