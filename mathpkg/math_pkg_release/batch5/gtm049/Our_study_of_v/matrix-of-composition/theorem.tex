If $f \in \mathcal{L}(U, V)$, $g \in \mathcal{L}(V, W)$ and $(u_i), (v_j), (w_k)$ are ordered bases of $U, V, W$, respectively, then
$$
(f; (u_i), (v_j))\,(g; (v_j), (w_k)) = (fg; (u_i), (w_k));
$$
or, more loosely,
$$
(f)(g) = (fg).
$$
