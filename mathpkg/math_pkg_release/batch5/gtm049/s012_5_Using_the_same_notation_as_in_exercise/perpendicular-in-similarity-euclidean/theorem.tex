In a similarity euclidean geometry $(A, \bot)$ determined by a hyperplane $H$ and a definite polarity $\bot$ of $\mathcal{P}(H)$, two lines (or higher-dimensional subspaces) are called perpendicular if their intersections with the hyperplane at infinity are orthogonal with respect to the polarity $\bot$.
