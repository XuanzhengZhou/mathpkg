For $f \in \mathcal{L}(V, V)$, define the \textbf{transpose} of $f$ to be the linear mapping $f^t \colon V^* \to V^*$ given by
$$v(\varphi f^t) = (vf)\varphi,$$
for all $\varphi \in V^*$ and $v \in V$.
