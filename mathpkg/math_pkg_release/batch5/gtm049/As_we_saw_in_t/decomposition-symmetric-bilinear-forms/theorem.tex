Let $\sigma$ be a symmetric, but not alternating, bilinear form on a vector space $V$ over a field $F$. Then

$$V = A_1 \oplus \cdots \oplus A_r \oplus V^\perp$$

where $A_1, \ldots, A_r$ are non-degenerate mutually orthogonal one-dimensional subspaces. Moreover $r = \operatorname{rank} \sigma$.
