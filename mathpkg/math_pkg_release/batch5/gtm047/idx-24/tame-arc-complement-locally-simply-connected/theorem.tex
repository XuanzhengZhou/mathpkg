Let $A$ be a tame arc in $\mathbf{R}^3$, and let $Q \in \operatorname{Bd} A$. Then $\mathbf{R}^3 - A$ is locally simply connected at $Q$. Similarly, if $S$ is a tame 2-sphere in $\mathbf{R}^3$, then each component of $\mathbf{R}^3 - S$ is locally simply connected at each point $Q$ of $S$.
