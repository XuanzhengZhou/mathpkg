For an open cell-complex $\mathcal{C}$, the \textbf{Euler characteristic} is defined in the same way as for complexes:
$$
\chi(\mathcal{C}) = V - E + F,
$$
where $V$, $E$, and $F$ are respectively the number of vertices, edges, and faces of $\mathcal{C}$.
