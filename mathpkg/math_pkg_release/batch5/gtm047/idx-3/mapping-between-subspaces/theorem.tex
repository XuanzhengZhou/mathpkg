Let $M$ and $N$ be sets, in topological spaces $[X, \theta]$ and $[Y, \theta']$. A function $f: M \rightarrow N$ is a mapping if $f$ is a mapping relative to the subspaces $[M, \theta|M]$ and $[N, \theta'|N]$.
