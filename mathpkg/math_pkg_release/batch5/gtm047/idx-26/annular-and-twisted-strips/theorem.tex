In the $2$-cell with strips decomposition $M = D \cup D' \cup \bigcup_{i=1}^{n} S_i$, a strip $S_i$ is called:
\begin{itemize}
\item \emph{annular} (relative to $D$) if $S_i \cup D$ is an annulus,
\item \emph{twisted} (relative to $D$) if $S_i \cup D$ is a M\"obius band.
\end{itemize}
