If $B_i$ is unknotted in $C_i$ ($i = 1, 2$), then every PLH

$$h: \text{Bd } C_1 \leftrightarrow \text{Bd } C_2, \quad \text{Bd } B_1 \leftrightarrow \text{Bd } B_2$$

can be extended to give a PLH

$$h': C_1 \leftrightarrow C_2, \quad B_1 \leftrightarrow B_2.$$
