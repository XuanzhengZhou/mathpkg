A set $M \subset X$ is pathwise connected if for each two points $P, Q$ of $M$ there is a path $p: [a, b] \rightarrow M$ from $P$ to $Q$ (or from $Q$ to $P$). If $M \subset X$, and $|p| = p([a, b]) \subset M$, then $p$ is a path in $M$.
