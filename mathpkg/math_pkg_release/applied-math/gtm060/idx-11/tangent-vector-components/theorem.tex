Let $U$ be a chart of an atlas for $M$ with coordinates $q_1, \ldots, q_n$. Then the components of the tangent vector to the curve $\mathbf{q} = \varphi(t)$ are the numbers $\xi_1, \ldots, \xi_n$, where

$$\xi_i = \left.\frac{d\varphi_i}{dt}\right|_{t=0}.$$
