A system is called **resonant of order $K > 0$** if there exists a relation
$$k_1 \omega_1 + \cdots + k_n \omega_n + k_0 = 0$$
with integers $k_0, k_1, \ldots, k_n$ for which $|k_1| + \cdots + |k_n| = K$, where $\omega_1, \ldots, \omega_n$ are the characteristic frequencies of the system.
