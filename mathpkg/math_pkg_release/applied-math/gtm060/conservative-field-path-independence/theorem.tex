\textbf{Theorem.} A vector field $F$ is conservative if and only if its work along any path $M_1 M_2$ depends only on the endpoints of the path, and not on the shape of the path.

Equivalently, a field is conservative if and only if its work along any closed contour is equal to zero.
