\textbf{Corollary 3.} Every closed system with two degrees of freedom ($n = 2$) which has a cyclic coordinate is integrable.

\textbf{Proof.} In this case the system for the remaining variables is one-dimensional and is immediately integrated by means of the energy integral $H(p', q') = c$.
