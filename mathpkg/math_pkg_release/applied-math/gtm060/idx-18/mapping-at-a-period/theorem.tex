Let $\dot{x} = f(x, t)$ be a system of differential equations with periodic coefficients of period $T > 0$.

The \textbf{mapping at a period} is the map
$$A: \mathbb{R}^n \rightarrow \mathbb{R}^n, \quad Ax(0) = x(T),$$
induced by the phase flow $g^t$ evaluated at time $T$, i.e., $A = g^T$.
