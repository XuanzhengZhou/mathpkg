Let $\lambda: \mathcal{E} \rightarrow M$ be a photon. If $\lambda$ has a future endpoint $z \in M$ (so the curve $\lambda$ is destroyed at $z$), then $\lambda$ is said to be \textbf{absorbed} (or \textbf{received}) at $z$. The point $z$ is called the \textbf{absorption point}.
