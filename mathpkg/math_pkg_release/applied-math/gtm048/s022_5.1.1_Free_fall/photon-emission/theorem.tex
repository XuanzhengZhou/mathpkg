Let $\lambda: \mathcal{E} \rightarrow M$ be a photon. If $\lambda$ has a past endpoint $x \in M$ (so the curve $\lambda$ is created at $x$), then $\lambda$ is said to be \textbf{emitted} (or \textbf{sent out}) at $x$. The point $x$ is called the \textbf{emission point}.
