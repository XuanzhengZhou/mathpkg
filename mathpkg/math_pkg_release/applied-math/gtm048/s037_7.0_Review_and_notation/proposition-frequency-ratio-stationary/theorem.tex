Let $X$ be a Killing vector field on $M$ and $\lambda: \mathcal{E} \to M$ a freely falling photon. Let $a, b \in \mathcal{E}$ with $a < b$. Then:
$$\tau = \frac{|X\lambda b|}{|X\lambda a|},$$
where $\tau = g(\lambda_*a, Z\lambda a)/g(\lambda_*b, Z\lambda b)$ is the frequency ratio.
