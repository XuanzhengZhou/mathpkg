Let $Q$ be a reference frame on a spacetime $(M, g, D)$ and let $\omega$ be the 1-form physically equivalent to $Q$ (i.e., $\omega(X) = g(Q, X)$ for all vector fields $X$). Then $Q$ is irrotational if and only if $Q$ is locally synchronizable (i.e., $\omega \wedge d\omega = 0$).
