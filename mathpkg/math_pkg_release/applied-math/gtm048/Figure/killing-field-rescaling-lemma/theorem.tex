Let $W$ be a nowhere zero Killing vector field on a connected manifold $M$, and let $\ell$ be a $C^\infty$ function on $M$. If $\ell W$ is also a Killing vector field, then $\ell$ is constant on $M$.

This lemma remains valid so long as $W$ is not identically zero.
