A curve $\gamma: E \rightarrow M$ is called \textbf{inextendible} iff any other curve $\zeta: F \rightarrow M$ satisfying $E \subset F$ and $\zeta|_E = \gamma$ is the curve $\gamma: E \rightarrow M$ itself.
