A semi-Riemannian manifold $(M, g)$ is called \textbf{complete} iff $U_x = M_x$ for all $x \in M$. Equivalently, $(M, g)$ is complete iff every geodesic $\gamma: E \rightarrow M$ can be extended to a geodesic $\tilde{\gamma}: \mathbb{R} \rightarrow M$ defined on the entire real line.
