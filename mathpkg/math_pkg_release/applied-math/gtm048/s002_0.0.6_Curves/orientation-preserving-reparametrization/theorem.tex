A curve $\zeta: F \rightarrow M$ is called an \textbf{(orientation-preserving) reparametrization} of $\gamma: E \rightarrow M$ if there exists a surjective $C^\infty$ map $\phi: F \rightarrow E$ with $\phi' > 0$ everywhere such that $\zeta = \gamma \circ \phi$.
