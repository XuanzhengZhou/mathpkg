If $P(x, y)$ is the transition function of a transient random walk with state space $R$, then

$$G(0, x) < \infty \text{ on } R$$

and

$$G(0, x) = F(0, x) = 0 \text{ on } R - R^+.$$

Proof: By P1.3,

$$G(0, x) = \lim_{n \to \infty} G_n(

In the recurrent case we first prove a few lemmas (P3, P4, P5) of independent interest, which will later be combined with P2 to give a complete description (T1) of the basic differences between recurrent and transient random walk.