For strongly aperiodic recurrent random walk in one dimension, for every $\epsilon > 0$,
$$F_n \geq \frac{(1-\epsilon)^n}{n-2}$$
for all sufficiently large $n$, where $F_n = P_0[T = n]$ is the probability of first return to the origin at time $n$.
