For one-dimensional aperiodic recurrent random walk,
$$\lim_{t \to 1} \frac{1 - E_0[t^{T_x}]}{1 - E_0[t^{T_0}]} = a(-x), \quad x \neq 0,$$
where $T_x = \min[n \mid n \geq 1, x_n = x]$ and $a(x)$ is the potential kernel.
