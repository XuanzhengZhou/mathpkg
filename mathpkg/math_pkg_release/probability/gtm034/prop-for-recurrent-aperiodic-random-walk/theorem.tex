For recurrent aperiodic random walk

$$E_0[N_z] = 1 \text{ for all } x \neq 0.$$

Proof: All that remains is to observe that

$$F^*(x,0) = F(0,x), \text{ and by T2.1 } F(0,x) = 1 \text{ for all } x \text{ in } R.$$

As a corollary of P3 we get a simple proof that $g_A(x,y) < \infty$ for all $x$ and $y$ if the random walk is aperiodic. The reduction to the case when $A = \{0\}$ and $x = y$ was accomplished in the proof of P1, and it is easy to see that

$$1 = E_0[N_z] > Q_n(0,x