$$T = \min [n \mid 1 \leq n \leq \infty; S_n > 0],$$

$$T' = \min [n \mid 1 \leq n \leq \infty; S_n \geq 0].$$

Thus $T$ is the first time that the random walk $x_n$ is in the set $B = [1,\infty)$. It is infinite if there is no first time. In the notation of Chapter III,

$$P[T < \

Similarly

$$\mathbf{P}[\mathbf{T}' < \infty] = \sum_{y=0}^{\infty} \Pi_{B'}(0,y),$$

$$\mathbf{P}[\mathbf{S}_{\mathbf{T}'} = y, \mathbf{T}' < \infty] = \Pi_{B'}(0,y) \text{ for } y \geq 0 \text{ if } B' = [0,\infty).$$

The real work of this chapter now begins with a lemma which imitates and extends equation (3) above.