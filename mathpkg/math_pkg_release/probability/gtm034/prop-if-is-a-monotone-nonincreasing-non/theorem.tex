If $p_n$ is a monotone nonincreasing, non-negative sequence such that

(1) $$\lim_{t \to 1} \sqrt{1 - t} \sum_{n=0}^{\infty} p_n t^n = 1,$$

then

(2) $$\lim_{n \to \infty} \sqrt{n\pi} p_n = 1.$$

Proof: We proceed in two stages. The first step uses only the non-negativity of $p

implies that $n^{1-a}p_n \rightarrow a$. The technique of the proof even suggests the further reduction of taking for $p(x)$ a monotone nonincreasing function such that $p(x) = p_n$ when $x = n$, and such that

(4) $$\lim_{x \to +\infty} x^{-a} \int_0^x p(t) dt = 1.$$

This is of course easy to do, and it remains to show that

(5) $$\lim_{x \to +\infty} x^{1-a}p(x) = a.$$

For each $c > 1$, the monotonicity of $p(x)$ gives

$$xp(cx) \leq \frac{1}{c-1} \int_0^x p(t) dt \leq xp(x).$$

Thus

$$\frac{xp(cx)}{\int_0^x p(t) dt} \leq \frac{\int_0^x p(t) dt - \int_0^x p(t) dt}{(c-1) \int_0^x p(t) dt} \leq \frac{xp(x)}{\int_0^x p(t) dt}.$$

Letting $x \rightarrow +\infty$, and using the hypothesis (4), the second inequality gives

$$\frac{c^a - 1}{c-1} \leq \lim_{x \to \infty} \frac{xp(x)}{\int_0^x p(t) dt} = \lim_{x \to \infty} x^{1-a}p(x).$$

Using also the left inequality one obtains

$$c^{a-1} \lim_{x \to \infty} x^{1-a}p(x) \leq \frac{c^a - 1}{c-1} \leq \lim_{x \to \infty} x^{1-a}p(x).$$

This holds for arbitrary $c > 1$, so we let $c \neq 1$ to obtain

$$\lim_{x \to \infty} x^{1-a}p(x) = \lim_{c \neq 1} \frac{c^a - 1}{c-1} = a,$$

proving (5) and hence P2

where $o(k,n)$ tends to zero uniformly in $k$ and $n$ as $\min(k, n-k) \to \infty$.

(b) For $0 \leq x \leq 1$

$$\lim_{n \to \infty} \mathbf{P}[N_n \leq nx] = \frac{2}{\pi} \arcsin \sqrt{x}.$$