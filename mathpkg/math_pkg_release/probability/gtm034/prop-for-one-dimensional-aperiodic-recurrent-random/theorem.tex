For one-dimensional aperiodic recurrent random walk with finite variance $\sigma^2 < \infty$

$$\lim_{n \to \infty} \sqrt{n} \mathbf{P}_0[\mathbf{T} > n] = \sqrt{\frac{2}{\pi}} \sigma.$$

Proof: We write, for $0 \leq t < 1$

(1) $U(t) = \sum_{n=0}^{\infty} U_n t^n = \sum_{n=0}^{\infty} t^n P_n(0,0) = \frac

when $|\theta| \leq \delta$. By T7.1 the integral in (1), taken from $\delta$ to $\pi$ and from $-\pi$ to $-\delta$, remains bounded as $t \to 1$ since the random walk is aperiodic. Hence those parts of the integral in (1) are error terms which go to zero when multiplied by $\sqrt{1-t}$, and (2) will be true if

(3) $$\lim_{t \to 1} \sqrt{1-t} \frac{1}{2\pi} \int_{-\delta}^{\delta} \frac{d\theta}{\theta^2 \sigma^2} + (1-t) = \frac{1}{\sqrt{2} \sigma}$$

for every $\delta > 0$, and that is easily verified by making the change of variable from $\theta$ to $x = \theta \sigma [2(1-t)]^{-1/2}$.

The next step uses the identity

$$\sum_{n=0}^{\infty} R_n t^n = [(1-t)U(t)]^{-1}$$

which follows from P1.2. Combined with (2) it gives

(4) $$\lim_{t \to 1} \sqrt{1-t} \sum_{n=0}^{\infty} R_n t^n = \sigma \sqrt{2}.$$

Observe that the sequence $R_n = P_0[T > n]$ is monotone nonincreasing, so that the stage is set for the application of Karamata's theorem. The strong form of this theorem, given in P20.2 implies that

$$\lim_{n \to \infty} \sqrt{n} R_n = \sigma \sqrt{2/\pi},$$

which proves P3.

If we combine equation (4) in the proof of P3 with P2, we have

(5) $$\lim_{t \to 1} \sqrt{1-t} \sum_{n=0}^{\infty} t^n P_x[T > n] = \sigma \sqrt{2} a(x)$$

for every $x \neq 0$. Karamata's theorem may again be applied to (5), and the conclusion is