One-dimensional aperiodic recurrent random walk has the property that

$$a(x) = \alpha x \text{ for all } x \geq 0, \text{ where } \alpha \text{ is a positive constant, if and only if the random walk is left continuous with } \sigma^2 = \alpha^{-1}.$$