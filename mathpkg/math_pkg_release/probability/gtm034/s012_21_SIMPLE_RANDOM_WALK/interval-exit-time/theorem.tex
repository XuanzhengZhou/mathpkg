For a random walk $x_n$ on the integers and an interval $[0,N]$, the \textbf{exit time} (or hitting time of the exterior) is the stopping time
$$T_N = \min \{ k \mid k \geq 1, \; x_k \in \mathbb{R} - [0,N] \}.$$
