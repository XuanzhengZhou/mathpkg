Suppose that $P(x,y) = P(y,x)$ is the transition function of symmetric one-dimensional random walk with characteristic function

$$\phi(\theta) = \sum_{x=-\infty}^{\infty} P(0,x)e^{ix\theta} = \overline{\phi(\theta)} = \phi(-\theta).$$

Then there exists a sequence of polynomials

(a) $$p_n(z) = \sum_{k=0}^{n} p_{n,k}z^k, \quad n \geq 0$$

with the property that

(b) $$p_{n,n} > 0 \text{ for } n \geq 0,$$

(c) $$\frac{1}{2\pi} \int_{-n}^{n} p_k(e^{i\theta})\overline{p_m(e^{i\theta})}[1 - \phi(\theta)] d\theta = \delta(k,m), \quad k,m = 0,1,2,\dots$$

These polynomials are uniquely determined by requirements (b) and (c). All the coefficients $p_{n,k}$ are non-negative. The Green function $g_N(i,j)$ of D21.1 has the representation

(d) $$g_N(i,j) = \sum

The limiting behavior of the orthogonal polynomials is described by

(e) $\lim_{n \to \infty} p_{n,n-k} = u(k), \quad k \geq 0,$

where $u(k)$ is the sequence defined by

(f) $\sum_{k=0}^{\infty} u(k)z^k = U(z) = V(z),$

with $U(z)$ and $V(z)$ as defined in D18.2.