$\sum_{t \in R} P_{n+1}(x,t) H_B(t,y) = H_B(x,y) - \sum_{t \in B} A_n(x,t) [\Pi_B(t,y) - \delta(t,y)]$, for $B \subset R$ with $1 \leq |B| < \infty$, $x \in R$, $y \in B$. Here

$$0 \leq a_n(x-t) = A_n(x,t) = \sum_{k=0}^{n} [P_k(0,0) - P_k(x,t)].$$

It will be convenient to specialize P1 to the set $C = \{0,c\}$, $c \neq 0$. A simple calculation, using the fact (see P11.2) that

$$\Pi_C(0,c) + \Pi_C(0,0) = 1,$$

gives