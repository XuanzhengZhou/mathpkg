For every aperiodic transient random walk in dimension $d \geq 1$, let $T = \min\{n \mid 1 \leq n \leq \infty; x_n = 0\}$. Then for every $x \neq 0$,

$$\lim_{n \to \infty} \frac{P_x[T > n]}{P_0[T > n]} = a(x),$$

where $a(x) = G(0,0) - G(x,0)$ is the potential kernel.
