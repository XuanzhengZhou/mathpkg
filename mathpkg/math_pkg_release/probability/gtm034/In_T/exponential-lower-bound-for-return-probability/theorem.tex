Let the random walk be aperiodic recurrent in one dimension. For every $\epsilon > 0$, there exists a constant such that for all sufficiently large $n$,

$$F_n = P_0[T = n] \geq \frac{(1 - \epsilon)^n}{n - 2}.$$
