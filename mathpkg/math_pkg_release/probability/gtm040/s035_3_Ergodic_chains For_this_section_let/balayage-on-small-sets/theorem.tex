If $\alpha h$ is finite and if $(I - P)h = f$, then $B^E h$ is finite-valued and
$$B^E h = h - \,^{E}\!N f.$$
If $f$ has support in $E$, then $B^E h = h$. If in addition $h$ is bounded, then $\lambda^E h = \alpha h$.
