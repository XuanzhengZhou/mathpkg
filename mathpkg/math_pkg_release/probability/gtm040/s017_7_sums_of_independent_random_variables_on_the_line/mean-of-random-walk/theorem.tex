The mean $m$ for a sums of independent random variables Markov chain with probability distribution $\{p_k\}$ is defined by
$$m = \sum_k k p_k$$
and is said to exist if and only if the positive and negative parts of the sum are not both infinite.
