If $P$ is a Markov chain representing sums of independent random variables on the line, if there are finitely many $k$-values and if they have greatest common divisor one, if $\sum p_k = 1$, and if $m = \sum kp_k$, then in order for the chain to be recurrent it is necessary and sufficient that $m = 0$.
