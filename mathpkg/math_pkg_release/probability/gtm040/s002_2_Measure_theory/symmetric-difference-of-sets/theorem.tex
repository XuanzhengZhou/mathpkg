For subsets $A$ and $B$ of a set $X$, the \textit{symmetric difference} $A \oplus B$ is defined by
$$A \oplus B = (A \cap \tilde{B}) \cup (B \cap \tilde{A}).$$
A point is in $A \oplus B$ if it is in $A$ or $B$ but not both.
