Any column vector $f$ with $\alpha f$ well defined and finite for which the limit
$$g = \lim_{n \to \infty} \bigl[ (I + P + \cdots + P^n)f \bigr]$$
exists and is finite-valued is called a \textbf{right charge} with \textbf{potential function} $g$.
