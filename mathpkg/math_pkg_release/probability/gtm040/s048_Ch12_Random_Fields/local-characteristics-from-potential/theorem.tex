For a finite Markov field $\{x_t\}$ with canonical neighbor potential $V$, the local characteristics satisfy
$$\mu_A^{\bar{A}}(\iota) = z^{-1} \exp\left\{ \sum_{B \in \bar{A}: B \cap A \neq \emptyset} V_B(\iota) \right\}$$
whenever $i_t = i'_t$ for all $t \in \bar{A}$.
