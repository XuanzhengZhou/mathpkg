A \textbf{random field} is given by $(\Omega, \mathcal{B}, \mu, \{x_t\})$, where $\mu$ is a probability measure on $(\Omega, \mathcal{B})$ such that
$$\Pr[x_t = i_t; t \in A] > 0$$
for all finite (non-empty) $A \subset T$ and arbitrary $i_t \in S$.
