A random field $\{x_t\}$ on a countable parameter set $T$ with neighbor system $\partial$ is a \textbf{Markov field} if for every $a \in T$, the local characteristic $\mu_a^T$ depends only on the configuration values at sites in $\bar{a} = \{a\} \cup \partial a$.
