---
role: proof
locale: en
of_concept: lemma-8-64
source_book: gtm040
source_chapter: "8"
source_section: "64"
---

**Proof:** The second condition is just the first for $E$, the set of all transient states. For the first condition, on every path which does not pass through $E$, $z(\omega

We shall now generalize our considerations. Let $z(\omega)$ be a non-negative random variable with $h_i = M_i[z]$ not necessarily finite. Suppose $z \geq z^{(1)}$, and define $m z(\omega) = \min(m, z(\omega))$. We agree to set $z - z^{(1)} = 0$ at all points where $z^{(1)}$ is infinite. Then

$$z(\omega) = \lim_{m \to \infty} m z(\omega).$$

The crucial property of the functions $m z$ is that

$$(m z)^{(1)} = m(z^{(1)}).$$

We denote the common value of $(m z)^{(1)}$ and $m(z^{(1)})$ by $m z^{(1)}$, and we define $m z^{(n)}$ analogously.
