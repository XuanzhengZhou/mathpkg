Let $\pi \geq 0$ be a row vector and suppose $\{f^{(k)}\}$ is a sequence of non-negative column vectors. Then

$$\pi \left( \liminf_{k} f^{(k)} \right) \leq \liminf_{k} \left( \pi f^{(k)} \right).$$

If $f = \lim_{k} f^{(k)}$ exists

Proposition 1-56: Let $\pi \geq 0$ be a row vector such that $\pi 1$ is finite. If $\{f^{(k)}\}$ is a sequence of column vectors such that $|f^{(k)}| \leq c1$ and $f = \lim_k f^{(k)}$ exists, then

$$\pi f = \lim_k \pi f^{(k)}.$$
