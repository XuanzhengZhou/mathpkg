If $P$ is a sums of independent random variables Markov chain defined on the integers, then the stochastic process whose $n$th outcome is the $s_n$th outcome in $P$ is a Markov chain $P^+$. Moreover,
$$P_{ij}^+ = P_{0,\,j-i}^+.$$
