A \textbf{Markov field} is a system of random variables indexed by the vertices of a graph, where the conditional distribution at each vertex given the values at all other vertices depends only on the values at neighboring vertices.