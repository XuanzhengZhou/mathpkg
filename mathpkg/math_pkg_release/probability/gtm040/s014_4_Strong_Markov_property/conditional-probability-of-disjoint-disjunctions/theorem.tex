Let $\{p_k\}$ be a sequence of statements whose truth sets are disjoint in pairs, let $\bigvee p_k$ be their disjunction, and suppose $\Pr[\bigvee p_k] > 0$. If $p$ is a statement for which $\Pr[p \mid p_k] = c$ whenever $\Pr[p_k] > 0$, then

$$\Pr[p \mid \bigvee p_k] = c.$$
