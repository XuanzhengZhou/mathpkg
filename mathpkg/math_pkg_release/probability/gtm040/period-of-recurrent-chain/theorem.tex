Let $P$ be a recurrent Markov chain and let $i$ be a fixed state of the chain. Define
$$T = \{k \mid (P^k)_{ii} > 0,\; k > 0\}.$$
The \textbf{period} of the chain for the state $i$ is the greatest common divisor $d = \gcd(T)$ of the integers in $T$.
