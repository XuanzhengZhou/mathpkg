If $h \geq 0$ is a $P$-superregular function and $\pi h = 1$, then for $i, j \in S^h$,
$$K^h(i, j) = K(i, j) / h_i,$$
where $K^h$ is the Martin kernel for the $h$-process. Moreover, $(\pi^h N^h)_j = h_j (\pi N)_j$.
