If $\{\pi^{(k)}\}$ is a sequence of non-negative row vectors converging to $\pi$, if $f$ is a column vector such that $0 \leq f \leq c1$, and if $\pi 1 = \lim_k \pi^{(k)} 1$ with $\pi 1$ finite, then $\pi f = \lim_k \pi^{(k)} f$.
