Let $\{\pi^{(k)}\}$ be a sequence of row vectors converging to $\pi$ and satisfying $|\pi^{(k)}|1 \leq M$. Suppose $f$ is a column vector with the property that for any $\delta > 0$ only finitely many entries of $f$ have absolute value greater than $\delta$. Then $\pi f = \lim_k \pi^{(k)} f$.
