A sequence $\{f_n\}$ of non-negative integrable functions is said to be 	extit{uniformly integrable} if for each $psilon > 0$ there is a number $k$ such that
$$\int_{\{x \mid f_n(x) > k\}} f_n \, d\mu < psilon$$
holds for every $n$.
