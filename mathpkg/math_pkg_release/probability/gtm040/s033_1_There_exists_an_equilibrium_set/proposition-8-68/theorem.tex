Let $P$ be an absorbing chain with fundamental matrix $N$. Define $a_i^{(r)} = M_i[a(\omega)^r]$, where $a(\omega)$ is the absorption time. Then the column vector $a^{(r)}$ indexed by transient states satisfies
$$a^{(r)} = \sum_{m=1}^{r-1} \binom{r}{m} N Q a^{(m)} + N \mathbf{1}.$$
