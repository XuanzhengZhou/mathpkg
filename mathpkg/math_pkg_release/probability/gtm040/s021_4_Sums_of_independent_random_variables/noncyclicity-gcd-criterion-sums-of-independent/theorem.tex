Let $P$ be a sums of independent random variables chain on the integers and let $c$ be the greatest common divisor of $\{k \mid p_k > 0\}$, where $p_k = P_{0k}$. Then $P_{ii}^{(n)} > 0$ only if $c \mid n$. Consequently, if $c > 1$, the chain is cyclic; if $c = 1$, the chain is noncyclic.
