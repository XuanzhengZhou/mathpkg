Let $A$ and $B$ be non-negative matrices and let $f$ be a column vector such that $A(Bf)$ and $(AB)f$ are both well defined. Then
$$A(Bf) = (AB)f.$$
In particular, if $C$ is a non-negative matrix, $n > 0$, and $C^n f$ and $C(C^{\,n-1}f)$ are well defined, then
$$C^n f = C(C^{\,n-1}f).$$
