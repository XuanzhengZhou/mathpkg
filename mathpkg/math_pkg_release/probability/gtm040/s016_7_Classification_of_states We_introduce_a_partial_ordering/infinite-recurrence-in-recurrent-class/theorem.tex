If a Markov chain is started in a recurrent class $S'$, then the chain is in every state of $S'$ infinitely often with probability one. In particular, if $i$ and $j$ are in $S'$, then $N_{ij} = +\infty$.
