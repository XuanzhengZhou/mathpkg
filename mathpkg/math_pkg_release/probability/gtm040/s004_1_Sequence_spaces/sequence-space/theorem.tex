A \textbf{sequence space} $\Omega$ is the set of all sequences $\omega = (x_0, x_1, x_2, \ldots)$ where each $x_n$ takes values in a denumerable state space $S$. The coordinate functions $x_n(\omega) = \omega_n$ give the state at time $n$.