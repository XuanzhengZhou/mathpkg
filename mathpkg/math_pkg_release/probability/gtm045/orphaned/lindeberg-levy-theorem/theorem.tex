Let $X_n$ be i.i.d. random variables with $E X_n = \mu$ and $0 < \sigma^2 X_n = \sigma^2 < \infty$. Then

$$\frac{S_n - n\mu}{\sigma \sqrt{n}} \xrightarrow{d} N(0, 1),$$

where $N(0,1)$ is the standard normal distribution with characteristic function $e^{-u^2/2}$.
