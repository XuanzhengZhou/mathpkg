Let $A$ be an event with $PA > 0$. The \textit{conditional probability of $B$ given $A$} is defined by

$$P_A B = \frac{PAB}{PA},$$

so that $PAB = PA \cdot P_A B$. The function $P_A$ on $\alpha$ is a probability: it is normed, nonnegative, and $\sigma$-additive.
