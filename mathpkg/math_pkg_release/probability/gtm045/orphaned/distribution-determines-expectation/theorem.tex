The distribution $P_X$ of $X$ determines the distributions of all r.v.'s $g(X)$ where $g$ is a finite Borel function, and

$$E g(X) = \int_{\mathbb{R}} g \, dP_X$$

in the sense that if either side exists, so does the other and they are equal.
