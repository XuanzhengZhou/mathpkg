If $X = \sum_j x_j I_{A_j}$ is an elementary random variable, the \textit{conditional expectation of $X$ given $A$} (with $PA > 0$) is

$$E_A X = \sum_j x_j P_A A_j = \frac{1}{PA} \sum_j x_j P(A A_j).$$

If $EX$ exists and is finite, then $E_A X$ exists and is finite.
