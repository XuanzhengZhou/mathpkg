The \textit{expectation} $EX$ of a simple random variable $X = \sum_{j=1}^{m} x_j I_{A_j}$ is defined by

$$EX = \sum_{j=1}^{m} x_j PA_j.$$

Properties: For any constant $c$, $E(c) = c$ and $EcX = cEX$. If $X \geq 0$ (all $x_j \geq 0$), then $EX \geq 0$. If $X \leq Y$, then $EX \leq EY$.
