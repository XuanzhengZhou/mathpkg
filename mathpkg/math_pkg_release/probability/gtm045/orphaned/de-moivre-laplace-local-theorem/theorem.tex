In the Bernoulli case with $p > 0$, $q = 1 - p > 0$, as $n \to \infty$,

$$P[S_n = j] \sim \frac{1}{\sqrt{2\pi npq}} e^{-x^2/2}, \quad x = \frac{j - np}{\sqrt{npq}},$$

uniformly on every finite interval $[a, b]$ of values of $x$, where $j$ varies with $n$ so that $x$ remains within $[a, b]$.
