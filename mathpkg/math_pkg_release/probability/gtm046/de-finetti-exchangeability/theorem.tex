Random variables $X_1, X_2, \cdots$ are exchangeable if their joint distribution functions satisfy $F_{k_1 \cdots k_m} = F_{1 \cdots m}$ for any distinct indices $k_1, \cdots, k_m$, i.e., the joint law is invariant under finite permutations.
