If $EY < \infty$, then $E(Y \mid Y_1, \cdots, Y_n) \xrightarrow{a.s.} Z < \infty$. If $E|Y|^r < \infty$ for some $r \geq 1$, then $E(Y \mid Y_1, \cdots, Y_n) \xrightarrow{a.s.} E(Y \mid Y_1, Y_2, \cdots)$, which reduces a.s. to $Y$ when $Y$ is defined on the $Y_n$.
