Let the $X_{nk}$ be centered at their conditional expectations. If $$\sum_k E |X_{nk}|^{2+\delta} \to 0$$ for a $\delta > 0$, then $\mathcal{L}(X_n) \sim \mathcal{L}(\sum_k Y_{nk})$ where $\mathcal{L}'(Y_{nk}) = \mathcal{N}(0, \sigma'^2_{nk})$.
