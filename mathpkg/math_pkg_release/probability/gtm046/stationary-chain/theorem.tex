A Markov chain is stationary if its law is determined by an invariant initial distribution $P_1(S)$ satisfying $P_1(S) = \int P_1(dx) P(x; S)$ and transition probability $P(x; S)$. Equivalently, for every $n$, the distribution of $(X_m, \ldots, X_{m+n})$ is independent of $m$.
