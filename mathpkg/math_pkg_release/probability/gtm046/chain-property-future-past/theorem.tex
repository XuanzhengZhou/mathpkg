Let $k_1 < \cdots < k_{n-1} < k_n < k_{n+1} < \cdots < k_{n+m}$ be arbitrary integers. For a chain $\mathcal{B}_n$, $$P^{\mathcal{B}_{k_1} \cdots \mathcal{B}_{k_n}} B_{k_{n+1}} \cdots B_{k_{n+m}} = P^{\mathcal{B}_{k_n}} B_{k_{n+1}} \cdots B_{k_{n+m}} \quad \text{a.s.}$$
