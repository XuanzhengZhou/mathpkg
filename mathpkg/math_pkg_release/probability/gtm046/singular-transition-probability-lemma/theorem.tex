For every fixed $x \in R$, the sequence $P_s^n(x, R)$ of singular parts of the $n$-step transition probabilities is nonincreasing and hence converges. The limit $\bar{P}_s(x, R) = \lim P_s^n(x, R)$ is called the singular transition probability.
