A random variable $\xi$ is $P$-invariant, or the sequence $\xi_1, \xi_2, \ldots$ is $P$-stationary, if for every Borel set $S$ and every $k$,
$$P[\xi_k \in S] = P[\xi_1 \in S].$$
That is, the probability distributions of all translates $\xi_k$ are identical.
