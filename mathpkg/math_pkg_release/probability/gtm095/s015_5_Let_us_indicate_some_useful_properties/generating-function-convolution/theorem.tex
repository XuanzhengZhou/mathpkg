Let $A(x) = \sum_{n \geq 0} a_n x^n$ and $B(x) = \sum_{n \geq 0} b_n x^n$ be generating functions. Then their product is the generating function of the convolution of the sequences:

$$\left( \sum_{i=0}^{n} a_i b_{n-i} \right)_{n \geq 0} \;\longleftrightarrow\; A(x) B(x).$$
