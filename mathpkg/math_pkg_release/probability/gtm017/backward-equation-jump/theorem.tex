For a jump process, the transition probability satisfies the \textbf{backward equation}:
$$\frac{\partial P(\tau, x; t, A)}{\partial \tau} = p(\tau, x) \left\{ P(\tau, x; t, A) - \int P(\tau, y; t, A) \Pi(\tau, x, dy) \right\}.$$
