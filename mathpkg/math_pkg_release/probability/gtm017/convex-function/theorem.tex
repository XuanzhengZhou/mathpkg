A function $f$ defined on $(0, \infty)$ is called \textbf{convex} if
$$f(\lambda x + (1 - \lambda)y) \leq \lambda f(x) + (1 - \lambda)f(y)$$
for all $\lambda$, $0 \leq \lambda \leq 1$, and all $x, y$ in its domain.

If $f''(x)$ exists and is non-negative everywhere, then $f$ is convex.
