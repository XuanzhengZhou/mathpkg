The unique function satisfying the axioms of entropy is
$$H(p_1, \dots, p_n) = -\lambda \sum_{k=1}^{n} p_k \log p_k,$$
where $\lambda > 0$ is a positive constant. Conventionally, $\lambda = 1$ and the logarithm is taken to base 2.
