A function $f$ is called an \textbf{invariant function} if $f(w) = f(Tw)$ almost everywhere. Equivalently, $f$ is invariant if $\{w \mid f(w) \leq x\}$ is invariant for every real $x$. A process is metrically transitive iff every invariant function equals a constant almost everywhere.
