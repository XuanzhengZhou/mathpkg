For a discrete-time weakly stationary process, the covariance sequence $r_k$ is positive definite and has the representation $r_k = \int_{-\pi}^\pi e^{ik\lambda} dF(\lambda)$ where $F$ is nondecreasing bounded on $[-\pi, \pi]$.
