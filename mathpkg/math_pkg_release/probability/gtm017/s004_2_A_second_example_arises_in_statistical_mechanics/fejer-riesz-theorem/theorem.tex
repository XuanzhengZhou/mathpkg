Every positive continuous trigonometric polynomial $p(\lambda) = \sum A_k \cos k\lambda + \sum B_k \sin k\lambda > 0$ can be represented as $p(\lambda) = |\sum_{k=0}^m b_k e^{-ik\lambda}|^2$, the absolute square of a one-sided trigonometric polynomial.
