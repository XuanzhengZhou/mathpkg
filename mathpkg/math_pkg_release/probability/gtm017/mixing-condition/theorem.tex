A strictly stationary process satisfies the \textbf{mixing condition} if for any two events $A, B$,
$$\lim_{j \to \infty} P(B \cap T^{-j} A) = P(B) P(A).$$

Mixing implies ergodicity. A stationary process of independent, identically distributed random variables is mixing.
\end{enumerate}
