Let $ be a random variable with probability distribution (X = a_i) = p_i$,  = 1, 2, \ldots$. The \text{expectation} of $, denoted $, is defined when $\sum_i |a_i| p_i < \infty$ as
2392921EX = \sum_{i=1}^{\infty} a_i p_i.2392921
The expectation operator $ is linear: for random variables , Y$ with finite expectations and constants $\alpha, \beta$,
2392921E(\alpha X + \beta Y) = \alpha EX + \beta EY.2392921
For independent random variables , Y$ with finite expectations,
2392921E(XY) = EX \cdot EY.2392921
