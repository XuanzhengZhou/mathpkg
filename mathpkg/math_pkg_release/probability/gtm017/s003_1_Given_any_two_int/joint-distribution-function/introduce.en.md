The joint distribution function of $k$ random variables completely characterizes their probabilistic behavior by specifying the probability that each variable falls below given thresholds simultaneously. It generalizes the univariate CDF to higher dimensions. A valid joint distribution function must be bounded, have limits 0 and 1 at the margins, and have non-negative $k$-th order differences.
