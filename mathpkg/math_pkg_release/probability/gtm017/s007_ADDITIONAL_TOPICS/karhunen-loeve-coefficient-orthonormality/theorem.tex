Let $Z_i = \sqrt{\lambda_i} \int_0^T X_t \varphi_i(t) \, dt$ where $\varphi_i$ are orthonormal eigenfunctions with eigenvalues $\lambda_i$, and $E[X_t X_\tau] = r(t,\tau)$. Then
$$E Z_i Z_j = \delta_{ij}.$$
