A strictly stationary process $\{X_n\}$ is called \textbf{ergodic} if the limiting time average
$$\lim_{n \to \infty} \frac{1}{n} \sum_{k=0}^{n-1} f(T^k w)$$
exists and equals the space average $E f(w)$ for almost all $w$ and every integrable function $f$.
\end{enumerate}
