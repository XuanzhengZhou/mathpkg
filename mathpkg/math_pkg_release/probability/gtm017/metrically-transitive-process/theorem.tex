A strictly stationary process (or its corresponding probability measure $P$) is called \text{metrically transitive} if the sigma-field of invariant sets is trivial, i.e., every invariant event has probability $0$ or $1$. Equivalently, every invariant function is constant almost everywhere. Metric transitivity is equivalent to ergodicity of the process.
