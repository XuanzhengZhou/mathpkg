A class of states $C$ of a Markov chain is called \text{closed} if $p_{j,k} = 0$ whenever $j \in C$ and $k \notin C$. A closed class is \text{minimal} if it contains no proper closed subset of states. The statement that the transition matrix is irreducible is equivalent to the class of all states being a minimal closed class.
