A random variable $X$ has the \textbf{binomial distribution} with parameters $m$ (sample size) and $p$ ($0 \leq p \leq 1$) if
$$P(X = r) = \binom{m}{r} p^r q^{m-r}, \quad r = 0, 1, \ldots, m,$$
where $q = 1 - p$.
$$\sum_{r=0}^{m} P(X = r) = (p + q)^m = 1.$$
