Let $G \subset \mathbb{C}$ be open. A set $\mathcal{F} \subset H(G)$ is compact in $H(G)$ (with the topology of uniform convergence on compact sets) if and only if $\mathcal{F}$ is closed and locally bounded.
