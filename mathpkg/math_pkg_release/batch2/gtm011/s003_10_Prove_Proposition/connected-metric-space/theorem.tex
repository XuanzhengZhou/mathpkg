A metric space $(X, d)$ is \textbf{connected} if the only subsets of $X$ which are both open and closed in $X$ are $\varnothing$ and $X$.

If $A \subset X$, then $A$ is a connected subset of $X$ if $(A, d)$ is a connected metric space.
