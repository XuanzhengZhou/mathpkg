Let $(X, \mathcal{T})$ be a topological space and let $Y$ be a subset of $X$.

(a) If $X$ is compact and $Y$ is a closed subset of $X

way to define a topology. The sequence of steps by which open sets are obtained in a metric space are as follows: the metric is given, then open balls are defined, then open sets are defined as those sets which contain a ball about each of their points. What we wish to mimic now is the introduction of balls; this is done by defining a neighborhood system on a set.