For stationary Markov chains, irreducibility with persistent states is equivalent to ergodicity. A non-irreducible chain has nontrivial invariant sets. For irreducible chains with persistent states, $\lim_n p_{i,m,j-m}^{(n)} = p_{j-m}$, which implies mixing.
