The Markov property can be equivalently stated as: for any sets $A, B$ defined by constraints on the past ($t < m$) and future ($t > m$) respectively,
$$P(AB \mid X_m) = P(A \mid X_m) P(B \mid X_m).$$
That is, conditional on the present state $X_m$, the past and future are independent.
