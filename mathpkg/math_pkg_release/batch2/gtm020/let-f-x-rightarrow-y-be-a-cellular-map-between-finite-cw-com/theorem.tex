$$\tilde{K}(S(Y)) \xrightarrow{S(f)'} \tilde{K}(S(X)) \xrightarrow{b(f)'} \tilde{K}(C_f) \xrightarrow{a(f)'} \tilde{K}(Y) \xrightarrow{f'} \tilde{K}(X)$$

If $A$ is a subcomplex of a finite CW-complex $X$ with inclusion map $j$, there is a morphism $\delta = (q^j)^{-1} b(j)^j$: $\tilde{K}(S(X)) \rightarrow \tilde{K}(X/A)$ such that the following sequence is exact:

$$\tilde{K}(S(X)) \rightarrow \tilde{K}(S(A)) \xrightarrow{\delta} \tilde{K}(X/A) \rightarrow \tilde{K}(X) \rightarrow \tilde{K}(A)$$
