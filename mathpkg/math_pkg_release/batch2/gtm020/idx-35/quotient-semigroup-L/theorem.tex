On $S_F(X, A)$ define an equivalence relation by declaring that $\alpha: \xi_0 \rightarrow \xi_1$ is equivalent to $\beta: \eta_0 \rightarrow \eta_1$ provided there exist vector bundles $\zeta$ and $\zeta'$ such that $\alpha \oplus 1: \xi_0 \oplus \zeta \rightarrow \xi_1 \oplus \zeta$ and $\beta \oplus 1: \eta_0 \oplus \zeta' \rightarrow \eta_1 \oplus \zeta'$ are isomorphic as difference isomorphisms. Let $[\alpha: \xi_0 \rightarrow \xi_1]$ denote the element in the quotient semigroup $L(X, A)$ (or $L_F(X, A)$) determined by $\alpha: \xi_0 \rightarrow \xi_1$. This equivalence relation is compatible with the semigroup structure on $S(X, A)$, making $L(X, A)$ a commutative semigroup. The relation is also preserved under induced morphisms $f^*: S(X, A) \rightarrow S(Y, B)$, yielding quotient semigroup morphisms $f^*: L(X, A) \rightarrow L(Y, B)$.
