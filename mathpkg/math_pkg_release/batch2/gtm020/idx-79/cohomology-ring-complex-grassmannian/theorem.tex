Let $c_i$ denote $c_i(\gamma_n)$, where $\gamma_n$ is the universal $n$-dimensional vector bundle. Then the cohomology ring $H^*(G_n(C^\infty), Z) = Z[c_1, \dots, c_n]$ and the classes $c_1, \dots, c_n$ are algebraically independent.
