For a vector bundle $E$, the secondary characteristic class $a_2(E)$ is defined by
$$
a_2(E) = c_1(E)^2 - 2c_2(E).
$$
In terms of traces, this equals $\text{Tr}(K^2)$, where $K$ is the curvature form of a connection on $E$.
