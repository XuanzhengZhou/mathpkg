Let $f(X)$ be an irreducible polynomial in $k[X]$ with roots $s_1, \ldots, s_n$ in $k_{\text{Alg}}$. Then $f(X)$ is separable over $k$ if the $s_i$ are pairwise distinct, and radical (or purely inseparable) over $k$ if the $s_i$ are all equal.
