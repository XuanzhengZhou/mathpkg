Given two modules $M$ and $N$, say left $R$-modules, every $R$-homomorphism from $M$ to $N$ is an element of the set of functions from $M$ to $N$; in particular, these homomorphisms form a set. The standard notation for this set is

$$\operatorname{Hom}_R(M, N).$$
