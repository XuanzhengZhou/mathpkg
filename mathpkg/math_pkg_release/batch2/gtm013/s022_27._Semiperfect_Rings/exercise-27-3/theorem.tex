Prove the following:
\begin{enumerate}
\item[(1)] If ${}_R U_S$ defines a Morita duality, then $R$ and $S$ are semiperfect.
\item[(2)] If $R$ is any ring and ${}_R E$ is injective with $S = \operatorname{End}({}_R E)$, then idempotents lift modulo $J(S)$.
\end{enumerate}
