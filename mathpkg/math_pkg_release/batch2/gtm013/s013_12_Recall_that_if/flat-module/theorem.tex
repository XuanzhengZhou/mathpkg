A right $R$-module $V_R$ is \text{flat} in case for every monomorphism ${}_R K \to {}_R M$ in ${}_R\mathbf{M}$, the induced $\mathbb{Z}$-homomorphism $V \otimes_R K \to V \otimes_R M$ is monic. Equivalently, the tensor functor $(V \otimes_R -)$ is exact (both left and right exact).
