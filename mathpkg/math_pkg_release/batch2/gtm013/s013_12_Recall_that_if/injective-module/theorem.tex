A left $R$-module $E$ is \text{injective} in case for every exact sequence
$$M' \xrightarrow{f} M \xrightarrow{g} M''$$
in ${}_R\mathbf{M}$, the sequence
$$\operatorname{Hom}_R(M'', E) \xrightarrow{g^*} \operatorname{Hom}_R(M, E) \xrightarrow{f^*} \operatorname{Hom}_R(M', E)$$
is exact.
