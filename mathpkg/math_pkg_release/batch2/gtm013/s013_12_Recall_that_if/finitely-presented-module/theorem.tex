A finitely generated module ${}_R M$ is \text{finitely presented} in case for every exact sequence
$$0 \to K \to F \to M \to 0$$
with $F$ finitely generated and free, the kernel $K$ is also finitely generated. A ring $R$ is \text{left coherent} if each of its finitely generated left ideals is finitely presented.
