A module $C$ in ${}_R\mathbf{M}$ is a \text{cogenerator} in case $C$ cogenerates every left $R$-module; that is, for each left $R$-module $M$, there exists a monomorphism
$$0 \to M \to C^A$$
for some set $A$ (equivalently, $\operatorname{Rej}_M(C) = 0$).
