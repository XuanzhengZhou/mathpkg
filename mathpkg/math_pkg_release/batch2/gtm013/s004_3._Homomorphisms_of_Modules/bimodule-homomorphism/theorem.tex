Given bimodules ${}_R M_S$ and ${}_R N_S$, an \textbf{$(R,S)$-homomorphism} from $M$ to $N$ is a function $f: M \to N$ that is linear over both $R$ and $S$. Explicitly, for all $r, r' \in R$, $s, s' \in S$, and $x, x' \in M$,
$$f(rxs + r'x's') = rf(x)s + r'f(x')s'.$$
