A pair $(P, p)$ is a **projective cover** of the module ${}_R M$ in case $P$ is a projective left $R$-module and

$$P \xrightarrow{p} M \rightarrow 0$$

is a superfluous epimorphism (i.e., $\operatorname{Ker} p \ll P$).
