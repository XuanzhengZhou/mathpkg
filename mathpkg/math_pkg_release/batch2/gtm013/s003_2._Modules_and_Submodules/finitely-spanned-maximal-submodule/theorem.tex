Let $M$ be a non-zero left $R$-module. If $M$ is finitely spanned, then $M$ contains a maximal proper submodule. More generally, if $M$ is spanned by a finite set $\{x_1, \ldots, x_n\}$, then there exists a submodule $N$ of $M$ that is maximal with respect to the property that $x_1 \notin N$.
