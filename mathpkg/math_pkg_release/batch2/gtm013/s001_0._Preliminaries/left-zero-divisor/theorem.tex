Let $R$ be a ring. An element $a \in R$ is a left zero divisor if there exists $b \neq 0$ in $R$ such that $ab = 0$. Right zero divisors are defined analogously. A zero divisor is an element that is either a left or right zero divisor.
