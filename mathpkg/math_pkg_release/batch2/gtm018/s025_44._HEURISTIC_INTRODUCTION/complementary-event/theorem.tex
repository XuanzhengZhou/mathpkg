If $E$ is an event, its complementary event $E'$ is the event that occurs if and only if $E$ does not occur. In set-theoretic terms, $E'$ is the complement of the set $E$ relative to the certain event $X$. Thus if $E = \{2,4,6\}$ (the event that a die shows an even number), then $E' = \{1,3,5\}$.
