A transformation $T$ from $X$ into $Y$ assigns in an obvious way a function $f$ on $X$ to every function $g$ on $Y$; $f$ is defined by $f(x) = g(T(x))$. It is convenient and natural to write $f = gT$.
