If a closed interval $F_0 = [a_0, b_0]$ is contained in the union of a finite number of bounded, open intervals $U_1, \cdots, U_n$ where $U_i = (a_i, b_i)$, then
$$b_0 - a_0 < \sum_{i=1}^{n} (b_i - a_i).$$
