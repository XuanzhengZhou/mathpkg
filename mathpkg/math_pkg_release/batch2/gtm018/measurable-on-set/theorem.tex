A real valued function $f$ defined on a measurable set $E$ is called **measurable on $E$** if $E \cap f^{-1}(M)$ is measurable for every Borel set $M$. Every measurable function is measurable on every measurable set.
