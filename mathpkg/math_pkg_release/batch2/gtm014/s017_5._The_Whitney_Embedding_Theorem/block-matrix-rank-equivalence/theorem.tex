Let $S$ be an $m \times n$ matrix where

$$S = \begin{pmatrix} A & B \\ C & D \end{pmatrix}$$

with $A$ a $k \times k$ invertible square matrix. Then $\operatorname{rank} S = k$ if and only if $D - CA^{-1}B = 0$.
