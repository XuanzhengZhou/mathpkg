Let $X$ be a differentiable $n$-manifold and let $c_1, c_2 \colon \mathbf{R} \to X$ be curves on $X$ based at $p \in X$. Then $c_1$ is \emph{tangent to $c_2$ at $p$} if for every chart $\phi$ on $X$ with $p \in \operatorname{dom} \phi$,
$$
(d\phi \cdot c_1)_0 = (d\phi \cdot c_2)_0.
$$
