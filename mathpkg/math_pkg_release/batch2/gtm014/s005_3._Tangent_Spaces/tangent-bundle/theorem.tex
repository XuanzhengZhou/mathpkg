Let $X$ be a differentiable manifold. The \emph{tangent bundle} of $X$ is the disjoint union of all tangent spaces:
$$
TX = \bigcup_{p \in X} T_p X.
$$
Let $\pi \colon TX \to X$ denote the natural projection sending each tangent vector $\alpha \in T_p X$ to its base point $p$.
