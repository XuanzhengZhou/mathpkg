Let $\pi: X \to Y$ be a submersion with $\dim X = n = \dim Y + 1$ and $q = \pi(p)$. Let $A$ be a finitely generated $C_p^\infty(X)$-module. If $V = A / \mathcal{M}_q(Y)A$ is a finite dimensional vector space over $\mathbb{R}$, then $A$ is a finitely generated $C_q^\infty(Y)$-module (via $\pi^*$).
