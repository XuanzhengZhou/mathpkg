Let $X$, $Y$, and $Z$ be smooth manifolds. Then $C^\infty(X, Y) \times C^\infty(X, Z)$ is homeomorphic in the Whitney $C^\infty$ topology to $C^\infty(X, Y \times Z)$ via the standard identification
$$(f, g) \mapsto f \times g$$
where $(f \times g)(x) = (f(x), g(x))$ for all $x \in X$.
