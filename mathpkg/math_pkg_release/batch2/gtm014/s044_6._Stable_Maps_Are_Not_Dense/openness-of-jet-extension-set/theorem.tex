Let $X$ and $Y$ be manifolds and $W$ an open subset of $C^\infty(X, Y)$. Then the set
$$A_W = \{ \sigma \in J^k(X, Y) \mid \exists f \in W \text{ and } x \in X \text{ with } \sigma = j^k f(x) \}$$
is open in $J^k(X, Y)$.
