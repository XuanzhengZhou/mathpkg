Let $(V, k_1, k_2)$ be a doubly-capacitated network. A necessary condition for the existence of a feasible flow is that for every $U$ with $\varnothing \subset U \subset V$,
$$k(U) = \sum_{g_U(e)=1} k_2(e) \; - \!\! \sum_{g_U(e)=-1} k_1(e) \geq 0,$$
where $k(U)$ denotes the right-hand quantity.
