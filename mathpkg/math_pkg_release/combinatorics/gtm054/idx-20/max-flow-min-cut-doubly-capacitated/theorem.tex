Let $(V, k_1, k_2)$ be a doubly-capacitated network and let $e_0 \in W$. If $(V, k_1, k_2)$ admits a feasible flow, then it admits a maximum flow $h$ through $e_0$ and
$$h(e_0) = \min\bigl\{ k_2(e_0) \bigr\} \cup \bigl\{ k_2(C; e_0) : C \text{ is a cut through } e_0 \bigr\}.$$
