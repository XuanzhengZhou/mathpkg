$\nu_{-1}(\Gamma_{(e)}) - \nu_{-1}(\Gamma) = 1 - m.$

If $e$ is an isthmus, no cycle contains $e$. Hence $\pi_{e}[\mathcal{Z}(\Gamma)] = \{\varnothing\}$, and $m = 0$.
If $e$ is not an isthmus, some cycle contains $e$; hence $\pi_{e}[\mathcal{Z}(\Gamma)] = \mathcal{P}(\{e\})$ and $m = 1$. Thus C2, C3, and C4 give (a
