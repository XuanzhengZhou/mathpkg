$\{i, j, k\} \in \mathcal{P}_3(\{1, \ldots, n\}) \Rightarrow W_{ij} \cap W_{ik} = \{x_i\}$.

If $y \in W_{ij} \cap W_{ik}$, then $h(y) = i$. If $y \neq x_i$, then $y$ is incident (by A24) with two vertices colored $j$ and two vertices colored $k$. Since $\rho(y) = m - 1$, $y$ is incident with no vertex of some color $r \in \{1, \ldots, m - 1\} + \{i, j, k\}$. Defining $h'$ as in A25, we obtain an $m$-coloring with respect to which Lemma A11b is again contradicted, whence A26 holds.

We next demonstrate
