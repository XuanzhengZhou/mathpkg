A function $h \in \mathbb{Q}^W$ is said to be \textbf{integral} if $h \in \mathbb{Z}^W$. One may thus speak of an integral capacity and an integral flow. A network $(V, k)$ is called an \textbf{integral network} if $k$ is an integral capacity.
