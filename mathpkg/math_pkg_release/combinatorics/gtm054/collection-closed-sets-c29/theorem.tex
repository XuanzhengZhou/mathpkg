Let $\%$ be the collection of closed sets of a closure structure $(V, c)$, and let $U_1, U_2 \in \mathcal{C}$. Then $U_2$ is a successor of $U_1$ in $(\mathcal{C}, \subseteq)$ if and only if $U_2 = c(U_1 + \{x\})$ for some $x \in U_2 + U_1$.
