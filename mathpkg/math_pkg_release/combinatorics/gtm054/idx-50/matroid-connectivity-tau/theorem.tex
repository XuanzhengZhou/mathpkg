The \textit{connectivity} of a matroid $\Lambda$ is given by
$$\tau(\Lambda) = \min\{m \in \mathbb{N} : \Lambda \text{ is } m\text{-separated}\},$$
with $\tau(\Lambda) = \infty$ if $\Lambda$ is $m$-separated for no $m \in \mathbb{N}$.
