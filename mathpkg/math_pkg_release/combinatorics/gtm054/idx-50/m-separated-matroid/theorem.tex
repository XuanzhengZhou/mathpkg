Let $\Lambda = (V, \mathcal{M})$ be a matroid with rank function $r$. $\Lambda$ is said to be \textit{$m$-separated} if there exists $U \in \mathcal{P}(V)$ such that
\begin{enumerate}
\item $m < \min\{|U|, |V \setminus U|\}$;
\item $m = r(U) + r(V \setminus U) - r(V)$.
\end{enumerate}
