A largest complete set in a graph $\Gamma$ is called a \textbf{clique}, and
$$\omega(\Gamma)$$
is used to denote its cardinality. Thus $\omega(\Gamma)$ is the size of the largest complete subgraph of $\Gamma$, called the \textbf{clique number} of $\Gamma$.
