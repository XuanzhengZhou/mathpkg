Let $\Gamma = (V, \mathcal{E})$ be a graph. The \textbf{complement} of $\Gamma$, denoted by $\Gamma'$, is the graph
$$\Gamma' = (V, \mathcal{P}_2(V) \setminus \mathcal{E}).$$
Equivalently, two distinct vertices are adjacent in $\Gamma'$ if and only if they are not adjacent in $\Gamma$.
