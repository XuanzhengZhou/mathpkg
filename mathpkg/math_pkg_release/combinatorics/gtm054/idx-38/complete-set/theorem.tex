A subset $S \subseteq V$ of the vertex set of a graph $\Gamma = (V, \mathcal{E})$ is called a \textbf{complete set} if
$$\mathcal{P}_2(S) \subseteq \mathcal{E},$$
i.e., every pair of distinct vertices in $S$ is joined by an edge, so the induced subgraph $\Gamma_S$ is a complete subgraph.
