For a bipartite graph $B = (\{V_1, V_2\}, \mathcal{E})$,

$$\delta_i(B) = \max\{\delta(U): U \subseteq V_i\} \quad (i = 1, 2),$$

written briefly $\delta_i$ when $B$ is understood from the context. Note that $\delta(\emptyset) = 0$, so $\delta_i \geq 0$ for any bipartite graph.