An independent vertex set (also called an "internally stable set") is an independent subset of $V$. That is, $S \subseteq V$ such that no two distinct vertices in $S$ are incident (equivalently, no two distinct vertices in $S$ are adjacent; $S$ contains no edges of the graph).