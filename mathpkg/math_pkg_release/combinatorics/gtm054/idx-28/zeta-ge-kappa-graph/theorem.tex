Let $\zeta(\Gamma)$ denote the largest integer $m$ for which it holds nonvacuously that if $U \in \mathcal{P}_m(V)$, then there is an elementary circuit in $\Gamma$ which contains $U$. If $\kappa(\Gamma) \geq 2$, then

$$\zeta(\Gamma) \geq \kappa(\Gamma).$$
