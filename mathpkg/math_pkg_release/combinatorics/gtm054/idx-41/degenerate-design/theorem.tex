The design $\Lambda$ is said to be **degenerate** if either $\bar{s}(V) > 0$ or $\bar{s}^*(E) > 0$. By IE8 this means that either some vertex lies in every block or some block contains every vertex. Clearly $\Lambda$ is degenerate if and only if $\Lambda^*$ is degenerate.
