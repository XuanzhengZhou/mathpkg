$$\kappa(\Gamma) = \min\{\sigma(a, z): \{a, z\} \in \mathcal{P}_2(V) + \mathcal{E}\}$$

whenever $\Gamma$ is not complete. In particular, if $\kappa(\Gamma) = 1$ and $|V| \geq 3$, then $\Gamma$ has an articulation vertex (cf. Exercise IIIB11). The terms "biconnected" and "2-connected" are equivalent for graphs. Similarly, the terms "tri-connected" and "3-connected" are equivalent for graphs.

If $|V| \geq 2$, then
