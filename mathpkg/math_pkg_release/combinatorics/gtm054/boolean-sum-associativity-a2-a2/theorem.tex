$za(x) = z \sum_{j=0}^{\infty} a_j x^j = \sum_{j=0}^{\infty} za_j x^j = (za)(x)$

for all $z \in \mathbb{C}$. With respect to these two operations, $\mathbb{C}^N$ is a vector space, with additive identity 0 being the sequence whose every value is 0.

If $a, b \in \mathbb{C}^N$, we define a product
