(Bose and Clatworthy [b.12]). Let $V$ be a set and let $\mathcal{Q} = \{Q_1, Q_2\} \in \mathbb{P}_2(\mathcal{P}_2(V))$. Suppose that E4 is satisfied for $m = 2$. For $i, j, k \in \{1, 2\}$, define the eight functions

$$p_{jk}^i: \{(x, y) \in V \times V : \{x, y\} \in Q_i\} \rightarrow \mathbb{N}$$

by

$$p_{jk}^i(x, y) = |\{z \in V : \{x, z\} \in Q_j; \{y, z\} \in Q_k\}|.$$

For $i \in \{1, 2\}$, if $p_{11
