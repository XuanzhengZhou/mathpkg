Let $M = [m_{ij}]$ be an $r \times s$ matrix where $m_{ij} \in \{0, 1\}$ for all $(i, j) \in \{1, \ldots, r\} \times \{1, \ldots, s\}$. Such a matrix is called a \textbf{$\{0,1\}$-matrix}. By a \textbf{line} of $M$ is meant either a row or a column of $M$.
