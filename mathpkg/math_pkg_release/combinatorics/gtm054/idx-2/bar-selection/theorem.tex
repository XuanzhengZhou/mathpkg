For any selection $s \in \mathbb{S}(\mathcal{P}(V))$, we define $\bar{s} \in \mathbb{S}(\mathcal{P}(V))$ by:

$$\bar{s}(S) = \sum_{T \in \mathcal{P}(V)} [S, T]\, s(T), \quad \text{for all } S \in \mathcal{P}(V),$$

where $[S, T] = 1$ if $S \subseteq T$ and $0$ otherwise.
