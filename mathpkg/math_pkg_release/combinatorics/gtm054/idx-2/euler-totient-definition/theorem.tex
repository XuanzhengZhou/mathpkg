The \textbf{Euler $\varphi$-function} is defined for all $n \in \mathbb{N}$ by
$$\varphi(n) = |\{k \in \{1, 2, \ldots, n\} : \gcd(k, n) = 1\}|,$$
i.e., the number of positive integers not exceeding $n$ that are relatively prime to $n$. For example, $\varphi(p) = p - 1$ whenever $p$ is prime.
