Let $\Lambda = \bigoplus_{i=1}^{n} \Lambda_i$ with components $\Lambda_i = (V_i, f_i, E_i)$ for $i = 1, \ldots, n$. If $(p, q) \in G(\Lambda)$, then to each $i \in \{1, \ldots, n\}$ there corresponds some $j \in \{1, \ldots, n\}$ such that $q[V_i] = V_j$ and $p[E_i] = E_j$. Moreover, $(p_{|E_i}, q_{|V_i})$ is a system-isomorphism from $\Lambda_i$ to $\Lambda_j$.

If $X$ is a set and if $G$ is a subgroup of $\Pi(X)$, then $G$ is said to be transitive on $X$ if for each $x, y \in X$, there exists some $p \

$\Pi(\{1, \ldots, n\})$. If $G$ and $G'$ are permutation groups, let $G$ wreath $G'$ denote the wreath product of $G$ by $G'$.
