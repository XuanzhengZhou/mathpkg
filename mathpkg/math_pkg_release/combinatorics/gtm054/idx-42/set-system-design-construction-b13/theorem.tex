Let integers $t_1, \ldots, t_p$, $v$ be given such that $0 < t_1 < \ldots < t_p < v$ and $v \geq 3$ and such that $t_{i+1} = t_i + 1$ holds for all indices $i \in \{2, \ldots, p - 1\}$. Then there exists a set system with $v$ vertices and design-type $(t_1, \ldots, t_p;)$.
