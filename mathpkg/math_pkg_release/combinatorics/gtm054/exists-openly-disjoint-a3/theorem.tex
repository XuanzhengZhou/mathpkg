max{$m$: there exists an openly-disjoint $m$-family of az-paths in $\Gamma}$
$$= \max\{h(x_0): h \text{ is a feasible flow in } \Gamma'\}.$$

We have observed that a set $S \subseteq V' + \{x_0\}$ meets every directed circuit through $x_0$ in $\Gamma'$ if and only if $S$ meets every az-path in $\Gamma$. For such a set, $j(S) = \sum_{x \in S} j(x) = |S|$. Thus,
