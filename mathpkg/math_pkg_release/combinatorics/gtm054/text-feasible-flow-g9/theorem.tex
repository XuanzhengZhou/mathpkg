$$\min\{h(e_0): h \text{ is a feasible flow}\} = \min\{|Q|: Q \in \mathbb{P}(X); Q \text{ is a chain, for all } Q \in \mathbb{Q}\}.$$

Consider an arbitrary cut $C = \sum_{x \in U} f^*(x)$ through $e_0$, where it may be assumed that $x_0 \in U \subseteq V + \{y_0\}$. Let

$$S = \{x \in X: x^1 \in U; x^2 \in V + U\},$$

$$T = \{x \in X: x^1 \in V + U; x^2 \in U\},$$

$$D = \{(x, y) \in X \times X: x < y; x^2 \in V + U; y^1 \in U\}.$$

By definition,
