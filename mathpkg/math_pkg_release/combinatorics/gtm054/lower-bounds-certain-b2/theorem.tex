$n(q_1, \ld

lower bounds for certain classes of Ramsey numbers. Prior to the main proof, we obtain some essential preliminary results.

First, it is immediate that if $n(q_1, \ldots, q_m: s)$ exists, then any set $W$ such that $|W| \geq n(q_1, \ldots, q_m: s)$ also satisfies the condition stated in the right-hand member of B2. Also, if $q_i \leq \bar{q}_i$ for $i = 1, \ldots, m$, then $n(q_1, \ldots, q_m: s) \leq n(\bar{q}_1, \ldots, \bar{q}_m: s)$.

Next we note that if $m = 1$, and if $1 \leq s \leq q$, then
