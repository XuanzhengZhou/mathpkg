Let $(V, D)$ be a directed graph and let $Z \subseteq V$. The matroid $\Lambda = (V, \mathcal{M})$ such that
$$\mathcal{I}(\Lambda) = \{A \in \mathcal{P}(V) : \text{there exists a } |A|\text{-family of pairwise-disjoint } AZ\text{-paths}\}$$
is called a \textbf{strict gammoid}.
