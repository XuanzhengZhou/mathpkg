Let $\Gamma = (V, f, E)$ be a multigraph. If for some $k \in \mathbb{N}$, $\rho(x) = k$ for all $x \in V$, then $\Gamma$ is called $k$-valent.

$\Gamma$ is called isovalent if it is $k$-valent for some $k$.
