Let $\Lambda$ be a system. A subsystem $\Gamma$ of $\Lambda$ is called:
\begin{itemize}
\item a submultigraph of $\Lambda$ if $\Gamma$ is a multigraph;
\item a subgraph of $\Lambda$ if $\Gamma$ is a graph.
\end{itemize}
