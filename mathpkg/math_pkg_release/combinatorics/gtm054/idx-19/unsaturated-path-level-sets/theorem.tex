Let $(V, k)$ be an integral network with $e_0 = (y_0, x_0) \in W$, and let $h$ be an integral feasible flow. For $i = 0, 1, 2, \ldots$, define

$$U_i = \{x \in V : \text{there exists an unsaturated } x_0x\text{-path of length } i \text{ but none of shorter length}\}.$$
