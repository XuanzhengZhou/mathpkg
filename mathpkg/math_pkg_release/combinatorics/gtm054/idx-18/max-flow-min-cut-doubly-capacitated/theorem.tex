Let $e_0 \in W$. If the doubly-capacitated network $(V, k_1, k_2)$ admits a feasible flow, then it admits a maximum flow $h$ through $e_0$ and

$$h(e_0) = \min\Bigl(\{k_2(e_0)\} \cup \{k_2(C; e_0): C \text{ is a cut through } e_0\}\Bigr).$$
