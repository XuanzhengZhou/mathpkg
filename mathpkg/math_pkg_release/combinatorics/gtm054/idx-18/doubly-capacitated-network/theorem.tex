A \text{doubly-capacitated network} is a triple $(V, k_1, k_2)$ where $k_i \in \mathbb{Z}^W$ for $i = 1, 2$, and $k_1(e) \leq k_2(e)$ for all $e \in W$. The functions $k_1$ and $k_2$ are called, respectively, the \text{lower} and \text{upper capacities}.
