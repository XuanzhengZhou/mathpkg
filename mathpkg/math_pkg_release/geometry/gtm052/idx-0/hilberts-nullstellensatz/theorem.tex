Let $k$ be an algebraically closed field, let $\mathfrak{a}$ be an ideal in $A = k[x_1, \ldots, x_n]$, and let $f \in A$ be a polynomial which vanishes at all points of $Z(\mathfrak{a})$. Then $f^r \in \mathfrak{a}$ for some integer $r > 0$.
