A field extension $K/k$ is separably generated if there is a transcendence base $\{x_i\}$ for $K/k$ such that $K$ is a separable algebraic extension of $k(\{x_i\})$. Such a transcendence base is called a separating transcendence base.
