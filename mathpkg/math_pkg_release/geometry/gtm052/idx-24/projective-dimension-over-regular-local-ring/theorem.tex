If $A$ is a regular local ring, then:
\begin{enumerate}
\item[(a)] for every $A$-module $M$, $\operatorname{pd}(M) \leq \dim A$;
\item[(b)] if $k = A/\mathfrak{m}$, then $\operatorname{pd}(k) = \dim A$.
\end{enumerate}
