Let $A$ be a complete regular local ring of dimension $n$ containing some field. Then
$$A \cong k[[x_1, \ldots, x_n]],$$
where $k$ is the residue field of $A$ and $k[[x_1, \ldots, x_n]]$ is the ring of formal power series in $n$ variables over $k$.
