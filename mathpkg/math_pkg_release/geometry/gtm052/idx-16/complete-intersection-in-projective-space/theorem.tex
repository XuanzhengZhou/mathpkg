A closed subscheme $Y$ of $\mathbf{P}_k^n$ is called a \emph{(strict, global) complete intersection} if the homogeneous ideal $I$ of $Y$ in $S = k[x_0, \ldots, x_n]$ can be generated by $r = \operatorname{codim}(Y, \mathbf{P}^n)$ elements.
