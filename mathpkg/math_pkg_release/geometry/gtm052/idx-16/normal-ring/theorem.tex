A noetherian ring $A$ is called \emph{normal} if for every prime ideal $\mathfrak{p} \subseteq A$, the localization $A_\mathfrak{p}$ is an integrally closed domain. A normal ring is a finite direct product of integrally closed domains.
