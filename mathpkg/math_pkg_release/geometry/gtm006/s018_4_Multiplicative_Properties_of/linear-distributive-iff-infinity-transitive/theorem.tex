Let $(R, T)$ be a planar ternary ring coordinatizing a projective plane $\mathcal{P}$. If $(R, T)$ is linear, then $(R, T)$ has associative multiplication and satisfies the left distributive law if and only if $\mathcal{P}$ is $((\infty), [0, 0])$-transitive.
