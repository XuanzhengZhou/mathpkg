Let $\mathcal{P}$ be a projective plane coordinatized by the planar ternary ring $(R, T)$, and let $\mathcal{P}^*$ be its dual plane coordinatized by $(R, T')$. If $(R, T)$ is linear with associative addition, then $(R, T')$ is also linear with associative addition.
