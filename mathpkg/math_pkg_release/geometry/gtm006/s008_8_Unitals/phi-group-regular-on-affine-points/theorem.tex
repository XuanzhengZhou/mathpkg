The set of all mappings $\phi_{a,b}$ defined on the affine plane $\mathcal{A}$ by
$$\phi_{a,b}: \langle x_1 e_1 + x_2 e_2 + e_3 \rangle \mapsto \langle (x_1 + a) e_1 + (x_2 + b) e_2 + e_3 \rangle$$
forms a group under composition which is regular on the points of $\mathcal{A}$.
