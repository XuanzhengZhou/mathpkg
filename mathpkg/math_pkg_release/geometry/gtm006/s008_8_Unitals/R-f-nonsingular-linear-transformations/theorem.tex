Let $F$ be a left vector space over the skewfield $K$. For each element $f \neq 0$ in $F$, define the mapping $R_f: F \to F$ by $R_f(x) = x f$. Then $R_f$ is a non-singular linear transformation of the left vector space $F$ over $K$.
