A semi-linear transformation $\phi$ from $V$ to $W$ with associated automorphism $\alpha$ is called a **linear transformation** if $\alpha$ is the identity automorphism of $K$. Equivalently, for all $v \in V$ and $k \in K$:
$$(kv)^\phi = k(v^\phi).$$
