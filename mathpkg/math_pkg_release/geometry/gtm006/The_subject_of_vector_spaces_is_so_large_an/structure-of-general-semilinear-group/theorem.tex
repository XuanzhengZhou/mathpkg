Let $V$ be a finite-dimensional left or right vector space over a skewfield $K$. Then
$$\Gamma L(V) = A \cdot GL(V),$$
where:
\begin{itemize}
\item $GL(V)$ is a normal subgroup of $\Gamma L(V)$,
\item $A \cap GL(V) = \{1\}$,
\item $A \cong \operatorname{Aut}(K)$.
\end{itemize}
