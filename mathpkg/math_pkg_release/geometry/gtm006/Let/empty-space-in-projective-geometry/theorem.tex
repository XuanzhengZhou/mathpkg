If the points of $\mathcal{P}(V)$ are taken as the most fundamental objects, then their intersection is the natural empty set: the zero-subspace of $V$, whose dimension is zero and whose $g$-dimension is $-1$, is the empty space in $\mathcal{P}(V)$.
