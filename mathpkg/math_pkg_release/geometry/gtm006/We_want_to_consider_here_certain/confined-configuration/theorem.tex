A configuration $\mathcal{C}$ is called \textbf{confined} if every one of its elements (every point and every line) is incident with at least three elements. A confined configuration has at least seven points and seven lines.
