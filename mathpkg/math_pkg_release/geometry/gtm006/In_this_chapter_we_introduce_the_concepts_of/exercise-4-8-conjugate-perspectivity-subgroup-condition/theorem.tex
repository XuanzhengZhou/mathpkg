If $\Gamma$ is a collineation group of a projective plane $\mathcal{P}$, show that if $\Gamma_{(V,\ell)} \neq 1$, then for any $\gamma \in \Gamma$, $\Gamma_{(V,\ell)}^\gamma = \Gamma_{(V,\ell)}$ if and only if $V^\gamma = V$ and $\ell^\gamma = \ell$.
