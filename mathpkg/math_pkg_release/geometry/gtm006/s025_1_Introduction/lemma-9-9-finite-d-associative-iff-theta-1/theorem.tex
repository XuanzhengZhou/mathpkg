Let $F$ be a finite field and let $D$ be the division ring constructed from $F$ with anti-automorphism $\theta$ and parameters $a, b$ as defined in Section 4. Then $D$ is associative if and only if $\theta = 1$.
