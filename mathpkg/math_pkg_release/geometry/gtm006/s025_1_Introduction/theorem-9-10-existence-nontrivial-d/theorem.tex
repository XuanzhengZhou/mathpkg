If $F = GF(p^n)$ where $p$ is a prime and $n > 1$, then there exists a non-trivial (non-associative) example of the division ring $D$ constructed from $F$ with a suitable anti-automorphism $\theta \neq 1$ and parameters $a, b$.
