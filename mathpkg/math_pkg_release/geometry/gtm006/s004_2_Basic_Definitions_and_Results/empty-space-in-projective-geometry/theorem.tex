The zero-subspace of $V$, whose (algebraic) dimension is zero and whose $g$-dimension is $-1$, is the \textbf{empty space} in $\mathcal{P}(V)$. It plays the role of the "natural" empty set: the intersection of any two distinct points of $\mathcal{P}(V)$ as fundamental objects yields the zero-subspace.
