In $\mathcal{P}(V)$, subspaces of $g$-dimension zero (i.e., algebraic dimension one) are called \textbf{points}. Subspaces of $g$-dimension one are called \textbf{lines}. Subspaces of $g$-dimension two are called \textbf{planes}. Subspaces of $g$-dimension $n-1$ are called \textbf{hyperplanes}.
