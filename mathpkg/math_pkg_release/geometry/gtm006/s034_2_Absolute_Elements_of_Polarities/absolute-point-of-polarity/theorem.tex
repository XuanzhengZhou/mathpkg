If $\alpha$ is a polarity of a projective plane $\mathcal{P}$, then a point $A$ is called an \textbf{absolute point} if $A$ is on $A^\alpha$. Dually, a line $m$ is called an \textbf{absolute line} if $m^\alpha$ is on $m$.
