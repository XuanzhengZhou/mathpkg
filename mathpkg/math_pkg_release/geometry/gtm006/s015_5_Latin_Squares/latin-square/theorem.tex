A latin square of order $n$ is an $n \times n$ matrix with entries from a set $R$ of $n$ distinct elements, usually taken to be $\{0, 1, 2, \dots, n-1\}$, such that each row and each column contains every element of $R$ exactly once.
