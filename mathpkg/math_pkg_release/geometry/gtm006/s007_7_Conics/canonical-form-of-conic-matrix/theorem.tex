Let $\mathcal{C}$ be a non-empty conic in $\mathcal{P}(V)$ over a field of characteristic $
eq 2$. Then a basis for $V$ can be chosen so that the symmetric matrix $A$ of the orthogonal polarity has the form:
$$A = egin{bmatrix} 0 & 0 & -1 \ 0 & 2 & 0 \ -1 & 0 & 0 nd{bmatrix}.$$
Consequently, the points of $\mathcal{C}$ are $\langle (x, y, z) angle$ satisfying $x^2 - 2yz = 0$ (or equivalently, any two non-empty conics are projectively equivalent under $PGL(V)$).
