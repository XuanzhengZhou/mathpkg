The subgroup of $PGL(V)$ fixing a non-empty conic $\mathcal{C}$ is called the orthogonal group. Equivalently, it consists of all elements of $PGL(V)$ that commute with the associated orthogonal polarity $\beta$.
