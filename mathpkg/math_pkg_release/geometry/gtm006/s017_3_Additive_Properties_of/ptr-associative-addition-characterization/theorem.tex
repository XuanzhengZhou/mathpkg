Let $(R, T)$ be a planar ternary ring coordinatizing a projective plane $\mathcal{P}$.

$(R, T)$ is linear with associative addition if and only if $\mathcal{P}$ is $((\infty), [\infty])$-transitive.
