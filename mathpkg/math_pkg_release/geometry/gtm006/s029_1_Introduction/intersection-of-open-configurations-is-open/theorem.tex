Let $\{A_a\}$ be a collection of sub-configurations of a configuration $C$. Then

(i) if the $A_a$ are all open then $\bigcap_a A_a$ is open,

(ii) if the $A_a$ are all confined then $\bigcup_a A_a$ is confined.
