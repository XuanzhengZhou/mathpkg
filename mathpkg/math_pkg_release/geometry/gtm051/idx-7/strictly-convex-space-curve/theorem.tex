Let $c: I \to \mathbb{R}^3$ be a closed curve in $\mathbb{R}^3$ with no self-intersections. The curve $c$ is called \emph{strictly convex} if through each point $c(t)$ of $c$ there passes a plane which has no other points in common with $c$.
