Let $F: U \to \mathbb{R}^m$ be differentiable at $x_0 \in U \subset \mathbb{R}^n$. If $L, L' \in L(\mathbb{R}^n, \mathbb{R}^m)$ are two linear maps such that

$$F(x) = F(x_0) + L(x - x_0) + o(|x - x_0|),$$
$$F(x) = F(x_0) + L'(x - x_0) + o(|x - x_0|),$$

as $x \to x_0$, then $L = L'$.
