A \textbf{vector field} on a differentiable surface $M$ is a differentiable mapping
$$X: TM \to TM$$
which satisfies $\pi \circ X = \operatorname{id}_M$, where $\pi: TM \to M$ is the canonical projection of the tangent bundle. Equivalently, $X(p) \in T_p M$ for every $p \in M$.
