Let $M$ be a differentiable surface with Riemannian metric $g$, and let $c: I \to M$ be a differentiable curve. The \textbf{length} of $c$ is defined by
$$L(c) := \int_I \sqrt{g_{c(t)}(\dot{c}(t), \dot{c}(t))} \, dt,$$
where $\dot{c}(t) \in T_{c(t)} M$ is the tangent vector to the curve at $c(t)$.
