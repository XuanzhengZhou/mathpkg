There is a one-to-one onto correspondence between points of $\mathbb{C}^n$ and maximal ideals of $\mathbb{C}[X_1, \ldots, X_n]$.

The maximal ideal corresponding to $(c_1, \ldots, c_n) \in \mathbb{C}^n$ is
$$((X_1 - c_1), \ldots, (X_n - c_n)).$$
