Let $C$ be a nonsingular curve, $Z$ a canonical divisor on $C$, and $D$ a divisor on $C$. Suppose $\dim L(D) > 0$. Then for any point $P \in C$,

$$\dim L(Z - D) \neq \dim L(Z - (D + P))$$

implies

$$\dim L(D) = \dim L(D + P).$$
