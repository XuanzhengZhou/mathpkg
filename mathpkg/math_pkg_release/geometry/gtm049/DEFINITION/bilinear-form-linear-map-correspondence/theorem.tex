Let $\sigma \in \mathcal{B}(V)$. The mapping $\varrho: V \to V^*$ defined by

$$a\varrho(b) = \sigma(a, b)$$

for all $a, b \in V$ is a linear map. The correspondence $\sigma \mapsto \varrho$ is a linear isomorphism of $\mathcal{B}(V)$ onto $\mathcal{L}(V, V^*)$.
