For any vectors $a, b$ in a euclidean space $(V, \sigma)$, define

$$\|a\| = +\sqrt{\sigma(a, a)}, \quad d(a, b) = \|a - b\|.$$

We call $\|a\|$ the \textbf{length} (or \textbf{norm}) of $a$, and $d(a, b)$ the \textbf{distance} from $a$ to $b$.
