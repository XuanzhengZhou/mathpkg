Let $$\sigma$$ be a non-degenerate symmetric bilinear form on $$V$$. A linear mapping $$f$$ of $$V$$ into $$V$$ is called **$$\sigma$$-symmetric** if
$$\sigma(af, b) = \sigma(a, bf)$$
for all $$a, b$$ in $$V$$.
