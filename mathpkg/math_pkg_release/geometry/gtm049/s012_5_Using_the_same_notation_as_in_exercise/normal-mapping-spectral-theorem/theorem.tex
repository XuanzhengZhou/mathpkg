If $$f$$ is a normal mapping on the Hilbert space $$(W, \sigma)$$ (i.e., $$ff^* = f^*f$$ where $$f^*$$ is the adjoint), then there exists a Cartesian basis of $$(W, \sigma)$$ all of whose vectors are eigenvectors of $$f$$.
