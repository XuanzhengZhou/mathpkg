If $V$ and $V'$ are vector spaces over the same field $F$, then a mapping $f$ of $V$ into $V'$ is called a \text{linear mapping} (or \text{homomorphism}) if
$$
(a+b)f = af + bf,
$$
$$
(xa)f = x(af),
$$
for all $a, b$ in $V$ and all $x$ in $F$.
