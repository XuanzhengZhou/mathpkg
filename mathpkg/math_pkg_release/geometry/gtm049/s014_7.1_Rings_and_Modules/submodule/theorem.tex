A submodule of an $R$-module $M$ is defined in exactly the same way as a subspace of a vector space: a subset $N \subseteq M$ that is itself an $R$-module with respect to the operations inherited from $M$.
