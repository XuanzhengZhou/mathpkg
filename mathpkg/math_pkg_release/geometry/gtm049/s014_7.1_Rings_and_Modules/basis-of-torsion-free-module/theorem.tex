For a finitely generated torsion-free $F[X]$-module $E$, a set $\{v_1, \ldots, v_n\}$ is called a \emph{basis} if it is a minimal generating set (equivalently, a maximal linearly independent set) and
$$E = [v_1] \oplus \cdots \oplus [v_n]$$
with each $[v_i] \cong F[X]$.
