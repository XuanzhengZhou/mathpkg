---
role: proof
locale: en
of_concept: for-each-set-a
source_book: gtm001
source_chapter: "9"
source_section: ""
---

We consider all ordered pairs $\langle r, x \rangle$ where $x \subseteq a$ and $r \subseteq a \times a$. If $r$ well orders $x$, then

$(\exists ! \beta)(\exists ! f_{r,x})[f_{r,x} \text{ Isom}_{r,E}(x, \beta)]$.

Using $f_{r,x}$ we define a function on $\mathcal{P}(a \times a) \times \mathcal{P}(a)$ in the following way

$F' \langle r, x \rangle = f_{r,x} x$ If $r$ We $x$
$= 0$ otherwise.

Since $F$ is a function whose domain is a set its range is also a set. Let $b = \mathcal{W}(F)$. Then

$y \in b \leftrightarrow (\exists r \subseteq a \times a)(\exists x \subseteq a)[y = F
