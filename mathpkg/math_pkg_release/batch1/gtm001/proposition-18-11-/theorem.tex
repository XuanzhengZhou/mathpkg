(1) $t \in T_\alpha \rightarrow \text{Ord}(\varphi(t)) < \text{Ord}(\exists^\alpha x_i \varphi(x_i))$.

(2) Ord$(\varphi) < \text{Ord}(\neg \varphi)$.

(3) Ord$(\varphi) < \text{Ord}(\varphi \wedge \psi)$ and Ord$(\psi) < \text{Ord}(\varphi \wedge \psi)$.

(4) $t \in T_\alpha \rightarrow \text{Ord}(\varphi(t)) < \text{Ord}(\hat{x}_i^\alpha \varphi(x_i))$.

The proof is left to the reader.
