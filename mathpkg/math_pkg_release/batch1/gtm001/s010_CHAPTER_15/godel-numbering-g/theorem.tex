\begin{enumerate}
\item $g(\exists^\alpha) \triangleq 2\alpha + 1$,
\item $g(\wedge^\alpha) \triangleq 2\alpha + 2$.
\end{enumerate}
For any finite sequence $s$: $g(s)$ is the maximum of $g$ over all quantifiers/abstractions in $s$.
