Rules of Inference:

(1) From $\varphi$ and $\varphi \rightarrow \psi$ to infer $\psi$.

(2) From $\varphi$ to infer $(\forall x)\varphi(x)$ where $\varphi(x)$ is obtained from $\varphi$ by replacing each occurrence of some free variable by $x$.
