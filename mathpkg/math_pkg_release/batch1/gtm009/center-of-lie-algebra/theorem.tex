The \text{center} of a Lie algebra $L$ is
$$Z(L) = \{z \in L \mid [xz] = 0 \text{ for all } x \in L\}.$$
$Z(L)$ is an ideal of $L$. Moreover, $Z(L) = \text{Ker}(\text{ad})$, where $\text{ad}: L \rightarrow \mathfrak{gl}(L)$ is the adjoint representation. $L$ is abelian if and only if $Z(L) = L$.
