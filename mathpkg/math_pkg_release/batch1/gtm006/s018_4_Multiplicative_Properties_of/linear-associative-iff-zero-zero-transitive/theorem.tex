Let $(R, T)$ be a planar ternary ring coordinatizing a projective plane $\mathcal{P}$. Then $(R, T)$ is linear with associative multiplication if and only if $\mathcal{P}$ is $((0), [0])$-transitive.
