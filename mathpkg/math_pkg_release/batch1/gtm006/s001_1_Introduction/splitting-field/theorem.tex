If $K$ is a field and $f(x)$ is a polynomial over $K$, a splitting field for $f(x)$ over $K$ is a field $F$ containing $K$ in which $f(x)$ factors completely into linear factors $(bx + c)$, and such that no proper subfield of $F$ containing $K$ has this property.
