If $V$ is a left vector space over the skewfield $K$, its dual space $V'$ is the set of all linear functionals on $V$, which forms a right vector space over $K$ under pointwise addition and right scalar multiplication: $v(fb) = (vf)b$.
