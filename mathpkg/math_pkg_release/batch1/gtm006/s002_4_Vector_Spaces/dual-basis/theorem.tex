If $e_1, e_2, \dots, e_n$ is a basis of the left vector space $V$ over $K$, the dual basis $e_1', e_2', \dots, e_n'$ of $V'$ is defined by $e_i e_j' = \delta_{ij}$ (Kronecker delta). Every vector $v = \sum x_i e_i$ satisfies $v e_j' = x_j$, so the dual basis extracts coordinates.
