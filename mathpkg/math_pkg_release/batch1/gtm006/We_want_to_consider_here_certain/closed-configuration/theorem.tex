A configuration $\mathcal{C}$ is called \textbf{closed} if every pair of distinct elements of the same type (two distinct points or two distinct lines) are incident with a common element (a line joining them or a point of intersection, respectively). A closed configuration that contains a quadrangle is a projective plane.
