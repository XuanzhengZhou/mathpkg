Let $D$ be the division ring constructed from a finite field $F$, an anti-automorphism $\theta$, and elements $a, b \in F$ satisfying conditions (1) and (2). Then $D$ is associative if and only if $\theta = 1$ (i.e., $\theta$ is the identity automorphism).