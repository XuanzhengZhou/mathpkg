If $q$ is an odd prime power and $F = \mathrm{GF}(q^2)$, then there always exists an André nearfield $F_\phi$ whose kernel is $\mathrm{GF}(q)$ and such that the elements of the kernel are precisely the elements fixed by all automorphisms in the image of $\phi$.