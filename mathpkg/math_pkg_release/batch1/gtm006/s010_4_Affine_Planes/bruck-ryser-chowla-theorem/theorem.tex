Let $n$ be the order of a finite projective plane. If $n \equiv 1 \pmod{4}$ or $n \equiv 2 \pmod{4}$, then $n$ is the sum of two squares of integers. Equivalently, no finite projective plane can exist whose order $n$ is congruent to $1$ or $2$ modulo $4$ unless $n$ is a sum of two squares.
