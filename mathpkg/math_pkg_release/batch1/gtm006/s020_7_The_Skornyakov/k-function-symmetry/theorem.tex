If $\alpha$ is a permutation of $\{w, x, y, z\}$, then
$$k(w, x, y, z) = (\operatorname{sgn} \alpha) \, k(w^\alpha, x^\alpha, y^\alpha, z^\alpha).$$
In particular, $k(w, x, y, z) = 0$ whenever any two arguments are equal.
