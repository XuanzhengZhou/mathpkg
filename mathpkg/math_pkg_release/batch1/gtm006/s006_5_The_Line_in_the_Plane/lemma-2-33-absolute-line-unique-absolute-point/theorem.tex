If a line $P + Q$ joining two distinct absolute points $P$ and $Q$ of a polarity $\beta$ is itself absolute, then the line contains exactly one absolute point. More generally, any absolute line of a polarity contains exactly one absolute point.
