Let $(R, T)$ be a planar ternary ring. For any $a, b \in R$, define the binary operations of addition and multiplication by:
$$
a \cdot b = ab = T(a, b, 0), \qquad a + b = T(1, a, b).
$$
Since $T$ is a ternary operation, both $ab$ and $a + b$ are uniquely determined by $a$ and $b$.
