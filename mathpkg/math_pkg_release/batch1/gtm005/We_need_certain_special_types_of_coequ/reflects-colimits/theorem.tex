A functor $G : A \to X$ \emph{reflects colimits} of $T : J \to A$ when every cone $\lambda : T \mathbin{\Rightarrow} a$ from $T$ to $a \in A$ for which $G \lambda : G T \mathbin{\Rightarrow} G a$ is a colimiting cone in $X$ is already a colimiting cone in $A$.
