The coend $\int^c T(c, c)$ is the colimit (specifically, the coequalizer) of the diagram
\[
\coprod_{f: c \rightarrow c'} T(c', c) \rightrightarrows \coprod_{c} T(c, c) \rightarrow \int^c T(c, c),
\]
where one map uses $T(f, 1_c)$ and the other uses $T(1_{c'}, f)$.