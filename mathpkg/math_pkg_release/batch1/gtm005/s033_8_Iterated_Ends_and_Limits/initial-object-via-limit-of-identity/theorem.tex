If $\lambda: d \to \mathrm{Id}_C$ is a cone over the identity functor $\mathrm{Id}_C: C \to C$, and $F: J \to C$ is a functor such that the composite cone $\lambda F: d \to F$ is a limiting cone for $F$, then $d$ is an initial object in $C$.
