A category object in the category $\mathbf{Set}$ of sets is exactly an ordinary small category. That is, the definition of an internal category in $\mathbf{Set}$ reduces precisely to the usual definition of a category whose objects form a set and whose arrows form a set.
