For each commutative ring $K$, the set $\operatorname{Matr}_K$ of all rectangular matrices with entries in $K$ is a category; the objects are all positive integers $m, n, \ldots$, and each $m \times n$ matrix $A$ is regarded as an arrow $A : n \to m$, with composition the usual matrix product.
