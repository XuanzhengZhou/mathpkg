For a pair of spaces $(X, A)$ with basepoint, the boundary homomorphism $\partial: \pi_2(X, A) \rightarrow \pi_1(A)$ together with the natural action of $\pi_1(A)$ on $\pi_2(X, A)$ forms a crossed module. This is the fundamental crossed module of the pair $(X, A)$.