If $H: C \to \mathbf{Set}$ is a functor, a \emph{universal element} of $H$ is a pair $\langle r, e \rangle$ consisting of an object $r \in C$ and an element $e \in Hr$ such that for every pair $\langle c, x \rangle$ with $x \in Hc$, there is a unique arrow $f: r \to c$ with $(Hf)e = x$.
