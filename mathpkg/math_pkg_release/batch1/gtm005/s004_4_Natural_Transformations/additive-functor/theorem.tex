If $A$ and $B$ are Ab-categories, a functor $T: A \rightarrow B$ is \textbf{additive} when every function $T: A(a, a') \rightarrow B(Ta, Ta')$ is a homomorphism of abelian groups; that is, $T(f + f') = Tf + Tf'$ for all parallel pairs $f, f': a \rightarrow a'$.