An arrow $f: a \rightarrow b$ in a category $C$ is \textbf{monic} (or is a \textbf{monomorphism}) if for any two parallel arrows $g, h: c \rightarrow a$, the equality $f \circ g = f \circ h$ implies $g = h$. In other words, $f$ is left-cancellable.