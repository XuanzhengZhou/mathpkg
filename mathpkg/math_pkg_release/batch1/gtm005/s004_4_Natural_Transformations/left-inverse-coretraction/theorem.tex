An arrow $g: b \rightarrow a$ is a \textbf{left inverse} (or \textbf{coretraction}) for $f: a \rightarrow b$ if $g \circ f = 1_a$. Then $f$ is called a \textbf{section} for $g$. Any arrow with a left inverse is necessarily monic.