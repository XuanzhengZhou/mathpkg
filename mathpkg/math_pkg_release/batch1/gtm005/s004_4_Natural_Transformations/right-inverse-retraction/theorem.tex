An arrow $h: b \rightarrow c$ is a \textbf{right inverse} (or \textbf{retraction}) for $f: c \rightarrow b$ if $f \circ h = 1_b$. Then $f$ is called a \textbf{retraction}. Any arrow with a right inverse is necessarily epi.