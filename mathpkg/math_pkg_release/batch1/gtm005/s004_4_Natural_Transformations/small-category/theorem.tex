Fix a universe $U$. A category $C$ is \textbf{small} if both the set of objects $\operatorname{Ob}(C)$ and the set of arrows $\operatorname{Arr}(C)$ are small sets (i.e., belong to $U$). A category is \textbf{locally small} if each hom-set $C(a,b)$ is a small set.