An object $t$ is \textbf{terminal} in $C$ if to each object $a$ in $C$ there is exactly one arrow $a \rightarrow t$. If $t$ is terminal, the only arrow $t \rightarrow t$ is the identity, and any two terminal objects of $C$ are isomorphic in $C$.