\begin{theorem}
Each composite of the canonical maps acting on an $n$-fold product in a braided monoidal category $M$ induces a braiding (an element in the braid group $B_n$). Two such composites are equal for all $M$ if and only if they give the same element in $B_n$.
\end{theorem}
