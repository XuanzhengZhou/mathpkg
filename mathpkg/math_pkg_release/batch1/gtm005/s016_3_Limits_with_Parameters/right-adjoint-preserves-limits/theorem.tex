Let $G : A \rightarrow X$ be a right adjoint functor (with left adjoint $F : X \rightarrow A$). Then $G$ preserves all limits which exist in its domain $A$. In particular, every right adjoint is continuous.

Dually, every left adjoint preserves all colimits which exist in its domain.
