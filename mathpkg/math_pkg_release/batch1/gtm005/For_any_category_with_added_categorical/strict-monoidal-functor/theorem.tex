A monoidal functor $(F, F_2, F_0) : M \rightarrow M'$ is said to be \text{strict} when $F_0$ and all the $F_2(a, b)$ are identities (recall that a monoidal category is strict when $\alpha$, $\lambda$, and $\rho$ are identities).
