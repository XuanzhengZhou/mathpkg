\text{For any functor $G: A \to X$ and object $x \in X$, the projection functor $P: (x \downarrow G) \to A$ preserves all kernel pairs.}

\text{In particular, $P$ carries monics in $(x \downarrow G)$ to monics in $A$.}
