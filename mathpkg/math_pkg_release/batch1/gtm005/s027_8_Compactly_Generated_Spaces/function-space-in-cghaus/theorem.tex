For $X, Y \in \mathbf{CGHaus}$, the \emph{function space} $Y^X$ is defined as $k(C(X, Y))$, where $C(X, Y)$ has the compact-open topology. The evaluation map $e: Y^X \times X \to Y$ is continuous, giving $\mathbf{CGHaus}$ the structure of a cartesian closed category.
