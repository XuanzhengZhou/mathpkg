A category $J$ is called \emph{connected} when to any two objects $j, k \in J$ there is a finite sequence of arrows

\[
j = j_0 \to j_1 \leftarrow j_2 \to \cdots \to j_{2n-1} \leftarrow j_{2n} = k
\]

(both directions possible) joining $j$ to $k$.
