A diagram consisting of vertices (labelled by objects of a category) and edges (labelled by arrows of the same category) is \emph{commutative} when, for each pair of vertices $c$ and $c'$, any two paths formed from directed edges leading from $c$ to $c'$ yield, by composition of labels, equal arrows from $c$ to $c'$.
