For an arrow $k: b \to b'$ in a category $C$, the function

$$
\hom(a, k): \hom(a, b) \to \hom(a, b')
$$

defined by $f \mapsto k \circ f$ for each $f: a \to b$ is sometimes written $k_*$ and called \text{composition with $k$ on the left}, or \text{the map induced by $k$}.
