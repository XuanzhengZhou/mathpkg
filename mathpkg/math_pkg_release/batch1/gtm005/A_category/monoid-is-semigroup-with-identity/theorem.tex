A monoid is exactly a semigroup with an identity element. Equivalently, a monoid may be described as a set $M$ equipped with an associative binary operation $M \times M \to M$ that has a two-sided identity element ($=$ unit).
