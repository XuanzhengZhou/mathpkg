A function is a binary relation in which each element is uniquely determined by its first term. Thus, functions that are maps of sets in $V$ are identified with their graphs. If $f$ is a function, we often write $W = f(U)$ instead of $\langle U, W \rangle \in f$.
