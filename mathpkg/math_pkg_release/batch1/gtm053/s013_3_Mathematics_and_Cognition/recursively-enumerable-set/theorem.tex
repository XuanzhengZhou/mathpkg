A set $E \subset (\mathbb{Z}^+)^n$ is called recursively enumerable if there exists a partial recursive function $f$ from $(\mathbb{Z}^+)^n$ to $\mathbb{Z}^+$ such that $E = D(f)$, i.e., $E$ is the domain of definition of $f$.