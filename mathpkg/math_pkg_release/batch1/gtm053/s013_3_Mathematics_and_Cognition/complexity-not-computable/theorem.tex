The function $C_u(f)$ (Kolmogorov complexity relative to an optimal family $u$) is not computable. More precisely, there is no algorithm that, given an index for a partial recursive function $f$, computes $C_u(f)$.
