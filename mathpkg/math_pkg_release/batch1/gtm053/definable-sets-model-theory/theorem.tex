For an $L$-structure $A$ and an $L$-formula $P(x_1, \ldots, x_n)$, the set
$$P(A) = \{ ar{a} \in A^n : A \models P(ar{a}) \}$$
is called 	ext{definable}. If $P(A)$ coincides with the graph of an operation $f : A^{n-1} 	o A$, then $f$ is called an $L$-definable operation.
