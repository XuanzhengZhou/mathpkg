Let $L$ be a language, let $M \subset N$ be sets, and let $\phi$ (resp. $\psi$) be an interpretation of $L$ in $M$ (resp. $N$). A formula $P$ in $L$ is called $(M,N)$-absolute if for all $\xi \in \overline{M}$ we have
$$|P|_M(\xi) = |P|_N(\xi).$$
