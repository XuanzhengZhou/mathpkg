Let $P(x)$ be a formula in SAr with one free variable $x$.

\textbf{(a)} An expression $Q$ satisfies $P$ if the number of $Q$ lies in the set $\{k \mid P(\bar{k}) \text{ is true}\}$.

\textbf{(b)} An expression $Q$ is displayed in $P$ if the display of $Q$ satisfies $P$.
