Consider the graph $\Gamma_2$ that is obtained from Figure 1 by identifying the vertices $a = p_0$, $b = q_0$, and $c = r_0$ (the apparent intersections of the edges inside the circle are not vertices). This graph is realizable on $S^2$ (the unit sphere in $\mathbb{R}^3$).
