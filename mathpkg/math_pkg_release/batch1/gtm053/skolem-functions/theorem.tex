For an $L$-structure $A$, for each $L$-formula $P(v_1,\ldots,v_n)$, define a 	ext{Skolem function} $g_P : A^{n-1} 	o A$ by:
$$g_P(a_1,\ldots,a_{n-1}) = egin{cases}
	ext{an element } a \in A : A \models P(a_1,\ldots,a_{n-1},a) & 	ext{if such exists,} \
a_0 & 	ext{if not}
nd{cases}$$
where $a_0 \in A$ is a fixed element. The closure of a subset under all Skolem functions yields an elementary submodel.
