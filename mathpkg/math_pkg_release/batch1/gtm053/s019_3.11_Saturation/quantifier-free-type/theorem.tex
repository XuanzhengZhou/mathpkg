The quantifier-free type of a tuple $\bar{a}$ in a structure $\mathbf{A}$ is defined as

$$\text{qftp}_{\mathbf{A}}(\bar{a}) := \{Q(\bar{x}) : Q \text{ is quantifier-free, } \mathbf{A} \vDash Q(\bar{a})\}.$$
