Recall that a set $X$ is \textit{transitive} if $Z \in X$ whenever $Z \in Y \in X$ for some $Y$.

An \textit{ordinal} is a transitive set $X$ of sets that is well-ordered by the relation $\in$ between its elements.
