Given an $L$-structure $A$ and an $L$-theory $T$,

(i) the set $T \cup \text{Diag}(A)$ is consistent if and only if $A$ can be embedded in a model of $T$.

(ii) the set $T \cup \text{CDiag}(A)$ is consistent if and only if $A$ can be elementarily embedded in a model of $T$.
