Let $\{A_\alpha\}_{\alpha < \kappa}$ be an elementary chain of $L$-structures. Then for every $\alpha < \delta \leq \kappa$, we have $A_\alpha \preceq A_\delta$. In particular, the union of an elementary chain is an elementary extension of each structure in the chain.
