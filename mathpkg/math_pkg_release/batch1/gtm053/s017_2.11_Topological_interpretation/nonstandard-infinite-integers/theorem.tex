In any nonstandard arithmetic, if $c$ is a nonstandard element, then $c \geq \bar{n}$ for every standard $n \in \mathbb{N}$. That is, nonstandard elements of arithmetic are ``infinite integers.'' (Here $x_1 \leq x_2$ is definable by $\exists y \; x_1 + y = x_2$.)
