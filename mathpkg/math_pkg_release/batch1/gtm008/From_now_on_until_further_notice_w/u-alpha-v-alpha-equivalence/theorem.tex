\begin{enumerate}
\item $U_{\alpha}^{(B)} \subseteq V_{\alpha}^{(B)}$, in particular,
$$(\forall u \in U_{\alpha}^{(B)}) (\exists v \in V_{\alpha}^{(B)}) [\llbracket u = v \rrbracket = 1].$$
\item $(\forall v \in V_{\alpha}^{(B)}) (\exists u \in U_{\alpha}^{(B)}) [\llbracket u = v \rrbracket = 1].$
\item $U_{\alpha+1}^{(B)}$ is complete.
\end{enumerate}
