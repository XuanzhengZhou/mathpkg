$$(\forall v \in V^{(B)}) (\forall d \subseteq V^{(B)}) [\mathcal{D}(v) \subseteq d \rightarrow (\exists u) [u \text{ is extensional } \land d = \mathcal{D}(u) \land \llbracket u = v \rrbracket = 1]].$$

In particular, each $v \in V^{(B)}$ can be represented by an extensional set.
