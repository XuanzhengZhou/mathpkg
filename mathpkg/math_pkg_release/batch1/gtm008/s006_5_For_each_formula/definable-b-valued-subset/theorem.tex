If $a \in M_{\alpha+1}$ and
$$\llbracket (\forall x)[x \in b \leftrightarrow x \in a \land \varphi(x, a_1, \ldots, a_n)] \rrbracket = 1,$$
then $b$ is defined over $M_{\alpha}$. That is, every definable $B$-valued subset of $a \in M_{\alpha+1}$ is defined over $M_{\alpha}$.
