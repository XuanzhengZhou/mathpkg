If $\varphi$ is a formula of $\mathcal{L}$, then

$$
(\forall a_1, \ldots, a_n \in M_{\alpha}) \llbracket \varphi(a_1, \ldots, a_n) \rrbracket_{M_{\alpha}} = \llbracket \varphi(a_1, \ldots, a_n) \rrbracket
$$

whenever $\varphi$ contains no quantifiers (i.e., $\varphi$ is a limited formula).
