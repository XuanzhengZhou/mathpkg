If $\langle M, P \rangle$ is a setting for forcing, then

$$
p \Vdash \varphi \leftrightarrow (\forall G')\bigl[G' \text{ is } P\text{-generic over } M \land p \in G' \to M[G'] \models \varphi\bigr].
$$
