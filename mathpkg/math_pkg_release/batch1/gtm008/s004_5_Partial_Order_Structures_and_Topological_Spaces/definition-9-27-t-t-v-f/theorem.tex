s of §8 show that in 1-8 Ord $(\varphi)$ is always reduced to lower values so that the definition is recursive.

We must now prove that the sequence $\langle \mathbf{T}_{\alpha} \mid \alpha \in \text{On} \rangle$ has the desired properties.
