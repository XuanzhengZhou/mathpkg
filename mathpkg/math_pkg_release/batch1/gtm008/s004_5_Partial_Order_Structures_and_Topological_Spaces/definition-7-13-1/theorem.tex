1. $(\forall \alpha)[a \leq F(\alpha)]$.
2. $(\forall \alpha, \beta)[\alpha < \beta \rightarrow F(\alpha) \leq F(\beta)]$.
3. $(\forall \alpha \in K_{\mathrm{II}})\left[ F(\alpha) = \bigcup_{\beta < \alpha} F(\beta) \right]$.
