---
role: proof
locale: en
of_concept: theorem-9-22-is-defined-over
source_book: gtm008
source_chapter: "5"
source_section: "5 For each formula"
---
$B^{M_{\alpha}}$ is a set. For $s \in B^{M_{\alpha}}$ we define

$f(s) \triangleq \mu_{\beta}((\exists a \in M_{\beta})[a$ is defined over $M_{\alpha} \wedge s = \{ \langle x, [[x \in a]] \rangle \mid x \in M_{\alpha} \}])$

$= 0$ if there is no such $\beta$.

Then

$(\exists \beta)[\beta \triangleq \sup_{s \in B^{M_{\alpha}}} f(s)]$.

If $a$ is defined over $M_{\alpha}$ and

$s = \{ \langle x, [[x \in a]] \rangle \mid x \in M_{\alpha} \}$

then $s \in B^{M_{\alpha}}$ and hence

$(\exists b \in M_{f(s)})[s = \{ \langle x, [[x \in b]] \rangle \mid x \in M_{\alpha} \}]$

where $b$ is defined over $M_{\alpha}$. Then $[[a = b]] = 1$ and $b \in M_{\beta}$.
