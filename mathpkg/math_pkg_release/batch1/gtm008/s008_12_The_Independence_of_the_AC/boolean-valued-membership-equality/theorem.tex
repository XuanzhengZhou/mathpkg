For $u, v \in V^{(B)}$,

1. $$[\![u \bar{\in} v]\!] \triangleq \sum_{y \in \mathcal{D}(v)} (v(y) \cdot [\![u = y]\!]).$$

2. $$[\![u = v]\!] \triangleq \prod_{x \in \mathcal{D}(u)} \left( u(x) \Rightarrow \sum_{y \in \mathcal{D}(v)} [v(y) \cdot [\![x = y]\!]] \right) \cdot \prod_{y \in \mathcal{D}(v)} \left( v(y) \Rightarrow \sum_{x \in \mathcal{D}(u)} [u(x) \cdot [\![x = y]\!]] \right).$$

These definitions are justified by a well-founded rank ordering, using the natural sum $\alpha \# \beta$ or Godel's pairing function $J_0$ to assign ordinals to atomic formulas.
