For $x, y \in V$,

1. $x \in y \leftrightarrow [\![\check{x} \in \check{y}]\!] = 1$ and $x \notin y \leftrightarrow [\![\check{x} \in \check{y}]\!] = 0$.

2. $x = y \leftrightarrow [\![\check{x} = \check{y}]\!] = 1$ and $x \neq y \leftrightarrow [\![\check{x} = \check{y}]\!] = 0$.

3. $(\forall u \in V^{(2)}) (\exists! v \in V) [\![u = \check{v}]\!] = 1$.
