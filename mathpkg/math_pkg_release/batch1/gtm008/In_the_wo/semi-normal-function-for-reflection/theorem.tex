For each formula $\varphi(a_0, \ldots, a_n)$ of $\mathcal{L}$ there exists a semi-normal function $F$ such that
$$(\forall \alpha)(\forall \beta)\bigl[\beta = F(\alpha) \to (\forall a_1, \ldots, a_n \in M_\alpha)$$
$$[\mathbf{M} \models (\exists x) \varphi(x, a_1, \ldots, a_n) \leftrightarrow (\exists a \in M_\beta)[\mathbf{M} \models \varphi(a, a_1, \ldots, a_n)]\bigr].$$
