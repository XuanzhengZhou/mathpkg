$$(\forall S \subseteq P)[\bar{\bar{S}} \leq \omega \land \text{Comp}(S) \rightarrow (\exists p \in P)(\forall q \in S)[p \leq q]]$$

(i.e., every countable compatible subset of $\mathbf{P}$ has a lower bound) holds in $M$, then every $\omega$-sequence of ordinals in $M[G]$ is already in $M$.
