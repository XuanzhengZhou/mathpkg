The operation $\operatorname{Def}(x)$ (the set of all subsets of $x$ definable with parameters from $x$) is absolute for standard transitive models of ZF. That is, if $M$ is a standard transitive model of ZF and $x \in M$, then $\operatorname{Def}(x)^M = \operatorname{Def}(x)$.
