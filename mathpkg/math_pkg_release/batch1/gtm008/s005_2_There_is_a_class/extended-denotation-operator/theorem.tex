$D$ can be extended to an operator $\tilde{D}$ defined for all closed unlimited formulas of $R(K, F)$ by adding
$$\tilde{D}((\forall x)\varphi(x)) \xrightarrow{\Delta} (\forall t \in T) \tilde{D}(\varphi(t)).$$
