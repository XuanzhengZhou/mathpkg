For each formula $\varphi$ there exists a $v \in V^{(\mathbf{B})}$ such that

$$\llbracket (\exists u) \varphi(u) \rrbracket = \llbracket \varphi(v) \rrbracket.$$

Equivalently, $(\exists v \in V^{(\mathbf{B})}) [\llbracket (\exists u) \varphi(u) \rrbracket = \llbracket \varphi(v) \rrbracket]$.
