1. $U_{\alpha}^{(\mathbf{B})} \subseteq V_{\alpha}^{(\mathbf{B})}$, in particular,
$$(\forall u \in U_{\alpha}^{(\mathbf{B})}) (\exists v \in V_{\alpha}^{(\mathbf{B})}) [\llbracket u = v \rrbracket = 1].$$

2. $(\forall v \in V_{\alpha}^{(\mathbf{B})}) (\exists u \in U_{\alpha}^{(\mathbf{B})}) [\llbracket u = v \rrbracket = 1].$

3. $U_{\alpha+1}^{(\mathbf{B})}$ is complete, i.e., for any subset $S \subseteq U_{\alpha+1}^{(\mathbf{B})}$ and any partition of unity $\{b_s \mid s \in S\}$, the Boolean sum $\sum_{s \in S} b_s \cdot s$ belongs to $U_{\alpha+1}^{(\mathbf{B})}$.
