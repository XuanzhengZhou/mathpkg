1. $V_{0}^{(\tilde{B})} \triangleq 0$.
2. $V_{\alpha}^{(\tilde{B})} \triangleq \{u \mid \mathcal{D}(u) \subseteq V_{\alpha}^{(\tilde{B})} \land u: \mathcal{D}(u) \rightarrow \tilde{B}\}$.
3. $V^{(\tilde{B})} \triangleq \bigcup_{\alpha \in On} V_{\alpha}^{(\tilde{B})}$.
