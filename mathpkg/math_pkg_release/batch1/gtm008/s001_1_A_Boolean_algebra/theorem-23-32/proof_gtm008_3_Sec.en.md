---
role: proof
locale: en
of_concept: theorem-23-32
source_book: gtm008
source_chapter: "3"
source_section: "3 A consequence of"
---
If $\mathbf{B}$ satisfies the c.c.c. and $cf(\aleph_\beta) > \aleph_0$, then

$$b = \sum_{j < \aleph_\beta} b_{ij} = \sum_{j < \gamma} b_{ij} \text{ for some } \gamma

Since such $\bar{r}$'s are dense beneath $b$ and $h_0(b) = 1$, we can find an $\bar{r} \leq b$ such that $h_0(\bar{r}) = 1$ and $\bar{r}$ has the above properties. In particular,

$$(\forall r' \in B)[0 < r' \leq \bar{r} \rightarrow (\exists p \in \Lambda)[p \cdot r' > 0]].$$

Choose $B' \subseteq \{r' \mid r' \in B \land 0 < r' \leq \bar{r}\}$ such that

$$(\forall r', r'' \in B')[(\exists p \in \Lambda)[p \cdot r' > 0 \land p \cdot r'' > 0) \rightarrow r' = r'']$$

and

$$(\forall r' \in B)[0 < r' \leq \bar{r} \rightarrow (\exists r'' \in B')(\exists p \in \Lambda)[p \cdot r' > 0 \land p \cdot r'' > 0]],$$

i.e., we identify those $r' \cdot r'' \in B$ for which $0 < r' \leq \bar{r}, 0 < r'' \leq \bar{r}$, and $pr' > 0 \land pr'' > 0$ for the same $p \in \Lambda$. Since $\bar{\Lambda} \leq \aleph_\alpha, \bar{B}' \leq \aleph_\alpha$. Since

$$(\forall \xi < \aleph_\alpha)(\forall r' \in B')(\exists \eta < \aleph_\beta)(\exists p \in \Lambda)[p \cdot r' > 0 \land p \cdot \bar{r} \leq \sum_{\eta' < \eta} b_{\xi \eta'}]$$

and $cf(\aleph_\beta) > \aleph_\alpha$, we can obtain a single $\eta > \aleph_\beta$ such that

$$(\forall \xi < \aleph_\alpha)(\forall r' \in B')(\exists p \in \Lambda)[p \cdot r' > 0 \
