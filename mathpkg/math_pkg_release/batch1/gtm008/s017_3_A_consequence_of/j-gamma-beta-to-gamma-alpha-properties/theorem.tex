The map $j: \Gamma_\beta \rightarrow \Gamma_\alpha$ has the following elementary properties:
\begin{enumerate}
\item $q \leq p \rightarrow j(q) \leq j(p)$.
\item $j''[p]_{P_\beta} = [j(p)]_{P_\alpha}$.
\end{enumerate}
