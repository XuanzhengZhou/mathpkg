$$[[V = M[F]]] = 1 \quad \text{in } \mathbf{V}^{(B)}.$$
The Boolean-valued universe $\mathbf{V}^{(B)}$ satisfies the statement that $V$ equals the generic extension $M[F]$ of the ground model $M$ by the generic filter $F$, with Boolean truth value 1.
