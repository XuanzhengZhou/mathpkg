---
role: proof
locale: en
of_concept: theorem-8-5
source_book: gtm008
source_chapter: "2"
source_section: "2 There is a class"
---
If $t_1 = \hat{x}^\alpha \psi(x^\alpha)$ is an abstraction term such that $t$ occurs in $t_1$ and $t_1$ occurs in $\varphi$, then $t$ occurs in $\psi(x^\alpha)$. Hence by Definition 8.4.

$$g(t_1) = 2\alpha + 2 \leq \text{Ord}^1 (\varphi).$$

If $t = k$ for some $k \in K$ then rank $(k) = \rho(k) < \alpha$.

If $t = \hat{y}^\beta \psi(y^\beta)$ with $\beta < \alpha$ then $g(t) < 2\alpha + 1 < \text{Ord}^1 (\varphi)$.

So if $t$ occurs in an abstraction term of $\varphi$ then Ord¹($\varphi$) > $g(t)$.

Remark. Thus any term of maximal grade occurring in $\varphi$ cannot occur within another term of $\varphi$. We shall use this result frequently in proofs to follow.
