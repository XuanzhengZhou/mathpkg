$$A_0 = T \cup \{X - a \mid a \in T\}$$

$$A_{\alpha+1} = \left\{a \mid (\exists f \in (A_\alpha)^\omega) \left[ \left[ a = \bigcup_{i < \omega} f(i) \right] \vee \left[ a = \bigcap_{i < \omega} f(i) \right] \right] \right\}$$

$$A_\alpha = \bigcup_{\beta < \alpha} A_\beta, \quad \alpha \in K_{\Pi},$$

then $A = \bigcup_{\alpha \in \aleph_1} A_\alpha$ is the set of all Borel sets in $X$.
