Thus there is a $B$ in $T$ such that no proper extension of $B$ has the finite intersection property.

Since $B$ has the finite intersection property $\{p_a(C) \mid C \in B\}$ has the finite intersection property for all $a \in A$. Therefore if

$$C_a = \{p_a(C)^- \mid C \in B\}$$

then $C_a$ is a collection of closed subsets of $X_a$ and $C_a$ has the finite intersection property. Since $\langle X_a, T_a \rangle$ is compact it follows from Theorem 3.17 that $\bigcap (C_a) \neq 0$ i.e.,

$$\exists b_a \in \bigcap (C_a).$$

If $b = \prod_{a \in A} \{b_a\}$ and if $N(b)$ is any neighborhood of $b$ in the product topology then

$$b_a \in p_a(N(b)) \in T_a.$$

Since

$$(\forall C \in B)[b_a \in p_a(C)^-]$$

$$(\forall C \in B)[p_a(N(b)) \cap p_a(C) \neq 0].$$

Consequently

$$(\forall C \in B)[N(b) \cap C \neq 0].$$

In particular since $S \subseteq B$

$$(\forall A \in S)[N(b
