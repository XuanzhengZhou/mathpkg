A sequence $x_1, x_2, \ldots$ of points of a metric space $(X, \varrho)$ is said to \text{converge} to the point $x$ if $\varrho(x_n, x) \to 0$ as $n \to \infty$. We then write $x_n \to x$. A sequence is \text{convergent} if it converges to some point of $X$.
