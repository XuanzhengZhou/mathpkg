A real-valued function $f$ on $\mathbb{R}$ is called \textbf{measurable} if $f^{-1}(U)$ is measurable for every open set $U$ in $\mathbb{R}$. In this definition, $U$ may be restricted to some base, or allowed to run over all Borel sets.
