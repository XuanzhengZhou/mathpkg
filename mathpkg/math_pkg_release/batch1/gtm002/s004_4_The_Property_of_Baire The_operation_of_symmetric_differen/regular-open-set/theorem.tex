An open set $G$ is called \textbf{regular open} if
$$G = G^{-'-'},$$
where $H^{-'}$ denotes the interior of the complement of $H$, i.e.,
$$G^{-'} = \text{int}(G') = (\bar{G})'.$$
Equivalently, $G$ is regular open if $G = \text{int}(\bar{G})$, the interior of its own closure.
