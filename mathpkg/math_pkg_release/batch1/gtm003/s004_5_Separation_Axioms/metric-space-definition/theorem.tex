By the \textbf{metric space} $(X, d)$ we understand the uniform space $X$ endowed with the metric $d$, whose uniformity is defined by the filter base $\{W_n : n \in \mathbb{N}\}$ where $W_n = \{(x, y) : d(x, y) < n^{-1}\}$. This defines a separated uniformity on $X$.
