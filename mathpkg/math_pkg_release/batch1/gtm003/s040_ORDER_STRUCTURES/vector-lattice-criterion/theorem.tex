Let $E$ be an ordered vector space over $\mathbb{R}$ whose positive cone $C$ is generating. If for each pair $(x, y) \in C \times C$ either $\sup(x, y)$ or $\inf(x, y)$ exists, then $E$ is a vector lattice.
