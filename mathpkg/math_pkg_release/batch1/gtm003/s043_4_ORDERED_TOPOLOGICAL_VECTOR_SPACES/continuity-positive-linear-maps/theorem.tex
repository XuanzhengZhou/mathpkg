Let $E, F$ be ordered l.c.s. with respective positive cones $C, D$. Suppose that $E$ is a Mackey space on which every positive linear form is continuous, and assume that $D$ is a weakly normal cone in $F$. Then every positive linear map of $E$ into $F$ is continuous.
