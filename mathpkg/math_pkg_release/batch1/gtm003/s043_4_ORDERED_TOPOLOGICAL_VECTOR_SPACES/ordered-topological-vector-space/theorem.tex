Let $L$ be a t.v.s. (over $\mathbb{R}$ or $\mathbb{C}$) and an ordered vector space; we say that $L$ is an \emph{ordered topological vector space} if the following axiom is satisfied:

\begin{itemize}
  \item[(LTO)] The positive cone $C = \{x : x \geq 0\}$ is closed in $L$.
\end{itemize}
