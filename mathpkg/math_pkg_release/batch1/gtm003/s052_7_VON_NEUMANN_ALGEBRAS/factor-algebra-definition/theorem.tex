A $W^*$-algebra $W$ is called a \text{factor} (or factor algebra) if its center $Z(W)$ is one-dimensional, i.e., if $Z(W) = \mathbb{C} e$. Every von Neumann algebra on a separable Hilbert space is a direct integral of factors.
