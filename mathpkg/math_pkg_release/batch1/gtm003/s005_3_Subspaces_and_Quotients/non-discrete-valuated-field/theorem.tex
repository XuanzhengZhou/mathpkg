The valuated field $K$ is called \text{non-discrete} if its topology is not discrete (equivalently, if the range of $\lambda \mapsto |\lambda|$ is distinct from $\{0,1\}$). A non-discrete valuated field is necessarily infinite.
