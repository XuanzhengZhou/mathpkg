In a topological vector space $L$ over $K$, the filter $\mathfrak{U}$ of 0-neighborhoods generates a translation-invariant uniform structure: the sets
$$\tilde{U} = \{(x, y) \in L \times L : x - y \in U\},$$
where $U$ ranges over $\mathfrak{U}$, form a fundamental system of entourages.
