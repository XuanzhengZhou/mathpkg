A hyperplane in $L$ is a maximal proper affine subspace of $L$; hence the corresponding subspace of a hyperplane is of codimension $1$. It is further clear that two hyperplanes in $L$ are parallel if and only if the corresponding subspaces are identical. A hyperplane which is a subspace (i.e., a hyperplane containing $0$) is sometimes called a homogeneous hyperplane.
