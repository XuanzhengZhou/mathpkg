Let $E, F$ be ordered l.c.s. with positive cones $C, D$. Suppose $E$ is a Mackey space on which every positive linear form is continuous, and $D$ is a weakly normal cone in $F$. Then every positive linear map of $E$ into $F$ is continuous.
