Let $p, q$ be continuous semi-norms on $E, F$ respectively, and let $U = \{x: p(x) < 1\}$, $V = \{y: q(y) < 1\}$. Then the gauge $r$ of $\Gamma(U \otimes V)$ is given by $$r(u) = \inf \left\{ \sum_i p(x_i)q(y_i) : u = \sum_i x_i \otimes y_i \right\}.$$ Moreover, $r(x \otimes y) = p(x)q(y)$.
