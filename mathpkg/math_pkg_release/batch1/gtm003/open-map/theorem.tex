A map $f: X \to Y$ between topological spaces is open if $f(G)$ is open in $Y$ for every open $G \subset X$. The natural (quotient) map $\phi: L \to L/M$ is an open map when $L$ is a t.v.s. and $M$ is a subspace.
