A subset $A$ of a vector space $L$ over $K$ is called radial (or absorbing) if for every $x \in L$ there exists $\varepsilon > 0$ such that $\lambda x \in A$ whenever $|\lambda| \leq \varepsilon$. Equivalently, $\bigcup_{n \in \mathbb{N}} nA = L$ when $K = \mathbb{R}$ or $\mathbb{C}$.
