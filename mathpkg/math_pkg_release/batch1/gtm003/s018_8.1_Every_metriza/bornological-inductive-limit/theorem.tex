Let $\mathfrak{T}$ be the inductive topology on $E$ with respect to a family of bornological spaces (and corresponding linear maps); each convex, circled subset of $E$ absorbing all bounded sets in $E(\mathfrak{T})$ is a $0$-neighborhood for $\mathfrak{T}$.
