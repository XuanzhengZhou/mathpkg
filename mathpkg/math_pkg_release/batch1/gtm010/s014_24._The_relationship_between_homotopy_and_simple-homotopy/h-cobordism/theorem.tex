An $h$-cobordism is a compact $p.l.$ $(n+1)$-manifold $W$ with boundary $\partial W = M_0 \cup M_1$ ($M_0 \cap M_1 = \emptyset$) such that $M_0 \hookrightarrow W$ and $M_1 \hookrightarrow W$ are strong deformation retractions.
