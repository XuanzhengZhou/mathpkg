If $M < L < K$ are CW complexes with $K \supset L$ and $L \supset M$, and if each component of $K-L$ is connected, then
$$\tau(K, M) = \tau(K, L) + \tau(L, M)$$
where the sum is taken in $Wh(\pi_1 M)$ using the inclusion-induced maps.
