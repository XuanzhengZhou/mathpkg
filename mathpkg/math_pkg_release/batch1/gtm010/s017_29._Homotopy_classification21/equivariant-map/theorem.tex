If $f: \Sigma^{2n-1} \to \Sigma^{2n-1}$ is a map and $h \in G'$, then $f$ is $(g, h)$-equivariant if $fg = hf$. Two $(g, h)$-equivariant maps $f_0$ and $f_1$ are equivariantly homotopic if there is a homotopy $\{f_t\}$ such that each $f_t$ is $(g, h)$-equivariant.
