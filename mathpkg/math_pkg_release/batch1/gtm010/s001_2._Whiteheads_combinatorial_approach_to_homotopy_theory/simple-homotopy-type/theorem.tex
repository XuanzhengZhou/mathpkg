$K$ and $L$ have the same simple-homotopy type if there is a finite sequence $K = K_0 \rightarrow K_1 \rightarrow \cdots \rightarrow K_q = L$ where each arrow represents a simplicial expansion or a simplicial collapse.
