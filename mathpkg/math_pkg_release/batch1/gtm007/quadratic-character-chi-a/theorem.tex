\textbf{Proposition 5.} Let $a$ be an integer not a square. There exists a character $\chi_a$ modulo $m = 4|a|$ such that for all primes $p$ not dividing $m$, $\chi_a(p) = \left(\frac{a}{p}\right)$. Moreover, $\chi_a \neq 1$ if $a \neq 1$.
