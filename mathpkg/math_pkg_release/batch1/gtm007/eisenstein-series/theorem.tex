For an integer $k > 1$, the Eisenstein series of index $k$ is
$$G_k(z) = \sum_{m,n \in \mathbb{Z}}' \frac{1}{(mz + n)^{2k}},$$
where $\sum'$ denotes summation over all $(m,n) \neq (0,0)$. $G_k$ is a modular form of weight $2k$ with $G_k(\infty) = 2\zeta(2k)$.
