A form $f(X_1, X_2)$ in two variables is called \textbf{hyperbolic} if we have

$$f \sim X_1 X_2 \sim X_1^2 - X_2^2.$$

(This means that the module $(k^2, f)$ corresponding to $f$ is a hyperbolic plane, cf. Definition 4.)
