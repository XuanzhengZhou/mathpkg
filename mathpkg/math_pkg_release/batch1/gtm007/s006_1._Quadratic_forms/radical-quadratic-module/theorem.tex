The orthogonal complement $V^0$ of $V$ itself is called the \textbf{radical} (or the \textbf{kernel}) of $V$ and denoted by $\operatorname{rad}(V)$. Its codimension is called the \textbf{rank} of $Q$.
