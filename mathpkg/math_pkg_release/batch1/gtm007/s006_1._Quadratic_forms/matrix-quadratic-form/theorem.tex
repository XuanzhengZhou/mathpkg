Let $(e_i)_{1 \le i \le n}$ be a basis of $V$. The \textbf{matrix} of $Q$ with respect to this basis is the matrix $A = (a_{ij})$ where $a_{ij} = e_i \cdot e_j$. For $x = \sum x_i e_i$, one has

$$Q(x) = \sum_{i,j} a_{ij} x_i x_j.$$

The matrix $A$ is symmetric.
