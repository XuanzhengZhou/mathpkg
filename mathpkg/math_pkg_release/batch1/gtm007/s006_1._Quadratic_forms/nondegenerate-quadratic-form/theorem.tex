If $V^0 = 0$, we say that $Q$ is \textbf{nondegenerate}; this is equivalent to saying that the discriminant of $Q$ is $\neq 0$ (in which case we view it as an element of the group $k^*/k^{*2}$).

In particular, $Q$ is nondegenerate if and only if $q_V: V \to V^*$ is an isomorphism.
