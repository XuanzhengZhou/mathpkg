An element $x$ of a quadratic module $(V,Q)$ is called \textbf{isotropic} if $Q(x) = 0$. A subspace $U$ of $V$ is called \textbf{isotropic} if all its elements are isotropic. One has:

$$U \text{ isotropic} \;\Longleftrightarrow\; U \subset U^0 \;\Longleftrightarrow\; Q|_U = 0.$$
