If $n$ is an integer $\geq 1$, then
$$n = \sum_{d \mid n} \phi(d),$$
where $\phi(d)$ denotes the Euler $\phi$-function ($\phi(d)$ is the number of integers $x$ with $1 \leq x \leq d$ that are coprime to $d$).
