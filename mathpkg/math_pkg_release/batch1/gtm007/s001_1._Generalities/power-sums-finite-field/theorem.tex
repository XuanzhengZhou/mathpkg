Let $u$ be an integer $\geq 0$. The sum
$$S(X^u) = \sum_{x \in K} x^u$$
(where $K$ is a finite field with $q$ elements) is equal to $-1$ if $u \geq 1$ and $u$ is divisible by $q-1$; it is equal to $0$ otherwise.

(We agree that $x^u = 1$ if $u = 0$ even if $x = 0$.)
