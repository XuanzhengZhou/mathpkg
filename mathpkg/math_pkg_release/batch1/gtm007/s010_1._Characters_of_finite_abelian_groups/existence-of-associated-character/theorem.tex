Let $a$ be a square-free integer, $a \neq 1$. There exists a character $\chi_a$ modulo $m = 4|a|$ such that for every prime $p$ not dividing $m$:
$$
\chi_a(p) = \left(\frac{a}{p}\right).
$$
Moreover $\chi_a \neq 1$.
