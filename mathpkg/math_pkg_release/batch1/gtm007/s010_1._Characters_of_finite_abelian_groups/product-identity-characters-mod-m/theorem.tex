For a prime $p \nmid m$, let $f(p)$ denote the order of $\bar{p}$ in $G(m)$ and $g(p) = \phi(m)/f(p)$. Then
$$
\prod_\chi (1 - \chi(p)T) = (1 - T^{f(p)})^{g(p)},
$$
where the product runs over all characters $\chi$ of $G(m)$.
