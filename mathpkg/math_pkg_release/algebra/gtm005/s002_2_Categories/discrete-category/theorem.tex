A category is \emph{discrete} when every arrow is an identity. Every set $X$ is the set of objects of a discrete category (just add one identity arrow $x \to x$ for each $x \in X$), and every discrete category is so determined by its set of objects. Thus, discrete categories are sets.
