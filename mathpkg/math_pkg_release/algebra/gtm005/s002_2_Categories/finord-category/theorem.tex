$\mathbf{Finord} = \mathbf{Set}_\omega$ is the category with objects all finite ordinals $n$ and arrows $f : m \to n$ all functions from $m$ to $n$. This is essentially the category of all finite sets, using just one finite set $n$ for each finite cardinal number $n$.
