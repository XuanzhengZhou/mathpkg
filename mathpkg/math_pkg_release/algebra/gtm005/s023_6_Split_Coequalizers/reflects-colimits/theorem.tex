A functor $G: A \to X$ \textbf{reflects colimits} of $T: J \to A$ when every cone $\lambda: T \to a$ from $T$ to $a \in A$ for which $G\lambda: GT \to Ga$ is a colimiting cone in $X$ is already a colimiting cone in $A$.
