A \textbf{category internal to Grp} (or \textbf{$G$-groupoid}) consists of two groups $C_1$ (arrows) and $C_0$ (objects), with domain, codomain, identity, and composition maps that are group homomorphisms satisfying the usual category axioms. Such an internal category is equivalent to a crossed module.