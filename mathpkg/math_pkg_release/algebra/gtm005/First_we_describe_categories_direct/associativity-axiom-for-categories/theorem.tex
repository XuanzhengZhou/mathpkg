For any objects and arrows in the configuration
$$a \xrightarrow{f} b \xrightarrow{g} c \xrightarrow{k} d$$
the following equality holds whenever the composites on either side are defined:
$$k \circ (g \circ f) = (k \circ g) \circ f.$$
