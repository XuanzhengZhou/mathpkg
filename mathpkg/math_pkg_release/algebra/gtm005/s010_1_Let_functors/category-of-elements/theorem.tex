For a functor $K: C \rightarrow \mathbf{Set}$, the \textbf{category of elements} $\int_C K$ (or $C/K$) has as objects all pairs $(c, x)$ with $c \in C$, $x \in Kc$, and as arrows $(c, x) \rightarrow (c', x')$ all arrows $f: c \rightarrow c'$ in $C$ such that $(Kf)x = x'$.