For an object $a$ in a category $C$ and a set $J$, the \emph{power} $a^J$ (if it exists) is the object representing the functor $c \mapsto \hom(J, \hom(c, a))$, i.e., there is a natural isomorphism $\hom(c, a^J) \cong \hom(J, \hom(c, a))$.
