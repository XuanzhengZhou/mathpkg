A category $C$ has all finite products if and only if it has a terminal object and binary products. Any finite product $a_1 \times a_2 \times \cdots \times a_n$ can be constructed by iterating binary products, with the terminal object as the empty product.
