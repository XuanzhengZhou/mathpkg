For any object $r \in D$, the hom-functor $\hom_D(r, -): D \rightarrow \mathbf{Set}$ preserves all limits that exist in $D$. Dually, the contravariant hom-functor $\hom_D(-, r): D^{\text{op}} \rightarrow \mathbf{Set}$ takes colimits in $D$ to limits in $\mathbf{Set}$.