The functor $Y: D^{\text{op}} \rightarrow [D, \mathbf{Set}]$ sending each object $r$ to the representable functor $\hom_D(r, -)$ is full and faithful. That is, for any $r, s \in D$,
\[
\operatorname{Nat}(\hom_D(r, -), \hom_D(s, -)) \cong \hom_D(s, r).
\]