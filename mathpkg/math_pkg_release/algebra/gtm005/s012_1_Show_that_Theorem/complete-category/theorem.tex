A category $C$ is \textbf{complete} (or \textbf{small-complete}) if every small diagram $F: J \rightarrow C$ (with $J$ a small category) has a limit in $C$. It is \textbf{cocomplete} if every small diagram has a colimit.