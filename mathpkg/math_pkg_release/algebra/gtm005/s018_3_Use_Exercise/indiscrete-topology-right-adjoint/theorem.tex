The forgetful functor $G: \mathbf{Top} \to \mathbf{Set}$ also has a right adjoint $D'$, which assigns to each set $S$ the indiscrete topology on $S$ (with only $S$ and $\emptyset$ open). Therefore $G$ preserves all colimits which may exist in $\mathbf{Top}$.
