The forgetful functor $G: \mathbf{Top} \to \mathbf{Set}$ has a left adjoint $D$ which assigns to each set $S$ the discrete topology on $S$ (i.e., all subsets of $S$ are open). Therefore $G$ preserves all limits which may exist in $\mathbf{Top}$.
