A preorder $P$ is \text{directed} if for any two elements $p, q \in P$ there exists an upper bound $r \in P$ with $p \leq r$ and $q \leq r$. It follows that any finite set of elements of $P$ has an upper bound in $P$. A directed preorder is also called a \text{directed set} or a \text{filtered set}.
