Let $S$ be a subcategory of a category $C$. The \emph{inclusion functor} $S \hookrightarrow C$ (or $S \rightarrow C$) is the functor which sends each object and each arrow of $S$ to itself, regarded as an object or arrow of $C$.
