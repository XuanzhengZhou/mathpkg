For any monoid $\langle c, \mu, \eta \rangle$ in a monoidal category, the general associative law holds:

$$\mu^{(n)}(\mu^{(k_1)} \square \cdots \square \mu^{(k_n)}) = \mu^{(k_1 + \cdots + k_n)}$$

valid for all natural numbers $n$ and $k_1, \ldots, k_n$.
