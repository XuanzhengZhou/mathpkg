The simplicial category $\Delta$ has as objects all finite ordinal numbers $n = \{0, 1, \ldots, n-1\}$ and as arrows $f : n \rightarrow n'$ all (weakly) monotone functions; that is, all functions $f$ such that $0 \leq i \leq j < n$ implies $f(i) \leq f(j)$ in $n'$.
