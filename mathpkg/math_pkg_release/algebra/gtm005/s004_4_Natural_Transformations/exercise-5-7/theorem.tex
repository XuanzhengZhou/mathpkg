An arrow $f: a \rightarrow b$ in a category $C$ is \textbf{regular} when there exists an arrow $g: b \rightarrow a$ such that $fgf = f$. Show that $f$ is regular if it has either a left or a right inverse, and prove that every arrow in $\mathbf{Set}$ with $a \neq \emptyset$ is regular.