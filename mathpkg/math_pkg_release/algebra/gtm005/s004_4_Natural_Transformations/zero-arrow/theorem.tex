In a category $C$ with a null object $z$, the \textbf{zero arrow} $0_{a,b}: a \rightarrow b$ is the unique arrow $a \rightarrow z \rightarrow b$ factoring through $z$. Any composite with a zero arrow is itself a zero arrow.