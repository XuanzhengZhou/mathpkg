An arrow $f: b \rightarrow b$ is called \textbf{idempotent} when $f \circ f = f$. An idempotent is said to \textbf{split} when there exist arrows $g: b \rightarrow c$ and $h: c \rightarrow b$ such that $f = h \circ g$ and $g \circ h = 1_c$.