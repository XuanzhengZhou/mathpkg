An arrow $f: a \rightarrow b$ in a category $C$ is \textbf{epi} (or is an \textbf{epimorphism}) if for any two parallel arrows $g, h: b \rightarrow c$, the equality $g \circ f = h \circ f$ implies $g = h$. In other words, $f$ is right-cancellable.