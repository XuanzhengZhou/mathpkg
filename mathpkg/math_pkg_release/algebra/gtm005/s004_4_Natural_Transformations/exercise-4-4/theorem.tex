For functors $S, T: C \rightarrow P$ where $C$ is a category and $P$ a preorder, show that there is a natural transformation $S \xrightarrow{\cdot} T$ (which is then unique) if and only if $Sc \leq Tc$ for every object $c \in C$.