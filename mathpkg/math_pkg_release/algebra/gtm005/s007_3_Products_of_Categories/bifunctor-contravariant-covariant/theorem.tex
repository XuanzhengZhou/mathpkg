A functor $B^{\text{op}} \times C \to D$ is called a \textbf{bifunctor, contravariant in $B$ and covariant in $C$}, with values in $D$. Equivalently, it is a bifunctor on $B$ and $C$ which is contravariant in its first argument and covariant in its second argument.
