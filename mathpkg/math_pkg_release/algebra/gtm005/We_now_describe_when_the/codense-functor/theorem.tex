A functor $K: A \to C$ is \emph{codense} if the dual condition holds: for every object $c \in C$, the functor $\hom_C(c, K-): A \to \mathbf{Set}$ is the right Kan extension of itself along $K$. Equivalently, $K^{\mathrm{op}}$ is dense.
