A \emph{bifunctor} contravariant in its first argument and covariant in its second is a functor $H: C^{\mathrm{op}} \times D \to E$. Equivalently, it is a family of functors $H(-, d): C^{\mathrm{op}} \to E$ for each $d$ and $H(c, -): D \to E$ for each $c$, agreeing on objects.
