If $K: D \to \mathbf{Set}$ is a functor and $r$ is an object in $D$ (where $D$ has small hom-sets), there is a bijection
$$y: \operatorname{Nat}(D(r, -), K) \cong Kr$$
which sends each natural transformation $\alpha: D(r, -) \to K$ to $\alpha_r(1_r)$, the image of the identity arrow $1_r: r \to r$.