The \textbf{simplicial category} $\Delta$ has as objects the finite ordinal numbers $[n] = \{0, 1, \ldots, n\}$, and as arrows $[n] \rightarrow [m]$ all (weakly) order-preserving functions. A \textbf{simplicial object} in a category $C$ is a functor $\Delta^{\text{op}} \rightarrow C$.