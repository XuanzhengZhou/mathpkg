The braid category $B$ is, under $+$, a \textbf{strict monoidal category}. The operation of addition of objects $m + n$ is commutative and one can define a transformation
$$\gamma_{m,n} : m + n \to n + m$$
by crossing $m$ strings over $n$ strings. This $\gamma$ is natural in $m$ and $n$.
