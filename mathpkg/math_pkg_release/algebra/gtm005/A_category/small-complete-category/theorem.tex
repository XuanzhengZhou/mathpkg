A category $C$ is called \textbf{small-complete} (usually just \textbf{complete}) if all small diagrams in $C$ have limits in $C$; that is, if every functor $F: J \rightarrow C$ to $C$ from a small category $J$ has a limit.
