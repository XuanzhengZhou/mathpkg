A category is \emph{discrete} when every arrow is an identity. Every set $X$ determines a discrete category by taking $X$ as the set of objects and adding exactly one identity arrow $x \to x$ for each $x \in X$. Conversely, every discrete category is determined by its set of objects. Thus, discrete categories are sets.
