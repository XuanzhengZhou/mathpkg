\text{Let $D$ be a category that is small-complete, has small hom-sets, and has a small cogenerating set $Q$.}

\text{If every set of subobjects of each object $d \in D$ has an intersection (i.e., a pullback of the corresponding monics), then $D$ has an initial object.}
