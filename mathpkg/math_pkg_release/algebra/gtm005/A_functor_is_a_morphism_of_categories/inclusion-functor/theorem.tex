For a subcategory $S$ of a category $C$, the \textbf{inclusion functor} is the injection (inclusion) map $S \rightarrow C$ which sends each object and each arrow of $S$ to itself (in $C$). This map is a functor.
