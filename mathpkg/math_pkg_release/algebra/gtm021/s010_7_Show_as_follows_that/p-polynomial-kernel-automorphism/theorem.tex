Let V = K^n and let f(T) be a p-polynomial, viewed as a morphism of algebraic groups V to G_a. Then there exists an automorphism phi of V such that the kernel of f(U) = f(T) composed with phi contains all (a, 0, ..., 0) in V.
