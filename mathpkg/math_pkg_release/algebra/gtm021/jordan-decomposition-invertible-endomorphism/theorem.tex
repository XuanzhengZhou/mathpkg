Let $x \in \operatorname{GL}(V)$.

(a) There exist unique $x_s, x_u \in \operatorname{GL}(V)$ such that $x_s$ is semisimple, $x_u$ is unipotent, $x_s$ and $x_u$ commute, and $x = x_s x_u = x_u x_s$.

(b) $x_s$ and $x_u$ are polynomials in $x$.
