Let $H$ be a closed subgroup of the algebraic group $G$, $N = N_G(H)$ its normalizer. Then
$$\mathfrak{n} \subset \mathfrak{n}_\mathfrak{g}(\mathfrak{h}) = \{\mathbf{x} \in \mathfrak{g} \mid [\mathbf{x}, \mathfrak{h}] \subset \mathfrak{h}\}.$$

When $\operatorname{char} K = 0$, equality holds.
