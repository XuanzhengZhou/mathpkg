Let $\mathfrak{A}_1 \subset \mathfrak{A}_2 \subset \cdots \subset \mathfrak{A}_q$ be a sequence of proper ideals in $R = k[[X_1, \cdots, X_n]]$. Then there exists a system of integrity $\{F_1, \cdots, F_n\}$ of $R$ and $q$ integers $1 \leq d_1 \leq d_2 \leq \cdots \leq d_q \leq n$ such that, if we set $S = k[[F_1, \cdots, F_n]]$, then $\mathfrak{A}_j \cap S$ is generated by $F_1, \cdots, F_{d_j}$. The classes $f_{d_j+1}, \cdots, f_n$ of $F_{d_j+1}, \cdots, F_n$ module $\mathfrak{A}_j$ are analytically independent over $k$, and $R/\mathfrak{A}_j$ is a finite module over $k[[f_{d_j+1}, \cdots, f_n]]$.

Proof. In case $q = 1$, Theorem 32 reduces to Theorem 31. We proceed by induction on $q$, and suppose that we have a system of integrity $\{G_1, \cdots, G_n\}$ of $R$ and $q-1$ integers $d_1 \leq d_2 \leq \cdots \leq d_{q-1}$ such that, in the ring $T = k[[G_1, \cdots, G_n]]$, the ideal $T \cap \mathfrak{A}_j$ is generated by $G_1,

$\mathfrak{A}_j \cap S$ must be distinct (Vol. I, Ch. V, § 2, Theorem 3, Complement 1). Therefore we must have $d_1 < d_2 < \cdots < d_q$. Since the integers $d_i$ all lie between 0 and $n$, it follows that $q \leq n$. As also (0) is a prime ideal in $R$, we have therefore: