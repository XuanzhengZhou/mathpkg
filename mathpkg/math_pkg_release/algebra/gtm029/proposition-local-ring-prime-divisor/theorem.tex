Let $\mathfrak{A}$ be an ideal in $\mathfrak{o}$, primary for $\mathfrak{m}$, and let $r$ be the order of $\mathfrak{A}$. We assume that $\mathfrak{o}'\mathfrak{A} \cap \mathfrak{o} = \mathfrak{A}$, where $\mathfrak{o}'$ is the semi-local ring defined in (9). We set

(10) $\mathfrak{A}_i = \mathfrak{o}'_i\mathfrak{A} \cap \mathfrak{o}, \quad i = 1, 2, \cdots, m$

or equivalently, since $\bigcap_{i=1}^{m} \mathfrak{C}'_i \subset \mathfrak{o}'$:

$$\bigcap_{i=1}^{m} \mathfrak{C}'_i = \mathfrak{C}'_i$$

i.e.,

$$\bigcap_{i=1}^{m} \mathfrak{o}'_i \mathfrak{A} = \mathfrak{o}' \mathfrak{A}.$$

Using (10) and the assumption that $\mathfrak{o}' \mathfrak{A} \cap \mathfrak{o} = \mathfrak{A}$, we find (11). Now each $\mathfrak{A}_i$ is, by definition, the contraction of an ideal in $\mathfrak{o}'_i$. Hence, by Proposition 3, we have that $c(\mathfrak{A}_i)$ is a power of $\bar{g}_i$, which proves (12).

Since $\mathfrak{A} \subset \mathfrak{A}_i$, we have $r(\mathfrak{A}_i) \leq r$. On the other hand, since $\mathfrak{A}_i \subset \mathfrak{o}'_i \mathfrak{A}$ we have $v_m(\mathfrak{A}_i) \geq v_m(\mathfrak{A}) = r$, where $v_m$ is the $m$-adic prime divisor of $\mathfrak{o}$. Hence $r(\mathfrak{A}_i) \geq r$, and thus

(15) $r(\mathfrak{A}_i) = r = r(\mathfrak{A})$.

Applying again Proposition 3 we find that if we set

(15a) $\mathfrak{A}_i: m^{r-s}_i = \mathfrak{B}_i$,

where $s_i = \text{degree of } c(\mathfrak{A}_i)$, then

(16) $\mathfrak{A}_i = m^{r-s}_i \mathfrak{B}_i$.

We set $s = s_

This completes the proof.

We call an ideal $\mathfrak{A}$ in $\mathfrak{o}$ a contracted ideal if we have $\mathfrak{o}'\mathfrak{A} \cap \mathfrak{o} = \mathfrak{A}$ for some semi-local ring $\mathfrak{o}'$ which is an intersection of suitable quadratic transforms $\mathfrak{o}'_1, \mathfrak{o}'_2, \cdots, \mathfrak{o}'_m$ of $\mathfrak{o}$.