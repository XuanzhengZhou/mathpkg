Let $A$ be a local ring, $K$ a subfield of $A$, and $\{x_1, \cdots, x_d\}$ a system of parameters of $A$. Then the elements $x_1, \cdots, x_d$ are algebraically independent over $K$.

Let $G(X_1, \cdots, X_d)$ be a non-zero polynomial over $K$ such that $G(x_1, \cdots, x_d)=0$. Denote by $F(X_1, \cdots, X_d)$ the lowest degree form of $G$, and by $s$ the degree of $F$. From $G(x_1, \cdots, x_d)=0$, we deduce that $F(x_1, \cdots, x_d) \in \mathfrak{q}^{s+1}$, where $\mathfrak{q}=\sum Ax_i$, whence $F(x_1, \cdots, x_d) \in \