We define a distance function on $E$ by

$$d(x, y) = 2^{-v(x-y)} \quad (\text{with } 2^{-\infty} = 0).$$

This distance satisfies the strong triangle inequality:

$$d(x, z) \leq \max\{d(x, y), d(y, z)\}.$$

We have $d(x, y) = 0$ if and only if $x - y \in \bigcap_{n=0}^{\infty} \mathfrak{m}^n E$.