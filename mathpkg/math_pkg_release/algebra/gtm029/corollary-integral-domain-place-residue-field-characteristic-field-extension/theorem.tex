If $K^\star$ is a finite algebraic extension of $K$ and $\mathcal{P}$ is a place of $K$, then the number of non-isomorphic extensions of $\mathcal{P}$ in $K^\star$ is not greater than the degree of separability $[K^\

$x\mathcal{P}_i = 0; i = 2, 3, \cdots, s - 1$. We show that there exists an element $y_s$ such that $y_s\mathcal{P}_1 \neq 0, \infty, y_s\mathcal{P}_i = 0, i = 2, 3, \cdots, s - 1$, and $y_s\mathcal{P}_s \neq \infty$. If $x\mathcal{P}_s \neq \infty$, there is nothing to prove. If $x\mathcal{P}_s = \infty$, we set $y_s = x/(x - 1)$ if $x\mathcal{P}_1 \neq 1$, and $y_s = x/(x + 1)$ if $x\mathcal{P}_1 = 1$ and the characteristic of the residue field of $\mathcal{P}_1$ is $\neq 2$. If the characteristic is 2 and $x\mathcal{P}_1 = 1$, we set $y_s = (x^3 + x^2 + x)/(x^3 + x + 1)$.

In a similar fashion we find, for each $i = 2, 3, \cdots, s$, an element $y_i$ such that $y_i\mathcal{P}_1 \neq 0, \infty, y_i\mathcal{P}_i \neq \infty$ and $y_i\mathcal{P}_j = 0$, if $j \neq 1$, $i(i = 2, 3, \cdots, s)$. If we then set $\xi_1 = y_2y_3 \cdots y_s$, we have $\xi_1\mathcal{P}_1 \neq 0, \infty; \xi_1\mathcal{P}_i = 0, i = 2, 3, \cdots, s$. The existence of $\xi_2, \xi_3, \cdots, \xi_s$ is proved in a similar manner.

The above Corollary of Theorem 12 can now be proved as follows:

Let $\mathcal{P}_1^\star, \mathcal{P}_2^\star, \cdots, \mathcal{P}_s^\

Let us prove, for instance, that there exists an element $u_1$ such that $u_1\mathcal{P}_1 = a_1, u_1\mathcal{P}_j \neq \infty$ if $j \neq 1$. We begin with the case $s = 2$. Let $z_1$ be an arbitrary element of $K$ such that $z_1\mathcal{P}_1 = a_1$. If $z_1\mathcal{P}_2 \neq \infty$, we set $u_1 = z_1$. If $z_1\mathcal{P}_2 = \infty$, then we may set $u_1 = z_1 / (1 + z_1\zeta_2)$.

We now assume that $s > 2$ and we use induction with respect to $s$. There exists then an element $z_1$ in $K$ such that $z_1\mathcal{P}_1 = a_1, z_1\mathcal{P}_j \neq \infty, j = 2, 3, \cdots, s - 1$. If also $z_1\mathcal{P}_s \neq \infty$, we set $u_1 = z_1$. If $z_1\mathcal{P}_s = \infty$, we may set $u_1 = z_1 / (1 + z_1\zeta_s)$.

This completes the proof of the lemma.

We shall conclude our study of extensions of places in algebraic field extensions by a theorem which is of importance for applications, since it covers a situation which occurs whenever two integral domains are given, one of which is integrally dependent on the other.