With the assumptions and notations as in Theorem 1, the decomposition (22) of $\mathfrak{A}$ is the only decomposition of $\mathfrak{A}$ into contracted ideals $\mathfrak{A}_i$ satisfying (23) and such that $c(\mathfrak{A}_i)$ is a power of $\bar{g}_i$.

Let $\mathfrak{A} = \mathfrak{A}_1 \cap \mathfrak{A}_2 \cap \cdots \cap \mathfrak{A}_m$ be another such decomposition. Then $c(\mathfrak{A}_i)$ divides $c(\mathfrak{A})$, and thus the degree $\sigma_i$ of $c(\mathfrak{A}_i)$ is not greater than $s_i$. Since $m^{r-\sigma_i}$ factors out from $\mathfrak{A}_i$, we can write $\mathfrak{A}_i = m^{r-s_i}\mathfrak{B}_i$, where $\mathfrak{B}_i$ is again the contraction of an ideal in $o'_i$. We have $m^{r-s}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m \subset m^{r-s_i}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m$ hence

(26) $m^{r-s}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m \subset m^{r-s_i}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m$ (= $\mathfrak{A}$).

On the other hand, $m^{r-s_i}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m \subset m^{r-s_i}\mathfrak{B}_1\mathfrak{B}_2 \cdots \mathfrak{B}_m$ and passing to the extended ideals in $o'_i$ we find that