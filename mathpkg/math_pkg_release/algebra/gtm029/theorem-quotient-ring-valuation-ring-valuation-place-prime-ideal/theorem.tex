(§4) said that any prime ideal (different from (1))

a center $\mathfrak{D}$ in $\mathfrak{D}$, and this center is a prime ideal containing $\mathfrak{P}$. Furthermore, we have $\mathfrak{D} \cap \mathfrak{Q} = \mathfrak{Q}$, since $\mathfrak{Q}$ is the center of $\mathfrak{Q}$ in $\mathfrak{Q}$.

REMARK. This corollary has already been proved in Vol. I, Ch. V, p. 259, without any assumption on zero divisors.

The places $\mathfrak{P}$ of a field $K$ which have given center $\mathfrak{p}$ in a given subring $\mathfrak{Q}$ of $K$ are among the places of $K$ whose valuation ring contains the quotient ring $\mathfrak{Q}$, but they are those which satisfy the additional condition $\mathfrak{M}_\mathfrak{Q} \cap \mathfrak{Q} = \mathfrak{Q} \mathfrak{Q}$. By Theorem 6, §4, we know that the integral closure of $\mathfrak{Q}$ in $K$ is the intersection of all the valuation rings $K_\mathfrak{Q}$ which contain $\mathfrak{Q}$. We shall now prove the following stronger result.