The assumptions being the same as in Theorem 19, except that $v$ may now have infinite rank, the dimension of the vector space $R^\star/\mathfrak{P}^\star$ (over the field $R/\mathfrak{P}$) is not greater than $e_1n_1 + \cdots + e_gn_g$.

Proof

0 < α₂ᵣ - α₁ᵣ < α₂ᵣ, and therefore, by definition of L₁, α₂ᵣ - α₁ᵣ ≠ Γ. Thus, distinct elements of L₁ belong to distinct Γ-cosets, and hence L₁ is a finite set, consisting of at most e₁ elements.

If xᵣ is any element of H₁ᵣ then v₁ᵣ(xᵣ) ∈ L₁ if and only if xᵣ ≠ Pᵣ. For, if xᵣ ∈ Pᵣ = RᵣP, then it is clear that v₁ᵣ(xᵣ) ≥ v₁ᵣ(y), for some y in P, and hence v₁ᵣ(xᵣ) ∉ L₁, since v₁ᵣ(y) ∈ Γ₄. Conversely, if v₁ᵣ(xᵣ) ∉ L₁, then v₁ᵣ(xᵣ) ≥ v₁ᵣ(y), for some y in P, and hence xᵣ = (xᵣ/y)y ∈ R₁ᵣP ∩ H₁ᵣ = RᵣP.

If follows from these remarks that if Aᵣ is any R-submodule of H₁ᵣ which contains Pᵣ as a proper subset then Aᵣ contains elements of least value and that this value is an element of L₁. We denote this minimum by v₁ᵣ(Aᵣ).

If for a given element αᵣ of L₁ there exist elements xᵣ in H₁ᵣ such that v₁ᵣ(xᵣ) = αᵣ, then the set of all elements yᵣ of H₁ᵣ such that v₁ᵣ(yᵣ) ≥ αᵣ is an R-submodule Aᵣ of H₁ᵣ which contains Pᵣ as a proper subset and is such that v₁ᵣ(Aᵣ) = αᵣ. If 0 = α₁ᵣ <

rank), assume also that the integral closure $R^\star$ in $K^\star$ of the valuation ring $R$ of $v$ is a finite $R$-module. Then

(13) $e_1n_1 + e_2n_2 + \cdots + e_gn_g = n,$

and

(14) $\dim R^\star/R^\star\mathfrak{P} = n.$