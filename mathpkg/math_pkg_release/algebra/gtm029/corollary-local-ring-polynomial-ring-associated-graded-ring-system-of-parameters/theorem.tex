Let $A$ be a local ring, $\mathfrak{

such that the associated graded ring $G_q(A)$ is isomorphic to a polynomial ring in dim $(A)$ variables over $A/q$.

$(c')$ For every system of parameters $x_1, \cdots, x_d$ of $A$, the initial forms $\bar{x}_i$ of the elements $x_i$ in $G_q(A)$ $(q = Ax_1 + \cdots + Ax_d)$ are algebraically independent over $A/q$ (whence $G_q(A)$ is isomorphic to a polynomial ring in $d$ variables over $A/q$).