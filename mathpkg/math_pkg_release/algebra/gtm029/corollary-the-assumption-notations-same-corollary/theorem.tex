The assumption and notations being the same as in Corollary 1, the initial form module of $\mathfrak{A}$ is the set of all forms of degree $r$ ($r = \text{order of } \mathfrak{A}$) which are divisible by $c(\mathfrak

hence $c(\mathfrak{B}_i) = \bar{g}_i^{\lambda_i}$. Furthermore, $r(\mathfrak{A}) = r - s + r(\mathfrak{B}_1) + r(\mathfrak{B}_2) + \cdots + r(\mathfrak{B}_m)$, and since $r(\mathfrak{B}_i) \geq \deg c(\mathfrak{B}_i) = s_i$, it follows that $r(\mathfrak{B}_i) = s_i$. Similarly, $c(\mathfrak{B}_i) = \bar{g}_i^{\lambda_i}, r(\mathfrak{B}_i) = s_i$. Since $\mathfrak{B}_i$ is a contracted ideal and $c(\mathfrak{B}_i)$ is a power of $\bar{g}_i$, we have $\mathfrak{o}'_i \mathfrak{B}_i \cap \mathfrak{o} = \mathfrak{B}_i$, by above Corollary 1. Similarly $\mathfrak{o}'_i \mathfrak{B}_i \cap \mathfrak{o} = \mathfrak{B}_i$, now, assuming, as we may, that $z_1 \neq \bar{g}_1(z_1, z_2)$, we have $\mathfrak{o}'_1 \mathfrak{B}_i = \mathfrak{o}'_1 t_1^{s_i}$ for $i > 1$ (Proposition 2, part (a)). Hence $\mathfrak{o}'_1 \mathfrak{A} = t_1^{r-s_1} \mathfrak{o}'_1 \mathfrak{B}_1$. Similarly $\mathfrak{o}'_1 \mathfrak{A} = t_1^{r-s_1} \mathfrak{o}'_1 \mathfrak{B}_1$. Hence $\mathfrak{o}'_1 \mathfrak{B}_1 = \mathfrak{o}'_1 \mathfrak{B}_1$ and hence $\mathfrak{B}_1 = \mathf

Since the ideals $B_i$ ($i=1, 2, \cdots, m$) are primary for $m$, we can take in (24) $j$ so large as to have

(25) $m^{r+j} \subset m^{r-s} B_1 B_2 \cdots B_m$.

For such a large value of $j$ we will have, by (17): $x_{1j} x_{2j} \cdots x_{mj} x_{m+1,j} \in A$. We may assume that $\bar{g}_1 \neq z_1$. Since $\bar{\psi}_2, \bar{\psi}_3, \cdots, \bar{\psi}_m, \bar{\psi}_{m+1}$ are not divisible by $\bar{g}_1$ and since the initial form $\bar{\psi}_2 \bar{\psi}_3 \cdots \bar{\psi}_{m+1}$ of $x_{2j} x_{3j} \cdots x_{m+1,j}$ is of degree $r - \rho_1$ it follows that $\frac{x_{2j} x_{3j} \cdots x_{m+1,j}}{t_1^{r-\rho_1}}$ is a unit in $o'_1$.

Hence, if we set $o'_1 A = t_1^r A'_1$, then $x_{1j}/t_1^{\rho_1} \in A'_1$, and thus $x_{1j} m^{r-\rho_1} \subset o'_1 A \cap o = A_1$, and $x_{1j} \in A_1: m^{r-\rho_1}$. Since $\rho_1 \geq s_1$, we have, by Corollary 1 of Proposition 3: $A_1: m^{r-\rho_1} = m^{\rho_1-s_1} B_1$. Hence $x_{1j} \in m^{\rho_1-s_1} B_1$. Similarly, $x_{ij} \in m^{\rho_1-s_1} B_1$, $i=1, 2, \cdots, m$, and we