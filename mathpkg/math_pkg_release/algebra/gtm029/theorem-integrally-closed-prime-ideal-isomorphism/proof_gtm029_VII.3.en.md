---
role: proof
locale: en
of_concept: theorem-integrally-closed-prime-ideal-isomorphism
source_book: gtm029
source_chapter: "VII"
source_section: "3"
---

This proof will be based on properties of integral dependence. We first of all achieve a reduction to the case in which $k$ is an infinite field. For this purpose we consider an algebraic closure $K$ of the field $k(X_1

A slight modification of the above proof makes it possible to avoid the use of the normalization theorem. For that purpose we fix an arbitrary transcendence basis $\{z_1, z_2, \cdots, z_d\}$ of the field $k(x_1, x_2, \cdots, x_n)/k$ such that the $z$'s belong to $k[x_1, x_2, \cdots, x_n]$ (for instance, we could take for $\{z_1, z_2, \cdots, z_d\}$ a suitable subset of $\{x_1, x_2, \cdots, x_n\}$). Each $x_i$ satisfies an equation of algebraic dependence, of the form $g_m(z_1, z_2, \cdots, z_d)x_i^{m} + \cdots + g_0(z_1, z_2, \cdots, z_d) = 0$, where the $g_v$ are polynomials with coefficients in $k$ and where we may assume that the leading coefficient $g_m$ is independent of $i$. Fix in the algebraic closure $k$ of $k$ a set of elements $\bar{z}_1, \bar{z}_2, \cdots, \bar{z}_d$ such that $g_m(\bar{z}_1, \bar{z}_2, \cdots, \bar{z}_d) \neq 0$ (this is possible since $k$ is an infinite field). Let $\mathfrak{q}_0$ be the kernel of the $k$-homomorphism $k[z_1, z_2, \cdots, z_d] \rightarrow k[\bar{z}_1, \bar{z}_2, \cdots, \bar{z}_d]$ determined by the conditions $z_i \rightarrow \bar{z}_i$. We denote by $\mathfrak{o}$ the quotient ring of $k[z_1, z_2, \cdots, z_d]$ with respect to $\mathfrak{q}_0$, by $\mathfrak{o}^{\star}$ the integral closure of $\mathfrak{o}$ in $k(x_1, x_2, \cdots, x_n)$,

isolated prime ideals of $\mathfrak{A}$, then the varieties $\mathcal{V}(\mathfrak{p}_1), \mathcal{V}(\mathfrak{p}_2), \cdots, \mathcal{V}(\mathfrak{p}_h)$ are the irreducible components of $\mathcal{V}(\mathfrak{A})/k$.

Since $\mathfrak{p}_1 \cap \mathfrak{p}_2 \cap \cdots \cap \mathfrak{p}_h$ is an irredundant representation of $\sqrt{\mathfrak{A}}$ as intersection of prime ideals, the corollary follows from the irreducibility of $\mathcal{V}(\mathfrak{p}_i)$ and from Corollary 2 (since $\sqrt{\mathfrak{A}} \in \mathbf{I}$, by that corollary).
