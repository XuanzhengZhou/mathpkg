Let $H$ be a subgroup of a group $G$. Two elements $s, s' \in G$ are said to be \textbf{congruent modulo $H$} if they belong to the same left coset of $H$, i.e., if $s^{-1}s' \in H$. This is written
$$s' \equiv s \pmod{H}.$$
