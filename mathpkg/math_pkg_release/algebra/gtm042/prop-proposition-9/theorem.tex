If K is a field of characteristic zero, the algebra K[G] is semisimple.

(For the basic facts on semisimple algebras, see, for example, Bourbaki [8] or Lang [10].)

To say that K[G] is a semisimple algebra is equivalent to saying that each K[G]-module V is semisimple, i.e., that each submodule $V'$ of V is a direct factor in V as a K[G]-module. This is proved by the same argument of averaging as that in 1.3: we choose first a K-linear projection $p$ of V onto $V'$, then form the average $p^0 = (1/g) \sum_{s \in G} sps^{-1}$ of its transforms by G.

Chapter 6: The group algebra

The projection $p^0$ thus obtained is $K[G]$-linear, which implies that $V'$ is a direct factor of $V$ as a $K[G]$-module.
