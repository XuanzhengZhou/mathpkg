Let T be the set of all cyclic subgroups of G. The homomorphisms

$$Q \otimes \text{Ind} : \bigoplus_{H \in T} Q \otimes R_k(H) \rightarrow Q \otimes R_k(G)$$

and

$$Q \otimes \text{Ind} : \bigoplus_{H \in T} Q \otimes P_k(H) \rightarrow Q \otimes P_k(G)$$

are surjective.

17.3 Proof of theorem 33

We have to show that $d$: $R_K(G) \rightarrow R_k(G)$ is surjective. By th. 39, $R_k(G)$ is generated by the various $\text{Ind}_H^G(R_k(H))$, where $H$ is $\Gamma_K$-elementary. Since $d$ commutes with $\text{Ind}_H^G$, it is enough to show that $R_k(H) = d(R_K(H))$. Hence we are reduced to the case where $G$ is $\Gamma_K$-elementary. In this case we have the following more precise result:
