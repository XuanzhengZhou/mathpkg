A complex number $x$ is called an **algebraic integer** if it satisfies an equation of the form
$$x^n + a_1 x^{n-1} + \cdots + a_n = 0,$$
where the $a_i$ are in $\mathbb{Z}$ (i.e., $x$ is integral over $\mathbb{Z}$).

Each root of unity is an algebraic integer.
