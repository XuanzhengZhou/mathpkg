A functor $F: \mathfrak{C} \to \mathfrak{D}$ is called \textbf{faithful} if for all objects $A, B$ in $\mathfrak{C}$, the induced map
$$F_{A,B}: \mathfrak{C}(A, B) \to \mathfrak{D}(FA, FB)$$
is injective.
