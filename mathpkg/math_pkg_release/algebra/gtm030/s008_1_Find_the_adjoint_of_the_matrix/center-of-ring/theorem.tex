The subring $\mathfrak{C} = C(\mathfrak{A})$ is called the center of the ring $\mathfrak{A}$. If $\mathfrak{A}$ contains an identity $1$, evidently $1 \in \mathfrak{C}$.

The center consists of all elements $c \in \mathfrak{A}$ such that $ca = ac$ for all $a \in \mathfrak{A}$.
