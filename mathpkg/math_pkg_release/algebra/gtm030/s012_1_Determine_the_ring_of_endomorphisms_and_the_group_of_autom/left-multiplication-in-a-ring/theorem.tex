Let $\mathfrak{A}$ be any ring. If $a$ is a fixed element of $\mathfrak{A}$, the \textbf{left multiplication} $a_l$ is the mapping $a_l: \mathfrak{A} \to \mathfrak{A}$ defined by

$$x a_l = ax$$

for all $x \in \mathfrak{A}$.
