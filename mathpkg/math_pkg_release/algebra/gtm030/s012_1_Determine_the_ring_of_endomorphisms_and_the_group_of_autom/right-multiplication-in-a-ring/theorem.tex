Let $\mathfrak{A}$ be any ring. If $a$ is a fixed element of $\mathfrak{A}$, the \textbf{right multiplication} $a_r$ is the mapping $a_r: \mathfrak{A} \to \mathfrak{A}$ defined by

$$x a_r = xa$$

for all $x \in \mathfrak{A}$.
