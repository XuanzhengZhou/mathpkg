The ascending chain condition is equivalent to the maximum condition: a module satisfies the ascending chain condition for submodules if and only if every non-vacuous collection of submodules contains a maximal submodule.
