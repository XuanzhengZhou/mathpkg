Let $\mathfrak{G}$ be a transformation group acting in a set $S$. Then $\mathfrak{G}$ defines an equivalence relation in $S$ by the rule that $x \equiv y \pmod{\mathfrak{G}}$ (read: $x$ is congruent to $y$ modulo $\mathfrak{G}$) if $y = x\alpha$ for some $\alpha$ in $\mathfrak{G}$.
