Let $\mathfrak{G}$ be an $M$-group and let $\mathfrak{H}$ be an invariant $M$-subgroup of $\mathfrak{G}$. The factor group $\bar{\mathfrak{G}} = \mathfrak{G}/\mathfrak{H}$ becomes an $M$-group by defining

$$(g\mathfrak{H})m = (gm)\mathfrak{H}$$

for every $g \in \mathfrak{G}$ and every $m \in M$.
