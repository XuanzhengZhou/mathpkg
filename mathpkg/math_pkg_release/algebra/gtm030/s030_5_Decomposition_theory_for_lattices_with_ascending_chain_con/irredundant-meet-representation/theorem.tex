The representation $a = q_1 \cap q_2 \cap \cdots \cap q_m$ as a meet of irreducible elements is called \textit{irredundant} if
$$q_1 \cap \cdots \cap q_{i-1} \cap q_{i+1} \cap \cdots \cap q_m > a$$
for all $i = 1, 2, \dots, m$.
