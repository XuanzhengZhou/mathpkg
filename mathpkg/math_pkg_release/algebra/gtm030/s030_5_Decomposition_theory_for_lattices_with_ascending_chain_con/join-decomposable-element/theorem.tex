An element $a$ of a lattice is \textit{(join) decomposable} if $a = a_1 \cup a_2$ where the $a_i$ are independent and $a_i \neq a$ for $i = 1, 2$. An element that is not join-decomposable is called \textit{(join) indecomposable}.
