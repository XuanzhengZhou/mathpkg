Let $\sim$ be an equivalence relation on a set $S$. Then any two equivalence classes are either equal or disjoint. Consequently, the equivalence classes form a partition of $S$ into non-overlapping subsets.
