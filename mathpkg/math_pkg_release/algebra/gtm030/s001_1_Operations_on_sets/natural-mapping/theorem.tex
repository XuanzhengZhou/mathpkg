If $S$ is a set and $\bar{S}$ is its quotient set relative to an equivalence relation $\sim$, then the natural mapping $\nu : S \to \bar{S}$ is defined by the rule that the element $a \in S$ is sent into the equivalence class $\bar{a}$ determined by $a$. This mapping is onto $\bar{S}$.
