The vacuous set $\varnothing$ is the set containing no elements. It is adjoined to the collection of subsets of any set $S$. The equation $A \cap B = \varnothing$ indicates that $A$ and $B$ are non-overlapping (disjoint), that is, they have no elements in common.
