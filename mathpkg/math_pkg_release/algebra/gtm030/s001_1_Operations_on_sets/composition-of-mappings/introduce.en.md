---
role: introduce
locale: en
content_hash: ""
written_against: ""
---

The composition (or resultant) of two mappings applies them in sequence: first $lpha$ maps from $S$ to $T$, then $eta$ maps from $T$ to $U$. The result is a mapping from $S$ directly into $U$.
