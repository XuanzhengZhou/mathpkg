A \text{transposition} is a cycle of the form $(ab)$, i.e., a permutation that interchanges two elements and leaves all others fixed. Every cycle can be expressed as a product of transpositions:

$$(i_1 i_2 \cdots i_r) = (i_1 i_2)(i_1 i_3) \cdots (i_1 i_r).$$
