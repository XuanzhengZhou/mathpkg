If $a$ and $b$ are any two elements of a group $\mathfrak{G}$ then the linear equation $ax = b$ has the solution $a^{-1}b$ in $\mathfrak{G}$. This is the only solution. Similarly the equation $ya = b$ has a unique solution in $\mathfrak{G}$.
