Similarly, we can define the \text{left multiplication} $a_l$ by $x \to ax$. The proof that the set $\mathfrak{G}_l = \{a_l\}$ is a transformation group is the same as for $\mathfrak{G}_r$ with the modification that
$$a_l b_l = (ba)_l.$$
This follows from $x a_l b_l = b(ax) = (ba)x = x(ba)_l$.
