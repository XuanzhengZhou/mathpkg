$[a]$ consists of the integral powers of the element $a$. Define $a^0 = 1$ and $a^{-n} = (a^{-1})^n$ if $n > 0$. Then $[a]$ consists of the elements $a^n$ for all integers $n$. The basic laws of exponents extend to all integers:
$$a^{m+n} = a^m a^n, \quad (a^m)^n = a^{mn}.$$
