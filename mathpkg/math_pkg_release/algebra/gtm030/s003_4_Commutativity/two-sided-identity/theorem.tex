An element is a \text{two-sided identity} if it is both a left identity and a right identity. From now on we refer to a two-sided identity simply as an (the) \text{identity} and we shall usually denote this element as $1$.
