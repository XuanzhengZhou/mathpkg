For elements $a, b$ in a ring $\mathfrak{A}$, the \emph{circle composition} $a \circ b$ is defined by
$$a \circ b = a + b - ab.$$

This operation is associative, making $(\mathfrak{A}, \circ)$ a semi-group. The zero element acts as the identity:
$$a \circ 0 = a = 0 \circ a.$$
