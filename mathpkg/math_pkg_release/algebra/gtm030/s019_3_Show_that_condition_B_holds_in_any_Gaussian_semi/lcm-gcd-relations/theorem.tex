Let $\mathfrak{S}$ be a Gaussian semi-group and let $[a, b]$ denote a least common multiple of $a$ and $b$.

Prove that:
\begin{enumerate}
  \item $(a, b) \cdot [a, b] \sim a b$,
  \item $[a, (b, c)] \sim ([a, b], [a, c])$.
\end{enumerate}
