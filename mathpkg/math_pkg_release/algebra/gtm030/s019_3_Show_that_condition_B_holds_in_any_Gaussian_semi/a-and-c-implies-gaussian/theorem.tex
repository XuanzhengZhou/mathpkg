If $\mathfrak{S}$ is a commutative semigroup with identity and cancellation law satisfying both condition A (every element has a factorization into irreducibles) and condition C (every pair has a GCD), then $\mathfrak{S}$ is Gaussian (i.e., a unique factorization semigroup).
