$$
\text{Theorem 1. If } X \text{ is a set of generators for a unitary module } \mathfrak{M},
\text{ then every element of } \mathfrak{M} \text{ can be written in the form}
$$
$$
a_1 x_1 + a_2 x_2 + \cdots + a_r x_r
$$
$$
\text{where } a_i \in \mathfrak{A} \text{ and } x_i \in X.
$$
