Every ordered field $(K, <)$ has a real closure $R$: a real closed algebraic extension of $K$ whose unique ordering extends the given ordering of $K$. The real closure is unique up to unique isomorphism over $K$ (as ordered fields). The real closure plays the role for ordered fields that the algebraic closure plays for arbitrary fields.
