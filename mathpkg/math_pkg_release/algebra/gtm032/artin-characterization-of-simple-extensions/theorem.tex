Let $\Phi$ be an infinite field and $P$ a finite dimensional extension field of $\Phi$. Then $P/\Phi$ is a simple extension (i.e., has a primitive element) if and only if there are only finitely many intermediate fields between $P$ and $\Phi$.
