Let $\Phi$ be an infinite field and $P$ a finite dimensional extension of $\Phi$. Then $P/\Phi$ is a simple extension if and only if there are only finitely many intermediate fields between $P$ and $\Phi$.
