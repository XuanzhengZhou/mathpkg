Let $A/\Phi$ be an algebraic extension. Then the subset $\Sigma \subseteq A$ of elements which are separable over $\Phi$ is a subfield containing $\Phi$. Moreover, every element of $A$ which is separable over $\Sigma$ is contained in $\Sigma$ (i.e., $\Sigma$ is separably closed in $A$).
