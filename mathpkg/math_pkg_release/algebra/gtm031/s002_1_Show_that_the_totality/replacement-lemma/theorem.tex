Let $x_1, x_2, \cdots, x_m$ be a set of $m > 1$ vectors and define $x_i' = x_i$ for $i = 1, 2, \cdots, m-1$ and $x_m' = x_m + \rho x_1$. Then the $x_i$ are linearly independent if and only if the $x_i'$ are linearly independent.
