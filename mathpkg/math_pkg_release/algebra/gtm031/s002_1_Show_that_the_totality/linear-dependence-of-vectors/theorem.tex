The vectors $x_1, x_2, \cdots, x_m$ are \textbf{linearly dependent} if there exist elements $\beta_i$ not all $0$ in $\Delta$ such that

$$
\beta_1 x_1 + \beta_2 x_2 + \cdots + \beta_m x_m = 0.
$$

A set consisting of a single vector $x$ is linearly dependent if and only if $x = 0$.
