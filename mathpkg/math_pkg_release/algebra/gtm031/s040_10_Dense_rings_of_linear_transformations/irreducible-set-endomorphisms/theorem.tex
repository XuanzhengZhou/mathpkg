A set $\mathfrak{A}$ of endomorphisms of a commutative group $\mathfrak{R}$ is called \textbf{irreducible} (or an irreducible set of endomorphisms) if the only subgroups of the additive group $\mathfrak{R}$ which are mapped into themselves by every $A \in \mathfrak{A}$ are $\mathfrak{R}$ and $0$.
