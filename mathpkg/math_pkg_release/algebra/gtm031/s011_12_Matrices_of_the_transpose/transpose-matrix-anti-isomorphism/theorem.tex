The mapping $(\alpha) \to (\alpha)'$ is an anti-isomorphism in the matrix ring $\Phi_n$. That is, the mapping is one-to-one and satisfies:

$$[(\alpha) + (\beta)]' = (\alpha)' + (\beta)', \quad [(\alpha)(\beta)]' = (\beta)'(\alpha)'.$$
