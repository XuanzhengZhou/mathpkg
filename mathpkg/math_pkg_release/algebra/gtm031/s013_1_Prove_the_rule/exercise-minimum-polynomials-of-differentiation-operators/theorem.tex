\text{Let } \Re \text{ be the vector space of polynomials of degree } \leq n-1 \text{ with real coefficients. Find the minimum polynomials of the differentiation operator } D \text{ and of the linear transformation } A \text{ such that } f(\lambda)A = f(\lambda + 1).
