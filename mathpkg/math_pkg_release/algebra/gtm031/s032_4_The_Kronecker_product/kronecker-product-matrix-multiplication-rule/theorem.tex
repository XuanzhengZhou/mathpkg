For matrices $(\alpha)$, $(\beta)$, $(\gamma)$, $(\delta)$ of appropriate sizes over a field $\Phi$,

$$((\alpha) \times (\beta))((\gamma) \times (\delta)) = (\alpha)(\gamma) \times (\beta)(\delta).$$
