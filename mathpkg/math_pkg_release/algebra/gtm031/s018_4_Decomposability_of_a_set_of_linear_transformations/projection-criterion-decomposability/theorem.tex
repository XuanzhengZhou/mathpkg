A set $\Omega$ of linear transformations on a vector space $\mathfrak{R}$ is decomposable if and only if there exists a projection $E \neq 0, \neq 1$ which commutes with every transformation in $\Omega$.
