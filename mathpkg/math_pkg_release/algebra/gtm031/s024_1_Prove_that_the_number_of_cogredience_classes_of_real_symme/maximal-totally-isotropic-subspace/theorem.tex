A subspace $\mathfrak{S}$ of a vector space $\mathfrak{R}$ with a hermitian scalar product $g(x, y)$ is called \text{totally isotropic} if $g(x, y) = 0$ for all $x, y \in \mathfrak{S}$. A totally isotropic subspace is \text{maximal} if it cannot be imbedded in a larger totally isotropic subspace.
