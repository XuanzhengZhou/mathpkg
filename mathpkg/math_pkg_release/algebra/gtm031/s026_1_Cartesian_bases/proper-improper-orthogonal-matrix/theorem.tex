Let $(\sigma)$ be an orthogonal matrix. Since $(\sigma)(\sigma)' = 1$, we have $(\det(\sigma))^2 = 1$, hence $\det(\sigma) = \pm 1$.

An orthogonal matrix $(\sigma)$ is called \textbf{proper} if $\det(\sigma) = 1$, and \textbf{improper} if $\det(\sigma) = -1$.
