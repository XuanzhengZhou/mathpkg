\textbf{Definition.} If $x' \in \mathfrak{R}'$ and $y \in \mathfrak{S}$, then $x' \times y$ is defined to be the linear transformation
$$x \mapsto g(x, x')y$$
belonging to the group $\mathfrak{P} = \mathfrak{R}' \times \mathfrak{S}$.
