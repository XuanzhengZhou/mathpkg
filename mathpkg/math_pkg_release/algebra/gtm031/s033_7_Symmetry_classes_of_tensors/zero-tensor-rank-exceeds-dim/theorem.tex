If $r > n = \dim \Re$, then for every skew-symmetric tensor $\Sigma \xi^{i_1 i_2 \cdots i_r} e_{i_1} \times \cdots \times e_{i_r}$,
$$\xi^{i_1 i_2 \cdots i_r} = 0 \quad \text{for all } i_1, i_2, \cdots, i_r.$$
Hence the only skew-symmetric tensor of rank $r > n$ is the $0$ tensor.
