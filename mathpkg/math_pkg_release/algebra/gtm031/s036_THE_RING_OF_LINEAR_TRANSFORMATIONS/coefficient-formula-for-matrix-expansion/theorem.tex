Let $A \in \mathfrak{L}$ and write $A = \sum_{i,j} \bar{\alpha}_{ij} E_{ij}$ as in the matrix expansion. Then the coefficients $\bar{\alpha}_{ij}$ are given explicitly by the formula

$$\bar{\alpha}_{ij} = \sum_{k=1}^n E_{ki} A E_{jk}.$$
