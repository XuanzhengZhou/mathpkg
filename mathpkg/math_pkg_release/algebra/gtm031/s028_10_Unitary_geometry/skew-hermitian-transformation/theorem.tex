A linear transformation $A$ on a unitary space is called skew-hermitian if $A' = -A$, where $A'$ denotes the transpose of $A$. Equivalently, in a unitary basis, the matrix $(\alpha)$ of $A$ is skew-hermitian: $(\alpha) = -(\bar{\alpha})'$.
