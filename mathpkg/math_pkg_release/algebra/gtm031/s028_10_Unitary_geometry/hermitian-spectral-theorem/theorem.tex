If $(\alpha)$ is a complex hermitian matrix, there exists a unitary matrix $(\sigma)$ such that $(\sigma)(\alpha)(\sigma)^{-1}$ is a real diagonal matrix. That is,
$$
(\sigma)(\alpha)(\sigma)^{-1} = \operatorname{diag}\{\rho_1, \rho_2, \cdots, \rho_n\}
$$
where each $\rho_i \in \mathbb{R}$.
