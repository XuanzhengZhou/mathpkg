A linear transformation $A$ on a unitary space is called hermitian if $A' = A$, where $A'$ denotes the transpose of $A$. Equivalently, if $(u_1, u_2, \cdots, u_n)$ is a unitary basis, then the matrix $(\alpha)$ of $A$ satisfies $(\alpha) = (\bar{\alpha})'$, i.e., the matrix is hermitian.
