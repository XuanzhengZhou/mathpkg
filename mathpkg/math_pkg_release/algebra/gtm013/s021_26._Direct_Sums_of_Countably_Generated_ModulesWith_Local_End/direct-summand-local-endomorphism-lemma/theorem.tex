Let $M$ be a module with a decomposition $M = K \oplus L$. Let $N$ be a direct summand of $M$ such that $N = N_1 \oplus \ldots \oplus N_n$ with each $\operatorname{End}(N_i)$ a local ring. Then there exist direct summands $K' \leq K$ and $L' \leq L$ such that

$$M = N \oplus K' \oplus L'.$$
