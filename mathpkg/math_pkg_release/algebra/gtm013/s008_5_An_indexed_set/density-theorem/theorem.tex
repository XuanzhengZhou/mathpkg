Let $M$ be a semisimple left $R$-module. Then the canonical image of $R$ in $\operatorname{BiEnd}({}_R M)$ is a dense subring. In particular, for any $x_1, \ldots, x_n \in M$ and $b \in \operatorname{BiEnd}({}_R M)$, there exists $r \in R$ such that
$$b x_i = r x_i \quad (i = 1, \ldots, n).$$
