Let $R$ be a commutative integral domain and let $M$ be a left $R$-module. $M$ is called *torsion-free* if $T(M) = 0$; that is, for every $0 \neq x \in M$, $I_R(x) = 0$, so $rx \neq 0$ for all $0 \neq r \in R$.
