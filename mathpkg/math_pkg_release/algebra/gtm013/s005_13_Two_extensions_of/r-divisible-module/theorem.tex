Let $R$ be a commutative integral domain. A left $R$-module $M$ is called $R$-*divisible* if for each $0 \neq a \in R$, $aM = M$; equivalently, for each $m \in M$ and $0 \neq a \in R$, the equation $m = ax$ has a solution $x \in M$.
