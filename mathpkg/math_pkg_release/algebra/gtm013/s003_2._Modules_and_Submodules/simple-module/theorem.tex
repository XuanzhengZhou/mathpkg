A module $M$ is \textbf{simple} if $M \neq 0$ and $M$ has no non-trivial submodules. Equivalently, a non-zero module is simple if and only if it is spanned by each of its non-zero elements. Simple modules are necessarily cyclic.
