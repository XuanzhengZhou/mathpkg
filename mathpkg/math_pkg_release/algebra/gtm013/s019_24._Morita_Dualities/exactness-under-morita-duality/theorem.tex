Let ${}_R U_S$ define a Morita duality. Then a sequence of $R$-homomorphisms

$$M_1 \xrightarrow{f} M_2 \xrightarrow{g} M_3$$

is exact if and only if

$$M_3^* \xrightarrow{g^*} M_2^* \xrightarrow{f^*} M_1^*$$

is exact. In particular, $f$ is epic (monic) if and only if $f^*$ is monic (epic).
