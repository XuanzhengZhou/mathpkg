Let ${}_R M$ be a module with dual $M^* = \operatorname{Hom}_R(M, U)$. Then:

\begin{enumerate}
\item[(1)] $l_M(M^*) = \operatorname{Rej}_M(U) = \operatorname{Ker} \sigma_M$;
\item[(2)] $l_M(r_{M^*}(K)) = K$ if and only if $U$ cogenerates $M/K$.
\end{enumerate}
