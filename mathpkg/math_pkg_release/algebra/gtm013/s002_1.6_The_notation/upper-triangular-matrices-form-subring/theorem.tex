The set of upper triangular matrices in $\mathbb{CFM}_{\Gamma}(R)$ forms a subring of $\mathbb{CFM}_{\Gamma}(R)$ and the set of upper triangular matrices in $\mathbb{RFM}_{\Gamma}(R)$ forms a subring of $\mathbb{RFM}_{\Gamma}(R)$. Parallel statements hold for the sets of lower triangular matrices.
