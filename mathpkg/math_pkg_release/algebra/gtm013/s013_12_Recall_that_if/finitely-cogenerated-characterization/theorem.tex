A left $R$-module $M$ is finitely cogenerated if and only if for every indexed set $(U_\alpha)_{\alpha \in A}$ of left $R$-modules and every monomorphism
$$0 \to M \to \prod_A U_\alpha,$$
there exists a monomorphism
$$0 \to M \to \prod_F U_\alpha$$
for some finite subset $F \subseteq A$.
