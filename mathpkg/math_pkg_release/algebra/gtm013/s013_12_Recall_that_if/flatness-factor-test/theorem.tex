Let $V$ be a flat right $R$-module and suppose that
$$0 \to K \xrightarrow{i_K} V \xrightarrow{f} V \to 0$$
is exact in $\mathbf{M}_R$. Then $V$ is flat if and only if for each (finitely generated) left ideal $I \leq {}_R R$,
$$KI = K \cap VI.$$
