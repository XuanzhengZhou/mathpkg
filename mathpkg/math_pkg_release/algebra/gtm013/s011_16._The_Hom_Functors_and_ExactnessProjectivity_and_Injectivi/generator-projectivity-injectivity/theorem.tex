Let $U$ be a left $R$-module and suppose that $G$ is a generator in ${}_R\mathcal{M}$.

(1) If $U$ is $G$-injective, then $U$ is injective.

(2) If $U$ is finitely generated and $G$-projective, then $U$ is projective.
