If a prime ideal $\mathfrak{p}$ contains a finite intersection $\bigcap_i \mathfrak{q}_i$, then $\mathfrak{p}$ contains some $\mathfrak{q}_i$. If the $\mathfrak{q}_i$ are primary, then $\mathfrak{p}$ contains the associated prime ideal $\mathfrak{p}_i$ of one of them.
