**Theorem 14.** Let $R$ be an integral domain, and let $f(X_1, \cdots, X_n)$ be a non-zero polynomial over $R$ in $n$ indeterminates. Let $Q$ be a subset of $R$ containing infinitely many elements. Then there exist elements $a_1, \cdots, a_n \in Q$ such that
$$f(a_1, \cdots, a_n) \neq 0.$$
