If $x_1, x_2, \cdots, x_n$ are fixed elements of an extension field $K$ of $k$, then $K$ contains the ring $k[x_1, x_2, \cdots, x_n]$, which is the least subring of $K$ that contains $k$ and the elements $x_1, x_2, \cdots, x_n$. This ring is an integral domain (since $K$ is a field).
