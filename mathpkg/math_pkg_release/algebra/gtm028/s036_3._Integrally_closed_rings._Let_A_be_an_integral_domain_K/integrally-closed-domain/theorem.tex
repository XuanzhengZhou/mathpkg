\text{An integral domain } A \text{ is called \textbf{integrally closed} if it contains every element of its quotient field } K \text{ that is integral over } A. \text{ Equivalently, } A \text{ is integrally closed if } A \text{ equals its own integral closure in } K.
