Given a vector space $V$ over a field $F$, a non-empty subset $W$ of $V$ is called a subspace, or a vector subspace, of $V$ if the relations $x, y \in W$ imply $x - y \in W$ (whence $W$ is a subgroup of the group $V$), and if the relations $a \in F, x \in W$ imply $ax \in W$.
