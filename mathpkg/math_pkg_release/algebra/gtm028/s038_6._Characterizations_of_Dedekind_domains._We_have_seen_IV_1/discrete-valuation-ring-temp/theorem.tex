A \textit{discrete valuation ring} is temporarily defined as a Dedekind domain having only one proper prime ideal. (This definition is equivalent to the one that will be given in Chapter VI.)

If $A$ is a Dedekind domain and $\mathfrak{p}$ a proper prime ideal of $A$, then the quotient ring $A_{\mathfrak{p}}$ is a discrete valuation ring.
