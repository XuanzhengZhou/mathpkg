If $\mathfrak{p} = \prod_i \mathfrak{p}_i$ where the $\mathfrak{p}_i$ are prime ideals, and if $\mathfrak{p}$ is invertible, then every ideal $\mathfrak{q}$ which contains $\mathfrak{p}$ and is prime coincides with some $\mathfrak{p}_i$, and each $\mathfrak{p}_i$ is invertible.
