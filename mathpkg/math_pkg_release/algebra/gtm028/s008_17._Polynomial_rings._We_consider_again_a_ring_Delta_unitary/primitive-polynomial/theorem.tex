Let $R$ be a unique factorization domain and let $f(x) \in R[x]$ be a non-zero polynomial. The polynomial $f(x)$ is called \text{primitive} if its coefficients have no common divisors other than units. Equivalently, $f(x)$ is primitive if and only if its content $c(f)$ is a unit in $R$.
