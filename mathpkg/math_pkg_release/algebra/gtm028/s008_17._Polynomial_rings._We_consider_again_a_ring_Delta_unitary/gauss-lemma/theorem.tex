Let $R$ be a unique factorization domain and let $f(x), g(x) \in R[x]$ be non-zero polynomials. Then:
\begin{enumerate}
\item $c(fg) = c(f) c(g)$ (up to multiplication by a unit).
\item The product of two primitive polynomials is primitive.
\end{enumerate}
