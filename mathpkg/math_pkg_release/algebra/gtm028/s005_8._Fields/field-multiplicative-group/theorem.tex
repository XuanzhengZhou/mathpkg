The three field axioms can be replaced by a single axiom: the elements of $F$ which are different from zero form a group with respect to multiplication. This group is called the \textbf{multiplicative group} of $F$.
