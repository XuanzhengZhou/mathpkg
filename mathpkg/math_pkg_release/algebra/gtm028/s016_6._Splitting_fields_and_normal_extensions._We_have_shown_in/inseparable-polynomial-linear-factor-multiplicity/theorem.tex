If $f(X)$ is an irreducible inseparable polynomial in $k[X]$, of reduced degree $n_0$ and exponent of inseparability $e$, then each linear factor in
$$f(X) = a_0(X - x_1)(X - x_2) \cdots (X - x_n), \quad x_i \in K$$
appears exactly $p^e$ times, where $p$ is the characteristic of $k$.
