Two subspaces $L$ and $L'$ of $S$ are linearly disjoint over $k$ if and only if the following condition holds:

(LD) Whenever $x_1, x_2, \dots, x_n$ are elements of $L$ which are linearly independent over $k$, then these elements $x_i$ are also linearly independent over $L'$.
