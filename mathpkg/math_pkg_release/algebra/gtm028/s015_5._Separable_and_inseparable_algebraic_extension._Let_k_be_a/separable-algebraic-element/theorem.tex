Let $K$ be an extension field of $k$. An algebraic element $x \in K$ over $k$ is called **separable** over $k$ if its minimal polynomial over $k$ is a separable polynomial. An algebraic extension $K/k$ is called **separable** if every element of $K$ is separable over $k$.
