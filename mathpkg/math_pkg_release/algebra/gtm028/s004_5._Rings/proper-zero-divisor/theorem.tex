\text{By a \textbf{proper zero divisor} is meant a zero divisor which is different from } 0.

\text{Hence a ring } R \text{ has proper zero divisors if and only if it is possible to have in } R \text{ a relation } ab = 0 \text{ with both } a \text{ and } b \text{ different from zero.}
