\text{In a ring } R \text{ with identity, no unit is a zero divisor.}

\text{If an element } a \text{ has an inverse } a^{-1}, \text{ then } a \text{ is neither a left zero divisor nor a right zero divisor.}
