An $R$-module is said to be simple (or irreducible) if it has exactly two submodules. These must necessarily be itself and $(0)$; the module $(0)$ is not simple according to this definition. A simple module can be described as one having a composition series of length one.
