In a euclidean domain $E$:

\text{(b)} If $a \neq 0$, then $\varphi(a) \geq \varphi(1)$.

\text{(c)} If $a$ and $b$ are associates, then $\varphi(a) = \varphi(b)$.

\text{(d)} If $\epsilon$ is a unit, then $\varphi(\epsilon) = \varphi(1)$, and conversely.
