A coclosed subset of $\operatorname{End} K$ (resp. $\operatorname{End}_k K$) is a subset $H$ such that for each $x \in H$, there exist $x_1, \ldots, x_n \in H$ with $x(ab) = \sum_i x(a)x_i(b)$ for all $a, b \in K$. A $K$-subbiring is a coclosed $K$-subring.
