Let $\varphi$ be a homomorphism from a field $k$ to a field $k'$. Then we let $\varphi$ also denote the extension to the homomorphism $\varphi: k[X] \to k'[X]$ such that $\varphi(\sum_0^n a_i X^i) = \sum_0^n \varphi(a_i) X^i$.
