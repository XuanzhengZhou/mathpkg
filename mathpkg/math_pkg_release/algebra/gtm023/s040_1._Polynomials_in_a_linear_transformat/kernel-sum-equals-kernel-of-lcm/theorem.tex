Let $\varphi: E \to E$ be a linear transformation on a finite-dimensional vector space over a field of characteristic $0$. Let $f, g \in \Gamma[t]$ be non-zero polynomials and let $v$ be their least common multiple. Then

$$K(v) = K(f) + K(g).$$
