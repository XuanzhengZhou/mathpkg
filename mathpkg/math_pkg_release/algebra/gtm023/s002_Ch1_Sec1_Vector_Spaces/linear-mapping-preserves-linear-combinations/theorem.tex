A linear mapping $\varphi$ preserves linear combinations:
$$\varphi\!\left(\sum_i \lambda^i x_i\right) = \sum_i \lambda^i \varphi x_i.$$
Conditions $\varphi(x+y) = \varphi x + \varphi y$ and $\varphi(\lambda x) = \lambda \varphi x$ are equivalent to this single condition.
