A family $(x_\alpha)_{\alpha \in A}$ is linearly independent if and only if every vector $x$ can be written in at most one way as a linear combination of the $x_\alpha$, i.e., the scalars $\lambda^\alpha$ in $x = \sum_\alpha \lambda^\alpha x_\alpha$ are uniquely determined by $x$.
