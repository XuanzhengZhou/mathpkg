Let $(x_\alpha)_{\alpha \in A}$ be a family of vectors in $E$. A vector $x \in E$ is a **linear combination** of the $x_\alpha$ if there exist scalars $\lambda^\alpha \in \Gamma$, only finitely many non-zero, such that
$$x = \sum_{\alpha \in A} \lambda^\alpha x_\alpha.$$
