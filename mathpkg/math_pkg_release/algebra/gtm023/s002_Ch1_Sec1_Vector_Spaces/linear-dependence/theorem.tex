A family $(x_\alpha)_{\alpha \in A}$ is **linearly dependent** if there exist scalars $\lambda^\alpha$, not all zero, such that
$$\sum_{\alpha} \lambda^\alpha x_\alpha = 0.$$
Such an equation is a **non-trivial linear relation**. If a subfamily is linearly dependent, the full family is also linearly dependent.
