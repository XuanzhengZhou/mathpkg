A subset $S \subset E$ is a **system of generators** for $E$ if every vector $x \in E$ is a linear combination of vectors from $S$. If every vector of $S$ is a linear combination of vectors from $T \subset S$, then $T$ is also a system of generators.
