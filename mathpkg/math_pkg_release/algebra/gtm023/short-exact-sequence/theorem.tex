A sequence of linear mappings
$$0 \longrightarrow E_1 \xrightarrow{\varphi} E \xrightarrow{\psi} E_2 \longrightarrow 0$$
is called a \textbf{short exact sequence} if $\varphi$ is injective, $\psi$ is surjective, and
$$\operatorname{Im} \varphi = \ker \psi.$$
