A group $G$ is called \textbf{commutative} or \textbf{abelian} if for each $x, y \in G$,
$$xy = yx.$$
In an abelian group one often writes $x + y$ instead of $xy$ and calls $x + y$ the sum of $x$ and $y$. Then the unit element is denoted by $0$.
