From every algebra $A$ we can obtain a second algebra $A^{\text{opp}}$ by defining

$$(xy)^{\text{opp}} = yx.$$

$A^{\text{opp}}$ is called the algebra opposite to $A$. It is clear that if $A$ is associative then so is $A^{\text{opp}}$. If $A$ is commutative we have $A^{\text{opp}} = A$.
