$L(E; E)$ together with composition as multiplication is an algebra, called the algebra of linear transformations of $E$ and denoted by $A(E; E)$. The identity transformation $\iota$ acts as unit element in $A(E; E)$. The algebra $A(E; E)$ is associative.

However, it is not commutative if $\dim E \geq 2$.
