A linear automorphism $\varphi: E \to E$ preserves the orientation if and only if
$$\det \varphi > 0.$$
For $\varphi = -\iota$, we have $\det(-\iota) = (-1)^n$, so $-\iota$ preserves orientation iff $n$ is even.
