Given an inner product space $E$, the correspondence $\varphi \mapsto \Phi$ defined by
$$\Phi(x, y) = (\varphi x, y)$$
establishes a linear isomorphism
$$\varrho: L(E; E) \xrightarrow{\cong} B(E, E)$$
between linear transformations and bilinear functions on $E$.
