A symmetric bilinear function $\Phi$ in $E \times E$ is called negative definite if

$$\Phi(x, x) < 0$$

for all vectors $x \neq 0$ in $E$. Similarly, $\Phi$ is called negative semidefinite if $\Phi(x, x) \leq 0$ for all $x \in E$.
