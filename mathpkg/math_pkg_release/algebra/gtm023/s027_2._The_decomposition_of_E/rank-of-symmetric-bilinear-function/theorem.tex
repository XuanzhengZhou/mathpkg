The rank of a symmetric bilinear function $\Phi$ in $E \times E$, where $E$ is a vector space of dimension $n$, is defined as

$$\operatorname{rank}(\Phi) = \dim E - \dim E_0,$$

where $E_0$ is the nullspace of $\Phi$. The bilinear function $\Phi$ is non-degenerate if and only if it has rank $n$.
