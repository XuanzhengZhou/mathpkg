Two quadrics $Q_1$ and $Q_2$ are called affine equivalent if there exists a one-to-one affine mapping of the affine space $A$ onto itself which carries $Q_1$ into $Q_2$. The relation of affine equivalence induces a decomposition of the set of all quadrics into affine equivalence classes.
