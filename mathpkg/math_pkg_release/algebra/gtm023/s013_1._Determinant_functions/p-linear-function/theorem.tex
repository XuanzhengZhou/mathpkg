A $p$-linear map from $E$ to the ground field $\Gamma$ is called a \text{$p$-linear function} in $E$.
As an example, let $f_1, \ldots, f_p$ be linear functions in $E$ and define $\Phi$ by
$$\Phi(x_1, \ldots, x_p) = f_1(x_1) \cdots f_p(x_p), \quad x_i \in E.$$
