Let $S$ be a linearly independent subset of a vector space $E$ and let $\varphi_0: S \rightarrow F$ be a set map into a second vector space $F$. Then $\varphi_0$ can be extended to a linear mapping $\varphi: E \rightarrow F$.
