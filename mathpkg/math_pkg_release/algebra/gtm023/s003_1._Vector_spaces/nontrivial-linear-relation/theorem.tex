An equation of the form

$$\sum_{\alpha} \lambda^{\alpha} x_{\alpha} = 0$$

with at least one $\lambda^{\alpha} \neq 0$ is called a \textbf{non-trivial linear relation} among the vectors $x_{\alpha}$.
