A set $\{\varphi_\alpha\}$ of linear transformations of $E$ is called semisimple if to every subspace $F_1 \subset E$ which is stable under each $\varphi_\alpha$ there exists a complementary subspace $F_2$ which is stable under each $\varphi_\alpha$.
