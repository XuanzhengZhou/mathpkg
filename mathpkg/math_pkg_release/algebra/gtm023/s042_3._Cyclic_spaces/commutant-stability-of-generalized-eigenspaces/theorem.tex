Let
$$E = E_1 \oplus \cdots \oplus E_r$$
be the decomposition of $E$ into the generalized eigenspaces of $\varphi$, and let $\pi_i$ be the corresponding projection operators. Then every $\psi \in C(\varphi)$ commutes with each $\pi_i$, and consequently each $E_i$ is stable under $\psi$.
