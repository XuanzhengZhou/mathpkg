Suppose that condition (iii) of Theorem I holds, and let $E = E_1 \oplus \cdots \oplus E_r$ be the decomposition of $E$ into generalized eigenspaces. Then to every $i$ ($i=1,\ldots,r$) there corresponds precisely one integer $j$ ($1 \leq j \leq s$) such that
$$E_i \cap F_j \neq 0.$$
