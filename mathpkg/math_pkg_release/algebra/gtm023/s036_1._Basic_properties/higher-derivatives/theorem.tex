The polynomial $d^r f$ ($r \geq 1$) is called the $r$-th derivative of $f$ and is denoted by $f^{(r)}$. We extend the notation to $r=0$ by setting $f^{(0)} = f$. It follows that $f^{(r)} = 0$ if and only if $r$ exceeds the degree of $f$.
