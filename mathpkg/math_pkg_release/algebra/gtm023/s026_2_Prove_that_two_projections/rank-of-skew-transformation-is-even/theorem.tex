The rank of a skew transformation is always even. Since every skew mapping is normal, the image space is the orthogonal complement of the kernel. Consequently, the induced transformation $\psi_1: \operatorname{Im} \psi \rightarrow \operatorname{Im} \psi$ is regular and again skew, so the dimension of $\operatorname{Im} \psi$ must be even.
