The transformation $j$ on an oriented Euclidean plane $E$ has the following properties:
1. $(jx, y) + (x, jy) = 0$
2. $(jx, jy) = (x, y)$
3. $j^2 = -1$
4. $\det j = 1$.

The transformation $j$ is called the canonical complex structure of the oriented Euclidean plane $E$.
