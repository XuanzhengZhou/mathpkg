Let $A$ be a partially ordered set and $A_1 \subset A$. An element $A \in A$ is a **least upper bound** (l.u.b.) for $A_1$ if:
1. $A$ is an upper bound for $A_1$.
2. If $B$ is any upper bound for $A_1$, then $A \leq B$.
