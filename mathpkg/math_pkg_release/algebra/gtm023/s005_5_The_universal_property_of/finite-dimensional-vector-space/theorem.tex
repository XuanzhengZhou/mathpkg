A vector space $E$ is called \textbf{finite-dimensional} if it has a finite basis. Equivalently, $E$ is finitely generated. The dimension $\dim E$ is then the number of vectors in any basis. If $E$ does not have finite dimension, it is called \textbf{infinite-dimensional}.
