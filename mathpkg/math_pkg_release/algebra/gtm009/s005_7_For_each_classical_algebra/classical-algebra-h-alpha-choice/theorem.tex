For each classical Lie algebra ($A_\ell$, $B_\ell$, $C_\ell$, $D_\ell$) described in (1.2), one can choose elements $h_\alpha \in H$ corresponding to a base $\Delta$ of the root system $\Phi$ such that the standard relations $[h_\alpha, x_\beta] = \beta(h_\alpha) x_\beta$ hold for all $\beta \in \Phi$.
