Define the \text{derived series} of a Lie algebra $L$ by $L^{(0)} = L$, $L^{(1)} = [LL]$, $L^{(2)} = [L^{(1)} L^{(1)}]$, and in general $L^{(i)} = [L^{(i-1)} L^{(i-1)}]$. $L$ is called \text{solvable} if $L^{(n)} = 0$ for some $n$.
