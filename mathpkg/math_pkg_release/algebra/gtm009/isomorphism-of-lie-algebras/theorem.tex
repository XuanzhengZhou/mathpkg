Two Lie algebras $L, L'$ over $F$ are called \text{isomorphic} if there exists a vector space isomorphism $\phi: L \rightarrow L'$ satisfying
$$\phi([xy]) = [\phi(x)\phi(y)]$$
for all $x, y \in L$. The map $\phi$ is called an \text{isomorphism of Lie algebras}.
