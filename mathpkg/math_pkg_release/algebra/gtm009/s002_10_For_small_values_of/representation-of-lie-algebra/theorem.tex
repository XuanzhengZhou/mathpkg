A \text{representation} of a Lie algebra $L$ is a homomorphism $\phi: L \rightarrow \mathfrak{gl}(V)$ ($V$ = vector space over $F$). Although we require $L$ to be finite dimensional, it is useful to allow $V$ to be of arbitrary dimension: $\mathfrak{gl}(V)$ makes sense in any case.
