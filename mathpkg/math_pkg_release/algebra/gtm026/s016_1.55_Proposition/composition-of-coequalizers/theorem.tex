Let $\mathcal{K}$ have coequalizer-mono factorizations. Then given $f: A \rightarrow B$ and $g: B \rightarrow C$ in $\mathcal{K}$ we have

(1) if $f, g$ are coequalizers then so is $f \cdot g$, and

(2) if $f \cdot g$ is a coequalizer, so is $g$.
