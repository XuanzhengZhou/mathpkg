---
role: proof
locale: en
of_concept: theorem
source_book: gtm026
source_chapter: "1"
source_section: "2.17"
---

(2.18) implies (2.19). By 2.5, there exists a unique $\Omega$-homomorphism $X\lambda: XT \longrightarrow XT'$ (where $XT'$ is an $(\Omega, E)$-algebra by virtue of $\psi_{XT'}^{-1}$) such that preservation of variables holds. To prove that composition is preserved, consider the diagram

$$
\begin{array}{c c c c c} A & \alpha & BT & \beta^ {\#} & CT \\ B \lambda & B T' & (B \lambda) ^ {\#} & CT' \\ BT' & CT' & (B \lambda) ^ {\#} & CT' \end{array}
$$

where the bottom morphism of the square can be regarded as $\Omega$-homomorphism with the help of $\psi^{-1}$. The remaining details are similar to the proof of 2.10 and need no repeating.

(2.19) implies (2.20). Set $\Gamma = \forall \lambda$. That composing with $\Gamma$ is a monoid isomorphism is clear, and it suffices to prove

$$
\begin{array}{c c c c c} \phi T & \phi T' \\ x ^ {\#} & x' ^ {\#} \\ VT & VT' \\ \Gamma & \Gamma' \end{array}
$$

To this end, $x^ {\#}.\Gamma = (\mathrm {i d} _ {\phi T} \circ x).\Gamma = (\mathrm {i d} _ {\phi T}.\phi \lambda) \circ^ {\prime} (x.\Gamma) = \phi \lambda \circ^ {\prime} x' = \phi \lambda.x' ^ {\#}.$

(2.20) implies (2.18). Let us begin by observing that we have symmetry: it is easy to check that $\Gamma^{-1}$ enjoys the same properties as $\Gamma$. Next, let us make some comments about the empty

For each nonempty set $X$ define the desired $\psi_X$ of 2.18 by $\delta \psi_X = \delta'$ where for all $\omega'$ in $\Omega'_n$,

$$\langle [v_1 \cdots v_n \omega'] \rangle, \Gamma^{-1}.r^{\#}$$

(2.21)

where $r: \mathbb{V} \longrightarrow X$ is any function such that $v_i r = x_i$; in case $n = 0$ let $r$ be arbitrary (but here we require that $X$ be nonempty). For an example see exercise 9. We need:

**
