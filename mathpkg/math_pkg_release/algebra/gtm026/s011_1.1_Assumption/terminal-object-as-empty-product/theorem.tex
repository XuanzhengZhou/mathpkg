The smallest product is the empty product (i.e., $I = \emptyset$), which is standardly called a \textbf{terminal object} of $\mathcal{K}$. A terminal object $T$ is an object such that for every object $A$ in $\mathcal{K}$ there exists a unique morphism $A \longrightarrow T$.
