---
role: proof
locale: en
of_concept: lemma-7
source_book: gtm026
source_chapter: "1"
source_section: "6.9"
---

The second statement is obvious from the first using 1.19 and 1.18. We turn to the proof of the first statement (which is hardly a surprise since the bialgebra condition is equational; cf. 1.4.22). All three closure properties can be established simultaneously by proper interpretation of the following generic cube induced by $f:(X, \xi, \theta) \rightarrow (Y, \xi', \theta')$ in $Set^S \times Set^T$ and $\alpha:(U^T)^n \rightarrow U^T$ (where $(X, \xi)^n = (X^n, \xi^n)$, $(Y, \xi')^n = (Y^n, (\xi')^n)$).

Of the six faces of the cube, all commute except possibly the left and right sides ($f$ is both an S-homomorphism and a T-homomorphism).

If the right side commutes, that is, if $(Y, \xi', \theta')$ is an S-T bialgebra, then the left side at least commutes when followed

reasoning, if $(Y, \xi', \theta')$ is a bialgebra and $f$ is injective then $(X, \xi, \theta)$ is a bialgebra.

If the left side commutes then the right side commutes at least when preceded by $f^n S$. Therefore, if $(X, \xi, \theta)$ is a bialgebra and if $f$ is surjective then $f^n S$ is also surjective (by 1.4.29 with $H = ()^n S$) and $(Y, \xi', \theta')$ is also a bialgebra.
