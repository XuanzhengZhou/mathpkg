1. $U$ is algebraic if and only if $U$ is Beck and $U$ satisfies the solution set condition at $K$ for every object $K$ of $\mathcal{K}$.

2. If $\mathcal{A}$ has a cogenerator and if either $\mathcal{A}$ or $\mathcal{K}$ is well powered, $U$ is algebraic if and only if $U$ is Beck. $\square$

Using 1.13 instead of 1.9, 1.21 takes the following form: