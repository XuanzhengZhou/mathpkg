To define a function $f$ on an ordinal $x$, it suffices to:
\begin{enumerate}
\item define $f(0)$,
\item define $f(y+1)$ in terms of $f(y)$ whenever $y+1 < x$, and
\item define $f(y)$ for limit ordinals $y < x$ in terms of the values $\{f(z) : z < y\}$.
\end{enumerate}
