Let $R$ be the set of all sets $x$ such that $x \notin x$. Then $R$ is a set of sets, but if $R \in R$ then $R \notin R$, whereas if $R \notin R$ then $R \in R$. The resolution is that $R$ is not a bona fide set but a proper class.
