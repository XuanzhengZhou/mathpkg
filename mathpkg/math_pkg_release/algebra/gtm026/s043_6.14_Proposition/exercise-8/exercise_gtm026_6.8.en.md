---
role: exercise
locale: en
chapter: "6"
section: "6"
exercise_number: 8
---

([Kennison and Gildenhuys '71]). Let $U: \mathcal{A} \to \mathbf{Set}$ be a functor such that $AU$ is finite for all $A$ in $\mathcal{A}$. Let $\mathcal{A}$ have and $U$ preserve finite products. Suppose given, also, a finitary theory $S$ in $\mathbf{Set}$ such that $\Psi: \mathcal{A} \to \mathbf{Set}^S$ is a full subcategory over $\mathbf{Set}$ closed under $S$-subalgebras.

(a) Show that $\mathcal{A}$ is a full subcategory of $S \otimes \boldsymbol{\beta}$-algebras. [Hint: a finite product of discrete spaces is discrete.]

(b) Show that $U$ is tractable. [Hint: in the notation of exercise 13 of section...]

(c) Show that if each $n\lambda$ is surjective then $\Gamma$ is an isomorphism onto $\mathcal{B}$. [Hint: use the universal property of $\Phi$.]

(d) Complete the argument by proving that $n\lambda$ is surjective. [Hint: as $n\lambda$ is a $\mathbf{T} \otimes \boldsymbol{\beta}$-homomorphism, $\operatorname{Im}(n\lambda)$ is closed; if $\lambda' : \mathbf{S} \to \mathbf{T}$ corresponds to $\mathbf{Set}^{\mathbf{T}} \to \mathbf{Set}^{\mathbf{S}}$, $\operatorname{Im}(\lambda') \subset \operatorname{Im}(\lambda)$ so it suffices to show $\operatorname{Im}(n\lambda')$ is dense; as $V$ is limit preserving and $A\Phi V$ is finite, the topology on $n\mathbf{T}$ corresponding to $(nT, n\mu)V$ is the optimal lift of $\psi_{n, (A, f)} : nT \to AU$ where $(A, f)$ ranges over $[n, U]$ and $AU$ is a finite discrete space; the subbasic open set $\{a\} \psi_{n(A, f)}^{-1}$, if nonempty, intersects the image of $n\lambda'$. $\psi_{n; (A, f)}$ is onto; since $[n, U]$ is a poset with infima ($U$ preserves products), subbasic open sets are basic.]
