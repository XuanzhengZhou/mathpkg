so that, for all $p$ in $E$, $xp \neq yp$. Thus 2 holds so, as proved above, 3 holds, and using 1.9, $pE = E$ for all $p$ in $E$. Given $p$, then, there is a $q$ in $E$ with $pq = \text{id}_X$. As $q$ is injective, $p$ and $q$ are mutually inverse bijections so that $q = p^{-1}$ and $q$ is in $E$.