---
role: exercise
locale: en
chapter: "1"
section: "010"
exercise_number: 24
---

Let $\alpha$ be a regular cardinal, let $T$ have rank $\leqslant \alpha$ and let $(X, \xi)$ be T-algebra possessing an inclusion-minimal set $A$ of generators with $\text{card}(A) = \alpha$. Show that every set generating $(X, \xi)$ has cardinal $\geqslant \alpha$ (and hence that any two minimal sets of generators have the same (cardinal). [Hint: if $\langle B \rangle = X$ then for $b$ in $B$ choose $S_b \subset A$ with $\text{card}(S_b) < \alpha$ and $b \in \langle S_b \rangle$; as $\bigcup S_b = A$, $\text{card}(B) \geqslant \alpha$.]

Exercises for the Primer on Set Theory

A. A left-to-right picture of the first few ordinals is

$$0 1 2 \cdots n \cdots \omega s(\omega) s^2(\omega) \cdots$$

Extend the picture to the right as far as you can. Then prove that the set of all countable ordinals is uncountable. [Hint: consider the least uncountable ordinal.]

B. Prove Cantor’s theorem: for any set $A$, $\text{card}(2^A) > \text{card}(A)$ (where $2^A$ means the set of all subsets of $A$). [Proof outline: no $f: A \longrightarrow 2^A$ can be surjective since (cf. Russell’s Paradox

E. Use Zorn’s lemma to prove that for any two sets $X, Y$, either $X$ admits an injection into $Y$ or $Y$ admits an injection into $X$. [Proof outline: use the partially ordered set of all $(A, f)$ with $A \subset X$ and $f: A \longrightarrow Y$ injective; show that a maximal $(A, f)$ is such that $f$ is onto or $A = X$.]

F. Selfreference is not really a set-theoretic phenomenon. Consider the truth value of the sentence “this sentence is false.”
