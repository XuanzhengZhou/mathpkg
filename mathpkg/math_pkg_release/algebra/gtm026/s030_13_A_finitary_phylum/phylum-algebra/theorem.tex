A **$(P, \Omega)$-algebra** is a pair $(X, \delta)$ where $X = (X(p))$ is a $P$-indexed family of sets and $\delta$ assigns to each $\omega \in \Omega_{p_1 \cdots p_{n+1}}$ a function

$$\delta_\omega: X(p_1) \times \cdots \times X(p_n) \rightarrow X(p_{n+1}).$$
