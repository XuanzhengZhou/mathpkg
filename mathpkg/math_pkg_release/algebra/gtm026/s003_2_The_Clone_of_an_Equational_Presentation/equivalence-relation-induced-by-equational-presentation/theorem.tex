For each set $A$, define an equivalence relation $E_A$ on $A\Omega$ by
$$
E_A = \{(p, q): \text{for all } (\Omega, E)\text{-algebras } (X, \delta) \text{ and all functions } f: A \longrightarrow X,\; pf^{\#} = qf^{\#}\}.
$$
The quotient set is denoted by $AT = A\Omega / E_A$.
