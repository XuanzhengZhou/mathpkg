$$\begin{array}{cccc}
QTX & \xi X & QX \\
Q\lambda

shows that the $\lambda$-law holds if and only if $\delta_x$ is a $\mathbf{T}$-homomorphism for all $x$ in $X$. Identifying $\text{Dyn}(X)$ with $\text{Set}^{\mathbf{X}}$ as in 2.13, we see that $\text{Set}^{\lambda}$ is isomorphic in $\text{Struct}(\text{Set})$ to $\text{Set}^{\mathbf{X} \otimes \mathbf{T}}$ which is algebraic over $\text{Set}$ (see 3.6.21). We shall prove below in 3.15 that $\mathcal{K}^{\lambda}$ is always algebraic over $\mathcal{K}$.