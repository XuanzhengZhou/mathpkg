If $M$ is a projective model, also $N(M, F)$ is a projective model.

Proof. Let $M$ be a projective model, over $R$, determined by $\{x_0, x_1, \cdots, x_n\}$, so that $M = \bigcup_{i=0}^{n} V(\mathfrak{o}_i)$, where $\mathfrak{o}_i = R[x_0/x_i, x_1/x_i, \cdots, x_n/x_i]$.

Let $\bar{\mathfrak{o}}_i$ be the integral closure of $\mathfrak{o}_i$ in $F$. Then $N(M, F) = \bigcup_{i=0}^{n} V(\bar{\mathfrak{o}}_i)$. Let $\{w_{i1}, w_{i2}, \cdots\}$ be a finite module basis of $\bar{\mathfrak{o}}_i$ over $\mathfrak{o}_i$.

If $i$ and $j$ are any two indices in the set $(0, 1, \cdots, n)$ and if $w_i$ is any element of $\bar{\mathfrak{o}}_i$, then upon writing the relation of integral dependence of