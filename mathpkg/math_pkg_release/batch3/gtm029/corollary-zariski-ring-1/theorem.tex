Let $A$ be a Zariski ring, and let $F$ and $G$ be two submodules of a finite $A$-module $E$. Then $\hat{A}(F \cap G) = \hat{A}F \cap \hat{A}G$.

We consider the mapping $g$ of the direct sum $F \oplus G$ into $E$ defined by $g(x, y) = x - y (x \in F, y \in G)$. The kernel $K$ of $g$ is the set of elements $(x, x)$ with $x \in F$ and $x \in G$, and is therefore isomorphic to $F \cap G$. From the exact sequence

$$0 \rightarrow K \xrightarrow{i} F \oplus G \xrightarrow{g} E$$

we deduce, by Theorem 11, the exact sequence

$$0 \rightarrow \hat{K} \xrightarrow{i} \hat{F} \oplus \hat{G} \xrightarrow{g} \hat{E},$$

where $\bar{g}$ is defined by $\bar{g}(\xi, \eta) = \xi - \eta (\xi \in \hat{F}, \eta \in \hat{G})$. Thus $\hat{K}$ may be identified with $\hat{F} \cap \hat{G}$, i.e., with $\hat{A}F \cap \hat{A}G$. Since $\hat{K} = \hat{A}K

In fact, $G:Aa$ is the submodule of all elements $x$ in $E$ such that $ax \in G$. To obtain the corollary, it suffices to apply Corollary 3 to the case $F=E$, $F'=G$ and to take for $f$ the mapping $x \rightarrow ax$ of $E$ into $E$.