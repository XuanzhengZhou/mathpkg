In $R = k[[X_1, \cdots, X_n]]$, any chain of prime ideals distinct from $R$ has at most $n+1$ terms.

The existence of maximal chains of prime ideals of $R$, i.e., of chains of prime ideals ($\neq R$) with $n+1$ terms, is immediately proved by the example of $(0) < (X_1) < (X_1, X_2) < \cdots < (X_1, \cdots, X_n)$. Now we have a more precise result: