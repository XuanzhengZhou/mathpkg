---
role: proof
locale: en
of_concept: proposition-let-ideal-primary-order-degree
source_book: gtm029
source_chapter: "Appendix"
source_section: "5"
---

If $\bar{g}$ does not divide $c(A)$ then, by the above corollary, $A = m^r$, $c(A) =

fact. If $x - x_j y_j \in \mathfrak{m}^{r+j+1}$ we set $x_{j+1} = x_j$, $y_{j+1} = y_j$. In the contrary case, we express the initial form $\overline{x - x_j y_j}$ (of degree $r+j$) as a linear combination $A_{r+j-\sigma} \bar{\varphi} + B_{j+\sigma} \bar{\psi}$, we fix elements $v$ and $w$ in $\mathfrak{o}$ such that $\bar{v} = B_{j+\sigma}$, $\bar{w} = A_{r+j-\sigma}$ and we set $x_{j+1} = x_j + v$, $y_{j+1} = y_j + w$. Then it is seen at once that $x - x_{j+1} y_{j+1} \in \mathfrak{m}^{r+j+1}$, $\bar{x}_{j+1} = \bar{\varphi}$, $y_{j+1} = \bar{\psi}$. This establishes (7) for all $j$.

We now define $\mathfrak{B}$ by $\mathfrak{B} = \mathfrak{A} : \mathfrak{m}^{r-\sigma}$, whence

(8) $\mathfrak{A} \supset \mathfrak{m}^{r-\sigma} \mathfrak{B}$.

Since $\mathfrak{B}$ is primary for $\mathfrak{m}$ we have $\mathfrak{m}^{r+j} \subset \mathfrak{m}^{r-\sigma} \mathfrak{B}$ for large $j$. For such a large $j$ relation (7) yields the inclusion $x_j y_j \in \mathfrak{A}$. Hence $\frac{x_j}{t_1^\sigma} \cdot \frac{y_j}{t_1^{r-\sigma}} \in \mathfrak{A}'$, where $\mathfrak{o}'\mathfrak{A} = t^r\mathfrak{A}'$. Since $\bar{y}_j$ is not divisible by $\bar{g}$ and is exactly of degree $r-\sigma$,

The characteristic form $c(\mathfrak{D})$ of $\mathfrak{D}$ divides $c(m^q \mathfrak{U})$, i.e., $c(\mathfrak{U})$. If then $\sigma$ is the degree of $c(\mathfrak{D})$ we have $\sigma \leq s$. Since $\mathfrak{D}$ is a contracted ideal, we have, by Proposition 3:

$$\mathfrak{D} = m^{r+q-\sigma} \mathfrak{E},$$

where $\mathfrak{E} = \mathfrak{D}: m^{r+q-\sigma}$ is again a contracted ideal. Since $o' \mathfrak{D} = o' m^q \mathfrak{U}$, it follows that $o' m^{r-\sigma} \mathfrak{E} = o' \mathfrak{U}$. By Corollary 1, applied to the ideal $\mathfrak{D}$ instead of to $\mathfrak{U}$, $m^{r-\sigma} \mathfrak{E}$ is a contracted ideal. Hence $m^{r-\sigma} \mathfrak{E} = o' m^{r-\sigma} \mathfrak{E} \cap o = o' \mathfrak{U} \cap o = \mathfrak{U}$. Therefore $\mathfrak{D} = m^q \mathfrak{U}$, which proves our assertion.
