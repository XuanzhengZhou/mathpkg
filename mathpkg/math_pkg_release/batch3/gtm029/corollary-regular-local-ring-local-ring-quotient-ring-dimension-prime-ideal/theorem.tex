Let $A$ be a regular local ring of dimension $d$, $m$ its maximal ideal. In order that a system $\{x_1, \cdots, x_d\}$ of elements of $m$ be a regular system of parameters of $A$, it is necessary and sufficient that the residue classes of the $x_i$ mod $m^2$ generate $m/m^2$ over $A/m$, or equivalently, that these residue classes be linearly independent over $A/m$.

Since the dimension of $m/m^2$ over $A/m$ is $d$, the two conditions about the $m^2$-residues of $x_1, \cdots, x_d$ are equivalent. The necessity of our condition is obvious. For the sufficiency we notice that if the condition is satisfied, then we have $m = \sum_{i=1}^{d} Ax_i +

We shall give later a partial proof of the fact that every quotient ring $A_p$ (p: prime ideal) of a regular local ring $A$ is a regular local ring. We now describe those factor rings $A/b$ of a regular local ring which are regular.