---
role: proof
locale: en
of_concept: lemma-simple-contracted-ideal-different-necessarily
source_book: gtm029
source_chapter: "Appendix"
source_section: "5"
---

If $P$ is a principal ideal, then the statement is trivial. We therefore assume that $P$ is primary for $m$. Let $r$ be the order of $P$, whence $o'P = t_1^rP'$ (we assume that $\bar{g} \neq z_1$). Let $P' = A'B'$ be a factorization of $P'$ in $o'$. We have to show that either $A'$ or $B'$ is the unit ideal. Let $a$ be the smallest (non-negative) integer with the property that $t_1^aA'$ is the extension of an ideal in $o$ (there exist integers with that property, since any ideal in

equal to the order $a$ of $\mathfrak{A}$ (Proposition 3), and similarly for $\mathfrak{V}$. Hence the order of $\mathfrak{m}^r \mathfrak{U V}$ is $a+b+r$ and the degree of $c(\mathfrak{m}^r \mathfrak{U V})$ is $a+b$. The degree of $c(\mathfrak{m}^{a+b} \mathfrak{P})$ is obviously $r$. Hence $r=a+b$, and from Theorem 1 it now follows that $\mathfrak{P}=\mathfrak{U V}$. Hence either $\mathfrak{A}$ or $\mathfrak{V}$ must be the unit ideal, showing that either $\mathfrak{A}'$ or $\mathfrak{V}'$ is the unit ideal. This completes the proof of the lemma.

The proof of the theorem can now be rapidly completed.

Suppose that we have two factorizations of a complete ideal $\mathfrak{A}$ into simple complete ideals. Among the simple factors there may occur the maximal ideal $\mathfrak{m}$. We therefore put into evidence the power of $\mathfrak{m}$ which occurs in both factorizations:

$$\mathfrak{A} = \mathfrak{m}^h \mathfrak{P}_1 \mathfrak{P}_2 \cdots \mathfrak{P}_n = \mathfrak{m}^h \tilde{\mathfrak{P}}_1 \tilde{\mathfrak{P}}_2 \cdots \tilde{\mathfrak{P}}_n,$$

where the $\mathfrak{P}_i$ and $\tilde{\mathfrak{P}}_j$ are simple complete ideals, all different from $\mathfrak{m}$. Let $r$ be the order of $\mathfrak{A}$ and $s$ the degree of the characteristic form $c(\mathfrak{A})$ of $\mathfrak{A}$. The latter is a product of the characteristic forms $c(\mathfrak{P}_i)$, $i=1, 2, \cdots, n$, and since each $\mathfrak{P}_i$

$\mathfrak{A} (\neq 0)$ can be factored into simple contracted ideals. The proof is as follows:

Let $\mathfrak{A} = \mathfrak{A}_1 \mathfrak{A}_2 \cdots \mathfrak{A}_n$ be a non-trivial factorization of $\mathfrak{A}$. Let $\{\bar{g}_1, \bar{g}_2, \cdots, \bar{g}_m\}$ be the set of directional forms of $\mathfrak{A}_1, \mathfrak{A}_2, \cdots, \mathfrak{A}_n$ and let $\mathfrak{o}' = \mathfrak{o}'_1 \cap \mathfrak{o}'_2 \cap \cdots \cap \mathfrak{o}'_m$, where $\mathfrak{o}'_i$ is the quadratic transform of $\mathfrak{o}$, relative to the directional form $\bar{g}_i$. Let $\mathfrak{o}'_i \cap \mathfrak{o} = \mathfrak{B}_i$ and let $\mathfrak{C} = \mathfrak{B}_1 \mathfrak{B}_2 \cdots \mathfrak{B}_n$. It is clear that $\{\bar{g}_1, \bar{g}_2, \cdots, \bar{g}_m\}$ is the set of directional forms of $\mathfrak{A}$. Therefore $\mathfrak{o}' \cap \mathfrak{o} = \mathfrak{A}$ (Proposition 4, Corollary 1). By Theorem 2 (and Proposition 4, Corollary 1) we also have $\mathfrak{o}' \mathfrak{C} \cap \mathfrak{o} = \mathfrak{C}$ (since each directional form of $\mathfrak{B}_i$ is also a directional form of $\mathfrak{A}$ and since, therefore, the directional forms of $\mathfrak{C}$ are in the set $\{\bar{g}_1, \bar{g}_2, \cdots, \bar{g}_

transform of $o$, relative to the directional form $\bar{g}$, and let $M_{\bar{g}}$ be the set of all simple complete ideals in $o$ having $\bar{g}$ as directional form. Then $\mathcal{P} \rightarrow \mathcal{P}'$, where $\mathcal{P} \in M_{\bar{g}}$ and $\mathcal{P}'$ is the transform of $\mathcal{P}$ (in $o'$), is a $(1,1)$ mapping of $M_{\bar{g}}$ onto the set of all simple complete ideals in $o'$.

For the proof, we shall assume that $z_1 \neq \bar{g}$. We note that if $\mathcal{P} \rightarrow \mathcal{P}'$ then $\mathcal{P} = t_1^{a}\mathcal{P}' \cap o$, where $a$ is the least integer such that $t_1^{a}\mathcal{P}'$ is the extension of an ideal in $o$. This shows that the mapping $\mathcal{P} \rightarrow \mathcal{P}'$ is univalent. Now, given any simple complete ideal $\mathcal{P}'$ in $o'$, define $a$ and $\mathcal{P}$ as above. Then $\mathcal{P}$ is a complete ideal in $o'$, and we have $o' \mathcal{P} = t_1^{a}\mathcal{P}'$. Clearly, $c(\mathcal{P})$ must be a power of $\bar{g}$ (Proposition 3), and $\mathcal{P}$ must be simple, for in the contrary case we would find at once that $\mathcal{P}'$ is not simple. This completes the proof.

(B) Starting with a given simple complete ideal $\mathcal{P}$, different from $m$, we consider its transform $\mathcal{P}'$ (this transform is an ideal in $o'$ introduced in (A), if $\mathcal{P} \in M_{\bar{g}}$). If $\mathcal{P}' \neq m'$ ($m' = \text{maximal ideal in } o'$) we may repeat the procedure and consider the transform $\mathcal{P}''$ of $\mathcal{P}'$ in a suitable quadratic transform $o''$ of $o'$. Since $

$\mathcal{P} = \mathfrak{m}^a \cap \mathfrak{q}$. We have only to prove that $\mathcal{P} \supset \mathfrak{m}^a \cap \mathfrak{q}$. Let $x$ be any element of $\mathfrak{m}^a \cap \mathfrak{q}$. Since $x \in \mathfrak{m}^a$, we have $x = t_1^a x'$, with $x'$ in $\mathfrak{o}'$. Since $x \in \mathfrak{q}$, we have $v_{\mathcal{P}}(t_1^a x') \geq v_{\mathcal{P}}(\mathcal{P}) = v_{\mathcal{P}}(t_1^a P')$, $v_{\mathcal{P}}(x') \geq v_{\mathcal{P}}(\mathcal{P}')$, whence $x' \in \mathcal{P}'$ (since $\mathcal{P}'$ is a $v_{\mathcal{P}}$-ideal) and thus $x \in t_1^a P' \cap \mathfrak{o} = \mathcal{P}$, which proves the equality $\mathcal{P} = \mathfrak{m}^a \cap \mathfrak{q}$. We now have to show that $\mathfrak{q} \subset \mathfrak{m}^a$. Suppose the contrary to be true, and let $b$ be the order of $\mathfrak{q}$ (whence $b < a$). The characteristic form $c(\mathfrak{q})$ of $\mathfrak{q}$ is then at most of degree $b$, and since $\mathfrak{m}^{a-b} \mathfrak{q} \subset \mathcal{P}$, also the degree of $c(\mathcal{P})$ is at most equal to $b$. On the other hand, the order $a$ of $\mathcal{P}$ is greater than $b$. Hence, by Proposition 3, $\mathfrak{m}$ factors out from $\mathcal{P}$, in contradiction with the fact that $\mathcal{P}$ is a simple ideal.

(D) The method of proof in (C) can be used to derive

as the one used in (C) we find that, in the first place, we must have $\mathfrak{Q}_j = \mathfrak{m}^{a_j} \cap \mathfrak{q}$, and—next—that $\mathfrak{q} \subset \mathfrak{m}^{a_j}$ (always using the fact that the degree of the characteristic form $c(\mathfrak{Q}_j)$ of $\mathfrak{Q}_j$ is equal to the order $a_j$ of $\mathfrak{Q}_j$). Hence $\mathfrak{Q}_j = \mathfrak{q}$, and this proves assertion (2).

Let $\mathfrak{Q}'_i = \mathfrak{q}'_j$ be the transform of $\mathfrak{q}_i$ and let $\mathfrak{q} = \mathfrak{Q}_j$ be the inverse transform of $\mathfrak{q}'_j$. If $a$ and $b$ are the orders of $\mathfrak{q}$ and $\mathfrak{q}_i$ respectively, then $b \geq a$. The two ideals $\mathfrak{q}_i$ and $\mathfrak{m}^{b-a}\mathfrak{q}$ have the same extension in $\mathfrak{o}'$, namely the ideal $t_1^b\mathfrak{q}'_j$. Since they are both contracted ideals, it follows that $\mathfrak{q}_i = \mathfrak{m}^{b-a}\mathfrak{q} = \mathfrak{m}^{b-a}\mathfrak{Q}_j$, and this proves assertion (3).

Let $a$ and $b$ be the orders of $\mathfrak{Q}_a$ and $\mathfrak{Q}_\beta$ respectively ($a \leq b$), whence $\mathfrak{o}'\mathfrak{Q}_a = t_1^a\mathfrak{q}'_a$, $\mathfrak{o}'\mathfrak{Q}_\beta = t_1^b\mathfrak{q}'_\beta$, and thus $\mathfrak{o}'\mathfrak{m}^{b-a}\mathfrak{

divisor of $0'$, then we have $v = v_P$, where $\mathcal{P}$ is the simple ideal (of rank 1) in $0$ whose transform is $m'$. In the contrary case, $v$ has a well defined directional form in $0'$, and if $0''$ denotes the corresponding quadratic transform of $0'$, then $v$ is of the second kind with respect to $0''$. If $v$ is the $m''$-adic prime divisor of $0''$, then we have $v = v_P$, where $\mathcal{P}$ is the simple complete ideal (of rank 2) in $0$ whose (second) transform in $0''$ is $m'$. In the contrary case we go on to a suitable quadratic transform $0''$ of $0''$. We have to show that after a finite number of steps we obtain a ring $0^{(h)}$ such that $v$ is the $m^{(h)}$-adic prime divisor of $0^{(h)}$, where $m^{(h)}$ is the maximal ideal of $0^{(h)}$. We shall show that the assumption that the above process does not terminate after a finite number of steps leads to a contradiction. Under such an assumption we will have an infinite strictly ascending chain of rings

$$0 < 0^{(1)} < 0^{(2)} < \cdots < 0^{(h)} < \cdots$$

with the following properties:

(1) Each ring $0^{(h)}$ is a quadratic transform of its immediate predecessor.

(2) $v$ is non-negative on any $0^{(h)}$, and its center in $0^{(h)}$ is the maximal ideal $m^{(h)}$ of $0^{(h)}$.

We now fix an element $\omega$ in the quotient field of $0$ such that the $v$-residue of $\omega$ is transcendental over the residue field $k (= 0/m)$ of $0$. Since the residue field of $0^{(h)}$ is an algebraic extension of the residue field of $0^{(h-1)}$, it follows that the $v$-residue of $\omega$ is also transcendental over $0^{(h)}/m^{(h)}$. Now

If $h=0$ then assertion (1) is vacuous, while (2) is obvious, since $v_g$ is in that case the $m$-adic prime divisor of $v$, and therefore every $v_g$-ideal is in that case a power of $m$. We therefore proceed by induction with respect to $h$.

Let $\mathfrak{D}'_i$ be the transform of $\mathcal{P}_i$ in $v'$ ($i=1, 2, \cdots, h$). Then $\mathfrak{D}'_i$ is a simple complete ideal $\mathcal{P}'_{i-1}$ in $v'$, of rank $i-1$, and $v_g = v_g'$, where $\mathcal{P}' = \mathfrak{D}'_h = \text{transform of } \mathcal{P}$. We have therefore, by our induction hypothesis:

$$m' = \mathfrak{D}'_1 > \mathfrak{D}'_2 > \cdots > \mathfrak{D}'_h = \mathcal{P}'.$$

Since $\mathcal{P}_i$ is the inverse transform of $\mathfrak{D}'_i$ (in the sense of (D)), it follows from statement (4) in (D) that

$$\mathcal{P}_1 > \mathcal{P}_2 > \cdots > \mathcal{P}_h = \mathcal{P}.$$

Since the strict inclusion $m > \mathcal{P}_1$ is obvious, assertion (1) is proved.

That each of the $h+1$ ideals $m, \mathcal{P}_1, \mathcal{P}_2, \cdots, \mathcal{P}_h$ is a $v_g$-ideal follows from statement (2) of (D) and from our induction hypothesis. Now, if $q'$ is any $v_g$-ideal in $v'$, then by our induction hypothesis, we have $q' = \mathfrak{D}'_1^{n_1} \mathfrak{D}'_2^{n_2} \cdots \mathfrak{D}'_h^{n_h}$. The inverse transform of $q'$ is clearly $\mathcal{P}_1^{n_

APPENDIX 6

MACAULAY RINGS

Let A be a noetherian ring, $\mathfrak{a}$ an ideal in $A$ and $a$ an element of $A$. We say that $a$ is prime to $\mathfrak{a}$ if $\mathfrak{a}: Aa = \mathfrak{a}$. This means that $a$ does not belong to any associated prime ideal of $\mathfrak{a}$ (Vol. I, Ch. IV, § 7, Corollary 2 to Theorem 11). We say that a sequence $\{a_1, \cdots, a_n\}$ of non-invertible elements of $A$ is a prime sequence if, for every $j, a_j$ is prime to the ideal $Aa_1 + \cdots + Aa_{j-1}$. (For $j=0$ we follow our usual convention that the empty set generates the zero ideal. Thus, a single element $a$ constitutes a prime sequence if and only if it is not a zero divisor.) It follows easily from Vol. I, Ch. IV, § 14, Theorems 30 and 31, that, if $\{a_1, \cdots, a_n\}$ is a prime sequence in $A$, and if $\mathfrak{p}$ is an associated prime ideal of $Aa_1 + \cdots + Aa_n$, then we have

$$h(\mathfrak{p}) \geq n,$$

equality holding if and only if $\mathfrak{p}$ is isolated.

We note that $\{a_1, a_2, \cdots, a_q\}$ is a prime sequence if and only if for each $i=1, 2, \cdots, q$, $a_i$ is not a zero divisor in the ring $A/(Aa_1 + Aa_2 + \cdots + Aa_{i-1})$.

We are going to devote several lemmas to the study of prime sequences in local rings.
