\text{Every unique factorization domain } A \text{ is integrally closed. In particular, if } x/y \in K \text{ (with } x,y \in A, y \neq 0\text{) is integral over } A \text{ and expressed in lowest terms, then } y \text{ must be a unit in } A, \text{ so } x/y \in A.
