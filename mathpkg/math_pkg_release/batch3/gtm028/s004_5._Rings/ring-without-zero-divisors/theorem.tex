\text{A ring } R \text{ is called a \textbf{ring without zero divisors} if } R \text{ has no proper zero divisors.}

\text{In a ring without zero divisors, the condition } ab = 0 \text{ implies } a = 0 \text{ or } b = 0.
