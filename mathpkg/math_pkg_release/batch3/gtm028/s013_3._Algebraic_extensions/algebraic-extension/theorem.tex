Let $K \supset k$ be a field extension. Then $K$ is called an \textbf{algebraic extension} of $k$ if every element of $K$ is algebraic over $k$. Extensions which are not algebraic are called \textbf{transcendental extensions}.
