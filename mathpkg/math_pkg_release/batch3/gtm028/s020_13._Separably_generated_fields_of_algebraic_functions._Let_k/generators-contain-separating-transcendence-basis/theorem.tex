Let $\{x_1, x_2, \cdots, x_n\}$ be a set of generators of $K/k$, $K = k(x_1, x_2, \cdots, x_n)$. If $K$ is separably generated over $k$, then already the set $\{x_1, x_2, \cdots, x_n\}$ contains a separating transcendence basis of $K/k$.
