If an $R$-module $M$ has one composition series of length $r$, then every composition series of $M$ has length $r$, and every normal series without repetitions has length at most $r$. The integer $r = l(M)$ is called the length of $M$.
