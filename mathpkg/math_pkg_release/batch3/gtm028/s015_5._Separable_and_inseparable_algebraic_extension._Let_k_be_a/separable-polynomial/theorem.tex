An irreducible polynomial $f(X) \in k[X]$ is called **separable** if $f'(X) \neq 0$, and **inseparable** if $f'(X) = 0$. An arbitrary polynomial in $k[X]$ is called separable if each of its irreducible factors is separable.
