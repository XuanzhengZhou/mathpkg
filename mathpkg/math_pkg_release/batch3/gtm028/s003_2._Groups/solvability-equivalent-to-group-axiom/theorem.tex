Let $G$ be a non-empty set closed under a given multiplication and satisfying the associative law (axioms $G_1$ and $G_2$). Then the solvability of both equations $ax = b$ and $xa = b$ for all $a, b \in G$ is equivalent to axiom $G_3$ (the existence of an identity element and inverses).
