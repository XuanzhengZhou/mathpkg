Let $R$ be a ring with identity and let $\mathfrak{a}$ be an ideal in $R$ having an irredundant primary representation $\mathfrak{a} = \bigcap \mathfrak{q}_i$ without imbedded components. Then
$$\lambda(\mathfrak{a}) = \sum_i \lambda(\mathfrak{q}_i).$$
