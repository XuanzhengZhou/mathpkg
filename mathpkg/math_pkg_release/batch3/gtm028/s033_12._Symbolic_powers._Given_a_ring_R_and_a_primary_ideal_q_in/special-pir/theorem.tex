A PIR (principal ideal ring) is called \textit{special} if it has only one prime ideal $\mathfrak{p} \neq R$ and if $\mathfrak{p}$ is nilpotent, that is, if $\mathfrak{p}^n = (0)$ for some integer $n > 0$.
