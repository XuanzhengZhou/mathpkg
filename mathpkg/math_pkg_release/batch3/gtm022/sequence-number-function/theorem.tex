There exists a strongly definable pairing function $P$ and associated projection functions $L, R$ such that for all $x, y$, $L(P(x,y)) = x$ and $R(P(x,y)) = y$. Furthermore, there is a strongly definable sequence number function $\operatorname{seq}(b, r)$ that encodes finite sequences of natural numbers.
