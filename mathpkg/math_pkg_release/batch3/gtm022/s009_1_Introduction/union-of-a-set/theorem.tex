\text{We can now form}
$$\cup c = \{x \mid x \in b \text{ for some } b \in c\}$$
\text{where } \cup c \text{ is independent of the particular } a \text{ (from ZF4) used.}
\text{For any sets } a \text{ and } b, \text{ we can form}
$$a \cup b = \cup \{a, b\}.$$
