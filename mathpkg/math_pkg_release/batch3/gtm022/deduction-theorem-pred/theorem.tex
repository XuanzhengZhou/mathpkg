Let $A \subseteq P(V, \mathcal{R})$ and $p, q \in P(V, \mathcal{R})$. If $A \cup \{p\} \vdash q$ and there is a proof of $q$ from $A \cup \{p\}$ in which Generalisation is not applied to any variable free in $p$, then $A \vdash p \Rightarrow q$.
