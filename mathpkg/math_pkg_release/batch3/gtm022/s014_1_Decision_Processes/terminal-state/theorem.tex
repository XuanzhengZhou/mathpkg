A state $[\sigma q s_\alpha \tau_1]$ is called \textbf{terminal} if there is no quadruple $(q, s_\alpha, c, d)$ in $M$.

In other words, the machine $M$ provides no instruction for the pair $(q, s_\alpha)$ of current internal state and scanned symbol, so the machine halts.
