For $1 \leq p < \infty$ and every $f \in \mathfrak{L}_p$ and $\varepsilon > 0$, there exists a simple function $\sigma \in \mathfrak{L}_p$ such that $|\sigma| \leq |f|$ and $\|\sigma - f\|_p < \varepsilon$. In particular, $\mathfrak{S} \cap \mathfrak{L}_p$ is dense in $\mathfrak{L}_p$.
