Let $f$ and $g$ be relations. We define the composition $[product, iterate]$ are also used of $f$ and $g$ to be the relation $g \circ f = \{(x, z) :$ for some $y, (x, y) \in f$ and $(y, z) \in g\}$.

The composition of $f$ and $g$ may be void. In fact, $g \circ f \neq \emptyset$ if and only if $(rngf) \cap (domg) \neq \emptyset$.