A \textbf{simple function} $s$ on $X$ assumes only finitely many values. If $\operatorname{rng} s = \{\alpha_1, \ldots, \alpha_n\}$ and $A_k = \{x : s(x) = \alpha_k\}$, then $s = \sum_{k=1}^{n} \alpha_k \xi_{A_k}$.
