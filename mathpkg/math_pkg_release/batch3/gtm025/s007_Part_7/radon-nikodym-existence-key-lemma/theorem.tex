Let $\mu$ and $\nu$ be finite measures on $(X, \mathcal{A})$. Then there exists $g \in \mathfrak{L}_2(X, \mathcal{A}, \mu + \nu)$ with $0 \leq g \leq 1$ $(\mu+\nu)$-a.e. such that $\int_X f(1 - g) \, d\nu = \int_X f g \, d\mu$ for every $f \in \mathfrak{L}_2(X, \mathcal{A}, \mu + \nu)$.
