Let $\{X_i\}_{i \in I}$ be a nonvoid family of topological spaces and let $X = \prod_{i \in I} X_i$. For each $i \in I$, define $\pi_i$ on $X$ by $\pi_i(x) = x_i$. The product topology on $X$ has as a subbase all sets of the form $\pi_i^{-1}(U_i)$ where $i \in I$ and $U_i$ is open in $X_i$.