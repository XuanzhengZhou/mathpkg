A sequence $(x_n)$ in a metric space $X$ is a Cauchy sequence if for each $\varepsilon > 0$ there exists $n_0 \in N$ such that $\varrho(x_m, x_n) < \varepsilon$ whenever $m, n \geq n_0$. A metric space $X$ is complete if each Cauchy sequence in $X$ converges to a point of $X$.