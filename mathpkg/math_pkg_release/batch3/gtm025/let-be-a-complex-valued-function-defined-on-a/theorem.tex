Let $f$ be a complex-valued function defined on a subinterval $J$ of $R$. Suppose that for every $\varepsilon > 0$, there is a $\delta > 0$ such that

(i) $\sum_{k=1}^{n} |f(d_k) - f(c_k)| < \varepsilon$

for every finite, pairwise disjoint, family $\{]c_k, d_k[\}_{k=1}^{n}$ of open subintervals of $J$ for which

(ii) $\sum_{k=1}^{n} (d_k - c_k) < \delta$.

see that $\sum_{k=1}^{\infty} (\psi(b_k) - \psi(a_k)) = 1$, and so $\sum_{k=1}^{n} (\psi(b_k) - \psi(a_k)) \geq \frac{1}{2}$ for sufficiently large $n$, while $\sum_{k=1}^{n} (b_k - a_k)$ is arbitrarily small.

(c) None of the functions $F$ of (18.8) is absolutely continuous. This is most easily seen from Theorem (18.15) infra.

We first set down some elementary properties of absolutely continuous functions.