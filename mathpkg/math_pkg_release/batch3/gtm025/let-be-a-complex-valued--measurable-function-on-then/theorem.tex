Let $f$ be a complex-valued $A$-measurable function on $X$. Then

(i) $f \in \mathfrak{L}_1$ if and only if $|f| \in \mathfrak{L}_1$,

and

(ii) if $f \in \mathfrak{L}_1$, then $\left| \int_X f d\mu \right| \leq \int_X |f| d\mu$.