Let $f$ be as in (21.74), and suppose further that $E$ is a Lebesgue measurable set such that $f(E') = \{0\}$. For $p > 1$, we have

(i) $$\int_E (f^{A(i)}(x))^p dx \leq \left(\frac{p}{p-1}\right)^p \int_E f(x)^p dx$$

for $j = r, l$; and

(ii) $$\int_E f^{A}(x)^p dx \leq \left(\frac{2p}{

and

(ii) $\lambda(M_t) \leq \frac{2}{(1-k)t} \int_G f d\lambda$.