Let $f$ and $g$ be $\mathcal{A}$-measurable. Then the sets

where

$$A_{\beta} = \begin{cases} \left( \{x : f(x) = \infty\} \cap \{x : g(x) = -\infty\} \right) \\ \bigcup \left( \{x : f(x) = -\infty\} \cap \{x : g(x) = \infty\} \right) & \text{if } a < \beta, \\ \emptyset & \text{if } a \geq \beta. \end{cases}$$

The set $\{x \in X : f(x) > a - g(x)\}$ is in $\mathcal{A}$ by (11.9); therefore, since $A_{\beta}$ is also in $\mathcal{A}$, the set $h^{-1}(]a, \infty[)$ is in $\mathcal{A}$. $\square$