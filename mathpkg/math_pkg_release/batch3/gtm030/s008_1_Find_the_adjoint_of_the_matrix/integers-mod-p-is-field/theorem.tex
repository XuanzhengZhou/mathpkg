If $m = p$ is a prime, then the difference ring $I/(p)$ is a field. Consequently, for any prime $p$ there exists a field containing $p$ elements.

More generally, if $m$ is composite, $I/(m)$ is not an integral domain.
