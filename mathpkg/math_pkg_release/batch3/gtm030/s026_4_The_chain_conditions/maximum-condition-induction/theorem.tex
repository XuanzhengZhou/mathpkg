Let $P$ be a property of submodules of a module such that $P(\mathfrak{N})$ holds if $P(\mathfrak{N}')$ holds for every $\mathfrak{N}' \supset \mathfrak{N}$. Then $P(\mathfrak{N})$ is true for all $\mathfrak{N}$.
