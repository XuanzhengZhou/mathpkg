Suppose that a ring $\mathfrak{A}$ has an identity $1$ and suppose $1$ has finite order $m$ in the additive group $(\mathfrak{A}, +)$. Then for any element $a \in \mathfrak{A}$,
$$ma = m(1a) = (m1)a = 0a = 0.$$
Hence every element of $\mathfrak{A}$ has finite additive order dividing $m$.
