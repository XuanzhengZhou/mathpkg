**Lemma 2.** In a commutative semi-group $\mathfrak{S}$ with identity and cancellation law satisfying Condition C,
$$(a, (b, c)) \sim ((a, b), c).$$
That is, the greatest common divisor operation is associative up to associates.
