**Lemma 4.** In a commutative semi-group $\mathfrak{S}$ with identity and cancellation law satisfying Condition C,
$$\text{if } (a, b) \sim 1 \text{ and } (a, c) \sim 1, \text{ then } (a, bc) \sim 1.$$
