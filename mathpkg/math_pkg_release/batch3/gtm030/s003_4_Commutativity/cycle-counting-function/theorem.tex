For a permutation $\alpha$ with disjoint cycle decomposition into cycles of lengths $r, s, \cdots, u$, define

$$N(\alpha) = (r - 1) + (s - 1) + \cdots + (u - 1).$$

Then $\alpha$ can be expressed as a product of $N(\alpha)$ transpositions, and $N(\alpha)$ determines the parity of $\alpha$.
