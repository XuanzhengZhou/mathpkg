Let $\mathfrak{A}$ be a Gaussian domain (UFD). A polynomial $f(x) = a_0 + a_1x + \cdots + a_nx^n \in \mathfrak{A}[x]$ is called \text{primitive} if the greatest common divisor of its non-zero coefficients $a_i$ is $1$ (or a unit).
