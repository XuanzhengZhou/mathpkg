If $\mathfrak{A}$ is a Gaussian domain (UFD), then $\mathfrak{A}[x]$ is also Gaussian, where $x$ is transcendental over $\mathfrak{A}$. Consequently, if $\mathfrak{A}$ is Gaussian and $x_1, x_2, \cdots, x_r$ are algebraically independent, then $\mathfrak{A}[x_1, x_2, \cdots, x_r]$ is Gaussian.
