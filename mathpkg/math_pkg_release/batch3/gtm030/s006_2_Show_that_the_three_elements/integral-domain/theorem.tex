A ring $\mathfrak{A}$ is called an \emph{integral domain} (or \emph{domain of integrity}) if the set $\mathfrak{A}^*$ of non-zero elements forms a sub-semi-group of the multiplicative semi-group. Equivalently, if $a \neq 0$ and $b \neq 0$ in $\mathfrak{A}$, then $ab \neq 0$.
