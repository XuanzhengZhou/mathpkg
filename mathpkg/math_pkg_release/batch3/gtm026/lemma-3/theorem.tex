Before proving 2.25, let us record that its hypotheses are available in the context of 2.24. We have already constructed a weakly free $(F', \

$\mathcal{A}$-morphisms such that $\eta.\psi U = \eta.\psi' U$, then $\psi = \psi'$. Form $j = \text{eq}(\psi, \psi')$ in $\mathcal{A}$ so that $j U = \text{eq}(\psi U, \psi' U)$ in $\mathcal{K}$. Since $\eta.\psi U = \eta.\psi' U$, there exists unique $f$ with $f.j U = \eta$. Since $(F', \eta')$ is weakly free, there exists $\Gamma: F' \longrightarrow E$ with $\eta'.\Gamma U = f$. Set $x: F' \longrightarrow F' = \Gamma.j.i$. Reading off the diagram above, we see that $\eta'.x U = \eta.i U = \eta'$, and $x \in C$. As $\text{id}_{F'} \in C$, $i.x = i$, that is we have $i.\Gamma.j.i = \text{id}_{F}.i$. Since $i$ is a monomorphism, $i.\Gamma.j = \text{id}_{F}$. It follows that $\psi = i.\Gamma.j.\psi = i.\Gamma.j.\psi' = \psi'$. We are done.