Let $(\mathbb{E}, \mathbb{M})$ be an image factorization system on a category $\mathcal{H}$.

\begin{enumerate}
  \item Every split monomorphism is in $\mathbb{M}$.
  \item Every split epimorphism is in $\mathbb{E}$.
\end{enumerate}
