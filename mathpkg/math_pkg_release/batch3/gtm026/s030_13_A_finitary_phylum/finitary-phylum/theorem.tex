A **finitary phylum** is a pair $(P, \Omega)$ where $P$ is a nonempty set and $\Omega = (\Omega_w)$ is a disjoint family of (possibly empty) sets indexed by words $w = p_1 \cdots p_n$ on the alphabet $P$ of length $n > 0$.

\cite{BirkhoffLipson70}
\cite{Gratzer69}
\cite{Higgins63}
