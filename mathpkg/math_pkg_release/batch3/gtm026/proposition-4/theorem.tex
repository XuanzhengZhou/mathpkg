1. $T$ is nontrivial.
2. For each set $X$, $X\eta: X \longrightarrow XT$ is injective.
3. $T: \text{Set} \longrightarrow \text{Set}$ is a faithful functor, that is, whenever $f, g: X \longrightarrow Y$ are distinct functions then $fT, gT: XT \longrightarrow YT$ are again distinct functions.