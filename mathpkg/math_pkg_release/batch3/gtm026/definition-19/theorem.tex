for all $\alpha \in \Delta(i, j)$. As diagrams are like functors, so morphisms of diagrams are like natural transformations. $\mathcal{K}^{\Delta}$ forms a category with pointwise composition $(\chi \chi')_i = \chi_i \chi'_i$ and pointwise identities $(\mathrm{id}_D)_i = \mathrm{id}_{D_i}$.

Each object $K$ of $\mathcal{K}$ induces the constant diagram $\tilde{K}$ in $\mathcal{K}^{\Delta}$ defined by $\tilde{K}_i = K$, $\tilde{K}_{\alpha} = \mathrm{id}_K$. Notice that an upper bound $(L, \psi)$ of $D$ is the same thing as a diagram morphism $\psi: D \longrightarrow L$. The constant diagram construction defines the embedding functor

where $\tilde{f} \colon \tilde{K} \longrightarrow \tilde{L}$ is the diagram morphism $f_i = f$; (warning: these are not the only diagram morphisms $\tilde{K} \longrightarrow \tilde{L}$).

The existence

embedding functor of 7.1, and let $(L, \psi)$ be an upper bound of $D$. Then $(L, \psi)$ is a colimit of $D$ if and only if $(L, \psi)$ is free over $D$ with respect to $(\cdot)$.