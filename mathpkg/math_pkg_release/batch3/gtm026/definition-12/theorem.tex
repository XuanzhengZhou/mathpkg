$$XT \xrightarrow{\xi} X$$
$$A \mapsto \sup(A' : A \in A)$$

where $A' = \text{Inf}(

It is now clear that the structure map $\xi: XT \longrightarrow X$ of the complete atomic Boolean algebra $X$ does satisfy the T-algebra laws 4.9 and 4.10 which amount to

$$\text{Sup}(A^\xi: x \in A) = x \quad \text{for all } x \in X$$

$$\text{Sup}(A^\xi: A^* \in \bar{\mathcal{A}}) = \text{Sup}(A^\xi: A\xi^{-1} \in \bar{\mathcal{A}}) \quad \text{for all } \bar{\mathcal{A}} \in XTT.$$

The first law, by 5.19, is the statement that every $x$ is the supremum of the atoms beneath it. The second law follows at once from 5.20.

If we start with a T-algebra $(X, \xi)$, $X$ becomes a complete atomic Boolean algebra by

$$x + y = (\text{prin}(x) + \text{prin}(y))\xi \quad \text{(the second "+" is symmetric difference)}$$

$$xy = (\text{prin}(x) \cap \text{prin}(y))\xi$$

$$0 = \emptyset\xi \quad (\emptyset \text{ is the empty family})$$

$$1 = (2^X)\xi \quad (2^X \text{ is the set of all subsets of } X)$$

$$\text{Sup } A = (\mathcal{A}_A)\xi \quad \text{where } \mathcal{A}_A = \{B \subset X: B \cap A \neq \emptyset\}$$

$$\text{Inf } A = (\mathcal{A}^A)\xi \quad \text{where } \mathcal{A}^A = \{B \subset X: A \subset B\}$$

$$A^\xi = \{A\}\xi$$

The T-homomorphism condition 4.11 reads as

$$(\text{Sup } (A^\xi: A \in \mathcal{A}))f = \text{Sup}(B^\xi: Bf^{-1} \

4. $\mathcal{U}$ cannot be extended to a larger collection with the finite intersection property and $\mathcal{U}$ is nonempty.

5. If $A$ is a subset of $X$ and $A \notin \mathcal{U}$ then $A$ has empty intersection with some element of $\mathcal{U}$.