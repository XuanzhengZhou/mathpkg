We remark, as an aside, that an operator domain may be viewed as a directed graph whose nodes are natural numbers and whose edges terminate at 1. Thus a directed graph suitable for "groups" as in 1.1 is

This point of view is a natural precursor to viewing an operator domain as a category, an approach which receives only brief treatment in this book (see 1.5.35, the notes to section 3, Exercises 2.1.25–27 and Exercise 3.2.7).

An $\Omega$-algebra is a pair $(X, \delta)$ where $X$ is a set and $\delta$ assigns to each $\omega$ in $\Omega_n$ an $n$-ary operation $\delta_\omega: X^n \longrightarrow X$. Given $\Omega$-algebras $(X, \delta)$ and $(Y, \gamma)$, an $\Omega$-homomorphism from $(X, \delta)$ to $(Y, \gamma)$ is a function $f: X \long