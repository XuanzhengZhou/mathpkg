$$xymzm = xyzmm \quad (m \text{ is associative})$$

$$xem = x = exm \quad (e \text{ is a two-sided unit for } m)$$

$$xixm = e = xxim \quad (xi \text{ is the multiplicative inverse of } x)$$

for all $x, y, z$ in $X$.

(Notice the use, in 1.1, of parenthesis-free “Polish notation,” e.g. $xymzm$ instead of $((x, y)m, z)m$. A formal proof that this notation works is given below in 1.11.)