$\gamma: C \longrightarrow DT'$ we clearly have $\beta^{\#\#}.\gamma^{\#\#} = (\beta.\gamma^{\#\#})^{\#\#}$. For $\alpha: A \longrightarrow BT'$, define $\alpha \circ \beta = \alpha.\beta^{\#\#}$. Then $(\alpha \circ \beta) \circ \gamma = (\alpha.\beta^{\#\#}).\gamma^{\#\#} = \alpha.(\beta.\gamma^{\#\#})^{\#\#} = \alpha \circ \gamma$. Define $X\eta': X \longrightarrow XT'$ by $X\eta' = X\eta.X\lambda$. Then for $f': A \longrightarrow B, (f.B\eta') \circ \gamma = f.B\eta.B\lambda.\beta^{\#\#} = f.\beta$. Also the diagram

shows that $(B\eta.B\lambda)^{\#\#} = \text{id}_{BT'}$, so $\alpha \circ \gamma B\eta' = \alpha.(B\eta.B\lambda)^{\#\#} = \alpha$. Given $a: A \longrightarrow BT$ and $b: B \longrightarrow CT$ we have

(where $B\lambda.(b.C\lambda)^{\#\#} = (b.C\lambda)^{\#\#} = b^{\#}.C\lambda$, as $C\lambda$ is a T-homomorphism). This shows that $T' = (T', \eta', \mu')$ is an algebraic theory and that $\lambda: T \longrightarrow T'$ is a theory map. To see that $(XT', X\mu')V = (XT', \xi_X)$, that is, that $\xi_X = XT'.\lambda.X\mu'$, observe that $\xi_X:(XT'T, XT'\mu) \longrightarrow (XT', \xi_X)$ and $XT'\lambda:(XT'T, XT'\mu) \longrightarrow (XT'T', \xi_{XT'})$ are T-homomorphisms by definition and that $X\mu':(XT'T', \xi_{XT'}) \longrightarrow (XT

T'-homomorphism such that $B\eta'.\beta^{###} = \beta$. Applying $V$, $\beta^{###} : (BT', \xi_B) \longrightarrow (CT', \xi_C)$ is a T-homomorphism such that $B\eta$. $B\lambda.\beta^{###} = \beta$. Therefore, $\beta^{###} = \beta^{##}$.