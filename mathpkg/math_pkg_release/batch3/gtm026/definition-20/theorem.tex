We are using broken arrows to denote $\mathcal{K}$-morphisms and solid arrows to denote morphisms admissible in $\mathcal{C}$. Here $\psi: D \longrightarrow \tilde{A}$ in $\mathcal{C}^A$ is arbitrary (i.e., $(A, \psi)$ is a lower bound of $D$) and $(\Gamma, m)$ is the factorization to be constructed with $(S, \Gamma)$ ranging over a small set depending only on $D$. It suffices to show that such $S$ ranges over a small set $\mathcal{S}$, for then $\Gamma$ ranges over the small set

$$\bigcup_{S \in \mathcal{S}} \bigcup_{i \in N(\Delta)} \mathcal{C}(D_i, S)$$

Here we have used the fact that $\Delta$ is small. Let $(e_i, m_i)$ be an E-M factorization of $\psi_i$. $E$ becomes a $\Delta$-diagram in $\mathcal{K}$ by diagonal fill-in:

and it is clear that $(A, m_i)$ is a lower bound of $E$. Hence, if $(C, \tau)$ is a quasi-colimit of $E$ there exists $t: C \longrightarrow A$ with $\tau_i.t = m_i$. Let $(F, \eta)$ be free over $C$ with respect to $U$ and let $t^{\#}: F \longrightarrow A$ be the unique $\mathcal{C}$-admissible extension of $t$. Let $(e, m)$ be an E-M factorization of $t^{\#}$ in $\mathcal{K}$. By hypothesis, $m$ admits an optimal lift $S$. Define $\Gamma_i = e_i.\tau_i.\eta.e$. As $\Gamma_i.m = \psi_i$ is admissible and $m$ is optimal, $\Gamma_i$ is admissible. Moreover, $(S, \Gamma)$ is a lower bound of $D$ because $(A, \psi)$ is and $m$ is a monomorphism. Finally, we must be sure that $S$ ranges over a small set. But $S$ is determined by the family $(E_i: i \in N(\Delta))$ and the latter ranges over a small set because $\mathcal{K}$ is E co

Another useful colimit theorem is: