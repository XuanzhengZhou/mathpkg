$$QT \times X \xrightarrow{Q\lambda} (Q \times X)T$$

$$(p, x) \mapsto \langle p, \text{in}_x T \rangle$$

(where $\text{in}_x: Q \longrightarrow Q \times X$ sends $q$ to $(q, x)$) is a distributive law of $- \times X$ over $T$.