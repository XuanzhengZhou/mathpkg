If $K$ is a complete partially-ordered set (including the empty infimum $1$), then a full subcategory $A$ of $K$ is algebraic over $K$ if and only if $A$ is closed under infima. For the inverse passage, define $x^{-} = \inf\{y \in A : x \leqslant y\}$.
