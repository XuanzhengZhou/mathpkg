\text{Let $x$ be an element of a finite-dimensional associative commutative algebra. If $x = x_S + x_N$ where $x_S$ is semisimple and $x_N$ is nilpotent, then $x_S$ is called the \textbf{semisimple part} of $x$ and $x_N$ is called the \textbf{nilpotent part} of $x$.}
