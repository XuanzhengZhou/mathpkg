A vector space $E$ is called the \textbf{direct sum} of subspaces $E_1, \ldots, E_r$, written
$$E = E_1 \oplus \cdots \oplus E_r,$$
if every vector $x \in E$ can be expressed uniquely as
$$x = x_1 + \cdots + x_r, \quad x_i \in E_i.$$
