Let $X$ be an arbitrary set and $E$ a vector space over $\Gamma$. The set of all mappings $f: X \to E$ with
$$(f+g)(x) = f(x) + g(x), \quad (\lambda f)(x) = \lambda f(x), \quad x \in X$$
forms a vector space, denoted by $(X; E)$. Its zero vector is the constant map $x \mapsto 0$.
