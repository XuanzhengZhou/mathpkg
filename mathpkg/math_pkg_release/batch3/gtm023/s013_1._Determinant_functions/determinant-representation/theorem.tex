Let $E$ be an $n$-dimensional vector space and fix a non-zero determinant function $\Delta$ in $E$. Then every skew symmetric $n$-linear map $\varphi: E^n \to F$ determines a unique vector $b \in F$ such that
$$\varphi(x_1, \ldots, x_n) = \Delta(x_1, \ldots, x_n) \cdot b, \quad x_i \in E.$$
