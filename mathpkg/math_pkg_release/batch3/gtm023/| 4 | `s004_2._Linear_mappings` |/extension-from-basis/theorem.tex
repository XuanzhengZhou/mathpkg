Let $(x_{\alpha})_{\alpha \in A}$ be a basis of a vector space $E$ and let $\varphi_0: \{x_{\alpha}\}_{\alpha \in A} \rightarrow F$ be a set map into a second vector space $F$. Then $\varphi_0$ can be extended in a unique way to a linear mapping $\varphi: E \rightarrow F$.
