If $\lambda$ is an eigenvalue of a selfadjoint transformation $\varphi$, the corresponding \textbf{eigenspace} $E(\lambda)$ is the set of all vectors $x$ satisfying

$$\varphi x = \lambda x.$$

That is,
$$E(\lambda) = \{ x \in E \mid \varphi x = \lambda x \}.$$
