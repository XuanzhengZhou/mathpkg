Let $E$ be an inner product space with inner product $(\cdot, \cdot)$. A system of $p$ vectors $x_v \neq 0$ $(v = 1, \ldots, p)$ in which any two distinct vectors $x_v$ and $x_\mu$ $(v \neq \mu)$ are orthogonal is linearly independent.
