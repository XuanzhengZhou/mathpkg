For $\varphi \in Q_1$ (the traceless subalgebra of $Q$), we have $\tilde{\varphi} = -\varphi$, and the fundamental identity yields
$$
\varphi^2 = -\det \varphi \cdot \iota = -(\varphi, \varphi) \iota.
$$
Thus every element of $Q_1$ squares to a negative scalar multiple of the identity.
