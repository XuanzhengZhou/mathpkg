Let $(x_{\alpha})_{\alpha \in A}$ be a family of vectors containing at least two vectors. Then the family is linearly dependent if and only if at least one of the vectors $x_{\beta}$ is a linear combination of the remaining vectors $x_{\alpha}$ ($\alpha \neq \beta$).
