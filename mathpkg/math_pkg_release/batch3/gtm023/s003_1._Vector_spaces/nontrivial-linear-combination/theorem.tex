Let $(x_{\alpha})_{\alpha \in A}$ be a family of vectors. A \textbf{non-trivial linear combination} of the vectors $x_{\alpha}$ is a linear combination $\sum_{\alpha} \lambda^{\alpha} x_{\alpha}$ where at least one scalar $\lambda^{\alpha}$ is different from zero.
