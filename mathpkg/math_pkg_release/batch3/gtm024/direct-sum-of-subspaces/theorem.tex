Let $M_1, \ldots, M_n$ be subspaces of $X$. They are \emph{independent} if each is disjoint from the span of the others. Their linear hull is the \emph{direct sum} $M_1 \oplus \cdots \oplus M_n$; each $x$ in it has a unique representation $x = \sum m_i$ with $m_i \in M_i$.
