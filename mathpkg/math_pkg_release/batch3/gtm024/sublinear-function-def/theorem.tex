A real-valued function $f$ on $X$ is positively homogeneous if $f(tx) = tf(x)$ whenever $x \in X$ and $t \geqslant 0$. Such a function is convex if and only if $f(x+y) \leqslant f(x) + f(y)$ for all $x, y \in X$. Convex positively homogeneous functions are called sublinear.