A hyperplane $H=[\phi;\alpha]$ \emph{supports} $A$ if $A$ lies in one closed half-space and $A\cap H\neq\emptyset$. A point in $A\cap H$ is a \emph{support point}; it is \emph{proper} if $H$ does not contain $A$.
