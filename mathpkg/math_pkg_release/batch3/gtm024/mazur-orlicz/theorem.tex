Given $g\in\operatorname{Conv}(X)$, $\{x_j\}\subset X$, $\{c_j\}\subset\mathbb{R}$, system $\phi(x_j)\geqslant c_j$ with $\phi\leqslant g$ has solution iff $\sum\alpha_k c_{j_k}\leqslant g(\sum\alpha_k x_{j_k})$ for all finite subsets and non-negative $\alpha_k$.
