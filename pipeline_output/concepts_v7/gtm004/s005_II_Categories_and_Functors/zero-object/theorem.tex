An object $0$ in a category $\mathfrak{C}$ is called a \textbf{zero object} if, for any object $X$ in $\mathfrak{C}$, the sets $\mathfrak{C}(X, 0)$ and $\mathfrak{C}(0, X)$ each consist of precisely one element.
