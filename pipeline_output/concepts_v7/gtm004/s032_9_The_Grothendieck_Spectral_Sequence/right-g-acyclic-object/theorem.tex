Let $G: \mathfrak{B} \to \mathfrak{C}$ be a left exact additive functor between abelian categories with enough injectives. An object $B$ in $\mathfrak{B}$ is called \textbf{(right) $G$-acyclic} if

$$
R^q G(B) = \begin{cases}
G(B), & q = 0, \\
0, & q \geq 1.
\end{cases}
$$
