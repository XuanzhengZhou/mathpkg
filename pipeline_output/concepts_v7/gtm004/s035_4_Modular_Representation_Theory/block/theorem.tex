\begin{definition}[Block]
\label{def:block}
Two simple $kG$-modules $S_i$ and $S_j$ are said to belong to the same \textbf{block} $\mathcal{B}$ if the vertices $S_i$ and $S_j$ lie in the same connected component of the extension graph $\Gamma(G)$.
\end{definition}
