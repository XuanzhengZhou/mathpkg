If $\mu$ is totally finite and assumes arbitrarily small values, then the group of invertible elements in $[\mathcal{M}]$ is not an open set in $[\mathcal{M}]$. (An element $u = [f]$ is invertible if and only if $\mu(\{x : f(x) = 0\}) = 0$.)
