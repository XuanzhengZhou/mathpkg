Let $E$ be a separated topological vector space over $\mathbb{K}$, let $A_k$ ($k = 1, \ldots, n$) be a finite number of compact convex subsets of $E$, and let $S = \bigcup_{k=1}^n A_k$. Then $\operatorname{conv} S$ is compact.
