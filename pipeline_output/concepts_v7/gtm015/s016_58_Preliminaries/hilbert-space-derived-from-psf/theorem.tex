If $\varphi$ is an admissible PSF on a $*$-algebra $A$, then there exists a unique $*$-representation $a \mapsto T_a$ of $A$ on the Hilbert space $H_\varphi$ (the completion of $A_\varphi = A/N_\varphi$) such that $(T_a x_\varphi | T_b x_\varphi) = \varphi(b, a)$ for all $a, b \in A$.
