If $\sigma$ is a closed subset of $\mathfrak{S}$ (the Riemann sphere) and $f \in \mathcal{A}(\sigma)$, the operator $\Phi(f)$ is denoted $f^\sigma(T)$ (or briefly $f(T)$ when $\sigma$ is clear from context). This extends the rational functional calculus notation.
