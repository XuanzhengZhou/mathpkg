\textbf{von Neumann Inequality.} If $T \in \mathcal{L}(H)$ satisfies $\|T\| \leq 1$, then for every complex polynomial $p$,
$$\|p(T)\| \leq \sup_{|z| \leq 1} |p(z)|.$$
Equivalently, the closed unit disk $\Delta_1$ is a spectral set for every contraction.
