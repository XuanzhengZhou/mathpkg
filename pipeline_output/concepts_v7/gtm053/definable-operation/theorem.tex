If $P(A)$ (the set defined by an $L$-formula $P(\bar{v})$ in an $L$-structure $A$) coincides with the graph of an operation $f : A^{n-1} \rightarrow A$, then $f$ is called an \text{$L$-definable operation}.
