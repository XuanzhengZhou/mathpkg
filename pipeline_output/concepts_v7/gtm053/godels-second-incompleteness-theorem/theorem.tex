Let $T$ be a recursively axiomatizable consistent theory containing Peano arithmetic. Then $T$ cannot prove its own consistency. Formally, if $\operatorname{Con}(T)$ is a sentence in the language of $T$ expressing the consistency of $T$, then $T \not\vdash \operatorname{Con}(T)$.
