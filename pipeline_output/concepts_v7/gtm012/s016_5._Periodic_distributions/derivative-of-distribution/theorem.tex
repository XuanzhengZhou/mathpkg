If $v \in \mathcal{P}$ and $u \in \mathcal{P}$, then integration by parts gives

$$\frac{1}{2\pi} \int_0^{2\pi} Dv(x) u(x) \, dx = -\frac{1}{2\pi} \int_0^{2\pi} v(x) Du(x) \, dx = -F_v(Du).$$

We define $DF$, for $F \in \mathcal{P}'$, by

$$(DF)(u) = -F(Du), \quad u \in \mathcal{P}.$$
