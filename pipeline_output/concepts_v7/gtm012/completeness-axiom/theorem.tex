\textbf{Completeness Axiom (O5).} If $A$ is a nonempty subset of $\mathbb{R}$ which is bounded above, then $A$ has a least upper bound.

Equivalently: a nonempty subset of $\mathbb{R}$ which is bounded below has a greatest lower bound.
