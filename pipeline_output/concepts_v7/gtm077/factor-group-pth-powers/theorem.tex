Let $\mathfrak{U}_p$ be the subgroup of elements of $\mathfrak{G}$ that can be represented as $p$th powers, where $p$ is a prime dividing the order $h$. Then $\mathfrak{G}/\mathfrak{U}_p$ is isomorphic to the subgroup of elements $C$ with $C^p = 1$, and both have order $p^e$.
