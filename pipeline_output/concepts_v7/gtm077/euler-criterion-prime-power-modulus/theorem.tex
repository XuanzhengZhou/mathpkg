If $q \mid p-1$ ($p$ odd prime) and $(a,p)=1$, then $x^q \equiv a \pmod{p^\alpha}$ is solvable iff it is solvable modulo $p$, which is equivalent to $a^{(p-1)/q} \equiv 1 \pmod{p}$. There are exactly $q$ solutions modulo $p^\alpha$.
