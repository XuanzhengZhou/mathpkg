Let $x_1, x_2, \ldots, x_n$ be a complete system of residues modulo $n > 0$. If $a$ and $b$ are integers with $(a, n) = 1$, then
$$a x_1 + b,\ a x_2 + b,\ \ldots,\ a x_n + b$$
is also a complete system of residues modulo $n$.
