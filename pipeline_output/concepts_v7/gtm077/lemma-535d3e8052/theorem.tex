All Gauss sums which belong to odd denominators are nonzero.

If $C(\omega)$ is a sum belonging to the odd denominator $\alpha$, then we obtain all sums with denominator $\alpha$ in the form $C(\alpha \omega)$, where $\alpha$ runs through a reduced residue system mod $\alpha$. For if $C(\omega_1)$ also belongs to the denominator $\alpha$, then the integer $\alpha$ can be determined so that that $\delta(\alpha \omega - \omega_1)$ is an integral ideal and for this ideal $C(\alpha \omega) = C(\omega_1)$ by (168). However, by Theorem 155 $C(\alpha \omega)$ only differs from $C(\omega)$ by the factor $\pm 1$. Thus it is enough to verify the non-vanishing of a single Gauss sum which belongs to the denominator $\alpha$.

Let us choose, corresponding to $\alpha$, an integral odd ideal $c$ relatively prime to $\alpha$ such that

$$\alpha c d = \alpha

Since

$$C(\varphi) = C\left(\frac{-\gamma^2 \pi \mu}{4}\right),$$

we have $C(\varphi)$ is also $\neq 0$.

Now let $\alpha$ and $\beta$ be odd relatively prime integers in $k$. Let

$$\omega = \frac{b}{b}$$

where $b$ is integral and relatively prime to $\alpha \beta$. By (169) and Theorem 155 we have

$$C\left(\frac{\omega}{\alpha \cdot \beta}\right) = C\left(\frac{\beta \omega}{\alpha}\right) C\left(\frac{\alpha \omega}{\beta}\right) = \left(\frac{\alpha}{\beta}\right)\left(\frac{\beta}{\alpha}\right) C\left(\frac{\omega}{\alpha}\right) C\left(\frac{\omega}{\beta}\right)$$

$$\left(\frac{\alpha}{\beta}\right)\left(\frac{\beta}{\alpha}\right) = \frac{C\left(\frac{\omega}{\alpha}\right) C\left(\frac{\omega}{\beta}\right)}{C\left(\frac{\omega}{\alpha \beta}\right)}. \tag{203}$$

We now apply Theorem 163 to each of these three sums. In this case we have $b_1 = 4b$ and

$$\frac{C\left(\frac{\omega}{\alpha}\right) \cdot C\left(\frac{\omega}{\beta}\right)}{C\left(\frac{\omega}{\alpha \beta}\right)} = \frac{1}{\sqrt{N(8b)}} \frac{C\left(-\frac{\gamma^2 \alpha}{4 \omega}\right) C\left(-\frac{\gamma^2 \beta}{4 \omega}\right)}{C\left(-\frac{\gamma^2 \alpha \beta}{4 \omega}\right)} \cdot e^{(\pi i/4)S(\text{sgn } \omega \alpha + \text{sgn } \omega \beta - \text{sgn } \omega \alpha \beta)}.$$

Hence we again express $\sqrt{N(8b)}$ as a Gauss sum by taking $\alpha = \beta = 1$ in this equation, by which the left-hand side becomes 1. By subst

integral ideals, say

$$\gamma = \frac{c}{d}, \quad \text{where } (c, 4b) = 1,$$

and choose an auxiliary integral ideal $m$ such that

$$bm = \mu \text{ odd}; \quad \mu \frac{\gamma^2}{\omega} = \frac{mc^2}{d} \text{ is set equal to } \alpha.$$

Then by (169) and Theorem 155 we have

$$C\left(\frac{-\gamma^2\alpha}{4\omega}\right) = C\left(\frac{-\chi\alpha}{4\mu}\right) = C\left(\frac{-\chi\mu\alpha}{4}\right) C\left(\frac{-4\chi\alpha}{\mu}\right) = \left(\frac{\alpha}{b}\right) C\left(\frac{-\chi\mu\alpha}{4}\right) C\left(\frac{-4\chi}{\mu}\right),$$

and we obtain three more equations if we replace $\alpha$ by 1, $\beta$, $\alpha\beta$. Furthermore $\chi\mu = \omega\sigma^2$ where $\sigma = mc$ is an integer. In this way we finally obtain from (203) and (204)

$$\left(\frac{\alpha}{\beta}\right) \cdot \left(\frac{\beta}{\alpha}\right) = v(\alpha, \beta) \frac{C\left(\frac{-\omega\alpha}{4}\right) C\left(\frac{-\omega\beta}{4}\right)}{C\left(\frac{-\omega}{4}\right) C\left(\frac{-\omega\alpha\beta}{4}\right)},$$

where $\omega$ is now an arbitrary number of the field for which $b\omega$ is integral and odd.

If we assume that at least one of the numbers $\alpha, \beta$, say $\alpha$, is primary, then by (168)

$$C\left(\frac{-\omega\alpha\beta}{4}\right) = C\left(\frac{-\omega\beta}{4}\right); \quad C\left(\frac{-\omega\alpha}{4}\right) = C\left(\frac{-\omega}{4}\right),$$

and from this follows
