An integral polynomial $f(x)$ is called primitive if its coefficients are relatively prime, i.e., if for every prime $p$,
$$f(x) \not\equiv 0 \pmod{p}.$$
Equivalently, the greatest common divisor of all coefficients of $f(x)$ is $1$.
