Every field obtained by adjunction of several algebraic numbers over $k$ can also be generated by adjunction of a single algebraic number over $k$.

Obviously, it is enough to prove the theorem for the adjunction of two numbers. Thus let $\alpha_1, \ldots, \alpha_n$ be the $n$ conjugates of a number $\alpha_1$ of relative degree $n$ and $\beta_1, \ldots, \beta_m$ the $m$ conjugates of $\beta_1$ of relative degree $m$. We will show that with suitable choice of $u$ and $v$ in $k$ the number $u\alpha_1 + v\beta_1 = \omega_{11}$ is a number generating the field $K(\alpha_1, \beta_1; k)$. We must prove that $\alpha_1$ and $\beta_1$ themselves—consequently also every number from $K(\alpha_1, \beta_1; k)$—are representable as rational functions of $\omega_{11}$ with coefficients from $k$.

For this

We now try to construct a rational function of $x$ which takes the values $\beta_k$ for $x = \omega_{1k} (k = 1, 2, \ldots, m)$. Recalling the Lagrange interpolation formula let us consider the expression

$$\Phi(x) = \sum_{i=1}^{n} \sum_{k=1}^{m} \beta_k \frac{H(x)}{x - \omega_{ik}}.$$

This $\Phi(x)$ is a polynomial in $k$. For since $H(\omega_{ik}) = 0$

$$\frac{H(x)}{x - \omega_{ik}} = \frac{H(x) - H(\omega_{ik})}{x - \omega_{ik}} = \sum_{h=0}^{nm} c_h \frac{x^h - \omega_{ik}^h}{x - \omega_{ik}} = G(x, \omega_{ik})$$

is obviously a polynomial in $x$ and $\omega_{ik}$ with coefficients in $k$, hence

$$\Phi(x) = \sum_{i=1}^{n} \sum_{k=1}^{m} \beta_k G(x, u\alpha_i + v\beta_k)$$

is a polynomial in $x$, whose coefficients are polynomial expressions in the $\alpha_i, \beta_k$ with coefficients in $k$, which are moreover formally symmetric in the quantities $\alpha_1, \ldots, \alpha_n$ as well as the quantities $\beta_1, \ldots, \beta_m$. Consequently these coefficients belong to $k$ and $\Phi(x)$ is a polynomial in $k$. If we set $x = \omega_{11}$, then $G(\omega_{11}, \omega_{ik})$ vanishes, except if $i = 1$ and $k = 1$, since $\omega_{11}$ is different from the remaining $\omega_{ik}$ by construction. However from this it follows that

$$\beta_1 = \frac{\Phi(\omega_{11})}{G(\omega_{11}, \omega_{11})}.$$

We show in an analogous manner that $\alpha_1$ can also be expressed in terms of $\omega_{11}$ and with this we have proved

and since $f(\theta) = 0$,

$$1 = Q(\theta)R(\theta)$$

$$\alpha = \frac{P(\theta)}{Q(\theta)} = P(\theta)R(\theta) = F(\theta),$$

where $F(x) = P(x)R(x)$ is again a polynomial over $k$. Finally let $g(x)$ be the remainder of $F(x)$ mod $f(x)$, which is also a polynomial over $k$ of degree $\leq n - 1$. Then

$$F(x) = q(x)f(x) + g(x),$$

$$F(\theta) = g(\theta),$$

so that in fact $\alpha$ is put into the form (34). If there are two polynomials $g(x)$ and $g_1(x)$ over $k$, of degree at most $n - 1$, such that $g(\theta) = g_1(\theta)$, then $g(x) - g_1(x)$ is a polynomial over $k$ with the root $\theta$, whose degree is $< n$. Thus $g(x) - g_1(x)$ is identically 0, that is, the coefficients of $g(x)$ and $g_1(x)$ agree.

**Theorem 54.** Every number $g(\theta)$ of the field $K(\theta)$ is likewise an algebraic number over $k$ of degree at most $n$. The relative conjugates of a number $\alpha = g(\theta)$ are the distinct numbers among the numbers $g(\theta_i)$ ($i = 1, 2, \ldots, n$). Each conjugate to $\alpha$ appears equally often among the $g(\theta_i)$.

For if $\theta_1, \ldots, \theta_n$ are the conjugates of $\theta$ with respect to $k$, we form the product

$$F(x) = \prod_{i=1}^{n} (x - g(\theta_i)).$$

The coefficients of this polynomial are integral rational combinations of $\theta_1, \ldots, \theta_n$, which are moreover symmetric in $\theta_1, \ldots, \theta_n$ and whose coefficients belong to $k$. Consequently $F(x)$ is a polynomial over $k$ and thus every number

represent all conjugates to each $\alpha_i$; however they represent each conjugate $q$ times. Consequently, $n$ is the largest relative degree which a number $\alpha$ in $K(\theta)$ can have with respect to $k$, and, with this, $n$ is identified as a number determined by the field $K(\theta)$ alone, which is independent of the choice of the generating number $\theta$. Thus $n$ is called the relative degree of the field $K(\theta)$ with respect to $k$. Hence the degree of each number of $K(\theta)$ is a divisor of the degree of the field.

We now modify the concept of the conjugate, keeping in mind the above theorem, by the following:

**Definition.** If $n$ is the relative degree of $K(\theta)$ with respect to $k$ and if $\alpha = g(\theta)$ is a number of $K(\theta)$, of degree $n/q$, then the system of $n$ numbers $\alpha_i = g(\theta_i)$ ($i = 1, 2, \ldots, n$) will be called the conjugates of $\alpha$ in the field $K(\theta)$ with respect to $k$. These are the conjugates of $\alpha$ with respect to $k$, each one taken $q$ times.

Accordingly the system of these conjugates as a whole depends only on $\alpha$, the ground field $k$, and the field $K$, but it is independent of the choice of the generating $\theta$. Since in the future we will be dealing exclusively with this concept of conjugate, the qualifier "in the field $K(\theta)$ with respect to $k$" will be omitted in general for the sake of simplicity.

Once we have brought the conjugates of the generating number $\theta$ into a definite order by the numbering $\theta_1, \theta_2, \ldots, \theta_n$, then the $n$ conjugates of an arbitrary number $\alpha$ in $K(\theta)$ acquire a definite numbering by representing $\alpha$ in the uniquely determined form $g(\theta)$ by Theorem 53 and then calculating the number $g(\theta_i)$ as the conjugate $\alpha_i$. We shall consider such a determination as done, then we prove:

**Theorem 55

Hence this polynomial in $\theta$ must vanish for all conjugates $\theta_i$, i.e.,

$$\begin{cases} P(\alpha_i, \beta_i, \gamma_i, \ldots) = 0, \\ Q(\alpha_i, \beta_i, \gamma_i, \ldots) \neq 0, \end{cases} \quad (i = 1, 2, \ldots, n).$$

Thus the $n$ numerical values

$$R(\alpha_i, \beta_i, \gamma_i, \ldots) = 0, \quad (i = 1, 2, \ldots, n)$$

which was to be proved.

In particular, it follows for each two numbers $\alpha, \beta$, in $K(\theta)$

$$\alpha_i \pm \beta_i = (\alpha \pm \beta)_i, \quad \alpha_i \beta_i = (\alpha \beta)_i, \quad \frac{\alpha_i}{\beta_i} = \left( \frac{\alpha}{\beta} \right)_i,$$

since, for example, for $\alpha = g(\theta)$ and $\beta = h(\theta)$,

$$g(\theta)h(\theta) = r(\theta),$$

where $g, h, r$ are polynomials of degree $\leq n - 1$. By the above theorem, from this one equation for the numerical value $\theta$ the $n$ equations

$$g(\theta_i)h(\theta_i) = r(\theta_i)$$

follow, that is,

$$\alpha_i \beta_i = (\alpha \beta)_i \quad (i = 1, 2, \ldots, n).$$
