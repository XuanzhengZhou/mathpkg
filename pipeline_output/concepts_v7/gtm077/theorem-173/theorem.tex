In order that an odd ideal $\mathfrak{a}$ can be made into a primary number of the field by multiplication by a square of an ideal, it is necessary and sufficient that the conditions

$$Q(\varepsilon, \mathfrak{a}) = +1$$

are satisfied for each totally positive singular number $\varepsilon$.

One usually calls Theorems 171 and 173 the first supplementary theorem.

In similar fashion we obtain the converse of Theorem 167 which concerns the residue character modulo numbers which are not odd. We call an odd integer $\alpha$ hyperprimary modulo 1, where 1 denotes a prime factor of 2, if $\alpha \equiv \xi

provided $\alpha$ belongs to the complex $R_1$. If we now just consider the functions $(\frac{\lambda}{\alpha}) = Q(\lambda, \alpha)$ for primary numbers $\alpha$, then we have $Q(\lambda, \alpha_1) = Q(\lambda, \alpha_2)$ if $\alpha_1$ and $\alpha_2$ belong to the same complex $R_1$ or $R_2$. Moreover, $Q(\lambda, \alpha_1\alpha_2) = Q(\lambda, \alpha_1)Q(\lambda, \alpha_2)$, so that $Q(\lambda, \alpha)$ is a group character of the group of order two which is formed from the elements $R_1, R_2$ where $R_2^2 = R_1$. Nevertheless this character is not the principal character; for by Theorem 169 there are infinitely many prime ideals $p$ for which $(\frac{\lambda}{p}) = -1$ while the characters $Q(\varepsilon, p)$ are equal to $+1$ for each of the $p$ independent totally positive squares $\varepsilon$ of ideals. Then by Theorem 173, $p$ can be made into a primary number by multiplication by a suitable $m^2$, say $\alpha = pm^2$. Then $Q(\lambda, \alpha) = (\frac{\lambda}{p}) = -1$. Consequently $Q(\lambda, \alpha)$ is the uniquely determined group character of the group $(R_1, R_2)$ which is not the principal character; hence it is $= 1$ if and only if the primary number $\alpha$ belongs to $R_1$, that is, if $\alpha$ is also hyperprimary modulo 1. Now for each singular primary number $\omega$ we have $Q(\lambda, \omega) = +1$, thus all odd singular primary numbers modulo 1 are also hyperprimary modulo 1.

Secondly, if $l$ does not belong to the principal class complex in the strict sense, then let us choose an odd ideal $r$ such that $\lambda = lr$ is a totally positive number. Since $r$ also does not belong to the strict principal class complex, there are, by Theorem 172, among the $e_0$

prime to $\lambda$, be hyperprimary, it is necessary and sufficient that

$$\chi(\alpha) = \left( \frac{\lambda}{\alpha} \right) \left( \frac{\alpha}{r} \right) = +1.$$

Theorem 167 asserts that the condition is necessary. The proof of the preceding theorem shows in the following way that it is sufficient. First suppose that $l$ is equivalent in the strict sense to the square of an ideal. Then we can find integers $\beta, \rho, \lambda$ such that

$$\lambda \beta^2 = \lambda_0 \rho, \quad \lambda_0 = \mathrm{lr}_1^2$$
$$\rho = \mathrm{rr}_1^2, \quad \lambda_0, \rho \text{ totally positive},$$

where $\beta$ is odd and relatively prime to $\alpha r$. Then

$$\chi(\alpha) = \left( \frac{\lambda \beta^2}{\alpha} \right) \left( \frac{\alpha}{r} \right) = \left( \frac{\lambda_0 \rho}{\alpha} \right) \left( \frac{\alpha}{r} \right) = \left( \frac{\lambda_0}{\alpha} \right) \left( \frac{\alpha}{\rho} \right) \left( \frac{\alpha}{r} \right) = \left( \frac{\lambda_0}{\alpha} \right),$$

and as shown above, $(\frac{\lambda_0}{\alpha}) = +1$ is the necessary and sufficient condition that the primary number $\alpha$ is also hyperprimary.

However, if $l$ is not in a principal class complex, then there is indeed a singular primary number $\omega_1$, for which $(\frac{\omega_1}{r}) = -1$; and $1, \omega_1$ represent simultaneously the two distinct residue class complexes mod 4l which arise from primary numbers. If $\alpha$ and $\omega_1^a$ ($a = 0$ or 1) belong to the same complex mod 4l, then by Theorem 166 $\chi(\alpha) = \chi(\omega_1^a) = (-1)^a$. Thus $\chi(\alpha) = +1$, if $\

To prove this we go back to Formula (199) for Gauss sums and use Theorem 156 which determines the value of a sum that belongs to a square denominator. By (169) we decompose the sum $C\left(\frac{\varepsilon}{4\omega}\right)$ belonging to the denominator $4a$, where $\varepsilon, a = 1$, into a sum with denominator $4$ and a sum with denominator $a$, by introducing an odd auxiliary ideal $c$ such that

$$ac = a \text{ number } \alpha, \quad \gamma = \frac{\alpha}{\omega} = \frac{c}{d}.$$

Then by (169)

$$C\left(\frac{\varepsilon}{4\omega}\right) = C\left(\frac{\varepsilon\gamma}{4\alpha}\right) = C\left(\frac{4\varepsilon\gamma}{\alpha}\right)C\left(\frac{\alpha\varepsilon\gamma}{4}\right),$$

and if $\varepsilon$ is primary, the right-hand side is

$$= \left(\frac{\varepsilon}{a}\right)C\left(\frac{\gamma}{\alpha}\right)C\left(\frac{\alpha\gamma}{4}\right).$$

In particular, it follows for $\varepsilon = 1$ that

$$C\left(\frac{1}{4\omega}\right) = C\left(\frac{\gamma}{\alpha}\right)C\left(\frac{\alpha\gamma}{4}\right)$$

and consequently

$$\left(\frac{\varepsilon}{a}\right) = \frac{C\left(\frac{\varepsilon}{4\omega}\right)}{C\left(\frac{1}{4\omega}\right)}.$$

(215)

We now apply reciprocity formula (199) to the last sums, by which these sums transform into sums with denominator $\varepsilon$, which can be determined directly by Theorem 156.

We obtain

$$\frac{C\left(\frac{\varepsilon}{4\omega}\right)}{|\sqrt{N(4a)}|} = \left|\sqrt{N\left(\frac{2}{\varepsilon}\right)}\right|e^{(\pi i/4)S(\text{sgn } \

is valid for each primary number $\varepsilon$ relatively prime to $\mathfrak{a}$. If we now assume that $\varepsilon$ is also a singular number then, by Theorem 156, we obtain the value $|\sqrt{N(\varepsilon)}|$ for the sum $C(-\gamma^2\omega/\varepsilon)$. Consequently

$$\left(\frac{\varepsilon}{\mathfrak{a}}\right) = e^{(\pi i/4)S(\text{sgn}\omega\varepsilon - \text{sgn}\omega)} \quad \text{if } \omega = \mathfrak{a}\mathfrak{d}, \mathfrak{a} \text{ odd},$$

and $\varepsilon$ is a singular primary number, $(\varepsilon,\mathfrak{a}) = 1$.

Finally if, in addition, $\varepsilon$ is totally positive, it follows that $\left(\frac{\varepsilon}{\mathfrak{a}}\right) = +1$ and, by Theorem 170, that $\mathfrak{a}$ as well as the different $\mathfrak{d}$ belongs to the principal class complex.

Since the differents of relative fields compose according to Theorem 111, it also follows from what has just been proved:

The relative different $\mathfrak{D}_k$ of a field $K$ with respect to a subfield $k$ is always equivalent to the square of an ideal in $k$.

Moreover, since the relative norm of $\mathfrak{D}_k$ is equal to the relative discriminant of $K$ with respect to $k$, we see that the relative norm is also equivalent to a square in $K$. Thus we have shown
