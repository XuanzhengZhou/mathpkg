The set of all integers under addition forms an infinite cyclic group generated by 1. Every subgroup (module) is again cyclic, consisting of multiples of some integer. The additive residue classes modulo $n$ form a cyclic group of order $n$.
