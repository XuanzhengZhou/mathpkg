For polynomials with coefficients that are algebraic integers in $K$, the content (the GCD of the coefficients, taken as an ideal) of a product equals the product of the contents:
$$J(P \cdot Q) = J(P) \cdot J(Q).$$
This generalizes Gauss's Lemma to several variables and arbitrary algebraic number fields.
