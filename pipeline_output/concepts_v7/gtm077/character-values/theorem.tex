If $A$ is an element of a finite Abelian group $\mathfrak{G}$ of order $f$, then $\chi(A)$ is an $f$th root of unity for every character $\chi$. Among the $h$ values $\chi_n(A)$ ($n = 1, \ldots, h$), each $f$th root of unity occurs exactly $h/f$ times.
