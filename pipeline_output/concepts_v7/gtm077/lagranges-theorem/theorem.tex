In a finite group $\mathfrak{G}$ of order $h$, the order $N$ of each subgroup $\mathfrak{U}$ is a divisor of $h$. The quotient $j = h/N$ is called the index of the subgroup relative to $\mathfrak{G}$.
