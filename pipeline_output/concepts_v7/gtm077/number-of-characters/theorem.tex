There are exactly $h$ distinct characters $\chi(A)$ of a finite Abelian group $\mathfrak{G}$ of order $h$. Each such character is an $h$th root of unity for every $A$. The characters themselves form a group isomorphic to $\mathfrak{G}$ under pointwise multiplication.
