A number $\alpha$ in $K(\theta)$ belongs to the ground field $k$ if and only if it equals all its $n$ conjugates. A number $\alpha$ generates $K(\theta)$ over $k$ (i.e., has degree $n$) if and only if all its conjugates are distinct.
