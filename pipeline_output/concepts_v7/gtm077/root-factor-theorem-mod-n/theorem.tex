If $a$ is a root of the integral polynomial $f(x)$ modulo $n$ (i.e., $f(a) \equiv 0 \pmod{n}$), then $f(x)$ is divisible by $x - a$ modulo $n$, and conversely.

That is, there exists an integral polynomial $g(x)$ such that
$$f(x) \equiv (x - a) g(x) \pmod{n}.$$
