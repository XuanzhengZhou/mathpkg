Every nonzero ideal $\mathfrak{a}$ in a number field $K$ of degree $n$ can be written in the form $\mathfrak{a} = (\alpha_1, \ldots, \alpha_r)$ with $r \leq n$, where the $\alpha_i$ are suitably chosen integers in $K$. In particular, every ideal is a finitely generated module of rank $n$.
