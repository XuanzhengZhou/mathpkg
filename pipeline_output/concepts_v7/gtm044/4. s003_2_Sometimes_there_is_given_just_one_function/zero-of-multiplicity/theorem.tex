Let $f(X)$ be analytic at a point $a \in \mathbb{C}_X$. Then $a$ is a \textbf{zero of multiplicity $n$}, or a \textbf{zero of order $n$}, if
$$f(X) = (X - a)^n h(X),$$
where $h(X)$ is analytic at $a$, and $h(a) \neq 0$. We also say $f$ has \textbf{order $n$} at $a$.