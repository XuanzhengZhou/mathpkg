$$\ll = \{z: \text{for some } (u,v) \text{ in } R \times R \text{ and some } (x,y) \text{ in } R \times R, z = ((u,v),(x,y))\},$$ and $\max [u,v] < \max [x,y]$, or $\max [u,v] = \max [x,y]$ and $u < x$, or $\max [u,v] = \max [x,y]$ and $u = x$ and $v < y$.
