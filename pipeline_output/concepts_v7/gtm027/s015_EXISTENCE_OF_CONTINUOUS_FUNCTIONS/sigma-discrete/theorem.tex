A family $\alpha$ of subsets of a topological space is \textbf{$\sigma$-discrete} if and only if $\alpha = \bigcup \{\alpha_n : n \in \omega\}$ where each $\alpha_n$ is a discrete family (each point of the space has a neighborhood intersecting at most one member of $\alpha_n$).
