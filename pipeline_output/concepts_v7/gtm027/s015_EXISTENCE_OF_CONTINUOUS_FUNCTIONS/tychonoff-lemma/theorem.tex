Let $X$ be a regular Lindelöf space. Then $X$ is normal.

Formally: if $X$ is regular and every open cover of $X$ has a countable subcover, then for every pair of disjoint closed subsets $A, B \subset X$, there exist disjoint open sets $U, V$ such that $A \subset U$ and $B \subset V$.
