A topology for a family $F$ of functions is **jointly continuous on a set** $A$ iff the evaluation map
$$P(f,x) = f(x)$$
is continuous on $F \times A$, where $A$ is a subset of the domain space $X$ and $P$ is restricted to $F \times A$.
