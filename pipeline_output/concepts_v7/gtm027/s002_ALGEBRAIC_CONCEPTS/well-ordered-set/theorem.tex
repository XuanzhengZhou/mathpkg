A relation $<$ is a \textbf{well ordering} of a set $A$ iff $<$ is a linear ordering of $A$ such that each non-void subset has a first member (a member which is less than or equal to every other member). If there exists a well ordering of $A$, then we say that $A$ \textbf{can be well ordered}.
