A function $f$ from a linear space $X$ over $F$ to a linear space $Y$ over $F$ is called a \textbf{linear function} iff
$$f(a \cdot x + b \cdot y) = a \cdot f(x) + b \cdot f(y)$$
for all members $a, b$ of $F$ and all $x, y$ in $X$.
