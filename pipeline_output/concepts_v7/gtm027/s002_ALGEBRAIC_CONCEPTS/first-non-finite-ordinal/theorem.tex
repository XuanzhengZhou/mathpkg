The first member of $\Omega'$ which does not have a finite number of predecessors is the \textbf{first non-finite ordinal} and is denoted $\omega$. In the construction of the ordinal numbers it turns out that the first non-finite ordinal is, in fact, the set $\omega$ of non-negative integers.
