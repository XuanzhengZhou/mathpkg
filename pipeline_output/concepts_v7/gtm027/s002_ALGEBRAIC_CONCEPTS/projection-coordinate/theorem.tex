The function $P_a$ which carries each point $x$ of the cartesian product onto its $a$-th coordinate $x_a$ is the \textbf{projection} into the $a$-th coordinate set. That is, $P_a(x) = x_a$. The map $P_a$ is also called the \textbf{evaluation at $a$}.
