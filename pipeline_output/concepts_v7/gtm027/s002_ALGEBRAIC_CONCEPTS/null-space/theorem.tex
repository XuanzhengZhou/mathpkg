If $f$ is a linear function, the set $f^{-1}[0] = \{x \in X : f(x) = 0\}$ is called the \textbf{null space} of $f$. The null space of a linear function is a linear subspace of the domain (provided the operations of addition and scalar multiplication are properly defined).
