The members of a topology $\mathfrak{J}$ are called \textbf{open} relative to $\mathfrak{J}$, or $\mathfrak{J}$-open, or simply \textbf{open sets}. The space $X$ of the topology is always open, and the void set is always open (as the union of the members of the void family).
