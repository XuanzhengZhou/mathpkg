The \textbf{indiscrete} (or \textbf{trivial}) topology for a set $X$ is the family whose only members are $X$ and the void set. The pair $(X, \mathfrak{J})$ with this topology is called an \textbf{indiscrete topological space}.
