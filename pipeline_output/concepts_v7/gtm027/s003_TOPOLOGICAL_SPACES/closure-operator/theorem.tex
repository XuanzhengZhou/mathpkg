The \textbf{closure operator} relative to a topology is the function $A \mapsto A^{-}$ that assigns to each subset $A$ of the topological space its closure. This operator completely determines the topology, since a set $A$ is closed iff $A = A^{-}$.
