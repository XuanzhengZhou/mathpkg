Let $\mathfrak{U}$ be the neighborhood system of a point $x$ in a topological space. Then:

\begin{enumerate}
\item Finite intersections of members of $\mathfrak{U}$ belong to $\mathfrak{U}$.
\item Each set which contains a member of $\mathfrak{U}$ belongs to $\mathfrak{U}$.
\end{enumerate}
