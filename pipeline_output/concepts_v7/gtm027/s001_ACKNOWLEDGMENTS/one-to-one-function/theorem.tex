A function $f$ is \textbf{one to one} (an injection) if and only if distinct points have distinct images. Equivalently, $f$ is one to one if and only if the inverse relation $f^{-1}$ is also a function.
