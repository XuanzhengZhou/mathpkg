A relation $R$ is \text{anti-symmetric} iff it is never the case that both $xRy$ and $yRx$ hold when $x \neq y$. Equivalently:
$$
R \cap R^{-1} \subset \Delta.
$$
In Kelley's terminology, $R$ is anti-symmetric iff $R \cap R^{-1}$ is void (when restricted to distinct points).
