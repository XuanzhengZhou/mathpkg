Let $\{X_a : a \in A\}$ be a family of topological spaces such that infinitely many of the coordinate spaces $X_a$ are non-compact. Then every compact subset of the product space $\prod_{a \in A} X_a$ (with the product topology) is nowhere dense.
