Every non-empty family $s$ of sets is the subbase for some topology, and this topology is uniquely determined by $s$. It is the smallest topology containing $s$ (that is, it is a topology containing $s$ and is a subfamily of every topology containing $s$).
