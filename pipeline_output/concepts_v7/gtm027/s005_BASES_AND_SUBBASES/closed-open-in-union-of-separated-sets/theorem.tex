Let $X$ be a topological space which is the union of subsets $Y$ and $Z$ such that $Y \sim Z$ and $Z \sim Y$ are separated. Then a subset $A$ of $X$ is closed (respectively open) if $A \cap Y$ is closed (respectively open) in $Y$ and $A \cap Z$ is closed (respectively open) in $Z$.
