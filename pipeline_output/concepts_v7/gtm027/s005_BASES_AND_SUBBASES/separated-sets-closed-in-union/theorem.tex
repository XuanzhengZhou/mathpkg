Two subsets $A$ and $B$ of a topological space $X$ are separated in $X$ if and only if $A$ and $B$ are disjoint and both $A$ and $B$ are closed in $A \cup B$ (with the relative topology). Equivalently, $A$ (or $B$) is both open and closed in $A \cup B$ and $A$ and $B$ are disjoint.
