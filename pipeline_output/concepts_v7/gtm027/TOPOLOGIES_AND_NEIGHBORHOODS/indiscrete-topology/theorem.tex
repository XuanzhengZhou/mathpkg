The family whose only members are $X$ and the void set is a topology for $X$. It is called the \textbf{indiscrete} (or \textbf{trivial}) topology for $X$, and $(X, \mathfrak{J})$ is then an indiscrete topological space.
