\text{Let } P \text{ be the gage of a uniformity } \mathfrak{u} \text{ for } X \text{. Let } A \text{ be the cartesian product of } P \text{ and the set of positive real numbers, and direct } A \text{ by } (p, r) \geq (q, s) \text{ iff } r \leq s \text{ and } p(x, y) \geq q(x, y) \text{ for all } x, y \in X \text{. Define } V_{p,r}(x) = \{y : p(x, y) < r\} \text{. Then } (V, \geq) \text{ is a uniform neighborhood system for } X \text{ whose associated uniformity is } \mathfrak{u}.
