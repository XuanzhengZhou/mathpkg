A net is a pair $(S, \geq)$ such that $S$ is a function and $\geq$ directs the domain of $S$.

If the domain $D$ of $S$ is clear from context, a net is often denoted $\{S_n, n \in D, \geq\}$ or simply $\{S_n, n \in D\}$.
