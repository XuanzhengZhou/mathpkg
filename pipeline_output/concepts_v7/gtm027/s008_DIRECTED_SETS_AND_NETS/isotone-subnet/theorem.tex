Suppose $N$ is a function on the directed set $E$ to the directed set $D$ such that $N$ is isotone (i.e., $N_i \geq N_j$ if $i \geq j$) and the range of $N$ is cofinal in $D$. Then $S \circ N$ is a subnet of $S$ for each net $S$ with domain $D$.
