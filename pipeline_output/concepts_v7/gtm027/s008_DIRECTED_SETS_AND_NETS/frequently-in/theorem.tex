A net $\{S_n, n \in D, \geq\}$ is frequently in a set $A$ if and only if a cofinal subset of $D$ maps into the set $A$.

Equivalently, the net is frequently in $A$ if and only if it is not eventually in the complement of $A$.
