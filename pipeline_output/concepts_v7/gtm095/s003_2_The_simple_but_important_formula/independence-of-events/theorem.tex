$$P(AB) = P(A)P(B).$$

In probability theory we often need to consider not only independence of events (or sets) but also independence of *collections* of events (or sets).

Accordingly, we introduce the following definition.
