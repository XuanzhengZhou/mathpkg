Weak convergence $P_n \xrightarrow{w} P$ occurs if and only if

$$\lim_{n \to \infty} \int_E f(x) \, P_n(dx) = \int_E f(x) \, P(dx)$$

for every function $f$ belonging to the class $BL$ of bounded continuous functions that satisfy a Lipschitz condition on the metric space $(E, \mathcal{E}, \rho)$.
