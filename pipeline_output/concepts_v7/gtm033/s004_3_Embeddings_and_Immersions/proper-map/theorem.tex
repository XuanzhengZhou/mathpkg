A map $f: X \to Y$ between topological spaces is **proper** if $f^{-1}(K)$ is compact for every compact set $K \subset Y$. Equivalently, $f$ is proper if the preimage of every compact set is compact.
