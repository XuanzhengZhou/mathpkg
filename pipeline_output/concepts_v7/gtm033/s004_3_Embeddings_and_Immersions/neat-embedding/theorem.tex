A $C^r$ embedding $f: A \to M$, where $A$ is a $\partial$-manifold, is a **neat embedding** if $f(A)$ is a neat submanifold of $M$. Equivalently, $f$ is neat if
$$f(\partial A) = f(A) \cap \partial M$$
and $f$ is transverse to $\partial M$ along $\partial A$.
