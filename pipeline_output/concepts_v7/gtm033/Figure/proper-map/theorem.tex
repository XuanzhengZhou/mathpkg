A continuous map $f: M \to N$ is \textbf{proper} if $f^{-1}$ takes compact sets to compact sets; equivalently, the preimage of every compact subset of $N$ is compact in $M$.

For an embedding $f: M \hookrightarrow N$, properness is equivalent to $f(M)$ being closed in $N$.
