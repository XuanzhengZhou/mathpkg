Let $M, N$ be connected $n$-manifolds without boundary. Their connected sum $M \# N$ is obtained by removing the interior of a smoothly embedded $n$-disk from each and gluing the resulting boundary spheres together via a diffeomorphism. The result is a connected $n$-manifold without boundary, well-defined up to diffeomorphism.
