The homological Euler characteristic equals the Euler characteristic (defined via vector field indices) for a compact manifold without boundary:
$$\sum_{k=0}^{n} (-1)^k \beta_k = \sum_{p} \operatorname{Ind}_p X$$
where $\beta_k$ are the Betti numbers and $X$ is a vector field with isolated zeros.
