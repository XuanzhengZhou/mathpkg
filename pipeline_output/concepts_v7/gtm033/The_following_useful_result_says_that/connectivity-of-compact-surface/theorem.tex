The \text{connectivity} $c(M)$ of a compact surface $M$ is the integer $c \geqslant 0$ (if it exists) having the following property: there exists a union $V = V_1 \cup \cdots \cup V_c \subset M$ of disjoint circles whose complement is connected, but any union of $c + 1$ disjoint circles disconnects $M$.
