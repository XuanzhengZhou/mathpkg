Let $W$ be a connected compact nonorientable $\partial$-manifold of dimension $n + 1 \geqslant 2$. A map $f: \partial W \to S^n$ extends to a continuous map $W \to S^n$ if and only if $\deg_2 f = 0$.
