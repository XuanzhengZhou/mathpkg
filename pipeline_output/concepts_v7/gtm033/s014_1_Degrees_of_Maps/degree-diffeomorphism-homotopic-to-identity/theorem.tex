Let $N$ be a compact connected oriented $n$-manifold without boundary and let $h: N \to N$ be a diffeomorphism homotopic to the identity map $\operatorname{id}_N$. Then for any regular value $z \in N$,
$$
\deg(h, z) = 1.
$$
Equivalently, $\deg(h) = 1$.
