

Chapter 9

SIGNED MEASURES AND INDEFINITE INTEGRALS

A real (finite) valued function $f$ on $X$ is locally $\mu$ integrable iff $\mu$ is a measure on a $\delta$-ring $\mathcal{A}$ of subsets of $X$ and $f\chi_A \in L_1(\mu)$ for all $A$ in $\mathcal{A}$. In this case $f.\mu$, the indefinite integral of $f$ with respect to $\mu$, is the function $A \mapsto \int_A f d\mu$ for $A$ in $\mathcal{A}$. This function is always countably additive and hence $f.\mu$ is a measure if $f$ is non-negative. Consequently $f.\mu$ is the difference of two measures, $f^+.\mu$ and $f^-.\mu$. These two measures have little to do with each other: one of them "lives on" the set $\{x: f(x) \geq 0\}$ and the other lives on $\{x: f(x) < 0\}$.

There are several natural questions. Which countably additive real valued functions on $\mathcal{A}$ are differences of measures? If a function is the difference of measures, may these measures be chosen so that they live on disjoint sets? Which countably additive real valued functions are indefinite integrals? This section is devoted to an investigation of these and related questions.

A signed measure is a real (finite) valued, countably additive function on a $\delta$-ring. Suppose $\mu$ is a signed measure on a $\delta$-ring $\mathcal{A}$ of subsets of $X$. A subset $B$ of $X$ is $\mu$ positive iff $B$ is locally $\mathcal{A}$ measurable (i.e., $A \cap B \in \mathcal{A}$ for all $A$ in $\mathcal{A}$) and $\mu(A) \geq 0$ for all $A$ in $\mathcal{A}$ that are subsets of $B$. There are alternative descriptions: for each $A$ in $\mathcal{A}$, $A \cap B \in \mathcal{A}$ and $\mu(A \cap B) \geq 0$

1 LEMMA If $\mu$ is a signed measure on $\mathcal{A}$, $A \in \mathcal{A}$ and $\mu(A) > 0$ then $A$ contains a $\mu$ positive set $B$ with $\mu(B) \geq \mu(A)$.

PROOF We assert that for each $e > 0$ there is a member $C_0$ of $\mathcal{A}$ such that $C_0 \subset A$, $\mu(C_0) \leq 0$ and each member $D$ of $\mathcal{A}$ with $D \subset A \setminus C_0$ has $\mu$ measure greater than $-e$. If this were not the case, then one could select recursively a disjoint sequence $\{D_n\}_n$ in $\mathcal{A}$ with $D_n \subset A$ and $\mu(D_n) \leq -e$ for all $n$. But every $\delta$-ring is closed under dominated countable union and $\bigcup_n D_n \subset A$. So $\bigcup_n D_n \in \mathcal{A}$ and $\mu(\bigcup_n D_n) = \sum_n \mu(D_n) = -\infty$, and this is a contradiction.

Select recursively, $C_{n+1}$ in $\mathcal{A}$ so that $C_{n+1} \subset A \setminus \bigcup_n^{n+1} C_k$, $\mu(C_{n+1}) \leq 0$ and each member $D$ of $\mathcal{A}$ with $D \subset A \setminus \bigcup_n^{n+1} C_k$ has $\mu$ measure $> -2^{-n-1}$. The argument of the preceding paragraph shows that this selection is always possible. Then $B = A \setminus \bigcup_n C_n$ is $\mu$ positive, and $\mu(B) = \mu(A) - \sum_n \mu(C_n) \geq \mu(A)$.

We recall that if $\mathcal{A}$ is a family of sets, then $\mathcal{A}_{\sigma}$ is the family of all unions of countably many members of $\mathcal{A}$. The upper variation $\mu^+$ of a signed measure $\mu$ on $\mathcal

follows. If $\bigcup_n A_n \in \mathcal{A}$, then $\sum_n \mu^+(A_n) = \sum_n \mu(A_n^+) = \mu(\bigcup_n A_n^+) = \mu^+(\bigcup_n A_n)$ so $\mu^+$ is a measure, and so is $\mu^- = (-\mu)^+$. Hence $\mu = \mu^+ - \mu^-$ is a Jordan representation of $\mu$.

It is worth noticing that if $\mu$ is a signed measure on $\mathcal{A}$ and the underlying space $X$ belongs to $\mathcal{A}_\sigma$ (as is the case for Borel measures for $\mathbb{R}^n$), then $X$ has a Hahn decomposition into a $\mu$ positive and a $\mu$ negative set.

The class $M(\mathcal{A})$ of all signed measures on a $\delta$-ring $\mathcal{A}$ is a vector space, and it is partially ordered by agreeing that, for $\mu$ and $\lambda$ in $M(\mathcal{A})$, $\lambda \geq \mu$ iff $\lambda(A) \geq \mu(A)$ for all $A$ in $\mathcal{A}$. Suppose that $\lambda \geq \mu$, $A \in \mathcal{A}$ and that $\{A^+, A^-\}$ is a Hahn decomposition of $A$ for $\mu$. If $\lambda \geq \mu$ and $\lambda \geq 0$, then $\lambda(A) = \lambda^+(A) = \lambda(A^+) \geq \mu(A^+) = \mu^+(A)$. Thus $\lambda \geq \mu^+$, and we have shown that $\mu^+$ is the smallest signed measure that is greater than or equal both 0 and $\mu$. Thus, $\mu^+$ is the supremum of $\{0, \mu\}$ relative to the ordering of $M(\mathcal{A})$. Consequently $M(\mathcal{A})$, with this ordering, is a vector lattice, and that $\mu \vee v = v + (\mu - v) \vee 0 = v + (\mu - v)^+$. In particular, the definition of $\mu

measure zero in the sense that $A \cap (X \setminus C) \in \mathcal{A}$ and $\mu(A \cap (X \setminus C)) = 0$ for all $A$ in $\mathcal{A}$.

A measure $\nu$ on $\mathcal{A}$ is said to be singular w.r.t. a measure $\mu$ on $\mathcal{A}$ iff $\nu$ lives on a set $C$ that is locally of $\mu$ measure zero. This is the case iff $\mu$ is singular w.r.t. $\nu$ because $\mu$ lives on the set $X \setminus C$, which is locally of $\nu$ measure 0, and consequently $\mu$ and $\nu$ have disjoint carriers. We agree that $\mu$ and $\nu$ are mutually singular, and write $\mu \perp \nu$, iff $\mu$ and $\nu$ have disjoint carriers. The measures $\mu$ and $\nu$ are locally $\perp$, and $\mu$ is locally $\perp$ to $\nu$, iff for each $A$ in $\mathcal{A}$ there are disjoint members $A_{\mu}$ and $A_{\nu}$ of $\mathcal{A}$ whose union is $A$ such that $\nu(A_{\mu}) = \mu(A_{\nu}) = 0$. The measure $\nu$ on $\mathcal{A}$ is absolutely continuous w.r.t. the measure $\mu$ on $\mathcal{A}$, $\nu \prec \mu$, iff $\nu(A) = 0$ whenever $\mu(A) = 0$ (we extend the definition on p. 113.)

4 LEBESGUE DECOMPOSITION THEOREM Suppose $\mu$ and $\nu$ are measures on a $\delta$-ring $\mathcal{A}$ of subsets of $X$, that $\nu_s(A) = \sup\{v(B): B \subset A, B \in \mathcal{A}$ and $\mu(B) = 0\}$ for all $A$ in $\mathcal{A}$ and that $\nu_c = v - \nu_s$. Then $\nu_s$ and $\nu_c$ are measures, $\nu_s$ is locally $\perp$ to $\nu_c$ and to $\mu$, and $\nu_c \prec \mu$.

6). If $f \in L_2(\mu + \nu)$ and $E \in \mathcal{A}$ then $|f\chi_E|^2$ is $(\mu + \nu)$ integrable and vanishes off a set of finite measure, so $|f\chi_E|$ is $(\mu + \nu)$ and hence $\mu$ integrable. Moreover $\int |f\chi_E| d\mu \leq \int |f\chi_E| d(\mu + \nu) \leq \|f\|_2 \|\chi_E\|_2$ by the Cauchy–Schwartz inequality and consequently $F = (f \mapsto \int_E f d\mu)$ is a bounded linear functional on $L_2(\mu + \nu)$. According to the Riesz theorem 6.12, there is $h$ in $L_2(\mu + \nu)$ such that $F(f) = \int fh d(\mu + \nu)$ for all $f$ in $L_2(\mu + \nu)$. Since $F(f) = F(f\chi_E) = \int_E f\chi_E h d(\mu + \nu)$, we may suppose $h = 0$ outside $E$, and since $F(f) \geq 0$ for $f \geq 0$, $h \geq 0$ $(\mu + \nu)$ a.e., and we may suppose $h \geq 0$. If $e > 0$ and $B = \{x : x \in E \text{ and } h(x) > 1 + e\}$, then $\mu(B) = F(\chi_B) = \int_E \chi_B h d(\mu + \nu) \geq (1 + e)(\mu + \nu)(B)$, whence $(\mu + \nu)(B) = 0$ and we may suppose that $0 \leq h \leq \chi_E$. Since $\mu(A) = h.(\mu + \nu)(A)$, $v(A) = \int_A 1 d(\mu + \nu) - \mu(A) = \int_A (1 - h) d(\mu + \nu)$ for each $A$ in $\mathcal{A}$ that is a subset

$B$ of $f.\mu$ is a $\delta$-ring, $B \subset A \subset B_{\sigma}$, and $f.\mu$ is a signed measure on $B$ that vanishes on all members of $B$ with $\mu$ measure zero.

PROOF We show only that $A \subset B_{\sigma}$, and leave to the reader the rest of the proof. Suppose $A \in A$ and that $B_n = \{x : x \in A, n < f(x) \leq n + 1\}$ for each integer $n$. Then $B_n \in A$ because $f$ is locally $A$ measurable, $|f\chi_{B_n}| \leq |n| + 1$ so $f\chi_{B_n} \in L_1(\mu)$, and hence $B_n \in B$. Consequently $A = \bigcup \{B_n : n \in \mathbb{Z}\} \in B_{\sigma}$.

It is worth noticing a few of the simple consequences of the condition that $A$ and $B$ be $\delta$-rings and $B \subset A \subset B_{\sigma}$. First: if $\mu$ is a measure on $A$ and $\nu = \mu|B$ is its restriction to $B$, then the integrals $I_{\mu}$ and $I_{\nu}$ are identical. Second: $B$ is an ideal in $A$ in the sense that $A \cap B \in B$ if $A \in A$ and $B \in B$. This is the case because $A \cap B$ is the union of a countable number of members of $B$ and the $\delta$-ring $B$ is closed under countable dominated union. It follows that if $C$ is also a $\delta$-ring and $C \subset A \subset C_{\sigma}$, then $\{B \cap C : B \in B \text{ and } C \in C\}$ is identical with $B \cap C$. Lastly: if $\mu$ is a measure on a $\delta$-ring $B$ and $\nu$ is a measure on $C$, then the integrals $I_{\mu}$ and $I_{\nu}$ are identical iff $\mu|B \cap C = \nu

never zero and $B$ is the domain of $(1/s).\mu$, then $B \subset A \subset B_\sigma$ and $s.(1/s).\mu = \mu|B$.

If $\mu$ and $\nu$ are measures on $A$ and $r$ is a locally $A$ measurable real valued function on the underlying space $X$ such that $\nu = r.\mu$ then $r$ is called a Radon–Nikodym derivative of $\nu$ w.r.t. $\mu$. It is not unique, but two such derivatives differ by a function with a support of locally $\mu$ measure zero. Radon–Nikodym derivatives of $\nu$ w.r.t. $\mu$ are denoted (ambiguously) $dv/d\mu$. If $f$ is both $\mu$ and $\nu$ integrable, then $\int(f)(dv/d\mu)d\mu = \int f dv$. If $\mu$ also has a derivative with respect to $\nu$, then $(d\mu/d\nu)(dv/d\mu) = 1$ locally $\mu$ a.e., and if $\nu$ has a derivative with respect to another measure $\pi$, then $(d\mu/d\nu)(dv/d\pi) = d\mu/d\pi$.

There is a natural definition of the integral of a function $f$ w.r.t. a signed measure $\nu$ on $A$. A function $f$ is $\nu$ integrable iff $f = \sum_n a_n \chi_{A_n}$ for sequences $\{a_n\}_n$ in $\mathbb{R}$ and $\{A_n\}_n$ in $A$ with $\sum_n |a_n| |v^+(A_n) + v^-(A_n)| < \infty$, and in this case $\int f dv = \sum_n a_n v(A_n) = \int f dv^+ - \int f dv^-$. Evidently $L_1(\nu) = L_1(v^+) \cap L_1(v^-)$. The measure $V_\nu = v^+ + v^-$ is called the variation of $\nu$. It is easy to see that for $A$ in $A$, $V_\nu(A) = \sup\{

(i) A subset $B$ of $\bigcup \{D : D \in \mathcal{D}\}$ is locally $\mathcal{A}$ measurable if it is piecewise in $\mathcal{A}$, in the sense that $B \cap D \in \mathcal{A}$ for each $D$ in $\mathcal{D}$, and

(ii) a member $A$ of $\mathcal{A}$ is of $\mu$ measure zero if it is piecewise of $\mu$ measure zero, in the sense that $\mu(A \cap D) = 0$ for each $D$ in $\mathcal{D}$.

Evidently the union of the members of a $\mu$ decomposition is locally $\mathcal{A}$ measurable, and its complement is locally of $\mu$ measure zero. A measure $\mu$ is decomposable iff there is a decomposition for $\mu$.

An example: If $X \in \mathcal{A}_{\sigma}$ and $\mathcal{D}$ is a disjoint countable subfamily of $\mathcal{A}$ covering $X$, then $\mathcal{D}$ is a $\mu$ decomposition for every measure $\mu$ on $\mathcal{A}$, so every measure $\mu$ on $\mathcal{A}$ is decomposable.

It is worth noticing that the property of being a decomposition for $\mu$ depends only on the families $\mathcal{A}$ and $\mathcal{N} = \{A : A \in \mathcal{A} \text{ and } \mu(A) = 0\}$.

9 PATCHWORK LEMMA Suppose $\mu$ is a measure on a $\delta$-ring $\mathcal{A}$ of subsets of $X$ and $\mathcal{D}$ is a decomposition for $\mu$. Then:

(i) if for each member $D$ of $\mathcal{D}$, $f_D$ is an $\mathcal{LA}$ measurable function with support $D$, then the function $f = \sum_{D \in \mathcal{D}} f_D$ is $\mathcal{LA}$ measurable, and,

(ii) if $\nu$ is a signed measure on $\mathcal{B}$ that is absolutely continuous w.r.t. $\mu$, then $\nu(C) = \sum_{D \in \mathcal{D}} \nu(C \cap D)$ for each $C$ in $\mathcal{B}$.

If $v = f.\mu$, the set $\{x : f(x) > 0\}$ is $v$ positive, and $\{x : f(x) \leq 0\}$ is $v$ negative, and together they provide a Hahn decomposition of $X$ for $v$. Thus:

11 COROLLARY (GLOBAL HAHN DECOMPOSITION) If $v$ is a signed measure and if the variation $V_v$ of $v$ is decomposable, then there is a Hahn decomposition of $X$ for $v$.

NOTE It is just as easy to derive the foregoing corollary directly from the patch work lemma: if $\mathcal{D}$ is a $V_v$ decomposition then $\bigcup \{D^+ : D \in \mathcal{D}\}$ is $v$ positive and $\bigcup \{D^- : D \in \mathcal{D}\}$ is $v$ negative.

Every regular Borel measure $\mu$ for a locally compact Hausdorff space $X$ is decomposable, and so the Radon–Nikodym theorem holds for such measures. This will follow immediately from a simple lemma. We agree that a subset $K$ of $X$ is $\mu$ tight iff it is compact, $\mu(K) > 0$, and $\mu(M) < \mu(K)$ for each compact proper subset $M$ of $K$. Each compact set $A$ contains a $\mu$ tight set $K$ with $\mu(K) = \mu(A)$—explicitly, $K = \{x : x \in A \text{ and for each open neighborhood } V \text{ of } x, \mu(V \cap A) \neq 0\}$. We recall that a set $S$ is a carrier for $\mu$ iff $S \in \mathcal{LB}^{\delta}(X)$ and its complement is locally of $\mu$ measure zero.

12 LEMMA If $\mu$ is a regular Borel measure for a locally compact Hausdorff space $X$ and $\mathcal{K}$ is a maximal disjoint family of $\mu$ tight sets, then each member of $\mathcal{B}^{\delta}(X)$ intersects only countably many members of $\mathcal{K}$ and $\bigcup

13 COROLLARY (RADON–NIKODYM THEOREM FOR BOREL MEASURES)
A signed measure that is absolutely continuous with respect to a regular Borel measure $\mu$ for a locally compact space is an indefinite integral w.r.t. $\mu$ of a locally Borel real valued function.

14 NOTE The classical Borel field for $X$, $\mathcal{B}_c(X)$ is the $\sigma$-field generated by the family of open subsets of $X$. It is easy to see that $\mathcal{LB}(X)$ contains $\mathcal{B}_c(X)$, but in general the two fields are not equal. Thus, it can happen that a function $f$ is $\mathcal{LB}(X)$ measurable but not $\mathcal{B}_c(X)$ measurable. But we do not know whether it is always possible to choose a Radon–Nikodym derivative $d\nu/d\mu$, where $\mu$ is a regular Borel measure and $\nu < \mu$, to be $\mathcal{B}_c(X)$ measurable.

SUPPLEMENT: HAAR MEASURE

Suppose $G$ is a locally compact Hausdorff topological group. A left Haar measure for $G$ is a regular Borel measure $\eta$ (a regular measure on $\mathcal{B}^\delta(G)$), not identically zero, that is invariant under left translation. That is, $\eta(z^{-1}B) = \eta(B)$ for all $z$ in $G$ and $B$ in $\mathcal{B}^\delta(G)$, and consequently $\int f(z^{-1}x) d\eta x = \int f(x) d\eta x$ for each $f$ in $L_1(\eta)$. According to 4.17 there is at least one left Haar measure for $G$; in fact, to a constant multiple there is precisely one.

We first observe that if $\eta$ is a left Haar measure for $G$ then $\eta(U) > 0$ for each non-empty open set $U$ in $\mathcal{B}^\delta(G)$ because every compact set is covered by finitely many left translates of $U$, and hence $\int g d\eta > 0$ for each

The following result is due to v. Neumann.

16 THEOREM (UNIQUENESS OF Haar Measure) If $\eta$ and $v$ are left Haar measures for $G$, then $\eta = kv$ for some positive real number $k$.

Proof Suppose $\mu$ and $\eta$ are left Haar measures for $G$, $g$ is a non-negative member of $C_c(G)$, not identically zero, and $f \in C_c(G)$. We show that the ratio $\int f d\eta / \int g d\eta$ is independent of $\eta$. According to the preceding proposition, the function $x \mapsto \int g(ux) d\mu u$ is continuous and it is strictly positive on $G$, whence the function $h$ defined on $G \times G$ by $h(x, y) = f(x)g(yx) / \int g(ux) d\mu u$ is continuous. Evidently $\int h(x, y) d\mu y = f(x)$ for each $x$ in $G$.

If $K$ is a compact support for $f$ and $S$ for $g$, then $K \times SK^{-1}$ is a compact support for $h$, so $h \in C_c(G \times G)$. Consequently by the Tonelli (or Fubini) theorem, $\int h d\eta \otimes \mu = \int \int h(x, y) d\mu y d\eta x = \int f(x) d\eta x$. On the other hand, $\int h d\eta \otimes \mu = \int \int h(x, y) d\eta x d\mu y = \int \int h(y^{-1}x, y) d\eta x d\mu y$ because $\eta$ is left invariant, $(x, y) \mapsto h(y^{-1}x, y)$ has a compact support and so $\int f(x) d\eta x = \int h d\eta \otimes \mu = \int \int h(y^{-1}x, y) d\mu y d\eta x$. Since $\mu$ is left invariant, this last integral is $\int \int h(y^{-1}, xy) d\mu y

and $f$ in $L_1(\eta)$, $\eta(Eb^{-1}) = \Delta(b)\eta(E)$ and $\int f(xb)d\eta x = \Delta(b)\int f(x)d\eta x$. Moreover, $\Delta$ is continuous and $\Delta(ab) = \Delta(a)\Delta(b)$.

It is a consequence of the preceding proposition that if $G$ is compact, then the unimodular function $\Delta$ is identically 1, since in this case $\Delta[G]$ is a compact subgroup of the multiplicative group of positive real numbers and $\{1\}$ is the only possibility. The group $G$ is unimodular iff the modular function $\Delta$ is identically 1. This happens iff each left Haar measure $\eta$ is also invariant under right translation; that is $\eta(Eb) = \eta(E)$ for all $b$ and all Borel sets $E$. Compact groups, and of course abelian groups, are unimodular, but even the group of all affine transformations, $x \mapsto ax + b$ with $a \neq 0$ fails to be unimodular.

A right Haar measure is a regular Borel measure that is invariant under right translation. If $\eta$ is left Haar measure and we set $\rho(E) = \eta(E^{-1})$ for all $E$ in $\mathcal{B}(G)$, then $\rho$ is a right Haar measure because $\rho(Eb) = \eta(b^{-1}E^{-1}) = \eta(E^{-1}) = \rho(E)$ for $E$ in $B(G)$ and $b$ in $G$. The measure $\rho$ is the right Haar measure corresponding to $\eta$. There is another description of this measure.

18 PROPOSITION The right Haar measure $\rho$ corresponding to $\eta$ agrees on $\mathcal{B}^{\delta}(G)$ with the indefinite integral $\Delta.\eta$.

Consequently $\rho$ and $\eta$ are mutually absolutely continuous.

PROOF If $f\Delta$ is $\eta$ integrable, by the previous result, $\int f(xb)\Delta(xb)d\eta x = \Delta(b)\int f(x)\Delta(x)d\

There is no difficulty in extending the notion of convolution to signed measures and the notion of convolution $\star_{\eta}$ to functions which are not necessarily non-negative. If the extension is made, then $L_1(\eta)$ becomes an algebra under $\eta$ and the space $M$ of regular Borel signed measures of finite total variation is a normed algebra under $\star$. Then the preceding conclusion implies that the map $f \mapsto f.\eta$, for $f$ in $L_1(\eta)$ is a norm preserving algebra homomorphism of $L_1(\eta)$ onto the subspace $M_a$ of $M$ which consists of signed measures that are absolutely continuous with respect to $\eta$. We notice that a member of $M$ is absolutely continuous w.r.t. $\eta$ iff it is absolutely continuous w.r.t. some (and hence every) left Haar measure on $G$, or equivalently w.r.t. some (and hence every) right Haar measure. Thus the space $M_a$ is independent of the choice of the Haar measure.

The $\eta$ convolution of functions, $\star_{\eta}$, depends on $\eta$ according to the prescription $(g \star_{\eta} h)(y) = \int g(x)h(x^{-1}y)d\eta x$ for $\eta$ a.e. $y$, and replacing $\eta$ by another left Haar measure changes this convolution by a scalar factor. However $\star_{\eta}$ is not at all the convolution which is appropriate for members of $L_1(\rho)$, where $\rho$ is the right Haar measure corresponding to $\eta$; $\star_{\rho}$ should be defined so that the map $f \mapsto f.\rho$ for $f$ in $L_1(\rho)$ is a multiplicative map of $L_1(\rho)$ into the algebra of regular signed measures of finite total variation. Since $\rho = \Delta.\eta$, this will be accomplished if the map $f \mapsto (1/\Delta)f$ of $L_1(\rho)$ onto $L_1(\eta)$ is multiplicative; that is,

$$\frac{1}{\Delta}(h \star_{\rho} f) = \frac{h}{\Delta} \star_{\eta} \frac{f}{\Delta}$$

for functions $