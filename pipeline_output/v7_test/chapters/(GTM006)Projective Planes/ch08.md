 of $C$. Full $n$-step extensions are defined in the obvious way. The point of this is that in a full extension we have made sure that everything we could add on has been added; thus, while every configuration is an extension of itself, a configuration is a full extension of itself if and only if it is closed. The next special type of extension is: if $C_i$ is a one-step extension of $C$ such that every element of $C_1$ not in $C$ is incident with exactly two elements of $C_1$ then $C_i$ is a free one-step extension of $C$. Again free $n$-step extensions (or free extensions) are defined accordingly, and a free full extension is the appropriate combination of the two. It is important to note that an arbitrary configuration $C$ in a closed configuration $P$ may have no free extensions in $P$ at all excepting, trivially, $C$ itself. We are now in a position to formalize the intuitive construction for $<\langle C \rangle>_\theta$.

**Theorem 11.3.** If $C$ is a sub-configuration of a

Proof. We shall construct $\mathcal{P}$ so that it will, itself, be $\mathcal{F}(\mathcal{C})$. Let $\mathcal{C} = \mathcal{C}_0$, and define a sequence $\mathcal{C}_i$ of configurations as follows: if $\mathcal{C}_i$ is given, then $\mathcal{C}_{i+1}$ will consist of all the elements of $\mathcal{C}_i$ (with their incidences) and in addition for each pair $X$ and $Y$ of two like elements of $\mathcal{C}_i$ which are not incident with a common element in $\mathcal{C}_i$, we define an element $XY$, which is unlike $X$ and $Y$ and is incident with $X$ and $Y$ only, and is to be in $\mathcal{C}_{i+1}$. It is then easy to see that each $\mathcal{C}_i$ is an $i$-step full free extension of $\mathcal{C}$, and if we let $\mathcal{P} = \bigcup \mathcal{C}_i$, then $\mathcal{P}$ is closed, and $\langle \mathcal{C} \rangle_{\emptyset} = \mathcal{F}(\mathcal{C}) = \mathcal{P}$. The uniqueness of $\mathcal{F}(\mathcal{C})$, up to isomorphism, is easy; we omit the proof.

Note that, as a result of Theorem 11.4, when we are discussing free extensions we may forget the over-configuration in which the extensions are taken.

Suppose $\mathcal{C}$ is contained in the closed configuration $\mathcal{P}$ and let $\mathcal{B} = \langle \mathcal{C} \rangle_{\rho}$, let $X$ be an element of $\mathcal{B}$. Then since $\mathcal{B}$ is the union of full extensions of $\mathcal{C}$, there is an integer $n$ such that $X$ is in the full $n$-step extension $\mathcal{C}_i$ of $\mathcal{C}$, but not in $\mathcal{C}_{i-1}$ (if $X$ is in $\mathcal{C}_i$, then $n = 0$). We define $n$ to be the $\mathcal{C}$-stage of $X$, written $s_{\mathcal{C}}

maximal $\mathcal{B}$-stage $n > 0$ and this element has at most two incidences with elements of lower $\mathcal{B}$-stage; since this element must have at least three incidence in $\mathcal{A}$, it has an incidence with at least one element of $\mathcal{A}$ whose $\mathcal{B}$-stage is greater than $n$, and this contradicts the maximality of $n$.

Corollary. For any configuration $\mathcal{B}$, $\mathcal{K}(\mathcal{F}(\mathcal{B})) = \mathcal{K}(\mathcal{B})$.

Exercise 11.6. Show that there is only one confined configuration with seven points and seven lines, none with seven points and eight lines, and find one with eight points and eight lines.

*Exercise 11.7. Find all confined configurations with nine points and nine lines, and show that there are none with nine points and fewer than nine lines.

Lemma 11.6. Suppose $\mathcal{A}$, $\mathcal{B}$, $\mathcal{C}$ are configurations, $\mathcal{A} < \mathcal{B} < \mathcal{C}$. Then if any two of the three statements below are true, so is the third:

(a) $\mathcal{C}$ is a free extension of $\mathcal{A}$,
(b) $\mathcal{C}$ is a free extension of $\mathcal{B}$,
(c) $\mathcal{B}$ is a free extension of $\mathcal{A}$.

Proof. That (b) and (c) imply (a) is of course trivial.

Suppose (a) and (c) are true. Then if $\mathcal{D}$ is a maximal free extension of $\mathcal{B}$ such that $\mathcal{D} < \mathcal{C}$, we will be done if we can show that $\mathcal{D} = \mathcal{C}$. Suppose $\mathcal{D} \neq \mathcal{C}$ so that the addition of any element of $\mathcal{C}$ not in $\mathcal{D}$ must result in a configuration that is not a free extension of $\mathcal{B}$, hence not of $\mathcal{D}$. Choose an element $X$ in $\mathcal{C}$, not in $\mathcal{D}$, of minimal $\mathcal{A}$-stage, and let $\mathcal{D}_

extension of a finite number of steps. But all three configurations can be themselves infinite.

**Exercise 11.8.** If $\mathcal{B}$ is an extension of $\mathcal{A}$ show that $\mathcal{A}$ is finite if and only if $\mathcal{B}$ is.

The word "free" is crucial in Lemma 11.6. We now give two exercises to show what happens if freeness is not assumed.

**Exercise 11.9.** Show that if the word "free" is deleted in (a), (b), (c) of Lemma 11.6 then (a) and (c) imply (b) and (b) and (c) imply (a).

That (a) and (b) do not imply (c) is shown by the following:

**Exercise 11.10.** Let $\mathcal{A}$ be an affine plane, $k$ and $m$ distinct parallel lines of $\mathcal{A}$ and $Y$ a point of $\mathcal{A}$ not on $k$ or $m$. Let $\mathcal{C}$ be a configuration consisting of $\mathcal{A}$, the point $X$ and the line $n$ where $X$ is on $k$, $m$ and $n$ while $n$ passes through $X$ and $Y$. Let $\mathcal{B}$ be the union of $\mathcal{A}$ and $n$. Then show:

(i) $\mathcal{A} < \mathcal{B} < \mathcal{C}$ (this is trivial);
(ii) $\mathcal{C}$ is an extension (free) of $\mathcal{A}$ and an extension of $\mathcal{B}$;
(iii) $\mathcal{B}$ is not an extension of $\mathcal{A}$.

**Lemma 11.7.** Let $\mathcal{A}$ and $\mathcal{B}$ be configurations in the closed configuration $\mathcal{P}$ such that each is contained in an extension of the other. Then there is a sub-configuration $\mathcal{C}$ of $\mathcal{P}$ which is a common extension of each. If $\mathcal{P} = \mathcal{F}(\mathcal{A}) = \mathcal{F}(\mathcal{B})$, then $\mathcal{C}$ can be chosen to be a free extension (of both $\mathcal{A}$

Now in the case $P = F(A) = F(B)$, suppose we had chosen $B_1$ to be the union of the $B$-socles of all the elements of $A$. Then the free extension $A_1$ of $A$ has been shown to be a free extension of $B_1$, and so, by Lemma 11.6, $B_1$ is itself a free extension of $B$. But any free extension of $B$ that includes $A$ must certainly include $B_1$, and clearly we could have chosen $A_1 = B_1$, so $B_1$ is the configuration $C$ of the last part of the lemma.

Corollary. If $A_1, A_2, \ldots, A_n$ are configurations in the closed configuration $P$, such that $P = F(A)$ for each $i=1, 2, \ldots, n$, and each $A_i$ is contained in a free extension of any $A_j$, then there is a unique configuration $B$ which is a free extension of all the $A_i$ such that any configuration which is a free extension of all the $A_i$ must contain $B$.

We are now in a position to begin to attack the problem of determining when two configurations $A$ and $B$ have the same (or isomorphic) free closures. Certainly if there is a configuration $C$ which is simultaneously a free extension of both $A$ and $B$, then $F(A) = F(B)$; in view of Lemma 11.7 we have the converse, which we state as a theorem, in more general form.

Theorem 11.8. Let $A$ and $B$ be openly finite configurations. Then $F(A)$ and $F(B)$ are isomorphic if and only if there is a sequence $C_1, C_2, \ldots, C_n$ of configurations such that $C_1 \cong A, C_2 \cong B$, and each $C_{i+1}$ is a free extension of $C_i$ or $C_{i+1}$ is a free extension of $C_{i+1}$.

Proof. If such a sequence exists, then clearly $F(C_i) = F(C_{i+1

The corollary gives us some invariants for free closures; one of our goals will be to show that the core and the open rank are a complete set of invariants in the openly finite case.

4. Classification Problems

In this section we want to investigate two problems: the first is to decide when two free closures are isomorphic (i.e., the isomorphism problem), and the second is to decide whether a given projective plane is a free closure or not. We cannot give answers to these problems in general, indeed none are known at this time; but with the condition of "openly finite" added on, complete solutions can be found. In view of Theorem 11.8, we define two configurations $\mathcal{A}$ and $\mathcal{B}$ to be free equivalent if there is a sequence $\mathcal{C}_0, \mathcal{C}_1, \mathcal{C}_2, \dots, \mathcal{C}_n$ of configurations, such that:

(i) $\mathcal{A} \cong \mathcal{C}_0, \mathcal{B} \cong \mathcal{C}_n$,
(ii) for each $i = 0, 1, \dots, n - 1$, one of $\mathcal{C}_i, \mathcal{C}_{i+1}$ is a free extension of the other.

It is easy to see that "free equivalence" is an equivalence relation, and Theorem 11.8 can then be rephrased as: if $\mathcal{A}$ and $\mathcal{B}$ are o.f., then $\mathcal{F}(\mathcal{A}) \cong \mathcal{F}(\mathcal{B})$ if and only if $\mathcal{A}$ and $\mathcal{B}$ are free equivalent. We shall attack our first problem therefore, by finding a "canonical" configuration which is free equivalent to a given one, and noting that this canonical configuration depends only on the open rank and the core of the given configuration (if it is o.f.).

Lemma 11.9. If $\mathcal{A}$ is a non-degenerate finite open configuration, then $\mathcal{A}$ is free equivalent to a configuration $\mathcal{W}_1$, consisting of a line $k$ with $t$ points on $k$, and two points not on $k$. Furthermore, $t =

(i) $\mathcal{C}^*$ has the smallest possible number of lines;
(ii) among those satisfying (i), $\mathcal{Q}^*$ has the maximal number of 3-incident points (in $\mathcal{C}^*$).

Now suppose, if possible, that $m$ is a 0-incident line in $\mathcal{C}^*$. By intersecting $m$ with $k_1$ and $k_2$ (that is, adding the points $mk_1$ and $mk_2$) and then deleting $m$ (now a 2-incident line), we would have a $\mathcal{Q}$-configuration which is free equivalent to $\mathcal{A}$ but has fewer lines than $\mathcal{C}^*$, violating (i). So $\mathcal{C}^*$ has no 0-incident lines. Next, suppose $m$ is a 1-incident line, incident with the point $X$ say. If $X$ is not on both $k_1$ and $k_2$, then suppose $X$ is not on $k_2$; we adjoin the point $mk_2$ and delete $m$; again this gives a $\mathcal{Q}$-configuration free equivalent to $\mathcal{A}$ but with fewer lines than $\mathcal{C}^*$ which violates (i), so $m$ did not exist. Suppose $X$ is the intersection point $k_1k_2$. If there is already a line on $P_1$ and $P_3$, let it be $y$; otherwise add the line $y = P_1P_3$. In either case adjoin the point $Z = ym$ and delete $m$; if the line $y$ had already existed this would violate (i) again, so we assume that $y$ was added as the union of $P_1$ and $P_3$. If we adjoin the line $w = ZP_2$ and delete $Z$; then we can delete $y$. Now the situation is that instead of the 1-incident line $m$, we have the 1-incident line $w$, but incident with the point $P_2$, since $P_2$ is not on $k_2$ this case has already been

lie on $u$, we can replace the quadrilateral $\mathcal{Q}^*$ by $\mathcal{L}$, where we write $X$ instead of $P_4$ and $u$ instead of $k_2$. This contradicts (ii). So $P_1$, say, must lie on $u$; there is another line $w$ on $X$, such that $w$ does not contain $P_4$ (since $X$ lies on at least 3 lines), and $w$ is a 3-incident line. Again, we distinguish two cases: $P_2$ is on $u$ or not. If it is on $u$, then let $R$ be a third point on $w$; $R$ is not 1-incident (since 1-incident points are on one of the $k_1$), so $R$ must be 3-incident. Hence we replace $\mathcal{Q}^*$ by $\mathcal{L}$, where the points are $P_1$, $P_2$, $P_3$, $R$, and the lines of $\mathcal{Z}$ are $u$ and $w$. This also violates the assumption (ii). So $P_2$ is not on $w$, and we can choose a 3-incident point $R$ on $w$, $R \neq X$. Then we replace both $P_3$ and $P_4$ in $\mathcal{Z}$ by $X$ and $R$, replacing $k_2$ by $w$ at the same time; this certainly increases the number of 3-incident points, and so violates (ii).

Consequently we now can suppose that there is no line $u = XP_3$. It is easy to see that there must be a line $w$ on $X$ which contains a point $Y$ not on any $k_i$; so as above, we discard $P_3$, $P_4$, $k_2$, replacing them with $X$, $Y$ and $w$. So we have:

(3) If there are any 3-incident points not on the $k_0$, or if there are any lines other than the $k_i$, then all the points $P_i$ are 3-incident.

This follows from observing that

Proof. Among all the o.f. configurations free equivalent to $\mathcal{A}$, choose one, $\mathcal{C}$ say, which has (1) the minimal number of 1-incident elements which are not incident with an element of $\mathcal{K}$, and (2) has no 2-incident elements. That (2) can be satisfied is evident, since any decreasing sequence of free equivalent o.f. configurations must be stationary after a certain point.

Suppose $\mathcal{C}$ has a 1-incident element not incident with $\mathcal{K}$, and let $X$ be such a one, incident with $m$ say. Choose $Q$ in $K$ and not on $m$, construct the configuration $\mathcal{C}$, consisting of $\mathcal{C}$ plus the line $QX$, and then the configuration $\mathcal{C}$, which results from deleting $X$ from $\mathcal{C}_1$. Clearly $\mathcal{C}_2$ is free equivalent to $\mathcal{C}$, and hence to $\mathcal{A}$, and has fewer 1-incident elements not incident with $\mathcal{K}$. So all the 1-incident elements of $\mathcal{C}$ are in fact incident with elements of $\mathcal{K}$.

Suppose that $X$ is a 3-incident element, incident with $k, m, n$ say, and suppose that $X$ is not in $\mathcal{K}$. Then none of $k, m, n$ can be 1-incident elements, since then $X$ would be their only incidence, which is impossible, as $X$ is not in $\mathcal{K}$. So all of $k, m, n$ are 3-incident.

Now the core of $\mathcal{C}$ must be $\mathcal{K}$. But if we let $\mathcal{L}$ be the set of all 3-incident elements of $\mathcal{C}$, then certainly $\mathcal{L}$ includes $\mathcal{A}$ as well as the finitely many 3-incident elements (as above) not in $\mathcal{K}$. But let $\mathcal{S}$ be the set of all 3-incident elements in $\mathcal{L}$ and not in $\mathcal{K}$; $\mathcal{S}$ is finite, so if we adjoin to it any finite set of elements

elements are on $k$. Finally the 0-incident elements can be shifted to points on $k$ as follows: suppose $X$ is a 0-incident point, then choose two points $P$ and $Q$ in $K$ and adjoin the two lines $PX$ and $QX$, then delete $X$; now shift to points on $k$, as above. Note that a 0-incident element will contribute two points on $k$ (as it must since open rank stays fixed). Now we have proved our lemma. (The open rank of $\mathcal{B}$ will be the number of points on $k$ but not in $\mathcal{K}$.) $\square$

**Theorem 11.11.** Let $\mathcal{A}$ and $\mathcal{B}$ be of $f$. configurations. Then $\mathcal{F}(\mathcal{A}) \cong \mathcal{F}(\mathcal{B})$ if and only if $r_0(\mathcal{A}) = r_0(\mathcal{B})$ and $\mathcal{K}(\mathcal{A}) \cong \mathcal{K}(\mathcal{B})$.

**Proof.** The theorem follows immediately from the two preceding lemmas, plus the Corollary to Theorem 11.8 since we have seen that the "canonical form" for an o.f. configuration depends only upon its open rank and its core. $\square$

Now we are ready to attack the next problem: if $\mathcal{P}$ is openly finitely generated, when it is a free closure? Suppose $\mathcal{P} = \langle \mathcal{A} \rangle_{\mathcal{G}}$, where $\mathcal{A}$ is o.f.; then let $\mathcal{K} = \mathcal{K}(\mathcal{P})$, and $\mathcal{B} = \mathcal{A} \cup \mathcal{K}$. Since $\mathcal{A}$ is o.f., only finitely many of its elements are not in the core of $\mathcal{A}$, hence only finitely many are not in $\mathcal{K} > \mathcal{K}(\mathcal{A})$. So $\mathcal{B}$ is o.f., $\mathcal{P} = \langle \mathcal{B} \rangle_{\mathcal{G}}$, and $\

This lemma violates the minimality of the open rank of $C$. So each $C_i$ is a free extension, and $P = F(C)$.

Theorem 11.13. If $P$ is a projective plane generated by an openly finite configuration, then either $P$ is its own core or there is an of. configuration $C \neq P$ such that $P = F(C)$.

Corollary. An of. generated projective plane is a free closure of an of. configuration unless every element of the plane is contained in a finite confined sub-configuration.

We finish this section with some results about homomorphisms and configurations.

Theorem 11.14. Let $A$ and $B$ be non-degenerate configurations such that $B$ is contained in a closed configuration $P$ and suppose $\beta$ is a homomorphism of $A$ onto $B$. Then $\beta$ extends to a homomorphism $\bar{\beta}$ of $F(A)$ onto $\langle B \rangle$.

Proof. First we show how to extend a homomorphism from $A$ onto $B$ to a homomorphism from $A_i$ into $\langle B \rangle$, where $A_i$ is a full free one-step extension of $A$. For any element $P$ in $A$ define $P^b = P^0$. If $X$ is in $A_i$, but not in $A$, then $X = yz$, for a unique pair of elements $y$ and $z$ of $A$. If $y^b \neq z^b$, then $X^b = y^z z^b$ in $\langle B \rangle$, is forced upon us, while if $y^b = z^b$, then we choose $X^b$ to be some point on $y^b$ which is also in $\langle B \rangle$. The non-degeneracy of $B$ assures us that such a point exists. Thus we can extend $\beta$ to a homomorphism of $F(A)$ into $\langle B \rangle$; but the homomorphic image of a closed configuration is obviously closed, so the extended homomorphism must be onto.

Corollary. Every projective plane is the homomorphic image of a free closure.

5. Collineations

We shall now use the machinery developed in the previous sections to prove a number of interesting results about collineations. As always, we write Aut $\mathcal{A}$ for the group of collineations of the configuration $\mathcal{A}$.

**Lemma 11.15.** If $\mathcal{B}$ is a full free extension of $\mathcal{A}$ then Aut $\mathcal{B}$ has a subgroup isomorphic to Aut $\mathcal{A}$.

**Proof.** Obviously it is only necessary to prove the lemma when $\mathcal{B}$ is a full free one-step extension of $\mathcal{A}$. Let $\beta$ be a collineation of $\mathcal{A}$ and let $P$ be any element of $\mathcal{B}$ which is not in $\mathcal{A}$. Since $\mathcal{B}$ is a free one-step extension of $\mathcal{A}$, there are exactly two elements $x, y$ of $\mathcal{A}$ which are incident with $P$ in $\mathcal{B}$. The fullness of the extension implies that $x^0$ and $y^0$, which clearly have no common element in $\mathcal{A}$, meet in $\mathcal{B}$. Defining $P^0$ as the intersection of $x^0$ with $y^0$ and defining $X^0 = X^0$ for all $X$ in $\mathcal{A}$ extends $\beta$ to be a collineation $\beta$ of $\mathcal{B}$, and the lemma is proved. (Note that we need both fullness and freeness to prove this lemma; it is easy to give examples where the lemma is false without both of these assumptions.)

**Lemma 11.16.** If $\mathcal{C} < \mathcal{P}$ and $\alpha$ is in Aut $\mathcal{P}$, then $\mathcal{C}$ is confined if and only if $\mathcal{C}^0$ is.

**Proof.** Trivial. $\square$

**Corollary.** For any $\alpha$ in Aut $\mathcal{P}$, $[\mathcal{K}(\mathcal{P})]^n = \mathcal{K}(\mathcal{P})$.

**Lemma 11.17.** If $\mathcal{P} = \langle \

Exercise 11.13. Let $\mathcal{A}$ be an off. configuration and $\mathcal{P} = \mathcal{F}(\mathcal{A})$; let $\theta$ be a polarity of $\mathcal{P}$. Show that the number of absolute elements of $\theta$ not in $\mathcal{K}(\mathcal{A})$ is finite.

Now we exhibit a confined configuration which has no non-identity collineation, and thus we will have a projective plane whose full group of collineations is the identity; this is, in some sense, as far from the desarguesian case as one might imagine. Let $\mathcal{C}$ be the configuration with the ten points 0, 1, 2, ..., 9 and the ten lines whose points are:

$$
\begin{array}{c c c c c}
0 & 1 & 2 & 3 & 2 & 5 & 8 \\
0 & 4 & 5 & 2 & 6 & 9 \\
0 & 6 & 7 & 3 & 6 & 8 \\
1 & 4 & 6 & 3 & 7 & 9 \\
1 & 5 & 7 & 4 & 8 & 9.
\end{array}
$$

Then we can argue as follows to show that $\mathcal{C}$ has only the identity collineation: the line 0123 is the only one with four points, so is fixed by any collineation $\alpha$, and since 489 is the only line not meeting 0123, it is fixed as well. Dually, 6 occurs on four lines, and is the only point with this property, so $6^2 = 6$, and 5 is the only point not joined to 6, so $5^2 = 5$. Now 067 and 157 are the only lines (other than 0123) not meeting the fixed line 489, so are fixed or interchanged; since each contains a different fixed point (5 or 6), each is fixed, so their intersection 7 is also fixed, and then the third point on each is fixed; that is, $0^2 = 0$ and $1^2 = 1$. Similarly 3 and 9 do not meet 5, so they

The configurations given above lead to examples of projective planes "without collineations" but with a polarity, and "without collineations" and also without a polarity (hence without a correlation).

Note that since the product of any two correlations is a collineation, any projective plane without non-trivial collineations can admit at most one correlation, which must be a polarity.

Exercise 11.17. If $\mathcal{C}$ is a configuration with a polarity $\theta$ and if $\bar{\theta}$ is the extension of $\theta$ to $\mathcal{C}$, the full free one-step extension of $\mathcal{C}$, show that all the absolute elements of $\bar{\theta}$ are in $\mathcal{C}$ and are absolute elements of $\theta$. Hence the extension of $\theta$ to the free closure of $\mathcal{C}$ has all its absolute elements in $\mathcal{C}$.

Exercise 11.17 illustrates the sharp contrast between polarities of free closures and the situation in desarguesian planes, or for that matter in finite planes (see Chapters II and XII). We can certainly construct a finite configuration $\mathcal{C}$ which admits polarities whose absolute points do not in any way resemble an oval and then, by Exercises 11.15 and 11.17, $\mathcal{F}(\mathcal{C})$ will also admit polarities with the same absolute points.

We have already proved enough to see that collineations and polarities of free closures act in a somewhat surprising way. However, whenever certain finiteness conditions are put on the generating configuration, the situation is not completely chaotic. We now sketch some very nice structure theorems about finite subgroups of the collineation groups of free closures of o.f. configurations which are not in fact open. The situation for free closures of finite open configurations is not dissimilar, but the proofs are considerably harder, so we deal with the case of an o.f. but not open, configuration. (Many of these results were proved by O'Gorman and a more complete study of these groups can be found in [2]). Let $\mathcal{A}$ be an o.f. configuration which is not open, $\mathcal{K} = \mathcal{K}(\mathcal{A})$, $\mathcal{P} = \mathcal{F}(\mathcal

either $k$ or $m$ (this is always possible: why?). Let $\mathcal{A}$ be freely extended by adding the $r$ lines $w_i = P X_i$, then the $r$ points $Y_i = w_i m$ (if $k = m$, then each $Y_i$ is $X_i$). Now deleting each $w_i$ (for each one has just two incidences) and then deleting $X_i$, we have a new configuration $\mathcal{A}$ which is free equivalent to $\mathcal{A}$. We map $\mathcal{A}$ onto $\mathcal{A}'$ by sending every element of $\mathcal{K}$ onto its image under $\alpha$, and sending $X_i$ to $Y_i$; this mapping, which we will call $\alpha$, is an isomorphism from $\mathcal{A}$ to $\mathcal{A}'$, and extends to a isomorphism of $\mathcal{F}(\mathcal{A})$ onto $\mathcal{F}(\mathcal{A}')$; since these two free closures are equal, we have constructed a collineation of $\mathcal{P}$ which acts as $\alpha$ on $\mathcal{K}$.

Now let $\Lambda$ be a finite subgroup of $\Gamma$. By Theorem 11.19 there is an o.f. configuration $\mathcal{B}$, free equivalent to $\mathcal{A}$, such that $\Lambda$ fixes $\mathcal{B}$. We now reduce $\mathcal{B}$, with respect to $\Lambda$ as it were, in a manner very similar to that used in the proof of Lemma 11.10. If $\mathcal{B}$ has any 2-incedent elements, then $\Lambda$ sends 2-incedent elements to 2-incedent elements, so $\Lambda$ equally fixes the configuration obtained from $\mathcal{B}$ by deleting all its 2-incedent elements. Any 1-incedent element is sent by $\Lambda$ to another, and so we can deal with these as before, making sure that in $\mathcal{B}$ they are all incident with an element of $\mathcal{K}$. Then we show, just as before, that all 3-incedent elements are in $\mathcal{K}$ itself, and then move all the 1-incedent elements (but instead of doing it one at a time,

there is a set of points $\{Q_1, \dots, Q_r\}$ on some line $m$ of $\mathcal{K}$ such that $\mathcal{F}(\mathcal{A}) = \mathcal{F}(\mathcal{K} \cup \{Q_1, \dots, Q_r\})$ and $\Sigma_2$ induces $S_r$ on $\{Q_1, \dots, Q_r\}$.

(i) Suppose $l = m$. Let $\alpha$ be the mapping from $\mathcal{K} \cup \{P_1, \dots, P_r\}$ to $\mathcal{K} \cup \{Q_1, \dots, Q_r\}$ given by $X^\alpha = X$ for all $X$ in $\mathcal{K}$ and $P_r^\alpha = Q_r$ for $i = 1, \dots, r$; then it is clear that $\alpha$ is an isomorphism between the two given generating configurations. As an interesting exercise, the reader should now mimic the proof of Lemma 11.15 to show that $\alpha$ extends to a collineation $\alpha$ of $\mathcal{F}(\mathcal{A})$ and that $\alpha^{-1} \Sigma_1 \alpha = \Sigma_2$.

(ii) Suppose $l \neq m$. In this case we shall show $\Sigma_2$ also induces $S_r$ on a set $\{R_1, \dots, R_r\}$ of points on $l$ such that $\mathcal{F}(\mathcal{A}) = \mathcal{F}(\mathcal{K} \cup \{R_1, \dots, R_r\})$ and then use (i).

Let $X$ be any point of $\mathcal{K}$ which is not incident with either $l$ or $m$ and define $R_1 = Q_1 X \cap l$ for $i = 1, \dots, r$. Since $\Sigma_2$ fixes $l, m$ and $X$, $\Sigma_2$ must induce the same group on $\{Q_1, \dots, Q_r\}$ and $\{R_1, \dots, R_r\}$, i.e. $\Sigma_2

XII. Polarities

1. Introduction

We saw in Chapter II that polarities in pappian projective planes are of two sorts: orthogonal or unitary. These were different in the first place because one was linear and the other was semi-linear; but when they actually possessed absolute points, they could also be characterized by the structure of these absolute points inside the plane. In the finite case the absolute points told the whole story: if a polarity is orthogonal, then its number of absolute points is $n+1$, where $n$ is the order of the plane, and in addition, these points are collinear when $n$ is even, and form a conic when $n$ is odd; while for a unitary polarity, $n$ must be a square and the number of absolute points is $n^{3/2}+1$, and in addition the set of absolute points and non-absolute lines form a combinatorial structure called a unital. In this chapter we shall generalise these results by showing that if $\alpha$ is a polarity of a finite projective plane of order $n$, and if $a(\alpha)$ represents the number of absolute points of $\alpha$, then always $n+1 \leq a(\alpha) \leq n^{3/2}+1$ (Theorems of Baer and Seib), and in addition: if $a(\alpha)=n+1$, then the absolute points are collinear when $n$ is even and form an oval when $n$ is odd; if $a(\alpha)=n^{3/2}+1$, then the absolute points and non-absolute lines form a unital. We shall see that an oval is exactly the right "non-desarguesian" generalization of a conic (Segre's Theorem). There are some other interesting results, such as the theorem that when $n$ is not a square, then $a(\alpha)=n+1$. We give some examples of polarities in non-desarguesian planes as well.

In view of the results of Chapter XI, where we can construct infinite planes with polarities whose number of absolute points is finite but arbitrary, it is clear that polarities in arbitrary infinite planes must be very different from those in finite planes, and we will have nothing to say about the infinite case in this chapter. It would be most interesting to be able to say more about the

from a unitary polarity of a projective plane, or can even be embedded in a projective plane, are not very well understood at this time.

2. Absolute Elements of Polarities

We recall that a correlation $\alpha$ of a projective plane $\mathcal{P}$ is a one-to-one mapping of the points onto the lines and the lines onto the points such that $A$ is on $m$ if and only if $m^n$ is on $A^n$. Thus a projective plane admits a correlation if and only if it is self-dual. A polarity is a correlation of order two and it is not known whether a self-dual plane must admit a polarity.

If $\alpha$ is a polarity of a projective plane $\mathcal{P}$ then a point $A$ (line $m$), is called absolute if $A$ is on $A^n$ ($m^n$ is on $m$). We begin our discussion of polarities by looking at the possibilities for the number of absolute elements and the configurations which they may form. Most of the results are due to Baer although some of the proofs are very different to those which he gave.

Lemma 12.1. Let $\alpha$ be a polarity of a projective plane $\mathcal{P}$. Then every absolute point of $\alpha$ is on a unique absolute line and, dually, every absolute line contains a unique absolute point.

Proof. We first make the trivial observation that, since $\alpha$ preserves incidence, for any pair of points $A$ and $B, A$ is on $B^n$ if and only if $B$ is on $A^n$.

Since the two claims of the lemma are self dual we need only prove one of them. If $m$ is any absolute line of $\alpha$ then, by definition, $m^n$ is on $m$ and so $m$ contains at least one absolute point. Put $A = m^n$ and suppose there is a second absolute point $B$ on $m$. Since $B$ is on $A^n$, $A$ must be on $B^n$ and thus, since $B$ is also on $B^n$ (by the assumption that $B$ is absolute), $B^n = AB = m$. But this implies $m

The $C$ has $n+v$ as a simple eigen root and $n$ as an eigen root with multiplicity $v-1$.

Proof. For $i=1,2,\ldots,v-1$ define a $1\times v$ vector $x_i=(0,0,\ldots,1,-1,\ldots,0)$ where the $i^n$ entry is $1$, the $i^n$ entry is $-1$ and all other entries are $0$. Then, since $x_iJ=0$ for all $i$, $x_iC=x_iI+x_iJ=nx_i$, so that $n$ is an eigen root with at least $v-1$ independent eigen vectors. Now if $y$ is the $1\times v$ vector with each entry $+1$ then $yC=ny+yJ=ny+vy=(n+v)y$, so that $n+v$ is also an eigen root. Since the total number of eigen roots of $C$ is at most $v$, $C$ must have $n$ as an eigen root of multiplicity $v-1$ and $n+v$ with multiplicity $1$.

Lemma 12.3. Let $\alpha$ be a polarity of a finite projective plane $\mathcal{P}$ of order $n$. Then $\alpha$ has at least one absolute point. Furthermore, if $n$ is not a square $\alpha$ has exactly $n+1$ absolute points.

Proof. Write $v=n^2+n+1$, label the points $X_1, X_2,\ldots,X_v$ in an arbitrary manner and then label the lines $m_1,m_2,\ldots,m_v$ so that $m_i=X_i^n$ for $i=1,2,\ldots,v$. If $A$ is the incidence matrix of $\mathcal{P}$ given by this labelling (i.e. $a_{ij}=1$ if and only if $X_i$ is on $m_j$), then, since $X_i$ is on $X_i^n=m_j$ precisely when $X_j$ is on $X_i^n=m_i$ $A$ is symmetric. Furthermore the point $X_i$ is absolute if and only if $a_{

Proof. If $X$ is any point of $m$ then, since $m$ is not absolute, $X^\alpha \neq m$. For any $X$ on $m$ define $X^\theta$ by $X^\theta = m \cap X^\alpha$; then $(X^\theta)^p = (X^\theta)^p \cap m = (X^\alpha) \cap m \cap m = X \cdot m^n \cap m = X$. Thus $\theta$ is an involutory permutation of the points of $m$ and, if $t$ is the number of points fixed by $\theta$, then $n + 1 - t \equiv 0$ (mod 2) or $t \equiv (n + 1)$ (mod 2). But $A^0 = A$ if and only if $A$ is on $A^0$, so that $t$ is also equal to the number of absolute points of $\alpha$ on $m$ and the lemma is proved. $\square$

Corollary. If $\alpha$ is a polarity of a finite projective plane $\mathcal{P}$ of even order $n$ then every line of $\mathcal{P}$ contains at least one absolute point.

Theorem 12.5. Let $\alpha$ be a polarity of a finite projective plane $\mathcal{P}$ of order $n$. Then $n$ has at least $n + 1$ absolute points.

Proof. If $a(\alpha)$ is the number of absolute points of $\alpha$ then, by Lemma 12.3, $a(\alpha) \geq 1$.

Case (a). $n$ is even.

By the corollary to Lemma 12.4, each line contains an absolute point of $\alpha$. If $t$ is the number of flags such that the point is an absolute point of $\alpha$ then, clearly, $t = a(\alpha)(n + 1)$. However, as every line is in at least one such flag, $t \geq n^2 + n + 1$ which gives $a(\alpha)(n + 1) \geq n^2 + n + 1 > n^2 + n$, i.e. $a(\alpha) > n$. Thus, since $a(\alpha

Proof. Case (a). Suppose $n$ is even.

If $m$ is a line joining two absolute points of $\alpha$ then we must show that every point of $m$ is an absolute point. We shall suppose that $X$ is a non-absolute point on $m$ and show that this leads to a contradiction. Let $m_1, m_2, \dots, m_n$ be the $n$ lines through $X$ distinct from $m$ and let $a_i$ be the number of absolute points of $\alpha$ on $m_i$. Then, by the corollary to Lemma 12.4, $a_i \geq 1$ for all $i$. Furthermore, since $X$ is non-absolute, the $a_i$ absolute points on $m_i$ are distinct from the $a_j$ absolute points on $m_j$ for $i \neq j$, and so the total number of absolute points not on $m$ is $\sum_{i=1}^{n} a_i \geq n$.

But there are at least two absolute points on $m$, and this implies that $\alpha$ has at least $n+2$ absolute points and gives the required contradiction.

Case (b). Suppose $n$ is odd.

If $m$ is a line joining two absolute points $A$ and $B$ we must show that $m$ cannot contain a third absolute point. So we assume $m$ contains a third absolute point $C$ and show that this gives a contradiction. By Lemmas 12.1 and 12.4, $A$ is incident with only one absolute line and each of the $n$ non-absolute lines through $A$ contains a further absolute point distinct from $A$. Thus each of the $n-1$ non-absolute lines other than $m$ through $A$ contains an absolute point distinct from $A$, $B$ and $C$. But now $\alpha$ has at least $(n-1)+3=n+2$ absolute points and the theorem is proved. []

As a direct corollary to Lemma 12.3 and Theorem 12.6 we have the following theorem which gives precise information about polarities in planes of non-square order.

Theorem 12.7. Let $\alpha$ be a polarity of a finite

Proof. Let $\mathcal{O} = \{A, A_1, \dots, A_n\}$. Then each of the lines $m_i = AA_i$ is a secant through $A$ and, clearly, $m_i \neq m_j$ if $i \neq j$. Thus $A$ has exactly $n$ secants through it. If $m$ is the remaining line through $A$ then, as $A$ is on $m, m$ is not an exterior line and, as $m \neq m_i$ for any $i$, is not a secant; thus $m$ is a tangent. $\square$

Exercise 12.1. If $\alpha$ is a polarity of a finite projective plane whose absolute points form an oval $\mathcal{O}$, show that the absolute lines of $\alpha$ are the tangents to $\mathcal{O}$.

Exercise 12.2. If $\mathcal{O}$ is an oval of a finite projective plane $\mathcal{P}$ show that $\mathcal{P}$ admits at most one polarity whose absolute points are the points of $\mathcal{O}$.

Exercise 12.3. If $\mathcal{O}$ is an oval of a finite projective plane $\mathcal{P}$ of order $n$, show that $\mathcal{O}$ has $n+1$ tangents, $\frac{1}{2} n(n+1)$ secants and $\frac{1}{2} n(n-1)$ exterior lines.

We have already seen that in $\mathcal{P}_2(q)$ if there are $q+1$ absolute points then they are either collinear or lie on a conic. In fact, as we claimed earlier, conics provide the only examples of ovals in $\mathcal{P}_2(q)$ for $q$ odd.

Theorem 12.9 (Segre's theorem). Every oval in a desarguesian plane of odd order is a conic.

Proof. This theorem has a very elegant and simple proof which we include in the first appendix at the end of the chapter. The proof given is essentially the original one of Segre's and could have been given in Chapter 2. Its "flavour" is very different to the rest of the other chapters, in that it uses homogeneous coordinates and the

Lemma 12.10. Let $\mathcal{O}$ be an oval in a projective plane of even order $n$. Then all the tangents to $\mathcal{O}$ are concurrent at a point called the knot (or nucleus) of $\mathcal{O}$.

Proof. Let $(X, m)$ be any flag such that $X$ is not in $\mathcal{O}$ and $m$ is a secant line. Since every line through $X$ contains either 0, 1 or 2 points of $\mathcal{O}$ and the number of points of $\mathcal{O}$ is odd, at least one line through $X$ is a tangent to $\mathcal{O}$. Let $X_1, X_2, \dots, X_{n-1}$ be the points of $m$ not on $\mathcal{O}$ and let $h_i$ be the number of tangents through $X_i$; then we have just shown $h_i \geq 1$ for all $i$.

If $A$ and $B$ are the points of $m$ on $\mathcal{O}$ then, by Lemma 12.8, $A$ and $B$ are on unique tangents $a$ and $b$ respectively. Each of the $n-1$ tangents to $\mathcal{O}$ other than $a$ or $b$ must intersect $m$ in one of the $X_i$. Thus $n-1 = \sum_{i=1}^{n} h_i$ and so, since $h_i \geq 1$, we have $h_i = 1$ for $i=1, 2, \dots, n-1$. Since $m$ was any secant this implies that no two tangents can intersect at a point on a secant.

Let $C = ab$. Since $C$ cannot lie on a secant, the join of $C$ to any point of $\mathcal{O}$ must be a tangent. Hence all the tangents to $\mathcal{O}$ are concurrent at $C$ and, furthermore, every line through $C$ is a tangent to $\mathcal{O}$. []

Clearly $\mathcal{O}$ together with its knot form a set of $n+2$ points such that no three are collinear and any

Theorem 12.11. Let $\alpha$ be a polarity of a finite projective plane $\mathcal{P}$ of order $n = s^2$. Then $a(\alpha) \leq s^2 + 1$.

Proof. From the proof of Lemma 12.3, $a(\alpha)$ is of the form $1 + s^2 + (k_1 - k_2)s$ where $k_1 + k_2 = s^4 + s^2$. Furthermore, by Theorem 12.5, $k_1 - k_2$ is non-negative and, since $k_1 + k_2$ is even, $k_1 - k_2$ is even. Thus $a(\alpha)$ is of the form $1 + 2rs + s^2$ where $r$ is a non-negative integer.

Putting $w = 1 + 2rs + s^2$ then the number $b$ of non-absolute lines is given by $b = n^2 + n + 1 - (1 + 2rs + s^2) = s^4 - 2rs$. Label the non-absolute lines $l_1, l_2, \dots, l_n$ and let $k(l_i)$ be the number of absolute points on $l_i$. Then clearly, the number of absolute point and non-absolute line flags is given by $\sum_{i=1}^{b} k(l_i)$. But there are $w$ absolute points and, by Lemma 12.1, each is incident with $s^2$ non-absolute line flags as $ws^2$ and so

$$\sum_{i=1}^{b} k(l_i) = ws^2.$$ (1)

Let $P$ be a fixed absolute point; then, by Lemma 12.1, every other absolute point is joined to $P$ by a non-absolute line and, furthermore, $P$ is on $s^2$ non-absolute lines and thus

$$\sum_{i=1}^{b} k(l_i) = (w - 1) + s^2.$$ (2)

If we now let $P$ vary over the absolute points then $k(l_i)$ will be counted once for each absolute point on $

Although in general polarities may behave very differently from the desarguesian situation, if the number of absolute points is the maximum possible then the absolute points form the same "type" of configuration as in the desarguesian case. In order to clarify this statement we define a special type of "design":

A $2 - (n, k, 1)$ design is a set of $v$ points with distinguished subsets called lines such that each line contains $k$ points and any two distinct points are contained in a unique line.

Clearly a finite projective plane of order $n$ is a $2 - (n^2 + n + 1, n + 1, 1)$ design.

**Exercise 12.7.** Show that a $2 - (n^2 + n + 1, n + 1, 1)$ design is a projective plane, if $n > 2$.

**Exercise 12.8.** If $\alpha$ is a unitary polarity of $\mathcal{P}_2(q^2)$ show that the set of absolute points and non-absolute lines form a $2 - (q^3 + 1, q + 1, 1)$ design.

In view of Exercise 12.8 we call a $2 - (s^3 + 1, s + 1, 1)$ design a unital. We now show that if the number of absolute points of a polarity of any plane of square order is a maximum, then the absolute points and non-absolute points form a unital, thus exhibiting the similarity with the desarguesian case.

**Theorem 12.12.** Let $\alpha$ be a polarity of a finite projective plane of order $s^2$. If $a(\alpha) = s^3 + 1$ then the set of absolute points and non-absolute lines forms a unital.

**Proof.** Using the notation of the proof of Theorem 12.11 and putting $v = s^3 + 1$ and $b = s^4 - s^3 + s^2$ in (1) and (3) we have

$$\sum_{i=1}^{b} k(l_i) = s^2(s^3 + 1),$$

$$\sum_{i=1}^{b} k

But $\sum_{i=1}^{b} (k(l_i) - (s+1))^2$ is the sum of squares of integers and, consequently, can only be zero if each summand is zero. Thus $k(l_i) = s+1$ for each $i$ and the theorem is proved. []

If $\mathcal{P}$ is a finite projective plane of order $n$ then a polarity $\alpha$ of $\mathcal{P}$ is called orthogonal if $a(\alpha) = n+1$ and unitary if $a(\alpha) = n^{3/2}+1$. As we have already seen, any polarity of a desarguesian plane is either orthogonal or unitary. Not much is known about polarities with $n+1 < a(\alpha) < n^{3/2}+1$, nor are many examples known. Each of the known examples is irregular in the following sense: a polarity $\alpha$ is called regular if there is an integer $t$ such that the number of absolute points on a non-absolute line (absolute lines through a non-absolute point) is 0.1 or $t+1$. Note that all polarities of finite desarguesian planes are regular.

We shall give examples of irregular polarities and of unitary and orthogonal polarities in non-desarguesian planes. As we noted earlier, there are no known regular polarities which are not orthogonal or unitary and it has been conjectured that any regular polarity must be either unitary or orthogonal.

5. Polarities in Translation Planes

If $\phi$ is a polarity and $\alpha$ a collineation then, clearly, $\phi\alpha\phi$ is a collineation.

Exercise 12.9. If $\alpha$ is a $(P,m)$-central collineation and $\phi$ is a polarity show that $\phi\alpha\phi$ is a $(m^a,P^b)$-central collineation.

Exercise 12.10. If $m$ is a translation line of a projective plane $\mathcal{P}$ and $\phi$ is a polarity of $\mathcal{P}$ show that $\mathcal{P}$ is a dual translation plane with respect to $m^b$.

As an immediate corollary to

$\alpha, \beta$ of $D$ as follows:

$$(m, 0)^{\theta} = [m^{\theta}, 0],$$ (1)

$$(0, y)^{\theta} = [0, -y^{\theta}].$$ (2)

Note that $0^{\alpha} = 0^{\beta} = 0$.

Exercise 12.11. Show that $\theta$ may be described as follows

$$(\infty) \leftrightarrow [\infty],$$ $$(m)^{\theta} = [m^{\theta}^{-1}],$$ $[k]^{\theta} = (k^{\theta})$$

$$(x, y)^{\theta} = [x^{\theta}, -y^{\theta}],$$ $[m, k]^{\theta} = (m^{\theta}^{-1}, -k^{\theta})$.

Since $\theta$ preserves incidence, $(x, y)$ is on $[m, k]$ if and only if $(m^{\theta}^{-1}, -k^{\theta})$ is on $[x^{\theta}, -y^{\theta}]$. Thus $mx + y = k$ if and only if $x^{\theta}m^{\theta}^{-1} - k^{\theta} = -y^{\theta}$ which gives

$$y^{\alpha} + x^{\theta}m^{\theta}^{-1} = k^{\alpha} = (mx + y)^{\alpha}.$$ (1)

Putting $y = 0$ in (1) and using $0^{\alpha} = 0$ we have

$$(mx)^{\alpha} = x^{\theta}m^{\theta}^{-1}.$$ (2)

Thus

$$(mx + y)^{\alpha} = (mx)^{2} + y^{\alpha}$$ (3)

so that $\alpha$ is additive.

Since $D$ is a division ring $(x(y + z))^{n} = (xy + xz)^{n}$ and so, by (1) and (2)

$$(y + z)^{\theta}x^{\theta}^{-1} = (xy)^{2} + (xz)^{2} = y^{\theta}x^{\theta}^{-1} + z^{\theta}x^{\theta}^{-1}$$

which simplifies to

$$(y + z)^{\theta}

Exercise 12.12. If $D_1 = \{x \in D | x^\alpha = x\}$ and $D_2 = \{x \in D | x^\alpha = -x\}$, show that, considered as additive groups, $D = D_1 \oplus D_2$.

Theorem 12.15. If $\mathcal{P}(D)$ has odd order $n$ and $\theta$ is a polarity determined by $\alpha$ and $\beta$ as in Exercise 12.11 then

$$a(\theta) = n|D_2| + 1$$

Proof. We have already shown that the number of absolute points on $[0] \setminus \infty$ is $|D_2|$. If $k$ is any element of $D$ then any point of $[k] \setminus \infty$ is of the form $(k, y)$. If $(k, y)$ is an absolute point, then $(k, y)$ is on $[k^\theta, -y^\theta]$ so that $k^\theta k + y = -y^\theta$. If we write $y = d_1 + d_2$, where $d_1 \in D_1$ $(i = 1, 2)$, then $k^\theta k + d_1 + d_2 = -d_1 + d_2$ and, hence, $2d_1 = -k^\theta k$. Thus if a point of $[k] \setminus \infty$ is absolute, then it is of the form $(k, -\frac{1}{2}k^\theta k + d_2)$; but clearly any point of this form is an absolute point of $\theta$. This shows that the number of absolute points on $[k] \setminus \infty$ is $|D_2|$ and proves the theorem.

Exercise 12.13. Show that $\theta$ is orthogonal if and only if $\alpha = 1$, and is unitary if and only if $|D_2| = \sqrt{n}$.

Example. In order to exhibit any examples it is natural to look for the simplest possible $\alpha$ and $\beta$. If we try to choose $\alpha = \beta$ then,

*Exercise 12.16. Let $F = GF(q)$ and let $D$ be a division ring whose elements are $x + \lambda y$ for all $x, y$ in $F$ where addition in $D$ is componentwise and multiplication is given by $(x + \lambda y)(u + \lambda v) = xu + \delta y^v v + \lambda(yu + x^v v)$ where $\sigma$ is in Aut $F$, $\sigma^2 = 1 \neq \sigma$ and $\delta \in F$, $\delta^0 \neq \delta$ (see Chapter IX, Section 4).

If $\alpha$ is a permutation of the elements of $D$ given by $(x + \lambda y)^n = x + \lambda y^n$, where $b^1^{n+e} = 1$, show that $\alpha$ is an involutory automorphism of $D$ and that the polarity $\theta$ of $\mathcal{P}(D)$ given by taking $\beta = \alpha$ has $|D|^{5/4} + 1$ absolute points.

*Exercise 12.17. Show that the polarity of Exercise 12.16 is irregular, if $q = 9$ or $q = 25$.

We conclude this section by remarking that the Hughes planes (which are not translation planes) also admit polarities. The polarities of the Hughes plane have been completely determined and are all irregular.

6. Appendix (Proof of Segre's Theorem)

Let $F = GF(q)$ and let $\mathcal{O}$ be any oval in $\mathcal{P}_2(q)$. If a triangle has its vertices on $\mathcal{O}$ then we say it is an inscribed triangle of $\mathcal{O}$; a circumscribed triangle is one whose sides are tangents to $\mathcal{O}$. The points of contact of the sides of a circumscribed triangle are the vertices of an inscribed triangle, and we call these two triangles mates.

Lemma 12.16. Every inscribed triangle of $\mathcal{O}$ and its mate are in perspective.

Proof. We may take the inscribed triangle as the triangle of reference for a homogeneous coordinatization of $\mathcal{P

$x_3 = h_2 x_1, x_1 = h_2 x_2$ of the lines $A_2 B, A_3 B$ are functions of $h_1$, connected by (1), which take once each of the non-zero values of $F$ distinct from $k_2, k_3$ respectively. On multiplying together the $q-2$ equations for $h_1 h_2 h_3$ thus obtained, we have

$$\Pi^3 = k_1 k_2 k_3,$$

(2)

where $\Pi$ denotes the product of the $q-1$ non-zero elements of $F$. (Note that (1) holds if $h_1 \neq k_1$ ($i=1, 2, 3$). If $h_1 = k_1$ then $h_1 = 1$ for $i \neq j$ so that $h_1 h_2 h_3 = k_1$). But $\Pi = -1$ (see Exercise 12.18, at the end of this appendix) and thus

$$k_1 k_2 k_3 = -1.$$

(3)

The points $a_2 a_3 = \langle \langle k_3, 1, k_2 k_3 \rangle \rangle, a_3 a_1 = \langle \langle k_3 k_1, k_1, 1 \rangle \rangle, a_1 a_2 = \langle \langle 1, k_1 k_2, k_2 \rangle \rangle$ are joined to $A_1, A_2, A_3$ respectively by the lines:

$$x_3 = k_2 k_3 x_2, \quad x_1 = k_3 k_1 x_3, \quad x_2 = k_1 k_2 x_1.$$

But, by (3), these lines are concurrent at $K = \langle \langle 1, k_1 k_2, -k_2 \rangle \rangle$ and the lemma is proved. $\square$

We now prove Segre's theorem and, without any loss of general

By (6), each of the $q-2$ points of $\mathcal{O}$ distinct from $A_1, A_2, A_3$ lies on the conic with equation $x_2x_3 + x_3x_1 + x_1x_2 = 0$ which, clearly, also contains $A_1, A_2, A_3$. Thus since the conic also has exactly $q+1$ points, the points of $\mathcal{O}$ are the points of the conic with equation $x_2x_3 + x_3x_1 + x_1x_2 = 0$.

Exercise 12.18. If $\Pi$ denotes the product of the $q-1$ non-zero elements of $GF(q)$, $q$ odd, show that $\Pi = -1$. (Hint: Use the fact that the multiplicative group of $GF(q)$ is cyclic and, hence, contains only one element of order 2.)

References

Since the chapter is self-contained no references (except to Chapter II) are needed for the understanding of the material. However the reader may feel that he wishes to know a little more about the known examples of polarities. Much more is known about polarities of translation planes than we have included here and the interested student should see [1]. The polarities of the Hughes planes have been completely determined and can be found in [2].

1. Ganley, M.J.: Polarities in Translation Planes. Geometriae Dedicata 1, 103-116 (1972).
2. Piper, F.C.: Polarities in the Hughes plane. Bull. London Math. Soc. 2, 209-213 (1970).<|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|><|endoftext|>

XIII. Collineation Groups

1. Introduction

This chapter takes up again the theme of collineations and collineation groups, but dealing with somewhat deeper theorems than we found in Chapter IV or in the various sections of the other chapters that have touched on collineations. We shall be concerned almost exclusively with finite planes, and our first concern will be with orbits, fixed points, and so on; Section 2 contains the very important Orbit Theorem, which is crucial to any work on collineation groups of finite planes: a collineation group of a finite projective plane has equally many point and line orbits. Section 3 develops a number of results about perspectivities, which besides their intrinsic interest, enable us to give interesting and useful characterizations of finite desarguesian planes in Section 4. In Section 5 we discuss Singer groups, already studied in Chapter II, and introduce difference sets. Hall's multiplier theorem is stated, but its proof is postponed to the Appendix (since the proof is more than usually unrelated to the geometric ideas of the theorem itself, involving as it does group algebras). It is worth commenting that the subject of Singer groups and difference sets was once one of the most active branches in the study of projective planes, but the difficulty in proving the central conjecture (a finite plane with a Singer group is desarguesian), or of finding a counter-example, has been so great that it has almost lain in abeyance for many years. Finally in Section 6 we consider some non-existence theorems, which are very like the Bruck-Ryser theorem, and include it as a special case. These are non-existence theorems for planes with collineation groups of specified types (e.g. of prime order, fixing a certain number of points); the proofs are extremely long and detailed, and involve a knowledge of number theory far beyond the level of this book, so they are mostly only quoted. But one result has an accessible proof, which is given here: a plane of order $n \equiv 2 \pmod{4}$, $n > 2$, can have no collineations of even order.

2. Some Orbit Theorems

If $\Pi$ is any collineation group of a projective plane $\mathcal{P}$ then $\Pi$ induces a permutation group,

permutation group $\Pi(l)$ on the lines of $\mathcal{P}$. It is natural to look for properties of these two representations of $\Pi$.

If $\mathcal{P}^{h_{\infty}}$ is a translation plane of order $n$ and if $\Pi = \text{Aut} \mathcal{P}$ and $\Lambda = \Pi_{l_{max}, l_{min}}$, then $|\Lambda| = n^2$. The group $\Lambda(P)$ has $n+1$ point orbits of length 1, namely the points of $l_{max}$ and one point orbit of length $n^2$. For any point $X$ on $l_{max}$, $\Lambda$ is transitive on the $n$ lines through $X$ and distinct from $l_{min}$, so $\Lambda(l)$ has $n+1$ line orbits of length $n$ and one of length 1. This example illustrates that $\Pi(P)$ and $\Pi(l)$ need not have the same orbit structure. In fact in this example $\Lambda(P)$ acts faithfully and regularly on the affine points of $\mathcal{P}^{h_{\infty}}$ whereas $\Lambda(l)$ is not faithful on any of its orbits. However if $\Pi$ is any finite collineation group then it is impossible for both $\Pi(P)$ and $\Pi(l)$ to be non-faithful on all of their orbits.

**Theorem 13.1.** If $\Pi$ is a finite collineation group of a projective plane $\mathcal{P}$ then $\Pi$ induces a faithful permutation group on every orbit of maximal length.

**Proof.** Since $\Pi$ is finite, all orbits have finite length and, consequently, there is certainly an orbit of maximal length which, without any loss of generality, we may assume to be a point orbit. (Otherwise the dual argument will prove the theorem.)

Let $P$ and $Q$ be any pair of distinct points in an orbit $\mathcal{O}$ of maximal length and let $\Pi_e$ be the subgroup of $\Pi$ fixing every point of $\mathcal{O}$. Then $\Pi$ induces a faithful permutation group on $\mathcal{O}$ if and only if $\Pi_e = 1$. We shall assume that $\Pi_e \neq 1$ and then show that

Corollary 1. Any collineation group of a finite projective plane induces a faithful permutation group on at least one orbit.

If a permutation group acts faithfully on a given orbit than we refer to that orbit as a faithful orbit.

If $\Gamma$ is an abelian group on a set $\mathcal{S}$ then, since every subgroup of $\Gamma$ is normal, $\Gamma$ acts regularly on each of its orbits. Thus an orbit of $\Gamma$ is faithful if and only if its length is equal to the order of $|\Gamma|$. This simple observation leads to another corollary to Theorem 13.1.

Corollary 2. If $\alpha$ is a collineation of a finite projective plane of order $n$ and if the order of $\alpha$ is $t$ then $t \leq n^2 + n + 1$.

We have already seen in Chapter II that $\mathcal{P}_2(q^n)$ admits a collineation of order $q^{2\alpha} + q^n + 1$. so that Corollary 2 gives a best possible upper bound.

Exercise 13.1. Let $\mathcal{P}$ be a projective plane with a collineation group $\Pi$. If $\Pi$ has a non-faithful orbit which contains a quadrangle show that $\Pi$ must leave some subplane of $\mathcal{P}$ invariant.

Exercise 13.2. If $\Pi$ is an abelian collineation group of a finite projective plane of order $n$ such that $|\Pi| \neq n^2 + n + 1$, show that $|\Pi| \leq n^2$.

As we saw by considering the translation group of a translation plane, it is not in general true that, for a collineation group $\Pi$, $\Pi(P)$ and $\Pi(l)$ have the same orbit structure. It is, however, true in the special case where $\Pi$ is cyclic, and to prove this we use the incidence matrix of the plane.

An $n \times n$ matrix $A$ of 0's and 1's is called a permutation matrix if each row of $A$ and each column of $A$ has exactly one entry 1.

Exercise 13.3. If $A$ is any perm

Similarly the $(i, k)$-entry of $AQ = \sum_{j=1}^{n} a_{ij} q_{jk} = a_{iy}$ where $y$ is uniquely determined by $P_v = l_k$. We must show $a_{xk} = a_{iy}$.

$$a_{xk} = 1 \Leftrightarrow P_x \in l_k$$
$$\Leftrightarrow P_x^{-1} \in l_k^{-1}$$
$$\Leftrightarrow P_i \in l_y$$
$$\Leftrightarrow a_{iy} = 1.$$

Thus $a_{xk} = a_{iy}$ and the lemma is proved. $[\quad]$

**Theorem 13.3.** A collineation of a finite projective plane has an equal number of fixed points and lines.

**Proof.** Let $\mathcal{P}$ be a finite projective plane and let $A$ be an incidence matrix for $\mathcal{P}$. Then, by Lemma 13.2, any collineation $\alpha$ can be represented by two permutation matrices $P, Q$ with $PA = AQ$. If $P = (p_{ij})$ then $p_{ij} = 1$ if and only if $P_i^T = P_j^T$ so that $p_{ii} = 1$ if and only if $P_i^T = P_j^T$ and the number of fixed points of $\alpha$ is the trace of $P$. Similarly the number of fixed lines of $\alpha$ is the trace of $Q$.

But since $PA = AQ$ and, by Theorem 3.14, $A$ is non-singular, we have $P = AQA^{-1}$ so that the trace of $P$ is equal to the trace of $Q$. $[\quad]$

**Corollary.** Let $\Pi$ be a cyclic collineation group of a finite projective plane $\mathcal{P}$. Then $\Pi(P)$ has the same cyclic structure as $\Pi(l)$.

**Proof.** Let $\Pi = \langle \pi \rangle$. If $P_x$ is the number of point cycles of length $x$ and $l_x$

As we saw from looking at the group of translations of a translation plane, neither Theorem 13.3 nor its corollary is true for arbitrary collineation groups. However we do have

**Theorem 13.4. (The orbit theorem)**$^5$. Let $\Pi$ be a collineation group of a finite projective plane $\mathcal{P}$. Then $\Pi$ has an equal number of point and line orbits.

**Proof.** Left $t_1$ be the number of points orbits of $\Pi$ and $t_2$ the number of line orbits. Further, for any $\alpha \in \Pi$, let $\chi(\alpha)$ be the number of fixed points of $\alpha$ which, by Theorem 13.3, is also the number of lines fixed by $\alpha$. Applying Result 1.14 to $\Pi(P)$ and $\Pi(l)$ respectively we get $|\Pi|t_1 = \sum_{\alpha \in \pi} \chi(\alpha) = |\Pi|t_2$. Hence $t_1 = t_2$.

**Corollary.** $\Pi$ is transitive on the points of $\mathcal{P}$ if and only if $\Pi$ is transitive on the lines of $\mathcal{P}$.

**Exercise 13.4.** Let $\mathcal{P}$ be a finite projective plane with a collineation group $\Pi$. Show that $\Pi$ is doubly transitive on the points of $\mathcal{P}$ if and only if $\Pi$ is transitive on the flags of $\mathcal{P}$ and on the anti-flags of $\mathcal{P}$. (Recall that a flag is an incident point-line pair while an anti-flag is a non-incident point-line pair.)

**Exercise 13.5.** Use Exercise 13.4 to show that $\Pi$ is doubly transitive on the points of $\mathcal{P}$ if and only if $\Pi$ is doubly transitive on the lines of $\mathcal{P}$.

**Exercise 13.6.** Let $\mathcal{P}^{\lambda \omega}$ be a finite affine plane with a collineation group $\Pi$. Show that $\Pi$ is transitive on the affine lines of $\mathcal{P}^{\lambda \

Lemma 13.5. Let $\Gamma$ be a permutation group on a finite set $\mathcal{S}$ and let $p$ be a prime. If $\mathcal{T}$ is a subset of $\mathcal{S}$ such that, for any $i \in \mathcal{T}, \mathcal{T}$ contains an element of order $p$ fixing $t$ and no other element of $\mathcal{S}$, then $\mathcal{T}$ is contained in an orbit of $\Gamma$.

Proof. Let $t_1, t_2$ be any two distinct elements of $\mathcal{T}$ and suppose that $t_1$ and $t_2$ lie in different orbits of $\mathcal{L}$. Let $\gamma_i \in \Gamma$, $(i=1, 2)$, have prime order $p$ and be such that $t_i^2 = t_i$ but $s^2 = s$ for all $s \in \mathcal{S}$, $s \neq t_i$. Then, for any $s \in \mathcal{S}, s \neq t_i$, the length of the orbit of $s$ under $\langle \gamma_i \rangle$ is $p$. However, since $\langle \gamma_i \rangle$ is a subgroup of $\mathcal{L}$, any orbit of $m$ must be a union of orbits of $\langle \gamma_i \rangle$. Putting $i=1$ we see that, since $t_1$ is the only orbit of length 1 under $\langle \gamma_i \rangle$ and since $t_2 \neq t_1$ the orbit of $t_2$ under $\mathcal{T}$ is partitioned into disjoint cycles of length $p$ by $\langle \gamma_i \rangle$ so that $|t_2| \Gamma \equiv 0$ (mod $p$) whereas $|t_1| \Gamma \equiv 1$ (mod $p$) (because $\langle \gamma_1 \rangle$ p