An object $0$ in a category $\mathcal{C}$ is called a zero object if it is both an initial object and a terminal object. That is, for every object $A$ of $\mathcal{C}$, there exist unique morphisms $0 \to A$ and $A \to 0$. A category possessing a zero object is called a category with zero.
