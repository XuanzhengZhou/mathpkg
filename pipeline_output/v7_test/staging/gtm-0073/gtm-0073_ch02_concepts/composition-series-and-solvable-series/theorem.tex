A subnormal series $G = G_0 > G_1 > \cdots > G_n = \langle e \rangle$ is a \emph{composition series} if each factor $G_i/G_{i+1}$ is simple.

A subnormal series $G = G_0 > G_1 > \cdots > G_n = \langle e \rangle$ is a \emph{solvable series} if each factor is abelian.
