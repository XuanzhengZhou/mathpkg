Let $R$ be a ring with identity and $A$ an $n \times m$ matrix over $R$. Performing a single elementary row [resp. column] operation on $A$ yields the matrix $EA$ [resp. $AE$], where $E$ is the elementary matrix obtained by performing the same elementary row [resp. column] operation on the identity matrix $I_n$ [resp. $I_m$].
