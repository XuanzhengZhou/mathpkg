If $A_1, \ldots, A_n$ are modules, then the direct sum $A_1 \oplus A_2 \oplus \cdots \oplus A_n$ satisfies the ascending [resp.\ descending] chain condition on submodules if and only if each $A_i$ satisfies it.
