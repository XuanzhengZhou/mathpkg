An ideal $Q (\neq R)$ in a commutative ring $R$ is \textbf{primary} if for any $a, b \in R$:
\[
ab \in Q \text{ and } a \notin Q \implies b^n \in Q \text{ for some } n > 0.
\]

If $Q$ is a primary ideal, then $\operatorname{Rad} Q$ is a prime ideal.
