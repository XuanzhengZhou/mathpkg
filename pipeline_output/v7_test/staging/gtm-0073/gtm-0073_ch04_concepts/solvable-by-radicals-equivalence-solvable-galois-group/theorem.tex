Let $K$ be a field and $f \in K[x]$ a polynomial of degree $n > 0$, where $\mathrm{char}\,K$ does not divide $n!$ (which is always true when $\mathrm{char}\,K = 0$). Then the equation $f(x) = 0$ is solvable by radicals if and only if the Galois group of $f$ is solvable.
