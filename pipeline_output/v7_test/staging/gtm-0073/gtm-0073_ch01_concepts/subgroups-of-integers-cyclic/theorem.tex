\begin{theorem}
Every subgroup $H$ of the additive group $\mathbb{Z}$ is cyclic. Either $H = \langle 0 \rangle$ or $H = \langle m \rangle$, where $m$ is the least positive integer in $H$. If $H \neq \langle 0 \rangle$, then $H$ is infinite.
\end{theorem}
