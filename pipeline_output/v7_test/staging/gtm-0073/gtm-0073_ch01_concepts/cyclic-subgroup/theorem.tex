\begin{definition}
A group $G$ is \textbf{cyclic} if $G = \langle a \rangle$ for some $a \in G$. The element $a$ is called a \textbf{generator} of $G$. A subgroup generated by a single element is a \textbf{cyclic subgroup}.
\end{definition}
