Let $A$ be a cyclic module of order $p^n$ ($p$ prime) over a PID $R$, generated by $a$. Then there exists a submodule $C$ of $A$ maximal with respect to $Ra \cap C = 0$, and then $A = Ra \oplus C$ (so $C$ is cyclic of order $p^m$ with $m \leq n$).
