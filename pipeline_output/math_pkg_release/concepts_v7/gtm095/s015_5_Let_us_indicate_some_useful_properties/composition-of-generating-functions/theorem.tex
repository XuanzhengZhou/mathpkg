The composition (or substitution) operation on generating functions is defined by

$$(A \circ B)(x) = A(B(x)),$$

which means that if $A(x) = \sum_{n \geq 0} a_n x^n$ and $B(x) = \sum_{i \geq 0} b_i x^i$, then

$$(A \circ B)(x) = \sum_{n \geq 0} a_n \left( \sum_{i \geq 0} b_i x^i \right)^n.$$
