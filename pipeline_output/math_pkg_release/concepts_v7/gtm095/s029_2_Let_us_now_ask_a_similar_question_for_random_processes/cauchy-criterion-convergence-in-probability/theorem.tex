A necessary and sufficient condition for a sequence $(\xi_n)_{n \geq 1}$ of random variables to converge in probability is that $(\xi_n)$ is fundamental in probability:

$$P\{|\xi_n - \xi_m| \geq \varepsilon\} \to 0, \quad n, m \to \infty,$$

for every $\varepsilon > 0$.
