Let $C$ be an irreducible nonsingular curve over $\mathbb{C}$. For each divisor $D$ on $C$, the Riemann-Roch space

$$L(D) = \{ f \in K_C \setminus \{0\} \mid \operatorname{div}(f) \geqslant -D \} \cup \{0\}$$

is a finite-dimensional vector space over $\mathbb{C}$.
