Suppose $x_1, x_2, \ldots, x_n$ are elements of a vector space $X$. A linear combination of these vectors is any vector $x$ of the form

$$x = a_1 x_1 + a_2 x_2 + \cdots + a_n x_n,$$

where $a_1, a_2, \ldots, a_n$ are scalars.
