A metric space is a set $S$ together with a given metric $d$. It is denoted by $(S, d)$.

When the metric $d$ is understood, one refers to the set $S$ alone as a metric space. For example, $\mathbb{R}$, $\mathbb{C}$, or $\mathbb{R}^n$ as metric spaces with their usual metrics.
