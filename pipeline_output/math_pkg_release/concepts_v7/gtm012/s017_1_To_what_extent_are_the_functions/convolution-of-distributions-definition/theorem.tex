For $F, G \in \mathcal{P}'$, the \textbf{convolution} $F * G$ is the distribution defined by
$$(F * G)(u) = F(G^\sim * u), \quad u \in \mathcal{P},$$
where $G^\sim(u) = G(\tilde{u})$ with $\tilde{u}(x) = u(-x)$.

If $v \in \mathcal{P}$, then $F * F_v = F_f$ where $f = F * v$.
