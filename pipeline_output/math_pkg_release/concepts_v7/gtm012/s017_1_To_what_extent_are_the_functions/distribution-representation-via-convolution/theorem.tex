If $F \in \mathcal{P}'$ and $u, v \in \mathcal{P}$, then
$$F_f(v) = F(\tilde{u} * v),$$
where $f = F * u$ and $\tilde{u}(x) = u(-x)$.
Equivalently, the regular distribution $F_f$ induced by $f = F * u$ acts on a test function $v$ as $F$ applied to $\tilde{u} * v$.
