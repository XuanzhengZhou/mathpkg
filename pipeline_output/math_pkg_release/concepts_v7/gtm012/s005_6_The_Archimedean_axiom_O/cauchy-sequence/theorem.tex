A sequence $(z_n)_{n=1}^{\infty}$ in $\mathbb{C}$ is said to be a \textbf{Cauchy sequence} if for each $\varepsilon > 0$ there is an integer $N$ such that
$$|z_n - z_m| < \varepsilon \quad \text{whenever } n \geq N \text{ and } m \geq N.$$
