An algebraic number $\alpha$ of degree $n$ is called an algebraic integer if in its irreducible equation over $k(1)$ with leading coefficient $1$, all coefficients are rational integers. Equivalently, $\alpha$ is a root of a monic polynomial with rational integer coefficients.
