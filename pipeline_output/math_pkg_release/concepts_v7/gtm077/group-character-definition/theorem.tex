A function $\chi(A)$ defined on an Abelian group $\mathfrak{G}$, taking complex values, is called a group character if for all $A, B \in \mathfrak{G}$,
$$\chi(AB) = \chi(A) \cdot \chi(B)$$
and $\chi(A) \neq 0$ for at least one element $A$.
