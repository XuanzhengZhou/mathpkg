Each positive integer $a > 1$ can be represented in one---and except for the order of the factors---in only one way as a product of primes:
$$a = p_1 p_2 \cdots p_k,$$
where $p_1, p_2, \ldots, p_k$ are positive prime numbers.
