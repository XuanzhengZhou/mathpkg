Let $p$ be a rational prime which does not divide $d$. Then $p$ splits in the field $K(\sqrt{d})$ into two distinct prime ideals $\mathfrak{p}, \mathfrak{p}'$ provided the congruence

$$x^2 \equiv d \pmod{4p}$$

(45)

can be solved in rational

is obviously divisible by $p$ and

$$\mathfrak{a}\mathfrak{a}' = \left( p^2, p \left( r - \frac{d + \sqrt{d}}{2} \right), p \left( r - \frac{d - \sqrt{d}}{2} \right), \frac{(2r - d)^2 - d}{4} \right)$$

$$= (p) \left( p, r - \frac{d - \sqrt{d}}{2}, r - \frac{d + \sqrt{d}}{2}, \frac{(2r - d)^2 - d}{4p} \right).$$

However, the last ideal factor is $= (1)$, for this ideal contains $p$ and the difference between the second and the third numbers, which is $\sqrt{d}$; thus this ideal contains the two relatively prime numbers $p$ and $d$. Finally we obtain from this

$$\mathfrak{p} = \left( p, r - \frac{d + \sqrt{d}}{2} \right), \quad \mathfrak{p}' = \left( p, r - \frac{d - \sqrt{d}}{2} \right).$$

The two prime ideals are moreover distinct, thus relatively prime, as $(\mathfrak{p}, \mathfrak{p}')$ contains the two coprime numbers $p, d$.

Conversely, if $x$ is a solution of (45), then the number

$$\omega = \frac{x + \sqrt{d}}{2}$$

is obviously an integer; moreover $\omega/p$ is not an integer, as $((\omega - \omega')/p)^2 = d/p^2$ is not an integer. Thus, since $p$ does not divide $\omega$ or $\omega'$, but does divide the product $\omega \omega'$, $p$ cannot be a prime ideal. Thus it splits in $K(\sqrt{d})$ into two prime factors which are distinct from one another by the above.

Moreover if $q$ is an odd prime factor of $d$, then the ideal

$$\mathfrak{q} = \left( q, \frac{d + \sqrt{d}}{2} \right) = \left( q, \frac{-d + \sqrt{d}}{2}
