For each integer $a$ relatively prime to $n$,
$$a^{\varphi(n)} \equiv 1 \pmod{n}.$$
In particular, if $n = p$ is a positive prime, then $\varphi(p) = p-1$ and for every integer $a$,
$$a^p \equiv a \pmod{p}.$$
