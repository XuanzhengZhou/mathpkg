If $x_1, x_2, \ldots, x_n$ form a complete system of residues modulo $n$ ($n > 0$), then
$$a x_1 + b, a x_2 + b, \ldots, a x_n + b$$
is also a complete residue system modulo $n$, provided $a$ and $b$ are integers and $(a, n) = 1$.
