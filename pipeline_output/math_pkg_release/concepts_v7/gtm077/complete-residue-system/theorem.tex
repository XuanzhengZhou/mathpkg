A set of $|n|$ integers that are pairwise incongruent modulo $n$ is called a complete system of residues modulo $n$. Such a set contains exactly one representative from each residue class modulo $n$.

Example: The numbers $0, 1, 2, \ldots, |n| - 1$ form a complete system of residues modulo $n$.
