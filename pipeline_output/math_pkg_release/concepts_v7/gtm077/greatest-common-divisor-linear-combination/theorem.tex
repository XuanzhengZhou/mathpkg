If $a$ and $b$ are integers not both zero, then their greatest common divisor $d = (a,b)$ can be represented in the form
$$d = ax + by$$
with integers $x$ and $y$. Moreover, every common divisor of $a$ and $b$ divides $d$.
