For a finite Abelian $p$-group:
(a) No element of a maximal independent system can be a $p$th power.
(b) Replacing $B_m$ by $B_m^u B_{m+1}^{x_{m+1}}\cdots B_r^{x_r}$ with $u \not\equiv 0 \pmod{p}$ preserves maximality.
(c) If $C^p$ is representable by powers of the $B_i$, then so is $C$.
