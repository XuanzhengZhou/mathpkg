An integer $g$ is called a primitive root modulo a prime $p$ if the powers $g, g^2, g^3, \ldots, g^{p-1}$ are all incongruent modulo $p$. Equivalently, the residue class of $g$ generates the cyclic group $\mathfrak{R}(p)$. There are $\varphi(p-1)$ primitive roots modulo $p$.
