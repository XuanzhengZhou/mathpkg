There are exactly $h$ distinct characters $\chi(A)$ of a finite Abelian group $\mathfrak{G}$ of order $h$. Each $\chi$ takes values that are $h$th roots of unity. The characters themselves form a group of order $h$ under pointwise multiplication, isomorphic to $\mathfrak{G}$.
