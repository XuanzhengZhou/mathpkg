An integer $a$ is called a root of an integral polynomial $f(x)$ modulo $n$ if
$$f(a) \equiv 0 \pmod{n}.$$
If $a$ is a root modulo $n$ and $a \equiv b \pmod{n}$, then $b$ is also a root modulo $n$. Thus roots modulo $n$ are properties of residue classes, not individual integers.
