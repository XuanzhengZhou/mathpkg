Let
$$A(x) = \alpha_p x^p + \cdots + \alpha_0, \quad B(x) = \beta_r x^r + \cdots + \beta_0$$
be polynomials with algebraic integer coefficients. If an integer $\delta$ divides all coefficients of the product $C(x) = A(x)B(x)$, then $\delta$ also divides all products $\alpha_i \beta_k$.
