If $\theta$ has degree $n$ over $k$, then every $\alpha \in K(\theta)$ is uniquely representable as $\alpha = c_0 + c_1\theta + \cdots + c_{n-1}\theta^{n-1}$ with $c_i \in k$. The powers $1,\theta,\ldots,\theta^{n-1}$ form a basis over $k$.
