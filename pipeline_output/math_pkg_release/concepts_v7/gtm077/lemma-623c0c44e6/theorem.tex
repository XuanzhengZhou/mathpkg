If $\beta$ belongs to $\mathfrak{A}$, then, provided (70) holds, $\beta$ also belongs to $\mathfrak{a}$.

If an equation

$$\beta = \sum_h \Gamma_h \alpha_h$$

with integers $\Gamma_h$ in $K$ holds, then the equations

$$\beta = \sum_h \Gamma_h^{(i)} \alpha_h \quad (i = 1, 2, \ldots, m)$$

with the corresponding relative conjugates also hold and by multi

These coefficients $\gamma$, as symmetric integral expressions in the $\Gamma_h^{(i)}$, are integers in $k$. For $x_h = \alpha_h (h = 1, 2, \ldots, q)$ Equation (71) is thus a number from the ideal $\mathfrak{a}^m$. Consequently $\beta^m/\mathfrak{a}^m$ is an integer; hence $\beta/\mathfrak{a}$ is also an integer, that is, $\beta$ occurs in $\mathfrak{a}$.

Thus, by (70), if we have a further pair of corresponding ideals,

$$\mathfrak{b} = (\beta_1, \ldots, \beta_s)_k \quad \text{and} \quad \mathfrak{B} = (\beta_1, \ldots, \beta_s)_K,$$

then we have
