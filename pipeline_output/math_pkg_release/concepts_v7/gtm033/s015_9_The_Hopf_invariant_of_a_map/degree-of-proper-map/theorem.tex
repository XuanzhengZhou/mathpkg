Let $U, V$ be noncompact oriented $n$-manifolds without boundaries and let $h \colon U \to V$ be a proper $C^{\infty}$ map. The \textbf{degree} of $h$ is defined as
$$
\deg h = \sum_{x \in h^{-1}(y)} \deg_{x} h
$$
where $y$ is a regular value of $h$.
