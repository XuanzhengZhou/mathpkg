Let $M$ and $N$ be compact oriented manifolds without boundaries. Then
$$
\chi(M \times N) = \chi(M) \cdot \chi(N).
$$

Let $\xi$ be an $n$-dimensional oriented vector bundle over a compact oriented $n$-manifold $M^{n}$ without boundary.
