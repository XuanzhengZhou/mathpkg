**Proposition 1.0.** Let $M$ be a compact $C^r$ manifold, $2 \leq r \leq \infty$. Then embeddings are dense in $C^r_W(M, \mathbb{R}^q)$ if $q > 2 \dim M$, while immersions are dense if $q \geq 2 \dim M$.
