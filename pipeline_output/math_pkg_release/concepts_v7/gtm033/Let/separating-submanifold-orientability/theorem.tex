Let $N$ be a simply connected manifold and $M \subset N$ a closed connected submanifold of codimension $1$, with $\partial M = \partial N = \varnothing$.

\textbf{Theorem 4.5.} If $M$ separates $N$ then $M$ is orientable if $N$ is orientable.
