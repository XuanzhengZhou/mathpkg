Let $f: M \to N$ be a $C^1$ map (where $M$ and $N$ are $C^r$ manifolds, $r \geqslant 1$). We call $f: M \to N$ an embedding if $f$ is an immersion which maps $M$ homeomorphically onto its image. To indicate this we may write $f: M \hookrightarrow N$.
