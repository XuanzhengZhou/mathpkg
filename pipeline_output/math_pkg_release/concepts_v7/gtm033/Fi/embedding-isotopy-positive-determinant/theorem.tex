Let $E^n = D^n$ or $\mathbb{R}^n$ and let $f_i : (E^n, 0) \to (V, x_0)$ be an embedding, $i = 0, 1$, where $n = \dim V$. If
$$\operatorname{Det}\bigl(D(f_1^{-1} \circ f_0)(0)\bigr) > 0$$
then $f_0$ and $f_1$ are isotopic rel $0$.
