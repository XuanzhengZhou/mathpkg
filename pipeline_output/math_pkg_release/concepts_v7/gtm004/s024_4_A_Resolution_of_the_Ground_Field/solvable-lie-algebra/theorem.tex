A Lie algebra $\mathfrak{g}$ is called solvable if there exists an integer $n \geq 0$ such that $\mathfrak{g}_n = \{0\}$, where $\mathfrak{g}_n$ is the $n$-th term of the derived series. The first integer $n$ for which $\mathfrak{g}_n = \{0\}$ is called the derived length of $\mathfrak{g}$.
