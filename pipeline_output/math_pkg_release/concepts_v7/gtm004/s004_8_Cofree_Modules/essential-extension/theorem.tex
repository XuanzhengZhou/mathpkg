Let $A$ be a submodule of a $\Lambda$-module $B$. Then $B$ is called an \emph{essential extension} of $A$ if every nonzero submodule of $B$ has nontrivial intersection with $A$, i.e., for every submodule $0 \neq H \subseteq B$ one has $H \cap A \neq 0$.
