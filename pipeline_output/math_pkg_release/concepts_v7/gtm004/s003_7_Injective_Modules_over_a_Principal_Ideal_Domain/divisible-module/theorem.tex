Let $\Lambda$ be an integral domain. A $\Lambda$-module $D$ is \textbf{divisible} if for every $d \in D$ and every $0 \neq \lambda \in \Lambda$ there exists $c \in D$ such that $\lambda c = d$. The uniqueness of $c$ is not required.
