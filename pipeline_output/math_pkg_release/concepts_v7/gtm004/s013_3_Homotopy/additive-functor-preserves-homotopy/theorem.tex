\text{Let}~F~\text{be an additive functor. If}~\varphi \simeq \psi : C \rightarrow D~\text{are homotopic chain maps, then}

$$F(\varphi) \simeq F(\psi) : F(C) \rightarrow F(D).$$

\text{Consequently, an additive functor}~F~\text{induces a functor between the homotopy categories.}
