\text{Church's Thesis} (also called the Church-Turing thesis) asserts that the class of intuitively computable (partial) functions on the natural numbers coincides with the class of (partial) recursive functions. Equivalently, every effectively computable function is Turing computable, or general recursive, or lambda-definable.
