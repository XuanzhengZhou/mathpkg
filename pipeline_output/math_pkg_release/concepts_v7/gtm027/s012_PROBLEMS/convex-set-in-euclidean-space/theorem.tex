A subset $A$ of Euclidean $n$-space $E_n$ is called \emph{convex} if and only if for every pair $x, y \in A$ and every real number $t$ with $0 \leq t \leq 1$, the point $t x + (1 - t) y$ belongs to $A$. Here $(t x + (1 - t) y)_i = t x_i + (1 - t) y_i$ for each coordinate $i$.
