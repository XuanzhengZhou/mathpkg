A property is a \textbf{metric invariant} if and only if, whenever it is possessed by a metric space $(X,d)$, it is also possessed by each metric space isometric to $(X,d)$. A metric invariant is not necessarily a topological invariant; for example, the property of having infinite diameter is a metric invariant but not a topological invariant.
