The \textbf{discrete topology} for a set $X$ is the family of all subsets of $X$. The pair $(X, \mathfrak{J})$ with this topology is called a \textbf{discrete topological space}. Every subset is both open and closed in the discrete topology.
