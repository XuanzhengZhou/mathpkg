Each ordered pair has a first coordinate and a second coordinate. The ordered pair with first coordinate $x$ and second coordinate $y$ is denoted $(x,y)$. Two ordered pairs are equal iff they have the same coordinates:
$$
(x,y) = (u,v) \iff x = u \text{ and } y = v.
$$
