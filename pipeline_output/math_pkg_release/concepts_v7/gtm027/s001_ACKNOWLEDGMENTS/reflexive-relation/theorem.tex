Let $R$ be a relation and let $X = (\operatorname{domain} R) \cup (\operatorname{range} R)$. The relation $R$ is \text{reflexive} iff each point of $X$ is $R$-related to itself. Equivalently:
$$
\Delta(X) \subset R.
$$
