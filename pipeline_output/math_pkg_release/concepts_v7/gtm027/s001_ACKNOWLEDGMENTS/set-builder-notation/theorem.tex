The set of all points $x$ such that a proposition $P(x)$ holds is denoted
$$
\{x : P(x)\}.
$$
Schematically,
$$
y \in \{x : P(x)\} \iff P(y).
$$
Two sets constructed from logically equivalent propositions are identical.
