A function $f$ on a set $X$ to a set $Y$ is \textbf{order preserving} (monotone, isotone) relative to an order $<$ for $X$ and an order $<$ for $Y$ if and only if $f(u) < f(v)$ or $f(u) = f(v)$ whenever $u$ and $v$ are points of $X$ such that $u \leq v$.
