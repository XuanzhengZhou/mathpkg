If $f$ is a function on $X$ to $Y$ and $A$ is a subset of $X$, then $f \cap (A \times Y)$ is a function called the \textbf{restriction} of $f$ to $A$, denoted $f \mid A$. Its domain is $A$ and $(f \mid A)(x) = f(x)$ for each $x$ in $A$.
