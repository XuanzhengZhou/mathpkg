Following the usual custom, the value of the function $\cdot$ at $(x, y)$ is written $x \cdot y$ instead of the usual functional notation $\cdot(x, y)$, and if no confusion seems likely, the symbol $\cdot$ may be omitted entirely and the group operation indicated by juxtaposition.
