The \textbf{infimum} (or \textbf{greatest lower bound}) of a subset $A$ of an ordered set is an element $t$ such that:
\begin{enumerate}
\item $t$ is a lower bound for $A$: $t \leq a$ for all $a \in A$;
\item if $s$ is any lower bound for $A$, then $s \leq t$.
\end{enumerate}
