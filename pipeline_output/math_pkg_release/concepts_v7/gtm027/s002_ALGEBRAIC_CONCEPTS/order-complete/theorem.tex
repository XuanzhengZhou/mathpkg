An ordered field is \textbf{order-complete} iff every non-void subset which has an upper bound has a least upper bound (or \textbf{supremum}). Equivalently (by 0.9), each non-void subset which has a lower bound has a greatest lower bound (or \textbf{infimum}).
