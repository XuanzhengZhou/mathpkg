Let $F$ be a family of functions on a set $X$ to a uniform space $(Y, \mathcal{V})$ and let $\mathfrak{U}$ be the uniformity of uniform convergence on $F$. The **topology of uniform convergence** (or the u.c. topology) is the topology induced by the uniformity $\mathfrak{U}$.
