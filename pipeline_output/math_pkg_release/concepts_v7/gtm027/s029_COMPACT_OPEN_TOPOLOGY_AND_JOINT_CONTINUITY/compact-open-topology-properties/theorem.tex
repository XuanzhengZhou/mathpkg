The compact open topology $\mathcal{C}$ contains the topology $\wp$ of pointwise convergence. The space $(F, \mathcal{C})$ is a Hausdorff space if the range space $Y$ is Hausdorff, and is regular if $Y$ is regular and the members of $F$ are continuous.
