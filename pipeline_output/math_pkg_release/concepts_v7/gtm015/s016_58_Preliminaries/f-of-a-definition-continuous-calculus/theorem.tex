With notation as in the continuous functional calculus theorem, for $f \in \mathcal{C}(\sigma(a))$ we define $f(a) = \Phi(f)$. In particular, if $p(z) = \sum c_{jk} z^j \bar{z}^k$ is a polynomial in $z$ and $\bar{z}$, then $p(a) = \sum c_{jk} a^j (a^*)^k$.
