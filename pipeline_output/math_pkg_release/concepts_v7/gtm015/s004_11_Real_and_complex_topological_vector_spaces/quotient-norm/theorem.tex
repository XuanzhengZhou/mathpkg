Let $E$ be a normed space and let $M$ be a closed linear subspace of $E$. For $u \in E/M$, define
$$\|u\| = \inf \{ \|x\| : x \in u \}.$$
Then $\|\cdot\|$ is a norm on $E/M$, called the \textbf{quotient norm}, and the norm topology on $E/M$ coincides with the quotient topology.
