If $E$ is an inner product space, then the mapping $y \mapsto y'$ of $E$ into $E'$ is conjugate-linear and isometric; that is,
\begin{align*}
(y + z)' &= y' + z', \\
(\lambda y)' &= \overline{\lambda} \, y', \\
\|y'\| &= \|y\|.
\end{align*}
