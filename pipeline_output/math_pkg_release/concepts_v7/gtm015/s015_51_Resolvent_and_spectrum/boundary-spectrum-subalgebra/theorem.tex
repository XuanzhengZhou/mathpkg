Let $B$ be a closed subalgebra of $A$ containing $1$ and let $x \in B$. Then
$$\partial \sigma_B(x) \subset \partial \sigma_A(x).$$
In particular, if $\sigma_B(x)$ has empty interior then $\sigma_B(x) = \sigma_A(x)$.
